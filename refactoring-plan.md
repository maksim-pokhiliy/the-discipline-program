### A) Переписать (или жёстко переделать)

1. **API routing в marketing**

   * убрать `api/[...path]` switch-router
   * сделать нормальные route handlers по ресурсам

2. **blog/shared мусор**

   * сейчас это выглядит как прямой баг/копипаста

---

### B) Большой рефакторинг (окупится быстро)

3. **Свести CRUD-hooks в генераторы**

   * у тебя уже есть мощные примитивы в `@repo/query` (optimistic, keys, invalidateMany).
   * осталось перестать копировать болванки хуков на каждую сущность.

4. **Систематизировать пакеты `@repo/api`**

   * разделить server/db/contracts/client (см. выше).

---

### C) Нормальные “подкрутить гайки”

5. **ESLint правило про queryKey перенести в общий конфиг**

   * сейчас оно только в admin.
