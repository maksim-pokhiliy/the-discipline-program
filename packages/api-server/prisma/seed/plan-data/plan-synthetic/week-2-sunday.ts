import type { CanonicalDay } from "../canonical-schema";

import { LBL } from "./refs";

export const DAY_WK2_SUN: CanonicalDay = {
  dayOfWeek: "SUNDAY",
  label: LBL.restDay,
  notes: null,
  sessions: [],
};
