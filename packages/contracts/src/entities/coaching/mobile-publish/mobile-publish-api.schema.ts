import { publishMobileResultSchema, publishMobileSchema } from "./mobile-publish.schema";

export const publishMobileRequestSchema = publishMobileSchema;
export const publishMobileResponseSchema = publishMobileResultSchema;
