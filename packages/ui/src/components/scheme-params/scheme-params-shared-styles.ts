import { type SxProps, type Theme } from "@mui/material/styles";

export const ADD_BUTTON_SX: SxProps<Theme> = { borderStyle: "dashed", borderWidth: 2 };

export const ITEMS_STACK_SX: SxProps<Theme> = { width: "100%", alignItems: "start" };
