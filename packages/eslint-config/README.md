# `@repo/eslint-config`

Collection of internal ESLint configurations consumed by every workspace package and app via `extends`. Pure config — no runtime code.

Update by editing the rule files in this package; consumers pick up changes on the next lint run.
