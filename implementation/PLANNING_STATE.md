# Planning State

> Last updated: 2026-05-18 (**Step 7.2 closed** — Platform HTTP routes для Block (4 route files mirror Step 6.4.5 Session pattern) + `handlePrismaError` ZodError DB-corruption defence (closes QA-003 carry-forward from Step 7.1). **4 commits** на `feat/training-domain` (`89f81e9c..bdb88fc1`): handler patch + 2 route commits + docs. 4 новых + 2 правки = 6 файловых касаний across `apps/platform/` + `packages/api-server/utils/`. id-addressed routes at `.../[planId]/blocks/[blockId]/...` (sessionId-free per OQ-a A1); assignLabels dedicated route file PUT (OQ-b B1); QA-003 fix landed в api-server NOT api-routes (OQ-c C1 — preserves existing `handleApiError` ZodError → 400 path для route-layer payload validation). Verifications all-green: `pnpm check-types` 16/16, `pnpm lint` 16/16, `pnpm --filter @repo/api-server test` 583/583 (581 → +2 prisma-error-handler), `pnpm test` 1068/1068 (1066 → +2), `pnpm dep:check` 0/1173 (+4 modules exact match). Stage 5 reviewer **APPROVED** (0 CRITICAL / 0 WARNING / 1 INFO — D-1 z-only import narrowing per manifesto 2.3). **Zero § 0 escalations; zero new planner-discipline flavours — tenth cleanest run в ряд** (6.3→6.4→6.4.5→6.5→6.6→6.7→7.0→7.1→7.2). 4 D-decisions all minor; 1 paper note (acceptance #15). Branch-cut override honored without surface. 1 transient Neon flake re-run green per `[[neon-dev-direct-url]]`. Husky pre-commit + commit-msg + pre-push clean all 4 commits без `--no-verify`. **Previous: Step 7.1 closed** — api-server `lmsBlockApi` (CRUD + reorder + assignLabels M:N) + `verifyBlockOwnership` guard + `mapToBlock` mapper shipped under `/feature` full pipeline (first invocation under `[[always-via-feature-skill]]`). 6 commits на `feat/training-domain` (`2c9b6d6a..8cf42432`): per-phase 3 atomic (guard → mapper → endpoint) + 2 review-fix (Stage 5 REV-001 helper extract + Stage 6 QA-002 concurrent test tightening) + 1 docs. 4 новых + 4 правки = 8 файловых касаний внутри `@repo/api-server`. 29 integration tests (25 admin + 4 guards). Verifications all-green: `pnpm check-types` 16/16, `pnpm lint` 16/16, `pnpm test` 1066/1066 (+30 от 1036), `pnpm --filter @repo/api-server test` 581/581 (552→581 exact +29 match), `pnpm dep:check` 0/1169 (+4 modules exact match). Stage 5 reviewer APPROVED iter2; Stage 6 QA APPROVED (0 CRITICAL / 3 WARNING / 22 INFO). **Zero § 0 escalations, zero new planner-discipline flavours — ninth cleanest run в ряд**. D-decisions all minor: D-1 helper typing via `Omit<typeof prisma, ...>` deny-list (extended Prisma client compat), D-2 Schema-cascade test via `archetype.findFirst` graceful skip, D-3 concurrent test winner-identification tightening (per QA-002), D-4 extra case 8 для planId mismatch defence-in-depth, D-5 `satisfies Prisma.BlockInclude` annotation. 1 paper deviation accepted (acceptance #15: contract types narrowed via Vitest method signature вместо explicit imports — zero functional impact). 10 deferred carry-forwards (5 new from Stage 6 QA: QA-001 `@@unique([sessionId, order])` schema constraint surface before Step 8, QA-003 router middleware для ZodError Step 7.2 prompt concern, QA-006 head-coach+archived composition test optional, QA-019 D-7 outcome-only test acceptable, QA-022 TxClient Omit fragile to Prisma upgrades; 5 pre-existing). Branch-cut override honored without surface. Husky pre-commit + commit-msg clean всех 6 commits без `--no-verify`. **Previous: Step 7.0 closed** — first sub-step of Step 7 (Block-level operations) decomposition; pure additive contract slice + cross-entity VOs. 2 atomic commits on `feat/training-domain` (`a3bcef6e` code + `523e16d9` docs), 16 files / +819/−1 LOC. First `/feature small` invocation under `[[always-via-feature-skill]]` rule; branch-cut override honored without surface. Stage 5 reviewer APPROVED (2 WARNING actioned/accepted, 3 INFO actioned/flagged). Verifications all-green: `pnpm check-types` 16/16, `pnpm lint` 16/16, `pnpm test` 1036/1036 (+66 Step 7.0 + ~12 baseline drift since 958), `pnpm dep:check` 0/1165. **Zero § 0 escalations, zero new planner-discipline flavours — eighth cleanest run in a row**. 7 D-decisions all minor: D-1 inline reviewer-fix (test naming clarity for refine-vs-strict distinction), D-2 pre-shipped Step-7.5 affordances в `_shared`, D-3 mock data fixup (applicableLevels typo caught pre-commit), D-4 test file LOC scaling artifact accepted, D-5 planner off-by-one in grep arithmetic, D-6 refine post-strip semantics documented inline, **D-7 Step 7.1 server-handler invariant flagged** (`labelIds: []` = tx delete-all + zero insert — must land в Step 7.1 prompt § 0 / § 3). **Next: Step 7.1 thesis cycle — api-server `lmsBlockApi` (CRUD + reorder + assignLabels + retryOnP2034 wrap + verifyBlockOwnership guard); `/feature` full pipeline (server slice scope + 15-20 integration tests).**)

## Workflow goal

End-to-end coach happy path:

1. Domain model from `analysis/` ported into `packages/api-server/prisma`.
2. CRUD tables for catalogs (Exercise, Label, Archetype, possibly more) in `apps/admin`, following existing module conventions.
3. Plan editor in `apps/platform/src/modules/plan-detail/` with week-based navigation.
4. Coach can: create catalog entities in admin → create plan in platform → program a week with realistic sessions → data persists per Prisma schema.
5. Athlete flow NOT built. Acceptable to extend Prisma model with athlete-facing entities and seed minimum data so smoke-test paths are not broken.

## Current step

**Step 7.2 closed (2026-05-18)** — Platform HTTP routes для Block + `handlePrismaError` ZodError DB-corruption defence shipped — third sub-step of Step 7 decomposition; first HTTP user-visible surface для Block (через route handlers, не UI пока). **4 commits на `feat/training-domain`** (`89f81e9c..bdb88fc1`): `89f81e9c feat(api-server): map mapper-side zoderror to internalservererror with dbcorruption kind` → `2bae5b21 feat(platform): add http routes for block create and reorder` → `8149d405 feat(platform): add http routes for block update delete and assignlabels` → `bdb88fc1 docs(step-07.2):...`. **4 новых + 2 правки = 6 файловых касаний** across two packages: new `apps/platform/.../[planId]/sessions/[sessionId]/blocks/{route.ts, reorder/route.ts}` + `apps/platform/.../[planId]/blocks/[blockId]/{route.ts, labels/route.ts}`; edits `packages/api-server/src/utils/prisma-error-handler.{ts (+8 LOC ZodError branch verified line 50-60), test.ts (+35 LOC 2 cases)}`. Each route ≤30 LOC thin wrapper над `lmsBlockApi`: `withCoachAuth + withAuthRateLimit(RATE_LIMIT_TIER.API) + createAuth*Handler(blockBy*ParamsSchema, request, response) + lmsBlockApi.METHOD(userId, ...params, data)`. **id-addressed Block routes at `.../[planId]/blocks/[blockId]/...`** (sessionId-free per OQ-a A1 ratification; mirror Session id-addressed precedent at top level — `verifyBlockOwnership` chain handles full block → session → day → week → plan JOIN). **assignLabels dedicated route file with PUT** (per OQ-b B1; not multiplex PATCH; mirror Session reorder sub-resource pattern). **QA-003 fix landed в api-server, NOT api-routes** (per OQ-c C1 ratification; preserves existing `handleApiError` ZodError → 400 path для route-layer payload validation; mapper-side ZodError now intercepted via `handlePrismaError` → `InternalServerError({kind: "DbCorruption", entity, issues})` → AppError path → 500 with structured details). `git diff 71803bb0..HEAD packages/api-routes/` empty — confirmation api-routes untouched. **`/feature small` Stage 5 Review APPROVED** (0 CRITICAL / 0 WARNING / 1 INFO — D-1 z-only import narrowing per manifesto 2.3 unused-imports rule). Root verification all-green: `pnpm check-types` 16/16, `pnpm lint` 16/16, `pnpm --filter @repo/api-server test` 583/583 (581 baseline + 2 new prisma-error-handler — exact match), `pnpm test` 1068/1068 (110 files, 1066 → 1068 = +2 prisma-error-handler), `pnpm dep:check` 0 violations / 1173 modules (+4 = exact match для 4 новых route files). **Zero § 0 STOP-and-surface escalations** — все 11 verbatim quote sections § 0.1-0.11 byte-for-byte matched HEAD `71803bb0` (zero-state grep clean: 0 hits для `lmsBlockApi` / `@repo/contracts/lms/block` / `ZodError` в expected loci). **Zero new planner-discipline flavours surfaced** — six existing checklists held the bar. **Tenth cleanest run в ряд** (Step 6.3 → 6.4 → 6.4.5 → 6.5 → 6.6 → 6.7 → 7.0 → 7.1 → 7.2). Branch-cut override honored без surface (no `feat/<slug>` cut). 1 transient Neon connection flake at api-server run-time — isolated re-run 19/19 green per `[[neon-dev-direct-url]]` known infra surface; не a code regression. 4 D-decisions all minor (D-1 `prisma-error-handler.test.ts` imports `{ z }` only — `ZodError` unused в test bodies, `safeParse.error` flow narrows via method chain; D-2 reorder route single-line `.then` chain — AST-equivalent с Session multi-line, lint-staged-stable; D-3 per-phase 3 atomic commits not squash — handler signature unchanged, no broken trees, no husky-squash trigger needed; D-4 2 new test cases appended end of describe block — non-intrusive placement). 1 paper note (acceptance #15: 15 commits range шире чем expected only-Step-7.x — includes 7.0 + 7.1 + planning closes; all clean no foreign refs). 5 deferred carry-forwards (all pre-existing from Step 7.1: QA-001 schema constraint surface before Step 8, QA-006 head-coach+archived composition optional, QA-019 D-7 outcome-only accepted, QA-022 TxClient Omit fragile к Prisma upgrades; QA-003 — **CLOSED via Step 7.2 Phase 1 patch**, removed from deferred list). Husky pre-commit + commit-msg + pre-push clean all 4 commits без `--no-verify` / `--no-edit` / `--no-gpg-sign`; subjects ≤80 chars fully lowercase. Output: `implementation/step-07.2/output.md`. Feature-dev: `.feature-dev/1779088244/` (`research.md` + `tasks.md` + `review.md` — `/feature small` pipeline; second `/feature small` invocation в workflow после Step 7.0).

**Step 7.1 closed (2026-05-18)** — api-server `lmsBlockApi` slice + `verifyBlockOwnership` guard + `mapToBlock` mapper shipped — second sub-step of Step 7 decomposition; first consumer of Step 7.0 Block contracts. **6 commits на `feat/training-domain`** (`2c9b6d6a..8cf42432`): `2c9b6d6a feat(api-server): add verifyblockownership guard for block ownership chain` → `0c0eb538 feat(api-server): add lms block mapper with embedded labels and intensity timecap parse` → `44945d92 feat(api-server): add lmsblockapi with crud reorder and assignlabels m:n` → `77870ccf fix(api-server): extract assertlabelsapplicable helper and add schema-cascade assertion` (Stage 5 REV-001 + REV-002 fix-loop) → `65035943 test(api-server): tighten concurrent assignlabels case to identify winner per qa-002` (Stage 6 QA-002 inline-fix) → `8cf42432 docs(step-07.1):...`. **4 новых + 4 правки = 8 файловых касаний** внутри `@repo/api-server`: new `endpoints/lms/block/{admin.ts (289 LOC), admin.test.ts (851 LOC), index.ts (1 LOC)}` + `mappers/lms/block.mapper.ts (33 LOC)`; edits `authz/guards.ts (+67 LOC verifyBlockOwnership) + guards.test.ts (+78 LOC 4 cases)` + `endpoints/lms/index.ts (+1)` + `mappers/lms/index.ts (+1)`. `lmsBlockApi` 5 методов mirror `lmsSessionApi` Step 6.1: `create(userId, planId, sessionId, data)` (outer `verifySessionOwnership` + planId match assertion + `verifyPlanEditable`; intra-tx Plan TOCTOU + Session existence + assertLabelsApplicable helper + `_max(order)+10` sparse-int + Block.create + conditional `createMany` BlockLabelAssignment с (i+1)*10; **wrapped в `retryOnP2034`** per Step 6.4.5 precedent); `update(userId, blockId, data)` (`verifyBlockOwnership`; **OQ-b1: silently ignores `labelIds` payload** — only intensity/timeCap/notes conditional-spread с `Prisma.JsonNull` для null clearing); `delete(userId, blockId)` (cascade Block → BlockLabelAssignment[] + Schema[] per FK onDelete: Cascade); `reorder(userId, planId, sessionId, data)` (mirror Session.reorder: complete-set + foreign-session + non-existent checks + array-tx `(i+1)*10`recompute);`assignLabels(userId, blockId, data)`(intra-tx`assertLabelsApplicable`+ **D-7: unconditional`deleteMany({where:{blockId}})`+ explicit`if (data.labelIds.length > 0)`short-circuit`createMany`** — verified `admin.ts:265-273`+ case 20`683-718`outcome: before=3→assignLabels([])→after=0; **wrapped в`retryOnP2034`** для unique-key SSI contention). `verifyBlockOwnership`returns`{status, sessionId, dayId, weekId, planId}`(extra`sessionId`vs Session guard — для Step 8 Schema chain reuse).`mapToBlock`parses`intensitySchema.parse`+`timeCapSchema.parse`на read (defensive-corruption surface;`b.intensity === null ? null : intensitySchema.parse(b.intensity)`); `mapToBlockWithLabels`extends +`[...b.labelAssignments].sort((a,x) => a.order - x.order).map(mapToLabel)`(defensive shallow copy + stable sort). 29 integration tests: 9 create (incl. case 9 concurrent + case 8 D-4 planId-mismatch defence-in-depth) + 4 update (incl. case 11 D-OQ-b1 verifying silent labelIds-ignore) + 2 delete (incl. case 13 D-2 cascade с`archetype.findFirst`graceful skip) + 4 reorder + 6 assignLabels (incl. case 20 D-7 invariant + case 21 OQ-c1 delete-then-insert + case 25 D-3 concurrent winner-identification tightened). Root verification all-green:`pnpm check-types`16/16 (12 cached),`pnpm lint`16/16 (12 cached),`pnpm test`1066/1066 (110 files; +30 от 1036 baseline = +29 new + 1 counting drift),`pnpm --filter @repo/api-server test`581/581 (73 files; 552→581 exact +29 match),`pnpm dep:check`0/1169 (+4 modules exact match для 4 новых файлов в planner range [3-5]). **Zero § 0 STOP-and-surface escalations** — все 11 verbatim quote sections § 0.1-0.11 byte-for-byte matched HEAD`11296bdf`. **Zero new planner-discipline flavours surfaced** — six existing checklists held the bar. **Ninth cleanest run в ряд** (Step 6.3 → 6.4 → 6.4.5 → 6.5 → 6.6 → 6.7 → 7.0 → 7.1). 5 D-decisions all minor (D-1 helper typing via `Omit<typeof prisma, "$connect" | "$disconnect" | "$on" | "$transaction" | "$use" | "$extends">`deny-list — extended Prisma client compat, не raw`Prisma.TransactionClient`; D-2 Schema-cascade via `archetype.findFirst`graceful skip; D-3 concurrent assignLabels test winner-identification per QA-002; D-4 extra case 8 planId mismatch coverage; D-5`satisfies Prisma.BlockInclude`annotation Stage 5 REV-007 inline). 1 paper deviation accepted (acceptance #15: contract types narrowed via Vitest method signature вместо explicit imports — functional impact zero). 10 deferred carry-forwards (5 new from Stage 6 QA — see § "Deferred sub-decisions" + § "Carry-forwards"; 5 pre-existing). Stage 5 reviewer **APPROVED iter2**; Stage 6 QA **APPROVED for Stage 7** (25 attacks / 0 CRITICAL / 3 WARNING / 22 INFO; WARNING breakdown: QA-001 schema-constraint absence latent → flag before Step 8, QA-002 inline-fixed via`65035943`, QA-003 router middleware → Step 7.2 prompt concern). Branch-cut override honored без surface (no `feat/<slug>`cut). Husky pre-commit + commit-msg clean all 6 commits без`--no-verify`; subjects ≤87 chars fully lowercase. Output: `implementation/step-07.1/output.md`. Feature-dev: `.feature-dev/1779080660/`(research / design / plan / review / review-iter2 / qa — first`/feature`full pipeline под`[[always-via-feature-skill]]`).

**Step 7.0 closed (2026-05-18)** — Block contract slice + cross-entity VOs `intensitySchema` + `timeCapSchema` shipped. **2 atomic commits** на `feat/training-domain` (`a3bcef6e feat(contracts): add lms/block slice with intensity and timecap shared vos` + `523e16d9 docs(step-07.0):...`). 16 files / +819/-1 LOC (12 new + 4 modified registration). First sub-step of Step 7 decomposition; first `/feature small` invocation under `[[always-via-feature-skill]]` rule (branch-cut override honored, no `feat/<slug>` cut). `lms/block/` 8 files mirror Session slice 1:1: `block.constants` (`MAX_NOTES_LENGTH=2000`, `MAX_LABELS_PER_BLOCK=10`), `block.schema` (`blockSchema` с embedded `labels: Label[]`, `createBlockSchema` с optional intensity/timeCap/notes/labelIds, `updateBlockSchema` alias, `reorderBlocksSchema`, `assignBlockLabelsSchema` empty-array-valid = clear-all per OQ-2A/D-7), `block-api.schema` (`blockBySessionParamsSchema {planId, sessionId}` + `blockByIdParamsSchema {planId, blockId}` + 4 wrapper request/response pairs), 3 test files (28 + 13 + ... cases). `lms/_shared/` extended: `intensity.ts` (5-dim Intensity Zod with refine at-least-one + 4 sub-schemas + 5 `as const` tuples + standalone `rpeSchema`/`paceSchema` per D-2 Step-7.5 affordance pre-shipping), `time-cap.ts` (`{min, max?, unit}` с refine max>min + `TIME_CAP_UNITS as const`), 16 + 9 test cases. Registration: `_shared/index +2`, `lms/index +1` (alphabetic `./block` between `./_shared` and `./day`), `package.json` exports `+1` (`./lms/block`), `README` LMS bullet (append `block` + Intensity/TimeCap shared primitives). Root verification all-green: `pnpm check-types` 16/16, `pnpm lint` 16/16, `pnpm test` 1036/1036 (+66 Step 7.0 + ~12 baseline drift since 958/929 family — counting noise per Step 6.6 reconcile; zero regression в существующих тестах), `pnpm dep:check` 0/1165 (+12 modules — exact match для 12 new files). `/feature small` Stage 5 reviewer **APPROVED** (2 WARNING — D-1 actioned inline test rename + positive strip case, D-4 accepted scaling artifact 323-LOC; 3 INFO — D-2 pre-shipped exports, D-6 refine post-strip documented, D-7 server-handler invariant flagged for Step 7.1). **Zero § 0 STOP-and-surface escalations** — все verbatim quotes § 0.1-0.6 совпали byte-for-byte с HEAD `a85eff4b`. **Zero new planner-discipline flavours surfaced** — eighth cleanest run в ряд. 7 D-decisions all minor (no architectural deviations). 8 deferred carry-forwards (1 new: D-7 Step 7.1 invariant; 7 pre-existing Step 7.1-8.0 progression). Husky pre-commit + commit-msg clean both commits без `--no-verify`. Output: `implementation/step-07.0/output.md`. Feature-dev: `.feature-dev/1779009158/` (`research.md` + `review.md`).

**Step 6.7 closed (2026-05-17)** — Session body shipped. **5 atomic per-layer code commits + 1 docs** `2ab958ea..daf6b293` на `feat/training-domain`: `chore(platform): add dnd-kit deps for session reorder` → `feat(ui): add generic labelselect autocomplete component` → `feat(platform): add useblurcommit hook for shared blur-commit text fields` → `refactor(platform): adopt labelselect+useblurcommit in plan-detail components` (subject shortened 104→76 chars per D-2 commitlint fixup без bypass) → `feat(platform): add session body with sessioncard list and dnd-kit reorder` (Phases 5+6+7+8 atomically). 19 files / +741/−122 LOC. Second UI surface workflow's, second consumer of Step-6.5 hooks. **Replace** `<Typography>No sessions</Typography>` placeholder в DayRow реальным `SessionList` (`DndContext` + `SortableContext` + Pointer/Keyboard sensors + verticalListSortingStrategy + optimistic local `sortedSessions` с `onError: setSortedSessions(previousOrder)` rollback per OQ-D D1 + `useEffect([sessions])` invalidate-resync) с N × `SessionCard` (per Session: dnd-kit `useSortable` с `disabled` while in-flight, drag-handle IconButton `cursor:grab+touchAction:none` per D-4, `SessionLabelSelect` + `SessionNotesField` + trailing kebab `Menu` → `ConfirmationModal` Delete как siblings внутри dnd Box per D-3 + plan-action-menu precedent) + `AddSessionButton` (instant-create через `useCreateSession.mutate({})` per OQ-F F1; server stores `{labelId:null, notes:null, order=max+10}`). **Concurrent cross-package extracts** (per OQ-A A1 + OQ-B B1 oба triggered): new `packages/ui/src/components/label-select/index.tsx` (73 LOC generic) + new `apps/platform/src/lib/hooks/use-blur-commit.ts` (52 LOC byte-equivalent extract from WeekNotes/DayNotesField); Phase 4 refactor existing 3 callsites (WeekNotes 68→36 + DayNotesField 62→32 + DayLabelSelect 69→28 LOC, behaviour-preserving). New SessionLabelSelect + SessionNotesField — thin wrappers. PlanDetailView adds 2nd `useLabelSearch({level:"SESSION"})` call; options drill 4 levels (PlanDetailView → WeekGrid → DayRow → SessionList → SessionCard) per OQ-C C1. **First `@dnd-kit/*` install в repo** (3 catalog entries `core ^6.3.1` + `sortable ^10.0.0` + `utilities ^3.2.2` per D-1; sortable bumped к ^10 vs prompt's ^8 hypothesis = current stable). Root verification all-green: `pnpm install` OK (+4 packages: 3 dnd-kit + 1 transient), `pnpm check-types` 16/16, `pnpm lint` 16/16, `pnpm test` 958/958 (zero test file deltas — 929→958 drift = counting noise across executor sessions per Step 6.6 reconcile, real baseline preserved), `pnpm dep:check` 0/1153 (2 modules ниже planner range [1155-1180] per D-7 — 0 violations preserved, critical AC hit). Browser smoke-test **13/13 green** (2026-05-17): 7-row render с "+ Add session" + zero SessionCards (нет placeholder); single SESSION-label preload GET; instant-create → empty SessionCard с drag-handle + Autocomplete + multiline notes + kebab; label select PUT + invalidate; notes blur-commit с trim+null-on-empty; F5 persistence; 2nd card create → drag-reorder с optimistic local + PUT + toast; F5 order persists; kebab→Delete→ConfirmationModal→Delete → DELETE + card removed; network-offline graceful degrade + eventual consistency on restore. Zero UI regression — Step 5 calendar viewport + WeekNotes (post-Phase-4 refactor) + Step 6.6 DayLabelSelect/DayNotesField (post-Phase-4 refactor) все intact под extracts. **Zero § 0 STOP-and-surface escalations** — все verbatim quotes § 0.1-0.9 matched HEAD `cfa4a792` byte-for-byte. **Zero new planner-discipline flavours surfaced** — seventh cleanest run в ряд. 7 D-decisions all minor: D-1 dnd-kit version pinning (sortable ^10 vs ^8 hypothesis), D-2 commitlint header fixup без bypass, D-3 Menu/Modal siblings layout per plan-action-menu canon, D-4 touchAction:none drag handle dnd-kit idiom, D-5 lint-fix import reorder (5th of family), D-6 test count drift counting noise, D-7 dep:check off-by-2 module accounting. 7 deferred carry-forwards (no new follow-ups identified). **Process rule reversal**: direct-exec convention (Step 6.4.5 D-1 → 6.5 → 6.6) reverted by user mid-Step-6.7 — saved `[[always-via-feature-skill]]` memory entry; from Step 7 onwards prompts wrap в `/feature` или `/feature small`. WORKFLOW.md per-step cycle § 4 already aligned. Не a new planner-discipline flavour (a-f) — process-level lesson. Husky pre-commit + commit-msg clean all 6 commits без `--no-verify`. Output: `implementation/step-06.7/output.md`.

**Step 6.6 closed (2026-05-17)** — DayRow header reshape shipped. **1 atomic per-layer commit** `8c08c218 feat(platform): reshape day-row header with label autocomplete and notes field` (6 files / +237/−17 LOC, single-package scope = `apps/platform/`, no `[[husky-cross-package-squash]]` trigger). Plus `f11e3e7d docs(step-06.6): write executor output report`. First production UI surface workflow's since Step 5 plan-detail shell. 2 new presentational components — **DayLabelSelect** (69 LOC, `Autocomplete<Label>` per `coach-owner-autocomplete` precedent §0.4 + slotProps + CircularProgress + conditional-spread for `exactOptionalPropertyTypes: true`) + **DayNotesField** (62 LOC, multiline `TextField` с blur-commit invariant — verbatim mirror `WeekNotes.commit()` block: committedRef + isFocusedRef + useEffect-sync-when-unfocused + trim + null-on-empty + client cap `DAY_CONSTANTS.MAX_NOTES_LENGTH=2000`). **DayRow** (49 → 95 LOC) owns 2 mutation hooks + 3-row stacked layout (weekday-header → responsive `direction={{xs:"column", md:"row"}}` label/notes row → "No sessions" placeholder). **WeekGrid** signature extended `{planId, monday, days, labelOptions, labelOptionsLoading}`; iteration inverted to `dayOfWeekValues.map((dayOfWeek, idx) => { const date = dates[idx]; if (!date) return null; ... })` (D-1) to satisfy `noUncheckedIndexedAccess: true` без `!` per `[[type-quality]]`. **PlanDetailView** calls `useLabelSearch({level:"DAY"})` once at view-mount; drills options + isLoading + planId + `weekData?.days ?? []` to WeekGrid (single source of truth per OQ-A A1; TanStack cache `["labels","search","DAY",undefined]` stable through week-navigation). Root verification all-green: `pnpm check-types` 16/16, `pnpm lint` 16/16, `pnpm test` 929/929 (no test deltas — `git diff 56f3802e..HEAD -- '**/*.test.*'` returns zero touched files; Step 6.5 entry's 958/958 was counting-method noise, real baseline = 929), `pnpm dep:check` 0/1146 (+2 modules exact match to 2 new files). Browser smoke-test **12/12 green** (2026-05-17): 7-row render, label preload via single GET, select+clear+F5 persistence, notes blur-commit с trim+null-on-empty + breadcrumb-stay per D7, network-offline graceful degrade, week-navigation cache preservation, zero UI regression. **Zero § 0 STOP-and-surface escalations** — все verbatim quotes § 0.1-0.7 matched HEAD `50d83f66` byte-for-byte. **Zero new planner-discipline flavours surfaced** — sixth cleanest run в ряд (Step 6.3 → 6.4 → 6.4.5 → 6.5 → 6.6). 5 D-decisions minor: D-1 narrow approach pre-anticipated, D-2 ESLint curly auto-fix family of 4 previous, D-3 single-hook location per OQ-A A1, D-4 React.FC mixed convention per-folder, D-5 `||` over `=== true` per coach-owner precedent. 4 deferred (ZWS strip OQ-C, @repo/ui LabelSelect extract OQ-B, useBlurCommit primitive 3rd-surface trigger, lazy fetch UX-feedback trigger). Husky pre-commit clean both commits без `--no-verify`. Output: `implementation/step-06.6/output.md`.

**Step 5 merged** (2026-05-15) — PR #191 `feat: training-domain port (schema, admin catalogs, plan-detail shell)` fast-forward-merged into `main`. 51 commits, Steps 1-5 batched. `feat/training-domain` deleted on `origin`; recreated locally from fresh `main` for Step 6.x work. Step-5-focused suites (contracts, ui, lms api-server) all green; full-suite IAM flake (Neon connection-pool exhaustion under serial-run) noted as pre-existing follow-up — not Step 5.

**Step 6 decomposed** into 8 atomic sub-steps after a thesis-revision cycle that surfaced two product-level corrections (see § "Decisions accepted" D7 + § "Deferred sub-decisions" Q10 / Session.name). Original Step 6 ("Day-level operations: add/edit/reorder/delete + Day.label autocomplete") was rejected by user: Day is a lazily-materialized calendar slot just like Week (D7), so no Day add/remove/reorder UX exists. Coach POV is **session-level operations + day metadata side-channel**, not Day CRUD. Decomposition (see § "Step queue") splits the work into one contract slice → one api-server slice → one day-metadata slice → one platform label mirror → routes → hooks → 2 UI sub-steps (header metadata, then session cards + reorder). Each sub-step = 1 prompt = 1 `/feature` session.

**Step 6.1 closed** (2026-05-15). HEAD on `feat/training-domain` past `e1f92e06 docs(step-06.1): write executor output report`. 6 commits since Step 6.0 close-out (`a580cf4b..e1f92e06`): `refactor → feat → test → fix (QA-001/002/004) → test (QA-003) → docs`. 8 new files (`_shared/date{,.test}.ts`, `_shared/index.ts`, `session/admin{,.test}.ts`, `session/index.ts`, `session.mapper.ts`) + 4 additive edits (`week/admin.ts` net −20/+1 after refactor + stranded-import cleanup, `endpoints/lms/index.ts`, `mappers/lms/index.ts`, `authz/guards.ts`); cumulative +1204/−20. **504/504 tests across 69 files green**, including 6 unit cases for `_shared/date.ts` + 14 integration cases for `session/admin.ts`. `TZ=Asia/Kolkata` proof 14/14 in 14.61s; T17 dev-time revert proved the TZ test is not vacuous (cases #4/5/8/11 fail without `Date.UTC(...)` anchor). `pnpm dep:check` 0/1112; root `check-types` + `lint` 16/16. `freezeLoadsAtCreation` and `Session.name` grep regressions: 0 matches in api-server `src/`. `verifySessionOwnership` guard returns `{ status, dayId, weekId, planId }` — reusable for Step 7 Block ownership chain.

Four planner-side miss'es surfaced and were resolved during execution; full anatomy in `IMPLEMENTATION_LOG.md` Step 6.1 entry + Lesson-learned addendum (instinct-engineering-naivety, fourth flavour):

1. **PROMPT-001 (Stage 4)** — commitlint `subject-case` rejected CamelCase identifier (`resolveWeekStartDate`) in the verbatim commit subject; ratified paraphrase to "date helpers" with helper names in body.
2. **QA-001 CRITICAL (Stage 6, ratified Fix-now)** — `reorder` validation specified subset-only, not complete-set; one-element subset silently produced duplicate `order=10`. Fixed by `prisma.session.count` length-mismatch check in `61bb3eeb`.
3. **QA-002 + QA-004 WARNING (Stage 6, ratified Fix-both-now)** — `create` transaction at default isolation race-able on `_max(order)`; outer `verifyPlanOwnership` racy with intra-tx plan state. Fixed by `Prisma.TransactionIsolationLevel.Serializable` + intra-tx plan re-check in `61bb3eeb`.
4. **QA-003 WARNING (Stage 7, ratified Yes-add-only-this)** — `delete` non-owner test missing; added in `ff8e8033`.

**Style deviation (ratified Stage 2)** — STYLE-001: `reorder` uses sequential `findUnique` awaits rather than the prompt's brittle nested-`await`-in-object-literal with `?? ""` fallback. Engineering naivety on planner side; documented in `[[planner-adversarial-review]]` memory + the four-flavours Lesson-learned addendum.

Deferred non-blockers (10 INFO findings): all routed to Step 6.4 HTTP-route layer or noted as cross-slice cleanup (array-length cap, identical error messages for week-missing/day-missing, P2034 retry, `PerformedSession` cascade regression-guard, etc.). Full list in `.feature-dev/1778843530/qa.md` + `IMPLEMENTATION_LOG.md` Step 6.1 entry.

(Step 5 detail and Step 6.0 detail live in `IMPLEMENTATION_LOG.md`. Not duplicated here.)

Four executor-resolved escalations through prompt-defined § 0 surface protocol and Stage 6 `AskUserQuestion`:

1. **CONTEXT-001 (Stage 1, planner)** — prompt § 3.3 quoted stale `lms/index.ts` / `package.json` exports map "current state" (omitted `plan-enrollment` and `./lms` root). Fixed in `6942b6cd fix(step-06.0): record actual lms registration state in prompt`; planner-discipline lesson recorded as third instinct-specing flavour (`instinct-registration-read`) in `IMPLEMENTATION_LOG.md` Lesson-learned addendum + durable form `feedback_planner_verbatim_registration` in memory.
2. **QA-001 (Stage 6, ratified Skip)** — int4 cap on `Session.order` rejected: codebase convention has no such cap, asymmetric to ratify here, future cross-slice cleanup.
3. **QA-002 (Stage 6, ratified Apply)** — duplicate-cuids defence-in-depth in `reorderSessionsSchema` accepted: `.refine((ids) => new Set(ids).size === ids.length)` added per `cms/label` pattern. Committed as `44af3f5b fix(contracts): reject duplicate cuids in reorder-sessions schema`.
4. **QA-003 (Stage 6, ratified Skip)** — `startDate` regex calendar-validity tightening rejected: regex is byte-identical to Step 5's `weekByPlanAndDateParamsSchema`; cross-slice job out of scope.

All planner binds soldered: no `Session.name`, no `Session.freezeLoadsAtCreation`, no `@prisma/client` imports in contracts, no code comments, no `as any` / `as unknown` / unjustified `!`, no `--no-verify` / `--no-edit` / `--no-gpg-sign` bypasses (commitlint subject-case + body-max-line-length failures root-cause-fixed). Full executor report: `implementation/step-06.0/output.md`.

(Step 5 detail — `lms/week` slice, `lmsWeekApi`, `resolveWeekStartDate` UTC anchor, `InlineEditText`, `plan-detail` calendar viewport, OQ-D redirect, the three executor-caught planner bugs — is in `IMPLEMENTATION_LOG.md` § "Step 05 — Plan-detail shell" + the Lesson-learned addendum. Not duplicated here; this section now narrates Step 6.)

**PR #192 merged into `main`** 2026-05-15T17:17:30Z — `feat: training-domain port (sessions + lazy materialization)`, 16 commits batching Steps 6.0 + 6.1 (`5e4144bc..6f09a8a4`). `feat/training-domain` deleted on `origin`; recreated locally from fresh `main` for Step 6.1.5 work.

**Step 6.2 thesis cycle (2026-05-15, post-PR-#192 close-out)** — drafting `lmsDayMetadataApi` + `getWeekResponseSchema` 7-day extension surfaced one architectural gate: D7-ratified embed `label: Label | null` is blocked by `contracts-lms-no-coaching-cms-billing` (`.dependency-cruiser.cjs:47-58`) because `Label` contract lives in `packages/contracts/src/entities/cms/label/`. `Label` and `Exercise` are LMS catalogs by every other signal — Prisma `@@map("training_labels"/"training_exercises")`, FK targets on `Day`, `Session`, `Block`, `BlockLabelAssignment`, `SchemaRow`, `OneRMRecord`. Step 4 took CMS namespace without architectural review. **Step 6.1.5** ratified — pure refactor sub-step under `/feature small`, corrects this: ~20 files moved (contracts + api-server endpoints + api-server mappers), enum-maps split (CMS-pure stays in `mappers/cms/enum-maps.ts`; Exercise enums extract to new `mappers/lms/exercise.enum-maps.ts`), 34 consumer imports updated across apps/admin, dep-cruiser `admin-no-lms` carve-out widened (4 new pathNot entries). 4 atomic commits, zero schema/analysis/seed changes. After 6.1.5 close-out, Step 6.2 prompt-writing proceeds with embed cleanly.

**Step 6.2 thesis ratified parametrically** (awaiting 6.1.5 close-out before prompt-writing): embed `label: Label | null` on Day + each Session (OQ-A=A; unblocked by 6.1.5); breaking `getWeekResponseSchema` captures `useWeek` + `WeekNotes` consumer update in same session (OQ-B=A); Session.label `applicableLevels.includes("SESSION")` enforcement added to `lmsSessionApi.{create,update}` inline as Step 6.1 follow-up (OQ-C); setLabel(null) / setNotes(null) on unmaterialized Day = no-op (OQ-D); two operations `setLabel` + `setNotes`, not one partial (OQ-E); namespace `lms/day` (OQ-F).

**Step 6.1.5 closed (2026-05-16)** — D8 implementation shipped. 56 files / +173/−159 LOC in one squashed commit `5332c034`. 5 deliverables: contracts move (`cms/{label,exercise}` → `lms/{label,exercise}`, 14 files), api-server endpoints + mappers move (6 files), `mappers/cms/enum-maps.ts` split (155 → 47 LOC stays CMS-pure; 109 LOC new `mappers/lms/exercise.enum-maps.ts`), 34 admin-side consumer imports updated (27 contract sed + 7 surgical `@repo/api-server/cms` → `lms`), `.dependency-cruiser.cjs` `admin-no-lms` carve-out widened (4 new `pathNot` entries). Root verification all-green: `pnpm check-types` 16/16, `pnpm lint` 16/16, `pnpm test` 874/874 (no test deltas; api-server 504/504 isolated, contracts 160/160), `pnpm dep:check` 0/1113, 8 grep regression sweeps all-zero. **One executor escalation** through § 0 protocol: `.husky/pre-commit` (`turbo check-types --filter="...[HEAD]"`) blocked the prompt's 4-atomic-per-layer commit strategy because Commit 1 (contracts move) left api-server with broken `@repo/contracts/cms/label` imports; executor `AskUserQuestion`'d the user with `deprecation-shim` vs `squash` alternatives; user ratified **squash**, shipped as the single `5332c034`. Logical revertability preserved through commit-body subsections (per-layer logical grouping documented). This is the **5th-flavour** planner-discipline miss `instinct-process-blindness` (after a/b/c/d enumerated in IMPLEMENTATION_LOG Step 6.1 + 6.1.5 lesson addenda) — adversarial pass для commit-strategy против live hook config не было; durable rule saved as `[[husky-cross-package-squash]]` memory entry. One executor inline fix (Q2 / D-OUTPUT-3) — Phase 2 check-types caught 7 admin route handlers importing `cms{Label,Exercise}AdminApi` from `@repo/api-server/cms` package barrel (which the Phase 3 sed sweep did not cover, since prompt § 3.3.1 only handled `@repo/contracts/cms/X` paths); resolved via a second surgical sed filtered by symbol-presence (`cmsLabelAdminApi` / `cmsExerciseAdminApi`) so as not to disturb other CMS consumers. Deferred: **symbol rename** `cms{Label,Exercise}AdminApi` → `lms*` (out of scope per "byte-identical move" prompt § 4 invariant; estimated 7 admin routes + 2 api-server self-exports + 4 tests; schedule low-priority post-Step-6.x); **WORKFLOW.md addition** documenting husky-cross-package squash rule (planner housekeeping). Memory hygiene done: `project_training_domain_workflow.md:14` updated (cms → lms paths + `[[husky-cross-package-squash]]` cross-link).

**Step 6.2 closed (2026-05-16)** — D7 surface shipped. 11 new + 12 modified files in one squashed commit `19c57a66 feat(training-domain): 7-day week response with embedded label + day-metadata side-channel`. 7 deliverables: new `lms/day` contract slice (7 files — `day.{constants,schema,types,schema.test}.ts` + `day-api.{schema,types}.ts` + `index.ts`); `getWeekResponseSchema` extension `{ week } → { week, days: DayWithSessions[7] }` with embedded `label: Label | null` on Day + each Session (D7-ratified, unblocked by 6.1.5 D8); `lmsDayMetadataApi.{setLabel, setNotes}` with Serializable tx + intra-tx plan TOCTOU re-check + `applicableLevels.includes("DAY")` validation for setLabel + lazy week/day upsert + OQ-D no-op short-circuit on null-on-unmaterialized; `lmsWeekApi.getByPlanAndDate` extended to return 7-day shape via single Prisma query with nested include (N+1 prevention); OQ-C inline Step 6.1 follow-up — `applicableLevels.includes("SESSION")` enforcement in `lmsSessionApi.{create, update}`; `mapToDaySlot(dayOfWeek, day | null)` builder in `mappers/lms/day.mapper.ts`; consumer adapt — `useUpdateWeekNotes.onSuccess` swap `setQueryData` → `invalidateQueries` (would have dropped `days` from cache otherwise). Root verification all-green: `pnpm check-types` 16/16, `pnpm lint` 16/16, `pnpm test` 926/926 (+52 net new), `pnpm dep:check` 0/1124, `TZ=Asia/Kolkata` day-admin 15/15, regression greps (`Session.name`, `freezeLoadsAtCreation`, `cms/{label,exercise}`) all-zero, `lms/day` import presence 2 hits. **Two escalations resolved**: (1) **CONTEXT-001 / 6th-flavour planner-discipline miss** — `apps/platform/src/app/api/platform/training-plans/[planId]/weeks/[startDate]/route.ts:20-22` GET handler manually wrapped `lmsWeekApi.getByPlanAndDate` in `{ week: ... }`; after § 3.2.3 api shape change this would have double-wrapped `{ week: { week, days } }` failing `getWeekResponseSchema.parse(...)`; prompt § 4 implicitly claimed "handled by the existing route" without verbatim handler read. Executor caught via § 0 STOP-and-surface; user ratified inline-fix (1-line handler unwrap added 4th to Phase 3 scope). PUT handler in same file verified unchanged (`upsertNotes` returns scalar `Week` per `updateWeekNotesResponseSchema = weekSchema`). Durable rule `[[planner-consumer-pattern-read]]` saved. (2) **Spec § 3.2.5 case 13 deviation** — concurrent `setLabel`+`setNotes` on unmaterialized Day was spec'd as "both succeed; last write wins on each field"; reality: Postgres SSI rejects one upsert on same `(weekId, dayOfWeek)` unique key with P2034 (row-grained conflict detection, ignores disjoint UPDATE columns). Executor rewrote test to pre-materialize Day then `Promise.allSettled` two UPDATEs. Durable rule `[[postgres-ssi-upsert-unique-key]]` saved; production retry-on-P2034 at HTTP layer deferred to Step 6.4. **Decisions of execution (output.md D-1..D-6, captured for revisit)**: `DAY_OF_WEEK_TO_PRISMA` duplicated in 3 callsites (hoist deferred to Step 6.4 4th callsite trigger); `mapToSessionWithLabel` inline spread (extract deferred to Step 6.6 if WeekGrid wants); `invalidateQueries` not partial-merge in `useUpdateWeekNotes.onSuccess`; `mappers/lms/index.ts` strict alphabetical (`day.mapper` first, `enum-maps` second); `getWeekResponseSchema.days: z.array(daySlotSchema).length(7)` strict cardinality; CONTEXT-001 inline-fix; subject reflow to 91 chars (commitlint cap 100). Plus `db042e73 docs(step-06.2): write executor output report` (output.md).

**Step 6.3 closed (2026-05-16)** — `lmsLabelPlatformApi` read-only platform mirror shipped. 3 new + 3 edited files in **2 atomic per-layer commits** (additive only, no broken intermediates): `4aca6ea0 feat(contracts): add label search params schema for platform mirror` → `29901fe3 feat(api-server): add lms label platform api with structural label/index barrel`. Plus `c651927b docs(step-06.3): write executor output report`. Deliverables exactly per § 5 prompt: `labelSearchParamsSchema` added to existing `lms/label/label-api.schema.ts` (no alias for response — reuses `getLabelsResponseSchema`); `lmsLabelPlatformApi.list(userId, query?)` in new `endpoints/lms/label/platform.ts` (`requireCoachLikeRole` outer, optional case-insensitive substring `nameLower contains q.toLowerCase()`, sort `nameLower asc`, hardcoded `LABEL_SEARCH_CAP = 50`); structural symmetry fix — new `endpoints/lms/label/index.ts` barrel (was missing, asymmetric vs siblings), `endpoints/lms/index.ts` flip `./label/admin` → `./label`. 11 integration test cases (10 required + 1 bonus sort) cover authz (non-coach reject + COACH/HEAD_COACH/ADMIN allow), search behavior (no query, substring, case-insensitive, mid-word, no-match), cap enforcement (60→50), SQL-safe special chars, sort order. **Cleanest executor run yet — zero new planner-discipline flavours surfaced; independent reviewer APPROVED with zero CRITICAL/WARNING/INFO findings across 6 files / +265 LOC.** One minor pattern-aligned deviation: Prisma `findMany` uses `{ ...(query !== undefined && { where: ... }) }` conditional-spread instead of prompt's verbatim `where: query !== undefined ? {...} : undefined` — required by `exactOptionalPropertyTypes: true` (TS2379), pattern-aligned with sibling `admin.ts:16-23`. Same family as Step 5 deviation #2 (already documented); not a new flavour. Verifications: `pnpm check-types` 16/16, `pnpm lint` 16/16, `pnpm test` 937/937 (+11), `pnpm dep:check` 0/1127, api-server isolated 538/538. Grep regressions all-zero. Husky hooks clean both commits without `--no-verify`. Smoke-test N/A (api-server-only; UI consumers Step 6.6/6.7).

**PR #193 merged into `main` 2026-05-16T12:35:00Z** as commit `fcff61cd Merge pull request #193 from maksim-pokhiliy/feat/training-domain`. Batched **Steps 6.1.5 → 6.5** (32 commits `5332c034..f578eab8`); spans `cms→lms` namespace correction (6.1.5), 7-day Week response + day-metadata side-channel (6.2), labels-platform mirror (6.3), platform routes + P2034 retry infra (6.4), session HTTP routes + Session.create retry-wrap (6.4.5), platform client API + hooks (6.5). **117 files / +8624 −261 LOC**. Zero CI-blocking findings during PR review. `feat/training-domain` deleted on `origin` post-merge; recreated locally from fresh `main` (HEAD `fcff61cd`) per `[[training-domain-workflow]]` convention. Next planner action: Step 6.6 thesis (DayRow header reshape — first UI surface).

**Step 6.5 closed (2026-05-16)** — Platform client API + hooks for Sessions / DayMetadata / Labels shipped. **3 atomic per-layer commits**: `fe6b04bc feat(platform): add labels query key and client api endpoints for lms vertical slice` → `7536fe4a feat(platform): add hooks for sessions day-metadata and label search` → `56f3802e docs(step-06.5):...`. **7 new + 4 edited files**. Phase 1: `labels.search(level?, q?)` namespace added to `platformKeys` + `AppLevelValue` type-only import. Phase 2: 3 client API factories — `createSessionsAPI` (create/update/delete/reorder over 4 Session routes from Step 6.4.5), `createDayMetadataAPI` (setLabel/setNotes over 2 day-metadata routes from Step 6.4), `createLabelsAPI` (search over labels-search route from Step 6.4); `endpoints/index.ts` +3 alphabetic exports; **`api/index.ts` wired** mandatory — `createApi(client)` factory enumerates each API explicitly. Phase 3: `useWeekMutation<TVars, TResult>({mutationFn, planId, startDate, successMessage, errorMessage})` helper — mirrors `useUpdateWeekNotes` pattern, generalized for 6-fold week-invalidating mutations (DRY per OQ-A ratification). Phase 4: 3 hook files using the helper — `use-sessions.ts` (4 hooks), `use-day-metadata.ts` (2 hooks), `use-label-search.ts` (1 query hook, `{level?, q?, enabled? = true}` object-arg per OQ-C ratification, default empty arg = preload-no-filter coach use case); `hooks/index.ts` +4 exports. **Idempotency-Key** auto-generated by `ApiClient.prepareRequest:111-113` per non-idempotent request — zero hook-side opt-in needed (framework-resolved OQ). **`extractValidationMessage` confirmed N/A** в этом repo (admin-v4-scoped memory entry); pattern — direct `notifyError(error, "Failed to X")` per `@repo/query`. Root verification all-green: `pnpm check-types` 16/16, `pnpm lint` 16/16, `pnpm test` 958/958 (baseline preserved per OQ-B no-test ratification), `pnpm dep:check` 0/1144 (was 0/1137; +7 modules = exact 7 new files; planner range `[1140, 1145]` hit). **Zero § 0 STOP-and-surface escalations** — all verbatim quotes matched HEAD `08b4c71f` byte-for-byte. **Zero new planner-discipline flavours**; 3 D-numbered minor justifications captured (hooks/index.ts cosmetic ordering swap resolved per § 0.5 minimize-diff spirit; planner § 5.3 grep count off-by-one in implementation-correct direction; prettier-compacted `update` arm pre-ratified husky pipeline). Husky pre-commit clean all 3 commits без `--no-verify`. Smoke-test N/A (hook-layer; first scenario-based smoke arrives Step 6.6 DayRow surface).

**Step 6.4.5 closed (2026-05-16)** — `lmsSessionApi.create` retry-wrap + 4 Session HTTP routes shipped. **3 atomic per-layer commits**: `90bfa3fe feat(api-server): wrap session create transaction with p2034 retry` → `98e514ce feat(platform): add http routes for session crud and reorder` → `317af4c0 docs(step-06.4.5):...`. **3 new + 2 edited files**. Phase 1: 1-line wrap of `create`'s `prisma.$transaction(..., Serializable)` in `retryOnP2034(() => ...)` — second/final production callsite of helper born in Step 6.4 (first was day-metadata in `013f8319`). `update`/`delete`/`reorder` byte-identical (no P2034 surface per § 0.3 verification). Phase 2: +1 integration case at end of `describe("create")` block mirroring Step 6.2 case 13 pattern (pre-materialize Day → `Promise.allSettled` two concurrent creates → assert at-least-one-fulfilled + `storedCount === fulfilledCount`). **Closes Step 6.1 QA-002 concurrency-repro carry-forward**. Phase 3: 4 verb handlers across 3 new route files — POST `.../days/[dayOfWeek]/sessions/`, PUT `.../sessions/reorder/` (sub-resource action endpoint via `createAuthPutByParamHandler` per OQ-B ratification — `createAuthActionHandler` body-less doesn't fit, no PATCH factory exists in auth land), multiplexed PUT+DELETE `[planId]/sessions/[sessionId]/` (id-addressed per Step 6.0 `sessionByIdParamsSchema = { planId, sessionId }` design + `enrollments/[enrollmentId]/route.ts` precedent). Reorder wraps `Session[] → { sessions }` via `.then(...)` chain for ratified `reorderSessionsResponseSchema` wrap-object shape. Root verification all-green: `pnpm check-types` 16/16, `pnpm lint` 16/16, `pnpm test` 958/958 (+1, hit dead-center of planner [957, 959]), `pnpm dep:check` 0/1137, `TZ=Asia/Kolkata` session admin 19/19. **Zero § 0 STOP-and-surface escalations**, **zero new planner-discipline flavours surfaced**. 4 D-numbered minor justifications (executor flow direct vs `/feature small` wrapper, labelId pattern-align in test, lint-staged auto-collapse pre-ratified, stale planner test-count 14 vs actual 18 — last absorbed as adjacent refinement of `[[planner-verbatim-registration]]`: ground test-count citations in actual `grep -c '^  it('` at prompt-write time). Husky pre-commit clean all 3 commits без `--no-verify`. Smoke-test N/A (api-server + route-layer; UI Step 6.7).

**Step 6.4 closed (2026-05-16)** — vertical slice for day-metadata + labels-platform shipped: 3 platform HTTP routes + 4 bundled deferred follow-ups + companion 503-Retry-After infra. **7 atomic per-layer commits** (additive only, no broken intermediates, no squash needed per `[[husky-cross-package-squash]]` analysis): `a45a7dc3 feat(contracts):...` → `4cb417d2 feat(api-routes):...` → `4b52e32d refactor(api-server):...` → `b0b23ae4 feat(api-server):...` → `013f8319 feat(api-server):...` → `82fdbb7f feat(api-server):...` → `3fde2c26 feat(platform):...`. Plus `624212e6 docs(step-06.4):...`. **6 new + 10 edited files / +533 −149 LOC**. Deliverables exactly per § 5 prompt: (a) `labelSearchParamsSchema` gained `.trim()` transform on `q` + optional `level: appLevelSchema` field; (b) `api-routes/error-handler.ts` gained 4-LOC 503 `Retry-After` branch (independent `if`, mirrors existing 429); (c) `DAY_OF_WEEK_TO_PRISMA` hoisted from 3 callsites to new `endpoints/lms/_shared/day-of-week.ts` (closes Step 6.2 D-3); (d) new `retryOnP2034(fn, opts?)` helper in `packages/api-server/src/utils/` (2 default attempts + `[50, 200]ms` jitter, exhaustion throws `ServiceUnavailableError({ retryAfter: 5, lastErrorCode: "P2034" })`, non-P2034 errors rethrown immediately); (e) applied to both `lmsDayMetadataApi.{setLabel, setNotes}` `prisma.$transaction(..., Serializable)` calls — `lmsSessionApi.create` deferred to Step 6.4.5; (f) `lmsLabelPlatformApi.list` signature `query?: string` → `query?: LabelSearchParams`, where-clause conditional-spread filtering by `nameLower contains` + `applicableLevels array_contains`, `LABEL_SEARCH_CAP` 50 → 500 + exported; (g) 3 platform route files (`GET /api/platform/labels/search`, `PUT .../weeks/[startDate]/days/[dayOfWeek]/{label,notes}`). Root verification all-green: `pnpm check-types` 16/16, `pnpm lint` 16/16, `pnpm test` 957/957 (+20 from 937, planner range 950-960 hit dead-center), `pnpm dep:check` 0/1134 (+7 modules per expectation). Stage 5 reviewer **APPROVED** (0 CRITICAL / 0 WARNING / 1 INFO — D-1 dead-var drop per manifesto 2.2). Stage 6 hostile QA **APPROVED** (80 attacks / 0 exploitable / 5 INFO non-actionable). **Zero § 0 STOP-and-surface escalations** — all 8 § 0.x verbatim quotes matched HEAD `d882e2ff` byte-for-byte at research stage. **Zero new planner-discipline flavours surfaced** — six existing (`[[scope-via-existing-patterns]]`, `[[coach-pov-first]]`, `[[planner-verbatim-registration]]`, `[[planner-adversarial-review]]`, `[[husky-cross-package-squash]]`, `[[planner-consumer-pattern-read]]`) covered all observed failure modes proactively. **Zero structural deviations**; 5 D-numbered minor justifications captured in `output.md` (dead-var drop, test framing collapse, error-handler test skip confirmed, package-name CLI correction, lint-staged auto-format). Husky pre-commit clean всех 7 commits без `--no-verify`. Smoke-test N/A (api-server + route-layer step; UI consumers Step 6.6/6.7).

## Step queue (draft, refined per iteration)

> Granularity will likely change as we learn. Steps named with provisional titles.

- **Step 1** — Model ratification (no code). Apply 4 ratified decisions (Week, Athlete-derived, full-scope, library-vs-config split) into analysis-artifacts. Output: updated `analysis/artifacts/05-synthesis/{domain-model.md, er-diagram.md}` + `analysis/artifacts/06-formalization/{schema.prisma, er-final.md, implementation-notes.md, types.ts}`. No edits to `packages/api-server`. Likely NOT `/feature` — pure documentation/spec change.

- **Step 2** — Prisma schema port + Archetype seed + minimal user/plan seed. Bring ratified schema into `packages/api-server/prisma/schema.prisma`. Wire seed: 1 coach (User+CoachProfile), 1 athlete (User+AthleteProfile), 1 empty TrainingPlan, all 34 archetypes (full canonical set, derived from `analysis/artifacts/06-formalization/implementation-notes.md` archetype catalog). NO seed for Exercise/Label — user creates via admin UI. `pnpm --filter @repo/api-server db:reset` per ADR-0019; no versioned migrations during workflow.

- **Step 3** — Admin Exercise CRUD + Phase 0 D5 schema refinement. **COMPLETED** 2026-05-13 (HEAD `51302f93`). `apps/admin/src/modules/exercises/` shipped as canonical reference template for future catalog-library CRUD modules. Smoke-test scenario in `implementation/step-03/output.md`.

- **Step 4** — Admin Label CRUD. **COMPLETED** 2026-05-14 (HEAD `252d7323`). Structural mirror of Step 3 Exercise module + `applicableLevels` multi-value checkbox widget (`FormGroup` + 3 `Checkbox`, `fieldState`-subscribed per Step 3.1 regression guard). No schema change. Smoke-test scenario in `implementation/step-04/output.md`.

- **~~Step 5~~ — Platform plan list / create-plan flow. DROPPED 2026-05-14.** Found already implemented as pre-existing base LMS infrastructure (see § "Current step"). Queue renumbered below; old 6-12 → 5-11.

- **Step 5** (was Step 6) — Plan-detail shell (calendar viewport). **COMPLETED** 2026-05-15. `lms/week` contract slice, `lmsWeekApi` (read + lazy notes-upsert with UTC-midnight anchor), platform week API route + hooks, `@repo/ui` `InlineEditText` + additive `PageHeader` extension, `plan-detail` calendar-viewport module, create-plan redirect (OQ-D). Smoke-test scenario in `implementation/step-05/output.md`.

- **Step 6** (was Step 7 + Step 8 absorbed) — Day-level + Session-level operations + day-metadata side-channel + platform-side Label read-mirror. **Decomposed into 8 atomic sub-steps** (2026-05-15) after the user reframed Day per D7 (lazy calendar slot, no add/remove/reorder UX). Coach POV: tag day with one label, leave a note, then program sessions inside the day. Sub-steps:

  - **Step 6.0** — `Session` contract slice + new `entities/lms/_shared/` namespace (`dayOfWeekSchema` mirror). **COMPLETED** 2026-05-15. `packages/contracts/src/entities/lms/{_shared,session}/` shipped, 160 tests across 11 files, 0 violations. `Session.freezeLoadsAtCreation` and `Session.name` regression-guarded against silent drift. Output: `implementation/step-06.0/output.md`.
  - **Step 6.1** — `lmsSessionApi` (CRUD + reorder via sparse-order recompute) + extract `resolveWeekStartDate` to `endpoints/lms/_shared/date.ts`. **COMPLETED** 2026-05-15. `mapToSession` mapper, `verifySessionOwnership` guard, 14-case integration test incl. `TZ=Asia/Kolkata` invariance proof. Reorder uses Serializable + complete-set check; create uses Serializable + intra-tx TOCTOU defence. Output: `implementation/step-06.1/output.md`.
  - **Step 6.1.5** — Label+Exercise namespace move (`cms/*` → `lms/*`). **COMPLETED** 2026-05-16. Pure refactor — corrected Step 4 architectural mismatch. **1 squashed commit** `5332c034 refactor(training-domain): move label and exercise from cms to lms namespace` (4-commit prompt strategy collapsed by `.husky/pre-commit` `turbo check-types --filter="...[HEAD]"` blocking intermediate broken-import trees — see [[husky-cross-package-squash]] memory + IMPLEMENTATION_LOG Step 6.1.5 lesson addendum). 56 files / +173/−159 LOC. Verifications: `pnpm check-types` 16/16, `pnpm lint` 16/16, `pnpm test` 874/874 (no deltas), `pnpm dep:check` 0/1113, 8 grep regressions all-zero. Deferred follow-up: symbol rename `cms{Label,Exercise}AdminApi` → `lms*` (out-of-scope per "byte-identical move" invariant). Output: `implementation/step-06.1.5/output.md`.
  - **Step 6.2** — `lmsDayMetadataApi` + `getWeekResponseSchema` 7-day shape with embedded `Label`. **COMPLETED** 2026-05-16. 11 new + 12 modified files in **1 squashed commit** `19c57a66 feat(training-domain): 7-day week response with embedded label + day-metadata side-channel` per `[[husky-cross-package-squash]]`. D7 surface shipped (per OQ-A/B/C/D/E/F ratifications + CONTEXT-001 inline-fix for platform route handler unwrap). Verifications: `pnpm check-types` 16/16, `pnpm lint` 16/16, `pnpm test` 926/926 (+52), `pnpm dep:check` 0/1124, `TZ=Asia/Kolkata` 15/15, regression greps all-zero. Two surfaced lessons: 6th-flavour `[[planner-consumer-pattern-read]]` (CONTEXT-001 route handler miss) + Postgres SSI knowledge `[[postgres-ssi-upsert-unique-key]]` (case 13 test rewrite). Deferred follow-ups: `DAY_OF_WEEK_TO_PRISMA` hoist (Step 6.4 trigger), `mapToSessionWithLabel` extract (Step 6.6 trigger), production P2034 retry at HTTP layer (Step 6.4). Output: `implementation/step-06.2/output.md`.
  - **Step 6.3** — `lmsLabelPlatformApi` — read-only platform mirror of admin labels. **COMPLETED** 2026-05-16. 2 atomic per-layer commits `4aca6ea0 feat(contracts):...` + `29901fe3 feat(api-server):...` (additive only — `[[husky-cross-package-squash]]` rule applies only to cross-package breaking changes). `labelSearchParamsSchema` added to `lms/label` slice (no response alias); `lmsLabelPlatformApi.list(userId, query?)` in new `endpoints/lms/label/platform.ts` with `requireCoachLikeRole` + Prisma `nameLower contains` substring + hardcoded `take: 50`. Structural symmetry fix: new `endpoints/lms/label/index.ts` barrel (was missing). 11 integration test cases. Cleanest executor run yet — zero new planner-discipline flavours. Output: `implementation/step-06.3/output.md`.
  - **Step 6.4** — Platform routes for day metadata + label search + companion P2034 retry infrastructure. **COMPLETED** 2026-05-16. 7 atomic per-layer commits `a45a7dc3..3fde2c26` + `624212e6 docs(step-06.4):...`. **6 new + 10 edited files / +533 −149 LOC**. Original Step 6.4 scope (Session routes + day-metadata + labels-platform + extended-week-GET) **narrowed**: extended week GET was already done in Step 6.2 CONTEXT-001 inline-fix; Session HTTP routes split out into new **Step 6.4.5** (cleaner per-step focus — different op-family POST/PUT/DELETE/PATCH vs PUT-only, plus retry-helper rebirth-then-apply naturally splits across the two steps). Deliverables: `labelSearchParamsSchema` += trim+level filter; `appErrorResponse` += 503 Retry-After branch; `DAY_OF_WEEK_TO_PRISMA` hoist to `_shared/day-of-week.ts` (closes Step 6.2 D-3); new `retryOnP2034` helper + applied to `lmsDayMetadataApi.{setLabel,setNotes}` (Session.create apply → Step 6.4.5); `lmsLabelPlatformApi.list` level-filter + cap 50→500 (driven by coach UX preload + applicability-based autocomplete); 3 platform route files. **Cleanest run since Step 6.3 — zero new planner-discipline flavours, zero § 0 escalations, Stage 5 + Stage 6 both APPROVED**. Output: `implementation/step-06.4/output.md`.
  - **Step 6.4.5** — Platform routes for Session API + apply `retryOnP2034` to `lmsSessionApi.create`. **COMPLETED** 2026-05-16. 3 atomic per-layer commits `90bfa3fe..317af4c0`. **3 new + 2 edited files**. Delivered exactly per § 3 prompt: Phase 1 wrap `create`'s Serializable tx in `retryOnP2034`; Phase 2 +1 concurrent-create integration case (closes Step 6.1 QA-002 carry-forward); Phase 3 4 verb handlers across 3 route files (POST `.../sessions/`, PUT `.../sessions/reorder/`, multiplexed PUT+DELETE `[planId]/sessions/[sessionId]/`). Reorder via PUT (idempotent, body-full) per OQ-B ratification — `createAuthActionHandler` body-less doesn't fit; no PATCH factory in auth land; PUT semantically correct for "replace ordering". Update/delete byte-identical (no P2034 surface). Output: `implementation/step-06.4.5/output.md`.
  - **Step 6.5** — Platform client API + hooks for Sessions / DayMetadata / Labels. **COMPLETED** 2026-05-16. 3 atomic per-layer commits `fe6b04bc..56f3802e`. **7 new + 4 edited files**. Delivered exactly per § 3 prompt: Phase 1 `labels.search` query key namespace; Phase 2 3 client API factories + barrel + `api/index.ts` wire-up; Phase 3 `useWeekMutation` generic helper; Phase 4 3 hook files (6 mutation + 1 query) + barrel. `Idempotency-Key` framework-auto per `ApiClient.prepareRequest:111-113`; no hook-side opt-in needed (OQ resolved by codebase). `extractValidationMessage` confirmed N/A в repo (admin-v4-scoped); pattern — direct `notifyError`. 3 D-decisions all minor. Output: `implementation/step-06.5/output.md`.
  - **Step 6.6** — UI part 1: DayRow header reshape (Day label Autocomplete + Day notes blur-commit field). **COMPLETED** 2026-05-17. 1 atomic per-layer commit `8c08c218 feat(platform): reshape day-row header with label autocomplete and notes field` (6 files / +237/−17 LOC). First production UI consumer трёх Step-6.5 хуков. 2 new components (DayLabelSelect + DayNotesField) + DayRow reshape (49 → 95 LOC) + WeekGrid signature extension + PlanDetailView wire (single `useLabelSearch({level:"DAY"})` call per OQ-A A1). `noUncheckedIndexedAccess: true` narrow без `!` через invert-iteration-axis approach (D-1, pre-anticipated в prompt § 3 Phase 3). Verifications: `pnpm check-types` 16/16, `pnpm lint` 16/16, `pnpm test` 929/929 (baseline preserved, zero test deltas), `pnpm dep:check` 0/1146. Browser smoke-test 12/12 green. Zero § 0 escalations, zero new planner-discipline flavours — six existing checklists held the bar proactively. 5 D-decisions all minor (TS narrow approach, ESLint curly auto-fix, single hook location, React.FC mixed convention, `||` over `=== true`). 4 deferred (ZWS strip, @repo/ui LabelSelect extract, useBlurCommit primitive, lazy fetch). Output: `implementation/step-06.6/output.md`.
  - **Step 6.7** — UI part 2: Session body + dnd-kit reorder + SessionCard + cross-package extracts. **COMPLETED** 2026-05-17. 5 atomic per-layer code commits + 1 docs (`2ab958ea..daf6b293`). 19 files / +741/−122 LOC. Second UI surface; second consumer of Step-6.5 hooks (`useCreateSession`/`useUpdateSession`/`useDeleteSession`/`useReorderSessions`). New SessionList (DndContext + SortableContext + optimistic local state + rollback per OQ-D D1), SessionCard (useSortable + ConfirmationModal Delete per OQ-E E1), AddSessionButton (instant-create per OQ-F F1), SessionLabelSelect + SessionNotesField (thin wrappers). Cross-package extracts (OQ-A A1 + OQ-B B1 triggered): `@repo/ui/LabelSelect` generic Autocomplete<Label> (73 LOC) + `@app/lib/hooks/useBlurCommit` byte-equivalent extract (52 LOC); Phase 4 refactored WeekNotes + DayNotesField + DayLabelSelect to consume extracts (behaviour-preserving). First `@dnd-kit/*` install в repo (3 catalog entries per OQ-G G1). PlanDetailView second useLabelSearch({level:"SESSION"}) drilling 4 levels per OQ-C C1. Verifications: `pnpm install` OK +4 packages, `pnpm check-types` 16/16, `pnpm lint` 16/16, `pnpm test` 958/958 (zero test file deltas), `pnpm dep:check` 0/1153 (D-7 module count off by 2; 0 violations preserved). Browser smoke-test 13/13 green. Zero § 0 escalations, zero new planner-discipline flavours — seventh cleanest run в ряд. 7 D-decisions all minor. Process rule reversal mid-step: direct-exec convention reverted by user — `[[always-via-feature-skill]]` saved; from Step 7 onwards prompts wrap в `/feature` или `/feature small`. Output: `implementation/step-06.7/output.md`.

  Sub-step granularity: 1 prompt = 1 `/feature` session = 1 close-out cycle. Steps 6.0-6.5 = backend/types, smoke-test N/A. Steps 6.6 + 6.7 = UI, scenario-based smoke-tests.

- **Step 7** (was Step 8) — Block-level operations. Block add/edit/delete inside a session; block-label M:N with order; block-level Intensity / TimeCap composites. **Decomposed into 6 sub-steps** (7.0 → 7.5) ratified parametrically at top-level thesis 2026-05-17. Sub-steps:

  - **Step 7.0** — Block contract slice + Intensity/TimeCap shared VOs + BlockLabelAssignment M:N contract. **COMPLETED** 2026-05-18. 2 atomic commits `a3bcef6e` + `523e16d9` на `feat/training-domain`. 16 files / +819/-1 LOC (12 new + 4 modified registration). First sub-step of Step 7 decomposition + first `/feature small` invocation под `[[always-via-feature-skill]]` rule (branch-cut override honored). `lms/block/` 8 files (block.constants + block.schema + block.types + block.schema.test + block-api.schema + block-api.types + block-api.schema.test + index) + `lms/_shared/` extended (`intensity.ts` 5-dim Zod + `time-cap.ts` + 5 `as const` tuples + standalone `rpeSchema`/`paceSchema` pre-shipped per D-2 Step-7.5 affordance). `blockSchema` с embedded `labels: Label[]` (D7 embed pattern); `assignBlockLabelsSchema` empty-array-valid = clear-all per D-7 server-handler invariant. Verifications: `pnpm check-types` 16/16, `pnpm lint` 16/16, `pnpm test` 1036/1036, `pnpm dep:check` 0/1165. Stage 5 reviewer APPROVED (2 WARNING actioned/accepted, 3 INFO actioned/flagged). Zero § 0 escalations; zero new planner-discipline flavours; eighth cleanest run в ряд. 7 D-decisions all minor. **D-7 carry-forward** flagged для Step 7.1 (server-handler tx delete-all + zero insert for empty `labelIds[]`). Output: `implementation/step-07.0/output.md`.
  - **Step 7.1** — api-server `lmsBlockApi` (CRUD + reorder + assignLabels M:N + retryOnP2034 wrap for create+assignLabels) + `verifyBlockOwnership` guard + `mapToBlock` mapper. **COMPLETED** 2026-05-18. 6 commits `2c9b6d6a..8cf42432` на `feat/training-domain`: per-phase 3 atomic (guard → mapper → endpoint) + 2 review-fix (Stage 5 REV-001 helper extract + Stage 6 QA-002 concurrent test tightening) + 1 docs. 4 новых + 4 правки = 8 файловых касаний внутри `@repo/api-server`. 29 integration tests (25 admin + 4 guards). 5 методов mirror `lmsSessionApi` Step 6.1. **D-7 invariant verified** (case 20 outcome before=3→assignLabels([])→after=0 + explicit `if (data.labelIds.length > 0)` short-circuit `admin.ts:265-273`); **OQ-b1** update silently-ignores `labelIds` verified (case 11); **D-4** extra case 8 для planId mismatch defence-in-depth. `verifyBlockOwnership` returns `{status, sessionId, dayId, weekId, planId}` (extra `sessionId` vs Session guard — для Step 8 Schema chain reuse). `mapToBlock` парсит `intensitySchema.parse` + `timeCapSchema.parse` на read (zero `as` casts per `[[type-quality]]`). Verifications: `pnpm check-types` 16/16, `pnpm lint` 16/16, `pnpm test` 1066/1066 (+30 от 1036), `pnpm --filter @repo/api-server test` 581/581 (552→581 exact +29 match), `pnpm dep:check` 0/1169 (+4 modules exact match). Stage 5 reviewer APPROVED iter2 (REV-001 helper extract + REV-002 cascade test actioned inline); Stage 6 QA APPROVED for Stage 7 (25 attacks / 0 CRITICAL / 3 WARNING / 22 INFO). Zero § 0 escalations; zero new planner-discipline flavours; **ninth cleanest run в ряд**. First `/feature` full pipeline под `[[always-via-feature-skill]]`. 5 D-decisions all minor (D-1 helper typing via `Omit<typeof prisma, ...>` deny-list — extended Prisma client compat; D-2 Schema-cascade test via `archetype.findFirst` graceful skip; D-3 concurrent test winner-identification per QA-002; D-4 extra planId-mismatch case; D-5 `satisfies Prisma.BlockInclude` annotation). 1 paper deviation accepted (acceptance #15: contract types narrowed via Vitest method signature). 10 deferred carry-forwards (5 new from Stage 6 QA — see § "Deferred sub-decisions"; 5 pre-existing). Husky pre-commit + commit-msg clean all 6 commits без `--no-verify`. Output: `implementation/step-07.1/output.md`. Feature-dev: `.feature-dev/1779080660/`.
  - **Step 7.2** — Platform HTTP routes для Block (4 route files mirror Step 6.4.5 Session pattern) + `handlePrismaError` ZodError DB-corruption defence (closes QA-003 carry-forward). **COMPLETED** 2026-05-18. 4 commits `89f81e9c..bdb88fc1` на `feat/training-domain`: handler patch + 2 route commits + docs. 4 новых + 2 правки = 6 файловых касаний across `apps/platform/` + `packages/api-server/utils/`. id-addressed routes at `.../[planId]/blocks/[blockId]/...` (OQ-a A1 sessionId-free, mirror Session id-addressed); assignLabels dedicated PUT route (OQ-b B1); QA-003 fix в api-server NOT api-routes (OQ-c C1 — preserves existing route-layer ZodError → 400). Verifications all-green: 16/16 check-types, 16/16 lint, 583/583 api-server tests, 1068/1068 root, 0/1173 dep:check (+4 modules exact match). Stage 5 reviewer **APPROVED** (0 CRITICAL / 0 WARNING / 1 INFO). Zero § 0 escalations; zero new planner-discipline flavours; **tenth cleanest run в ряд** (6.3→6.4→6.4.5→6.5→6.6→6.7→7.0→7.1→7.2). Second `/feature small` invocation в workflow после Step 7.0. Output: `implementation/step-07.2/output.md`. Feature-dev: `.feature-dev/1779088244/`.
  - **Step 7.3** — Platform client API + hooks (`useCreateBlock` / `useUpdateBlock` / `useDeleteBlock` / `useReorderBlocks` / `useAssignBlockLabels`) + `useLabelSearch({level:"BLOCK"})` третий callsite. `/feature small`.
  - **Step 7.4** — UI BlockList + BlockCard + AddBlockButton + label preload Context refactor (5-level prop drilling trigger materializes per Step 6.6/6.7 deferred). `/feature` full pipeline.
  - **Step 7.5** — Intensity + TimeCap UI composites (form-driven per VO additive optional fields). `/feature`. Pre-shipped affordances from Step 7.0 D-2 (`as const` tuples + standalone `rpeSchema`/`paceSchema`) consumed here.

  **Coach validation pause** между Step 7.5 close-out и Step 8 thesis cycle per `[[training-domain-validation-gate]]` — 3 options surface (full Schema editor / middle path с rich text в Block.body / full rich text drop Steps 7-9). Most expensive next surface (Schema editor = 25+ form variants); explicit gate перед commit makes sense.

- **Step 8** — Schema editor. Archetype picker (from seeded archetype catalog), `archetypeParams` form per archetype (driven by `archetypeParamsSchema`), schema body switching (rows vs subSchemas based on `kind`).

- **Step 9** — SchemaRow editor. Per-rowKind forms (EXERCISE, REST, FOOTNOTE, STANDALONE_LOAD, PLACEHOLDER, INNER_LADDER_MARKER, CONNECTOR, REST_SLOT, REP_DEFINITION, STANDALONE_URL); shared Load / RepNotation / Intensity / Tempo / Side / Media / CompoundRep composites.

- **Step 10** — End-to-end coach happy path smoke-test + cleanup. Manual scenario validated; defects fixed.

Some steps (especially 9, 10) will likely split into sub-steps. Granularity locked at thesis time, not now.

## Decisions accepted

### 2026-05-12 — Foundation architecture (D1-D4 finalized)

- **D1 (Calendar Week as entity).** Explicit `Week` model between `TrainingPlan` and `Day`. Week is a **calendar slot** (Mon-Sun ISO week), not a relative "week 1, week 2" of a fixed program.
  - `Week { id, planId, startDate Date (Monday of ISO week), notes String?, createdAt, updatedAt }`. Unique `(planId, startDate)`.
  - `Day` no longer has `order Int`. Replaced by `dayOfWeek DayOfWeek` enum (MONDAY..SUNDAY). `Day { id, weekId, dayOfWeek, labelId?, notes?, ts }`. Unique `(weekId, dayOfWeek)`.
  - New enum `DayOfWeek { MONDAY, TUESDAY, WEDNESDAY, THURSDAY, FRIDAY, SATURDAY, SUNDAY }`.
  - Derived (not stored): week end date = `startDate + 6 days`; ISO year+week number; per-day calendar date.
  - Plan is a "train", weeks are slots the coach fills as time moves forward. No fixed end. Was OPEN ("Week / Plan entities — out-of-scope") — closed here.
- **D2 (Athlete = User+AthleteProfile, no profileAttributes).** No standalone `Athlete` model. `OneRMRecord.userId → User.id`. `PerformedSession.userId → User.id`. `profileAttributes` (Phase 6 placeholder for dual-value resolver / level / RX-SC tier) **dropped entirely** — premature without concrete UX. Future additions (e.g., `level`, `modalityTier`) go as explicit enum columns on `AthleteProfile`, not jsonb. Was OPEN ("Connection to TrainingPlan/PlanEnrollment") — closed here.
- **D3 (Full-scope port at Step 2).** All ratified entities — catalogue (Exercise, Label, Archetype), plan-content (Week, Day, Session, Block, BlockLabelAssignment, Schema, SchemaPairing, SchemaRow), athlete-facing (OneRMRecord, PerformedSession, PerformedExerciseInstance) — go into `packages/api-server/prisma/schema.prisma` at Step 2, even though athlete-flow UI/API stays out of scope. Avoids a second schema-change wave mid-workflow.
- **D4 (Library vs Configuration split).** `Exercise` and `Label` are **libraries** — created/managed by coach in admin UI, used for future analytics. NOT seeded; coach populates via admin during smoke-test. `Archetype` is **configuration** — part of the model itself; full canonical set (34) MUST be seeded at Step 2. No admin CRUD for Archetype (UI-editing it is meaningless without parser+renderer updates). `archetypeParamsSchema` lives in DB (Prisma model) rather than code to allow patching without redeploy.

### 2026-05-13 — Exercise model refinement (D5 ratified)

- **D5 (defaultDemoUrl → defaultDemoUrls String[]).** Single URL field replaced by Postgres native string array. Coach can attach multiple demo videos per exercise without limit. Native `String[]` over `Json?` for type-safety and no JSON parsing overhead. Applies to both `analysis/artifacts/06-formalization/schema.prisma` (anchor spec) and real `packages/api-server/prisma/schema.prisma`. Step 3 Phase 0 implements the schema refinement (with analysis-artifact sync) before any UI work.

### 2026-05-15 (post-Step-6.1) — Label+Exercise belong in LMS namespace (D8 ratified)

- **D8 (`Label` and `Exercise` contracts + api-server endpoints/mappers live in `lms/*`, not `cms/*`).** Surfaced during Step 6.2 thesis cycle when D7-ratified embed `label: Label | null` in `getWeekResponseSchema` failed the dep-cruiser rule `contracts-lms-no-coaching-cms-billing` (`.dependency-cruiser.cjs:47-58`, forbids `contracts/lms/*` → `contracts/cms/*`). Every other signal already classifies both entities as LMS catalogs: Prisma `@@map("training_labels")` + `@@map("training_exercises")`, FK relations from `Day.labelId`, `Session.labelId`, `BlockLabelAssignment.labelId`, `SchemaRow → Exercise (via OneRMRecord)`, `OneRMRecord.exerciseId`. Step 4 (Admin Label CRUD) placed the contract in `cms/label/` mirroring the earlier Step 3 (Admin Exercise CRUD) — neither step ran an architectural review against `docs/BOUNDED-CONTEXTS.md` §1+§8 (which lists CMS as "marketing surface"; Label/Exercise are not marketing). Ratified consequences:
  - **`packages/contracts/src/entities/cms/{label,exercise}/`** → **`packages/contracts/src/entities/lms/{label,exercise}/`** (14 files, 706 LOC, no internal logic change).
  - **`packages/api-server/src/endpoints/cms/{label,exercise}/`** → **`endpoints/lms/{label,exercise}/`** (4 files, 839 LOC).
  - **`packages/api-server/src/mappers/cms/{label,exercise}.mapper.ts`** → **`mappers/lms/{label,exercise}.mapper.ts`** (37 LOC).
  - **`mappers/cms/enum-maps.ts` split** — CMS-pure (Currency/PriceInterval/BlogCategory/ContactStatus) stays; Exercise enums (Equipment/MovementType/CanonicalCompoundType) extract to new `mappers/lms/exercise.enum-maps.ts`.
  - **dep-cruiser `admin-no-lms` carve-out widened** — 4 new `pathNot` entries for new lms paths so admin keeps Library access.
  - **No Prisma changes** — `@@map("training_*")` already correct.
  - **No analysis-artifacts changes** — model semantics unchanged.
  - **Historical paper-trail preserved** — Step 3 / Step 4 `IMPLEMENTATION_LOG.md` entries + `.feature-dev/` artifacts keep the `cms/label` / `cms/exercise` mentions (point-in-time truth). New entries write the new namespace.
  - **Step 6.1.5** implemented this **in 1 squashed commit** (`5332c034`, 2026-05-16). Originally planned as 4 atomic per-layer commits under `/feature small`, collapsed at executor run-time after `.husky/pre-commit` blocked the intermediate broken-import tree — 5th-flavour planner-discipline miss `instinct-process-blindness` (see [[husky-cross-package-squash]]). Step 6.2 prompt-writing now unblocked.

### 2026-05-15 — Day = calendar slot, mirror of D6 (D7 ratified)

- **D7 (Day is a lazily-materialized calendar slot, mirror of D6).** The "day-level operations" framing in the original queue Step 6 — Day add / edit / reorder / delete — was rejected by the user during the Step 6 thesis cycle: "ты не можешь редактировать ось времени. при входе на plan details ты видишь неделю — 7 строк, по одной на каждый день, и когда тренер заходит — ему не нужно 'создавать дни', ему нужно управлять тренировочными сериями. думай как тренер". Ratified consequences:
  - **No coach-facing "add day" / "delete day" / "reorder day" UX.** `dayOfWeek` is a fixed enum axis (D1); the 7 weekday rows are always visible because they are a calendar fact, not data. Day slot identity = `(weekId, dayOfWeek)`.
  - **A `Day` DB row materializes lazily** via `prisma.day.upsert` (per `(weekId, dayOfWeek)`) on:
    - First `Session` created in the slot (Step 6.1 — `lmsSessionApi.create` issues an atomic `$transaction` that connect-or-creates the Week, then connect-or-creates the Day, then creates the Session).
    - First label set on the day (Step 6.2 — `lmsDayMetadataApi.setLabel`).
    - First notes set on the day (Step 6.2 — `lmsDayMetadataApi.setNotes`).
  - **Day is addressed externally by `(planId, startDate, dayOfWeek)`**, not by `dayId`. The `dayId` is server-internal; client never sees it as an address (only as a field on returned `Session` rows for cache invalidation).
  - **No POST-create-day / DELETE-day routes.** No "clear day" UI in Step 6.x (a Day-row that becomes empty after the last session-delete + null label + null notes is **left as a breadcrumb**; auto-cleanup races concurrent writes and saves no meaningful space).
  - **`getWeekResponseSchema` (Step 6.2)** returns all 7 weekdays as `{ dayOfWeek, label, notes, sessions: Session[] }`, materialized-or-not is invisible to the client: an unmaterialized slot is just `{ ..., label: null, notes: null, sessions: [] }`. Client never branches on materialization state.
  - Drives a future prose clarification of `analysis/artifacts/05-synthesis/domain-model.md §1.1` (Day) — to be done in Step 6.2 if the spec'ing surfaces a divergence; not done now because the Day section already aligns with the calendar-slot framing.

### 2026-05-14 — Week = calendar slot (D6 ratified)

- **D6 (Week is a lazily-materialized calendar slot, not a managed entity).** The plan-detail surface is a **calendar viewport**, not a week-list manager. Settled during the Step 5 thesis discussion. Decisions:
  - No coach-facing "add week" / "remove week" / "add first week" UX. The coach navigates the calendar axis (week = viewport unit, **no free calendar scroll**) and programmes whichever slot they need — empty or not.
  - A `Week` DB row materializes **lazily** — upsert by `(planId, startDate)` on the first `Day` created in that week (Step 6, via `connectOrCreate`/upsert) or the first per-week note. Navigating past empty weeks creates nothing; an empty slot = no `Week` row.
  - Weeks are addressed by `(planId, startDate)`, not `weekId` — the client computes the viewport's `startDate`; the row may not exist. Step 5's Week API surface is therefore read-mostly: `GET .../weeks/[startDate]` + a notes upsert. **No POST-create-week / DELETE-week routes.** Week-row creation as a side-effect of `Day` creation is a Step 6 concern.
  - Plan-detail body layout: **7 full-width day rows** (Mon–Sun), not 7 columns. A Day is a nested document (Session→Block→Schema→SchemaRow), not a calendar event; 7 columns at `maxWidth="lg"` give ~140px/day and content dies. Today's date gets the "Thu 14"-with-bright-circle row-label treatment.
  - Week navigation: prev/next + jump-to-date + "today"; default on open = current calendar week, `?week=<startDate>` URL param overrides.
  - Drove a ratified **prose clarification** of `analysis/artifacts/05-synthesis/domain-model.md §1.0` — the loose "add / remove / clone / clear week" phrasing reframed to calendar-slot + lazy-materialization wording. **No schema change** — the `Week` Prisma model already supports this; no `06-formalization` touch.

### Deferred sub-decisions (default hypothesis applied; revisit on contact)

- **Order semantics**: **sparse integers (10/20/30)** per Phase 4 Q6 (ratified in `analysis/artifacts/06-formalization/er-final.md §5 #7`). Earlier draft in this file mistakenly proposed sequential; reverted 2026-05-12 after Step 1 executor flagged the divergence. Step 2 seed and any insert-helpers must use sparse increments; renumber only on collision.
- **Phase 7 archetypes (super-set)**: include in archetype seed at Step 2. `Intensity` Zod schema admits optional `hrZone` and `numericPace`.
- **Migrations stance**: no versioned migrations during workflow; `db:reset` per schema change per ADR-0019. **Note (Step 2 finding)**: `db:reset` script in this repo runs `prisma db push --force-reset && tsx scripts/apply-sql-checks.ts` only — does NOT auto-seed. After every schema change run `db:reset` then `db:seed` explicitly.
- **`TrainingPlan` naming**: keep as plan-shell (creator/status/name). Plan-content lives below Week.
- **Plan edit/rename UI**: ~~`useUpdateTrainingPlan` exists but no edit UI~~ — **resolved in Step 5** (inline name/description edit in the plan-detail `PageHeader` via the `InlineEditText` primitive).
- **QA-001 — `@db.Date` ↔ local-midnight boundary (Step 5 finding; Step 6+ hard carry-forward)**: `getMonday` / `parseDateParam` (`@repo/shared`) build **local-midnight** `Date`s; Prisma serializes `@db.Date` columns via `toISOString()` (UTC) — so a local-midnight `Date` on a positive-UTC-offset server persists one day early. Step 5 fixed it for `Week.startDate` via `resolveWeekStartDate` in `packages/api-server/src/endpoints/lms/week/admin.ts` (re-anchors the Monday to `Date.UTC(...)` before the Prisma boundary; proven with a `TZ=Asia/Kolkata` test). **Any future step writing a `Date` into a `@db.Date` column must do the same.** Step 6.1 (`lmsSessionApi.create`) transitively materializes a `Week` row via `connectOrCreate` → hits this directly; Step 6.1 prompt must extract `resolveWeekStartDate` into a shared module (`endpoints/lms/_shared/date.ts`) and reuse it in both `lmsWeekApi` and `lmsSessionApi`.
- **Q10 — `Session.freezeLoadsAtCreation` (Step 6.0 deferred; carry-forward indefinite)**: field exists in Prisma (`schema.prisma:639`, default `false`) as an edge-case toggle for "testing weeks" (per `analysis/artifacts/05-synthesis/edge-cases.md §2.4`, `06-formalization/implementation-notes.md §3.8`, `00-meta/phase-06-prompt.md Q10`). The default coach workflow uses live formula resolution (DP2): 1RM is dynamic, percentage-based loads resolve against the athlete's current 1RM, snapshot mode is **harmful** in the default workflow (athlete sees stale numbers after a fresh PR). **`Session` contract layer does NOT expose this field** in Step 6.0 nor any later 6.x sub-step. The DB column stays default-false; nobody writes or reads it through the contract. Revisited only when (and if) a concrete coach use-case for testing-week toggle materializes — at which point a dedicated sub-step adds it to contract + UI in one shot (D5-Exercise-precedent).
- **Session.name (Step 6.0 deferred; carry-forward indefinite)**: Prisma schema has no `Session.name` field; do NOT add one to the contract "for flexibility". Session identity for coach UX is `(order, label?, notes?)`. Step 6.7 `SessionCard` header will render `label.name || "Session N"` (N derived from `order`). If a real coach need for distinct session naming surfaces, add as a schema change + analysis-artifact sync (D5 precedent), not as an instinct-add field.
- **Symbol rename `cms{Label,Exercise}AdminApi` → `lms{Label,Exercise}AdminApi`** (Step 6.1.5 deferred; ~13 files mechanical sed). The moved api-server endpoints still export their CMS-prefixed symbol names because the refactor was strictly path-only ("byte-identical move" prompt § 4 invariant). Estimated blast radius: 7 admin route handlers + 2 api-server self-exports + 4 test files. Schedule as its own atomic PR after Step 6.x stabilizes — low priority (runtime unaffected; only naming consistency).
- ~~**WORKFLOW.md husky-squash guidance**~~ — **RESOLVED** 2026-05-16 in `4d93c60a docs(workflow): document husky-squash strategy and six-flavour planner-discipline checklist`. WORKFLOW.md "Language & commit conventions" section now has explicit squash-exception paragraph + commitlint 100-char subject cap mention; "Lessons learned" section expanded from 4 brief bullets to structured 6-flavour procedural checklist with memory-link cross-refs + 4 adjacent bindings (prompt-internal consistency, memory hygiene, executor escalation, Postgres SSI).
- ~~**`DAY_OF_WEEK_TO_PRISMA` hoist to `endpoints/lms/_shared/day-of-week.ts`**~~ — **RESOLVED** 2026-05-16 in `4b52e32d refactor(api-server): hoist day-of-week prisma map to lms shared module`. New 13-LOC `_shared/day-of-week.ts` exporting the const; barrel `_shared/index.ts` += `export * from "./day-of-week"`; 3 callsite admin files (`session`, `day`, `week`) merged imports `{ DAY_OF_WEEK_TO_PRISMA, resolveWeekStartDate } from "../_shared"` and dropped local declarations + dropped now-unused `@prisma/client` type-only segment. 47/47 session/day/week endpoint tests green post-hoist; no behavioural diff.
- **`mapToSessionWithLabel` extraction as named symbol** (Step 6.2 deferred per output.md D-2). Currently inline `{ ...mapToSession(s), label: s.label ? mapToLabel(s.label) : null }` inside `mapToDaySlot`. Single use site; extract if Step 6.6 WeekGrid builds the same shape on the client.
- ~~**Production retry-on-P2034 at HTTP layer**~~ — **FULLY RESOLVED** 2026-05-16. Helper `retryOnP2034` shipped in Step 6.4 `b0b23ae4` (2 default attempts + `[50, 200]ms` jitter, exhaustion → `ServiceUnavailableError({ retryAfter: 5, lastErrorCode: "P2034" })`). Companion 503 `Retry-After` branch in `4cb417d2`. Applied to `lmsDayMetadataApi.{setLabel, setNotes}` in Step 6.4 `013f8319` and to `lmsSessionApi.create` in Step 6.4.5 `90bfa3fe`. All 3 production callsites of Serializable transactions in LMS now retry-wrapped. Step 6.1 QA-002 concurrency-repro carry-forward closed via Step 6.4.5 +1 integration test.
- ~~**`endpoints/lms/exercise/index.ts` structural symmetry fix**~~ — **RESOLVED** 2026-05-16 in `859ac2c2 refactor(api-server): add endpoints/lms/exercise/index.ts barrel for structural symmetry`. New 1-line barrel file (`export * from "./admin";`) + `endpoints/lms/index.ts` flipped `./exercise/admin` → `./exercise`. All 15 packages check-types green (FULL TURBO cached on the api-server commit since no semantic change). `cmsExerciseAdminApi` reachable through the new barrel; symbol rename to `lmsExerciseAdminApi` remains separately deferred per Step 6.1.5.
- **Label autocomplete preload + filter-by-applicability (Step 6.4 product req, ratified mid-thesis 2026-05-16, drives Steps 6.6/6.7 UI design)**: coach opens label-select form → all applicable labels preloaded server-side; in-form filter narrows by `applicableLevels` matching the host context (Day picker → `level=DAY`; Session picker → `level=SESSION`; Block picker → `level=BLOCK`). Driven `lmsLabelPlatformApi.list` signature change (`query?: LabelSearchParams = { q?, level? }`), `applicableLevels { array_contains: level }` filter at Prisma layer, `LABEL_SEARCH_CAP` bump 50 → 500 (5x headroom over realistic coach catalog ~100-300). When Step 6.6/6.7 wire `useLabelSearch` hook, it should default to preload-no-filter call once per form mount + on-keystroke refine.
- **Step 6.4 Stage 6 QA INFO findings** (5 non-actionable but flagged for future): (1) client debounce for `labels/search` GET — Step 6.6 hook concern; (2) `applicableLevels=[]` dev-only sanity check — production rows can't have empty array via Zod `.min(1)`; (3) `retryOnP2034.attempts` unbounded option — add `.max(10)` clamp if a caller ever passes pathological value; (4) inverted `jitterMsRange: [200, 50]` mathematically degenerate but safe — defaults correct; (5) inner intra-tx TOCTOU re-check (Step 6.2 inheritance) not directly integration-testable without prisma mock per `[[no-tech-debt-in-mocks]]` — covered indirectly by passing Day suite.
- **Idempotency-key reuse with mismatching body documentation** (Step 6.4 deferred): `wrapAuthHandler(JSON_CONFIG)` throws `ConflictError("Idempotency-Key reuse with different request body")` per `idempotency/with-idempotency.ts:54`. Acceptable behavior but undocumented at route level; surface contract to Step 6.5 hook layer so clients know to use fresh keys on body changes.
- **ZWS / control-char normalization on Day notes** (Step 6.6 deferred per OQ-C C1). Domain `analysis/artifacts/05-synthesis/domain-model.md §1.1` defines notes as "free-text"; coach-owned field; ZWS edge rare. Trigger: QA reports "notes appear empty but have characters". Implementation: hoist `normalizeText` helper from `packages/contracts/src/entities/lms/label/label.schema.ts:7` to shared module, apply in DayNotesField `commit()` before trim.
- **`@repo/ui` LabelSelect extraction** (Step 6.6 deferred per OQ-B B1). Premature at 1 callsite. Trigger: Step 6.7 SessionLabelSelect shows ≥ 90% duplication with DayLabelSelect → extract to `@repo/ui/label-select` with props `{value, options, isLoading, onChange, disabled}` (current DayLabelSelect API stays as the public surface). Step 6.7 thesis must assess duplication threshold at thesis time.
- **WeekNotes ↔ DayNotesField shared blur-commit primitive** (Step 6.6 deferred). 2 callsites — DRY premature. Trigger: 3rd surface (BlockNotes? — Step 7 candidate). Implementation: extract `useBlurCommit({value, onCommit})` hook returning `{draft, setDraft, handleFocus, handleBlur}` pair, mirroring shared invariant of both existing implementations (committedRef + isFocusedRef + useEffect-sync-when-unfocused + trim + null-on-empty).
- **`labelOptions` lazy fetch on Autocomplete open** (Step 6.6 deferred). Step 6.4 ratified preload server-side (load once on view mount, не on each Autocomplete `onOpen`). Trigger: coach reports slow first render of plan-detail после 2-3 weeks production use. Implementation: convert `useLabelSearch` to `enabled: false` + lazy trigger from DayLabelSelect via `onOpen` callback. Current preload is the explicit coach UX preference per Step 6.4 mid-thesis ratification — don't pre-optimize.
- **NEW: Step 7.1 Stage 6 QA-001 — `@@unique([sessionId, order])` constraint on `Block` model** (WARNING severity, surface before Step 8). Currently concurrent `Block.create` relies полностью на Postgres SSI false-positive detection + `retryOnP2034` (mirrors Session pattern). Safe для текущего coach-edit-plan workload (admin не hot path); latent regression surface when Step 8 Schema adds more concurrent write paths to same Block chain. **Action when triggered**: planner discuss with user перед Step 8 thesis — schema change OOS Step 7.x, нужен dedicated mini-step (Step 8.0 prepend `feat(api-server): add unique constraint on block sessionid+order` with `db:reset` + analysis-files touched `06-formalization/{schema.prisma, er-final.md §4 cardinality matrix}` sync).
- ~~**Step 7.1 Stage 6 QA-003 — router middleware для `ZodError` from mapper**~~ — **CLOSED 2026-05-18 via Step 7.2 Phase 1 patch** (`89f81e9c feat(api-server): map mapper-side zoderror to internalservererror with dbcorruption kind`). Fix landed в **api-server's `handlePrismaError`** (NOT api-routes — per Step 7.2 OQ-c C1 ratification): new branch intercepts `ZodError` (raised by `mapToBlock`'s `intensitySchema.parse`/`timeCapSchema.parse` on DB-corrupt JSON) и re-throws как `InternalServerError("Block content failed schema validation", {kind: "DbCorruption", entity, issues: [...]})` → AppError path в `handleApiError` → 500 with structured details. Existing route-layer `ZodError → 400 VALIDATION_ERROR` path в api-routes preserved (correct для payload validation). Universal fix — applies to any future mapper-side Zod parse (Schema entity Step 8, all other mappers). +2 test cases в `prisma-error-handler.test.ts`.
- **NEW: Step 7.1 Stage 6 QA-006 — HEAD_COACH + ARCHIVED plan composition test** (INFO severity, optional). Currently покрыто индивидуально (test 2 = self-coach archived; guards test 3 = head-coach bypass on active); composition (head-coach trying to edit archived plan via Block surface) не тестируется явно. Correctness by composition: `verifyBlockOwnership` returns `{status: ARCHIVED, ...}` для head-coach via bypass; `verifyPlanEditable(owner)` throws `ForbiddenError("Plan is archived; edits not allowed")`. Optional 10-line test for completeness; не blocker для Step 7.2/7.3.
- **NEW: Step 7.1 Stage 6 QA-019 — D-7 invariant test outcome-only (no SQL-spy on createMany call count)** (INFO severity, acceptable). Case 20 verifies `BlockLabelAssignment.count === 0` post-call (outcome-based). Implementation has explicit `if (data.labelIds.length > 0)` guard (correct). SQL-spy-based test (assert `tx.blockLabelAssignment.createMany` call count === 0) would catch implementation-style regressions (someone removing the `if` guard) but requires Prisma client instrumentation per `[[no-tech-debt-in-mocks]]` rule (rejected). Outcome test is sufficient.
- **NEW: Step 7.1 D-1 / Stage 6 QA-022 — `TxClient` Omit deny-list fragile to Prisma major upgrades** (INFO severity, flag for upgrade prompts). `TxClient = Omit<typeof prisma, "$connect" | "$disconnect" | "$on" | "$transaction" | "$use" | "$extends">` correct для Prisma 6 (mirrors internal `ITXClientDenyList`); future Prisma client additions (`$metrics`, `$runCommandRaw`, etc) would leak into `TxClient` shape and be callable inside `assertLabelsApplicable` helper. **Action when triggered**: any `/upgrade @prisma/client` prompt должен re-verify omit list shape против new Prisma version's internal `ITXClientDenyList`.

## Rules / invariants

- `analysis/source/` and `analysis/artifacts/00-meta/`, `01-inventory/`, `02-patterns/`, `03-content/`, `04-structure/` — read-only forever.
- `analysis/artifacts/05-synthesis/` and `06-formalization/` — living source of truth, updated synchronously with every Prisma schema change. Each such update mentioned explicitly in `IMPLEMENTATION_LOG.md`.
- Every step touching Prisma updates seed in the same session; smoke-test scenario cannot be valid without coherent seed.
- Schema changes require thesis approval cycle before prompt finalization.
- Each step lives under `implementation/step-NN/` with `prompt.md` and `output.md`.
- Pre-existing implementations of this domain are deleted (4th attempt). Do not search git history or memory for them. If something surfaces accidentally, halt and surface to user.

## Next action

1. **Step 7.3 thesis cycle** — planner action. **Fourth sub-step of Step 7 decomposition** (7.0 contracts → 7.1 api-server → 7.2 routes → **7.3 client API + hooks**). Scope: **Platform client API + TanStack hooks для Block** — `apps/platform/src/lib/api/endpoints/blocks.ts` (Block API factory с 5 methods mirror `createSessionsAPI` Step 6.5) + `apps/platform/src/lib/hooks/use-blocks.ts` (5 mutation hooks через `useWeekMutation` helper) + `useLabelSearch({level:"BLOCK"})` третий callsite в PlanDetailView (Step 6.6 + 6.7 precedent: DAY level в DayLabelSelect, SESSION level в SessionLabelSelect, BLOCK level будет в BlockLabelSelect). Mirror Step 6.5 pattern (Sessions/DayMetadata/Labels client API + hooks). **`/feature small`** pipeline per scope (3-4 files: blocks.ts endpoint factory + use-blocks.ts hooks + api/index.ts wire-up + hooks/index.ts barrel; single package = `apps/platform/`). Branch-cut override mandatory.

   **Thesis cycle should resolve (5-6 OQ each with hypothesis)**:

   - **(a) Verbatim-read existing surfaces per `[[scope-via-existing-patterns]]`** at thesis-write time:

     - `apps/platform/src/lib/api/endpoints/sessions.ts` (Step 6.5 — canonical mirror for `createBlocksAPI` factory shape: create/update/delete/reorder методов; verify ApiClient method signatures, Idempotency-Key auto-generation).
     - `apps/platform/src/lib/hooks/use-sessions.ts` (Step 6.5 — 4 mutation hooks pattern: `useCreateSession` / `useUpdateSession` / `useDeleteSession` / `useReorderSessions` через `useWeekMutation` helper).
     - `apps/platform/src/lib/hooks/use-week-mutation.ts` (Step 6.5 D-A1 helper — `useWeekMutation<TVars, TResult>({mutationFn, planId, startDate, successMessage, errorMessage})` generalized week-invalidating boilerplate; reuse if hooks fit Week-cache pattern).
     - `apps/platform/src/lib/api/index.ts` (current `createApi(client)` factory — verify Block API wire-up location).
     - `apps/platform/src/lib/hooks/index.ts` (current barrel — verify alphabetic insertion point для new 5 hook exports).
     - `apps/platform/src/lib/api/endpoints/index.ts` (current barrel — verify Block API export alphabetic insert).

   - **(b) `useAssignBlockLabels` hook shape** — assignLabels — это unique 5th mutation (Session's 4 hooks don't have analog). Hypothesis: full mirror `useUpdateSession` pattern (mutation hook returns `{mutate, mutateAsync, isPending, ...}`); accepts `{blockId, labelIds: string[]}` payload; invalidates `platformKeys.weeks.byDate(planId, startDate)` post-success (same cache key as Session mutations). **Decision point**: где `planId + startDate` извлекаются для invalidation? — `useAssignBlockLabels` нуждается в обоих для `useWeekMutation` helper, но `assignLabels` API call принимает только `blockId`. Hypothesis: hook signature `useAssignBlockLabels(planId: string, startDate: string)` returns `{mutateAsync({blockId, labelIds}: {blockId, labelIds: string[]}) => Promise<Block>}` — callsite (Step 7.4 BlockLabelMulti) знает контекст PlanDetailView (planId + startDate); hook acts as bridge. **Прав?**

   - **(c) `useLabelSearch({level:"BLOCK"})` третий callsite** — added в PlanDetailView (mirror Step 6.6 + 6.7 placement: hook called once at view-mount, options drilled через WeekGrid → DayRow → SessionList → SessionCard → **BlockList → BlockCard** = 6 prop-drill levels). Hypothesis: matches existing pattern (single `useLabelSearch` call per level, 3 calls total на view-mount: DAY + SESSION + BLOCK). **Note**: 6-level drill triggers React Context refactor — but **Context refactor lands в Step 7.4** when BlockCard surfaces materialize (Step 7.3 hook-only, Context premature). **Прав?**

   - **(d) Per `[[planner-consumer-pattern-read]]`** — Step 7.3 IS first client consumer of Step 7.2 HTTP routes: verify `grep -rln "training-plans.*blocks" apps/platform/src/lib/` returns 0 hits at thesis time (only `apps/platform/src/app/api/...` should have Block route strings — server-side). § 0 zero-state explicit.

   - **(e) Mandatory adversarial pass per `[[planner-adversarial-review]]`**: 3-4 axes (hooks-layer minimal subset, mostly inherited from Step 6.5):
     - Mutation error path — `notifyError(error, "Failed to X")` per `@repo/query` (Step 6.5 D-2 pattern: direct notifyError, NOT extractValidationMessage per memory entry [[validation-message-helper]] which is admin-v4-scoped). Confirm Step 7.3 follows this.
     - Cache invalidation — `useWeekMutation` invalidates `platformKeys.weeks.byDate(planId, startDate)` (Step 6.5 helper behavior). For Block mutations targeting a Block inside Session inside Day inside Week — invalidating the entire week is correct (refetches full 7-day tree per `getWeekResponseSchema`).
     - Idempotency-Key auto — inherited via `ApiClient.prepareRequest:111-113` per Step 6.5 D-finding; no opt-in needed in hook layer.
     - No optimistic updates — defer per Step 6.5 OQ-D ratification ("invalidate-only; revisit if UX flicker surfaces"). Block mutations same — invalidate-only.

   Commit strategy per `[[husky-cross-package-squash]]` pre-check: scope = `apps/platform/` only (no contract / api-server / api-routes change; pure consumer layer). Single-package additive = per-phase atomic commits OK, no squash trigger. Likely 3 commits (api factory + hooks + barrels wire-up) + 1 docs, или 1 squash если `/feature small` Stage 7 prefers.

2. **Step 7.4** — UI BlockList + BlockCard + AddBlockButton + label preload Context refactor (5-level prop drilling trigger materializes per Step 6.6/6.7 deferred). `/feature` full pipeline. **OQ-3 from Step 7 thesis (BlockLabelMulti widget shape — extend `@repo/ui/LabelSelect` to `multi` mode vs fork `MultiLabelSelect`)** resolved here.

3. **Step 7.5** — Intensity + TimeCap UI composites (form-driven per VO additive optional fields). `/feature`. Pre-shipped affordances from Step 7.0 D-2 (`as const` tuples + standalone `rpeSchema`/`paceSchema`) consumed here.

4. **Coach validation pause перед Step 8** (per `[[training-domain-validation-gate]]`) — после Step 7.5 close-out **surface to user**: 3 options для Step 8 (full Schema editor / middle path с rich text в Block.body / full rich text drop Steps 7-9). Mini coach-validation gate с current MVP (Plan + Sessions + Blocks с empty bodies / placeholder textarea) перед commit на самую дорогую часть workflow.

5. **Step 8 thesis cycle** (after coach validation pause per item 4) — Schema editor (Archetype picker + archetypeParams form per archetype + schema body switching rows vs subSchemas based on `kind`). New domain layer below Block. **Consumes `intensitySchema` from `lms/_shared` (pre-extracted Step 7.0)** + adds Schema entity contracts (likely Step 8.0 sub-step). Likely decomposes mirror of Step 7 pattern. **Pre-Step-8 surface**: QA-001 schema `@@unique([sessionId, order])` constraint absence — concurrent Block.create relies полностью на SSI + retryOnP2034; Step 8 adds Schema rows writing to same chain, может increase contention. **Action**: planner surface для discussion перед Step 8 — schema change OOS Step 7.x, нужен dedicated mini-step OR Step 8.0 prepend (schema change + db:reset + analysis-files touched). **README pre-existing inconsistency** carry-forward (Step 7.0 deferred) — Step 8.0 contract-touching step is natural place to append `day` / `exercise` / `label` to LMS bullet + `schema` itself.

6. **Step 9** — SchemaRow editor (per-rowKind forms: EXERCISE, REST, FOOTNOTE, STANDALONE_LOAD, PLACEHOLDER, INNER_LADDER_MARKER, CONNECTOR, REST_SLOT, REP_DEFINITION, STANDALONE_URL — 10 discriminator branches) + shared Load / RepNotation / Intensity / Tempo / Side / Media / CompoundRep composites.

7. **Step 10** — End-to-end coach happy path smoke-test + cleanup + workflow close-out.

8. **Push consideration** (orthogonal to step queue): `feat/training-domain` currently **15 commits ahead of `main`** post-Step-7.2 close: `a85eff4b` PR #194 close-out + `a3bcef6e/523e16d9/11296bdf` Step 7.0 (code + output + planning close) + `2c9b6d6a..8cf42432` Step 7.1 (6 commits) + `71803bb0` Step 7.1 planning close-out + `89f81e9c..bdb88fc1` Step 7.2 (4 commits) — будет 16 после forthcoming Step 7.2 planning close-out commit. Sub-step push cadence per Step 6.x precedent — accumulate sub-steps locally, batch full Step 7 (7.0 → 7.5) в single PR после Step 7.5 close-out (mirror PR #193 batched Steps 6.1.5 → 6.5 = 32 commits, PR #194 batched 6.6 + 6.7 = 9 commits). Не blocker для Step 7.3 thesis.
