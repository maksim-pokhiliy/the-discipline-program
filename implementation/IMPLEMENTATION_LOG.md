# Implementation Log

> Append-only log of executed steps. Newest at the top.

## Entry format

```
## Step NN — <short title>

- **Date**: YYYY-MM-DD
- **Feature-dev artifacts**: `.feature-dev/<ts>/` (if `/feature` was used)
- **Prompt**: `implementation/step-NN/prompt.md`
- **Output**: `implementation/step-NN/output.md`
- **Summary**: 3-5 lines on what changed.
- **Open questions**: bullets raised during execution, status (resolved/deferred).
- **Deferred decisions**: deliberately postponed items.
- **Analysis/-files touched**: paths under `analysis/artifacts/05-synthesis/` or `06-formalization/` updated this step (or "none").
- **Smoke-test status**: passed/blocked/not applicable.
```

---

<!-- entries appended below this line, newest first -->

## Step 07.1 — `lmsBlockApi` (CRUD + reorder + assignLabels M:N) + `verifyBlockOwnership` guard + `mapToBlock` mapper

- **Date**: 2026-05-18
- **Feature-dev artifacts**: `.feature-dev/1779080660/` (`research.md` 209 LOC + `design.md` 301 LOC + `plan.md` 184 LOC + `review.md` 271 LOC + `review-iter2.md` + `qa.md` 221 LOC — first `/feature` full pipeline под `[[always-via-feature-skill]]`; one fix-loop iteration на Stage 5 (REV-001 helper extract + REV-002 Schema cascade test) + Stage 6 QA APPROVED).
- **Prompt**: `implementation/step-07.1/prompt.md` (1755 LOC)
- **Output**: `implementation/step-07.1/output.md`
- **Summary**: **Second sub-step of Step 7 decomposition** (api-server слой; first consumer Step 7.0 Block contracts). Поставлен `lmsBlockApi` с 5 методами mirror `lmsSessionApi` Step 6.1 (`create` / `update` / `delete` / `reorder` / `assignLabels` M:N), `verifyBlockOwnership` guard с extra JOIN layer (block → session → day → week → plan; returns `{status, sessionId, dayId, weekId, planId}` для reuse в Step 8 Schema chain), `mapToBlock` + `mapToBlockWithLabels` с runtime `intensitySchema.parse` / `timeCapSchema.parse` на read (zero `as` casts per `[[type-quality]]`), 29 integration tests (25 в `block/admin.test.ts` + 4 в `guards.test.ts`). **6 commits на `feat/training-domain`** (`2c9b6d6a..8cf42432`): per-phase (`guard → mapper → endpoint`) + 2 review-fix (`fix(api-server): extract assertlabelsapplicable helper and add schema-cascade assertion` per REV-001+REV-002 + `test(api-server): tighten concurrent assignlabels case to identify winner per qa-002` per Stage 6 QA-002) + 1 docs. **4 новых + 4 правки = 8 файловых касаний** внутри `@repo/api-server`. Ключевые инварианты verified: **D-7** (`admin.ts:265` explicit `if (data.labelIds.length > 0)` short-circuit вокруг `createMany`; verified case 20 `683-718` before=3→call→after=0 + `BlockLabelAssignment.count === 0`); **`retryOnP2034` wraps только create+assignLabels** (Serializable + unique-key SSI contention), update/delete/reorder без обёртки; **OQ-b1 update silently ignores `labelIds`** в payload (only intensity/timeCap/notes conditional-spread; case 11 verifies); **`Prisma.JsonNull` marshalling** для clearing intensity/timeCap; **planId defence-in-depth** в create+reorder (D-4 extra case 8 для regression-guard); **`BLOCK_WITH_LABELS_INCLUDE satisfies Prisma.BlockInclude`** (D-5 inline Stage 5 REV-007 closed). Root verification all-green: `pnpm check-types` 16/16 (12 cached), `pnpm lint` 16/16 (12 cached), `pnpm test` 1066/1066 (110 files; +30 от 1036 baseline ≈ +29 new + 1 counting drift), `pnpm --filter @repo/api-server test` 581/581 (73 files; 552→581 exact +29 match), `pnpm dep:check` 0/1169 (+4 modules = exact match для 4 новых файлов в planner range [3-5]). **Zero § 0 STOP-and-surface escalations** — все 11 verbatim quote sections (§ 0.1-0.11) byte-for-byte matched HEAD `11296bdf`. **Zero new planner-discipline flavours surfaced** — six existing checklists held the bar. **Ninth cleanest run в ряд** (Step 6.3 → 6.4 → 6.4.5 → 6.5 → 6.6 → 6.7 → 7.0 → 7.1). Branch-cut override honored без surface (no `feat/<slug>` cut). Husky pre-commit + commit-msg clean all 6 commits без `--no-verify`; subjects ≤87 chars fully lowercase.
- **Open questions** (no executor escalations; 5 D-numbered minor justifications в `output.md` + 1 paper deviation accepted):
  - **D-1 (`assertLabelsApplicable` helper typing via Omit deny-list, not `Prisma.TransactionClient`)** — prompt § 3 Phase 3 helper sig cited `tx: Prisma.TransactionClient`; extended Prisma client (soft-delete `$extends` в `db/client.ts`) ломает базовый тип (`tsc` TS2345 на `coachProfile.findUnique` `Exact<T,...>` vs `SelectSubset<T,...>` incompatibility). Решено через `type TxClient = Omit<typeof prisma, "$connect" | "$disconnect" | "$on" | "$transaction" | "$use" | "$extends">` — повторяет внутренний Prisma `ITXClientDenyList` shape, применённый к extended client. Helper extracted Stage 5 REV-001 fix-loop, файл 300→289 LOC, DRY-долг закрыт без `as`-cast hacks. **Planner-discipline note**: future api-server prompts с `tx`-typed helpers — quote the deny-list pattern в § 3 directly, не raw `Prisma.TransactionClient`. Не a new flavour; adjacent refinement of `[[planner-verbatim-registration]]` (canonical mirror file's `tx` arg pattern было читано verbatim, но Prisma type drift via $extends не surfaced thesis-time).
  - **D-2 (Schema-cascade test через `cleanupRaw.archetype.findFirst()` graceful skip)** — Stage 5 REV-002 caught: первая итерация case 13 ограничилась `BlockLabelAssignment` cascade (per prompt § 3 Phase 4 case 13 spec), не verified Schema cascade despite acceptance #10 demand. Schema model требует FK на seeded Archetype (D4); чтобы тест валиден без seed run, executor применил runtime `archetype.findFirst({select: {id, kind}})` → `if (archetype)` create minimal Schema row → assert post-delete `schema.findMany({blockId})` returns `[]`. Pragmatic за graceful-skip (тест passes на свежей DB), cleanup в `finally` для safety. **Planner-discipline note**: future cascade tests с FK depend на seeded entities — explicit `findFirst` pattern принимать как канон, не invent stub-create flows.
  - **D-3 (concurrent `assignLabels` test tightened per QA-002)** — Stage 6 QA-002 WARNING: первая версия case 25 проверяла `stored ∈ allowedFinalStates` (статический массив `[[A,B], [C]]`) — гипотетический regression-interleave мог совпасть с allowed shape, но НЕ соответствовать никакому fulfilled call'у (test passes молча). Inline fix commit `65035943`: `fulfilledKeys = fulfilled.map(r => JSON.stringify(r.value.labels.map(l => ({labelId: l.id}))))` → assert `fulfilledKeys.toContain(storedKey)` — stored state обязан соответствовать labels вернутый каким-то fulfilled call'ом. **Planner-discipline note**: concurrent-state tests с two-winner-shapes — assertion shape "stored matches one of THE winners" сильнее чем "stored matches one of ALLOWED states" (winner identification vs shape allow-list). Принять как канон для future concurrent-write tests.
  - **D-4 (extra test case 8 для planId mismatch — defence-in-depth coverage)** — prompt § 3 Phase 4 case list содержал 7-8 cases в `describe("create")` без явного `planId mismatch` test. `lmsBlockApi.create` имеет inline `if (owner.planId !== planId) throw NotFoundError` (admin.ts:71-73) — defence-in-depth поверх `verifySessionOwnership`. Без явного теста regression может пройти незамеченным. Executor добавил case 8 — другой active plan того же coach'а; ожидание `NotFoundError("Session not found", {sessionId, planId})`. Один extra in-spec тест, no scope creep. **Planner-discipline note**: when api method имеет multi-source param consistency check (route param + guard-returned param), explicit test obligatory. Adjacent refinement of `[[planner-adversarial-review]]` 6-axis sweep (TOCTOU axis includes "outer-vs-intra-param mismatch", не только outer-vs-intra-state).
  - **D-5 (`BLOCK_WITH_LABELS_INCLUDE satisfies Prisma.BlockInclude` annotation)** — Stage 5 INFO REV-007 предложил `satisfies`-аннотацию для compile-time include-shape проверки. Принят (1 keyword). Не нарушает prompt § 8 anti-pattern #6 ("не хойстить BLOCK_WITH_LABELS_INCLUDE в shared module") — константа остаётся локальной в `admin.ts`. Minor type-quality polish.
  - **Acceptance #15 paper deviation accepted** — prompt § 5 acceptance #15 expected `@repo/contracts/lms/block` consumed в 3 files (admin.ts + admin.test.ts + block.mapper.ts); actual 2 files (admin.ts + block.mapper.ts) — test file uses object literals + Vitest method-signature narrowing instead of explicit type imports. Functional impact: zero (Vitest narrows from method signature; type errors caught на call site). **Planner-discipline note**: future test prompts могут explicitly require `import {type CreateBlockData} from "@repo/contracts/..."` если defensive typing нужно (1-line cost). Не a real issue; paper-only deviation.
- **Deferred decisions** (10 carry-forwards: 5 новых от Stage 6 QA + 5 pre-existing):
  - **NEW: QA-001 — `@@unique([sessionId, order])` constraint на `Block` model** — WARNING severity. Concurrent `Block.create` опирается полностью на Postgres SSI false-positive detection + `retryOnP2034`. Безопасно для текущего coach-edit-plan workload (admin не hot path), но при росте concurrency (Step 8 Schema добавляет ещё нагрузку на цепочку) может потребоваться явный DB constraint. **Action**: planner discuss with user перед Step 8 thesis cycle — schema change OOS для Step 7.x, нужен dedicated mini-step или Step 8.0 prepend.
  - **NEW: QA-003 — router middleware для `ZodError` из mapper'а** — WARNING severity, Step 7.2 prompt concern. Если DB-corrupt JSON в `intensity`/`timeCap` (manual SQL edit, future migration mistake), `mapToBlock`'s `intensitySchema.parse` бросит `ZodError`, который `handlePrismaError` пропустит unchanged → opaque 500 на HTTP layer. Step 7.2 prompt должен включать route middleware: catch ZodError separately, surface как `InternalServerError` с `{kind: "DbCorruption", entity: "Block"}` structured payload.
  - **NEW: QA-006 — HEAD_COACH + ARCHIVED composition test** — INFO severity. Currently покрыто индивидуально (test 2 = self-coach archived; guards test 3 = head-coach bypass на active); composition (head-coach trying to edit archived plan via Block) не тестируется явно. Correctness by composition (verifyBlockOwnership returns ARCHIVED status; verifyPlanEditable throws). Optional 10-line test for completeness; не blocker для Step 7.2.
  - **NEW: QA-019 — D-7 test outcome-only, no SQL-spy assertion** — INFO severity. Case 20 verifies `BlockLabelAssignment.count === 0` post-call (outcome-based). Implementation has explicit `if (length > 0)` guard (correct). SQL-spy-based test (assert `tx.blockLabelAssignment.createMany` call count === 0) would catch implementation-style regressions (someone removing the guard) but requires Prisma instrumentation per `[[no-tech-debt-in-mocks]]` rule (rejected). Outcome test is sufficient; flag for awareness.
  - **NEW: QA-022 — `TxClient` Omit deny-list fragile to Prisma major upgrades** — INFO severity. Deny-list `Omit<typeof prisma, "$connect" | "$disconnect" | "$on" | "$transaction" | "$use" | "$extends">` correct для Prisma 6; future Prisma client additions (`$metrics`, `$runCommandRaw`, etc) would leak into TxClient. Flag for future Prisma upgrade prompts (`/upgrade @prisma/client`) to re-verify omit list shape.
  - **Pre-existing carry-forwards (re-flagged)**:
    - **Hoist `BLOCK_WITH_LABELS_INCLUDE` в shared module** — Step 8 trigger (when Schema entity ships and also wants the include).
    - **`mapToBlockWithSchemas` mapper** — Step 8 (Schema entity).
    - **React Context для label preload** — Step 7.4 trigger (5-level prop drilling materializes per Step 6.6/6.7 deferred).
    - **Symbol rename `cms{Label,Exercise}AdminApi` → `lms*`** — Step 6.1.5 deferred, low priority.
    - **`useLabelSearch({level:"BLOCK"})` third callsite** — Step 7.4 prompt должен add к PlanDetailView preload pattern.
- **Analysis/-files touched**: **none** (no Prisma schema change; no domain semantics change; pure api-server slice добавление под existing model).
- **Smoke-test status**: **N/A** — api-server-only step, no user-visible surface. Smoke resumes Step 7.4 (BlockList UI surface).
- **Process note — first `/feature` full pipeline под `[[always-via-feature-skill]]`** (Step 7.0 was `/feature small`; Step 7.1 is first full pipeline invocation since rule codification). Stage 5 reviewer fired one iteration of REV-001/REV-002 (legitimate code-quality improvements: helper extraction + cascade test add); Stage 6 QA fired QA-002 inline tightening — both зашиплены extra commits (`77870ccf` + `65035943`) before final docs commit `8cf42432`. **Pattern**: full pipeline для multi-method api slices = consistent ROI на review/QA stages (catches helper-extraction opportunity + concurrent-test loose-assertion); future Step 7.4 + Step 8 (larger surfaces) — оставлять `/feature` full pipeline; Step 7.2 + 7.3 (routes + hooks, smaller surface) — `/feature small` per planner pick.

## Step 07.0 — Block contract slice + Intensity/TimeCap shared VOs + BlockLabelAssignment M:N contract

- **Date**: 2026-05-18
- **Feature-dev artifacts**: `.feature-dev/1779009158/` (`research.md`, `review.md` — `/feature small` pipeline per `[[always-via-feature-skill]]`; first sub-step under the new rule).
- **Prompt**: `implementation/step-07.0/prompt.md`
- **Output**: `implementation/step-07.0/output.md`
- **Summary**: **First sub-step of Step 7 decomposition** (Block-level operations). Pure additive contract slice: `lms/block` (8 files — block.constants, block.schema, block.types, block.schema.test, block-api.schema, block-api.types, block-api.schema.test, index) + cross-entity VOs `intensitySchema` + `timeCapSchema` в `lms/_shared` (4 files — intensity.ts + intensity.test, time-cap.ts + time-cap.test) + 4 registration edits (lms/\_shared/index +2, lms/index +1, package.json exports +1, README LMS bullet). **2 atomic commits** на `feat/training-domain` (started at `a85eff4b` post-PR-#194 close-out): `a3bcef6e feat(contracts): add lms/block slice with intensity and timecap shared vos` (74 chars) → `523e16d9 docs(step-07.0): write executor output report`. **16 files / +819/-1 LOC** (production code ~210 + test code ~610). `blockSchema` embedded `labels: Label[]` (per Step 6.2 D7 embed pattern); `intensitySchema.refine` at-least-one-dimension (5 additive optional sub-fields: `effortPercent` union value|range, `rpe` value 1-10, `pace` 4-enum, `hrZone` Z1-Z5, `numericPace` { value, distanceUnit, paceType }) — rejects `{}` empty pollution per OQ-1A; `timeCapSchema.refine` `max > min when set`. `assignBlockLabelsSchema = { labelIds: cuid[].max(MAX_LABELS_PER_BLOCK=10).unique }` — empty array valid (= clear all labels per D-7 server-handler invariant); `reorderBlocksSchema` mirrors session reorder (min 1, unique). Address shapes: `blockBySessionParamsSchema { planId, sessionId }` (day-scope-free per OQ-2A) + `blockByIdParamsSchema { planId, blockId }`. Branch-cut override honored (no `feat/<slug>` cut). Root verification all-green: `pnpm check-types` 16/16, `pnpm lint` 16/16, `pnpm test` 1036/1036 (+66 Step 7.0 + ~12 baseline drift since 958 — same counting-noise family as Step 6.6→6.7 reconcile note; zero regression in existing tests), `pnpm dep:check` 0 violations / 1165 modules. **Zero § 0 STOP-and-surface escalations** — все verbatim quotes § 0.1-0.6 matched HEAD `a85eff4b` byte-for-byte. **Zero new planner-discipline flavours** — six existing checklists held. **Eighth cleanest run in a row** (Step 6.3 → 6.4 → 6.4.5 → 6.5 → 6.6 → 6.7 → 7.0). Husky pre-commit + commit-msg clean both commits без `--no-verify`. **`/feature small` pipeline (first invocation under `[[always-via-feature-skill]]`)** — Stage 5 reviewer APPROVED (2 WARNING actioned/accepted via D-1/D-4, 3 INFO actioned/flagged via D-2/D-6/D-7), Stage 0 branch-cut override honored without surface (no fresh `feat/<slug>` attempt).
- **Open questions** (no executor escalations; 7 D-numbered minor justifications в `output.md`):
  - **D-1 (reviewer WARNING 1 fix actioned inline — test rename + positive strip case)** — original test name `"rejects unknown dimension key (strict additivity)"` empirically misleading: Zod `z.object` strips unknown keys by default; `{unknown: 1}` rejects via refine (strip → empty → fail), NOT via strict mode. Executor renamed к `"rejects an object with only an unknown key (refine-driven, not strict)"` + added positive case `"strips unknown keys when a known dimension is present (Zod default passthrough)"` empirically documenting strip behavior. 15→16 cases в `intensitySchema describe`. Inline reviewer-fix per manifesto (test naming clarity), not § 0 escalation. **Planner-discipline note**: future contract tests with refine + Zod's default-strip should test BOTH "only-unknown rejects" AND "known + unknown strips known-only-result" — defensive clarity for strict-vs-refine distinction.
  - **D-2 (pre-shipped affordances в `_shared` beyond § 3 minimal spec)** — `_shared/intensity.ts` and `_shared/time-cap.ts` export extra symbols: `HR_ZONES`, `NUMERIC_PACE_DISTANCE_UNITS`, `NUMERIC_PACE_TYPES`, `PACE_VALUES`, `TIME_CAP_UNITS` (5 `as const` tuples — used by Step 7.5 form widgets for `<Select>` options), `rpeSchema` + `paceSchema` standalone (used by Step 7.5 sub-widget composition), `RpeIntensity` + `PaceValue` + `TimeCapUnit` (extra inferred types). Additive-only, zero collision risk, saves a Step-7.5 round-trip refactoring. **Planner-discipline note**: pattern is "pre-extract observable affordances when shipping shared VOs" — applies to future \_shared additions (e.g. Schema Step 8.0 may ship similar archetype-driven const tuples in `_shared`).
  - **D-3 (mock data fix in tests: `applicableLevels: ["BLOCK"]` vs initial `["BEGINNER"]`)** — first-pass mock Label object used `"BEGINNER"` which is NOT in `APP_LEVELS = ["DAY", "SESSION", "BLOCK"]` enum (per `label.constants.ts:6`). 3 tests failed initial run; executor re-read `label.constants.ts` and corrected. Instinct-specing на mock data caught pre-commit. Family of Step 6.7 D-4 React.FC convention check — non-blocker, but reinforces "verify all literals against actual contract enums at write-time".
  - **D-4 (block.schema.test.ts 323 LOC > 300 manifesto cap — accept scaling artifact)** — Block has 5 operations (vs Session 3) → ~67% more cases naturally; 7.6% overage = scaling artifact, not code smell. Reviewer flagged WARNING 2, executor + reviewer accepted as-is. Refactor into multiple test files not justified (single-source-of-truth-per-schema-file convention). Adjacent refinement of manifesto: 300-LOC cap is heuristic; entity-with-N-ops where N > 3 may exceed proportionally without smell.
  - **D-5 (planner off-by-one in § 5.2 grep expected count)** — planner's expected `'"./lms/'` count = 10; actual = 9. Pattern `'"./lms/'` (trailing slash) doesn't match root entry `"./lms":` (no slash). Content correct (10 total `./lms*` entries: 1 root + 9 subpaths). **Planner-discipline note**: grep pattern arithmetic in § 5.2 must distinguish root-entry vs subpath-entry pattern characters; for trailing-slash patterns count = subpath-only.
  - **D-6 (refine post-strip semantics documented inline via D-1)** — `intensitySchema.refine` runs AFTER `z.object` strip. `{}` and `{unknown: 1}` both fail same code path; `{rpe: ..., unknown: 1}` succeeds with `unknown` stripped. NOT added `.strict()` (deviation from § 3 minimal spec would be smaller, but session.schema also passthrough — mirror-target consistency). Documented через D-1 positive test case rather than code comment per `[[global-preferences]]`.
  - **D-7 (`assignBlockLabelsSchema.labelIds: []` semantics flag for Step 7.1)** — contract surface accepts empty array as valid (no `min(1)`); semantic "empty = wipe all labels" NOT encoded в Zod refine (impossible to express in schema layer). **Step 7.1 api-server handler MUST implement: `if (labelIds.length === 0)` → tx delete-all `BlockLabelAssignment` for blockId, no insert.** Reviewer INFO 3 flagged для downstream awareness. **Planner action required**: Step 7.1 prompt § 0 / § 3 must include this invariant as hard requirement (zero-insert tx semantic, NOT just bulk-recreate with empty array).
- **Deferred decisions** (8 carry-forwards, mostly pre-existing; 1 new from D-7):
  - **README pre-existing inconsistency about `day` / `exercise` / `label` missing from LMS bullet** (out of Block-additive scope per § 4; flag as housekeeping for next contract-touching step — possibly Step 8.0 Schema entity prompt should append all 4 missing entries + Schema itself in one bullet edit).
  - **Block api-server (`lmsBlockApi`)** — Step 7.1 first consumer. **D-7 invariant MUST land in Step 7.1 prompt** (empty `labelIds[]` = tx delete-all + zero insert handler semantics).
  - **Block HTTP routes** — Step 7.2.
  - **Block client API + hooks** (`useCreateBlock` / `useUpdateBlock` / `useDeleteBlock` / `useReorderBlocks` / `useAssignBlockLabels`) — Step 7.3.
  - **Block UI** (BlockList, BlockCard, BlockLabelMulti widget, AddBlockButton) — Step 7.4. OQ-3 (BlockLabelMulti shape: extend `@repo/ui/LabelSelect` to `multi` mode vs fork to `MultiLabelSelect`) deferred to 7.4 thesis.
  - **Intensity / TimeCap UI editors** — Step 7.5 (form-driven per 5-dim Intensity + 3-field TimeCap; `as const` tuples + standalone schemas из D-2 pre-shipped affordances ready for consumption).
  - **Schema entity contracts** — Step 8.0. Consumes `intensitySchema` from `_shared` (pre-extraction в Step 7.0 was именно для этой reuse — verifies D-2 affordance rationale).
  - **`.strict()` mode on `intensitySchema`** (D-6 deferred): currently passthrough mirrors session.schema; revisit if UI silent typos surface in Step 7.5 form widgets.
- **Analysis/-files touched**: **none** (no Prisma schema change; no domain semantics change; pure additive contract slice + cross-entity VO extraction).
- **Smoke-test status**: **N/A** — contract-only step, no user-visible surface, no api-server / platform / UI code. Smoke-test resumes Step 7.4 (BlockList UI surface — first scenario-based browser smoke since Step 6.7).

## PR #194 merged into `main` (Steps 6.6 + 6.7 batch)

- **Date**: 2026-05-17T??:??:??Z (merge commit `376e694f`; clock not captured at merge — recreate-branch step retroactive close-out)
- **Merge commit**: `376e694f Merge pull request #194 from maksim-pokhiliy/feat/training-domain`
- **PR URL**: https://github.com/maksim-pokhiliy/the-discipline-program/pull/194
- **Scope**: 9 commits / 25 files / +978/-139 LOC. Batches **Step 6.6** (DayRow header reshape — 1 atomic code commit) + **Step 6.7** (Session body + dnd-kit reorder + SessionCard + cross-package extracts — 5 atomic per-layer code commits). First two UI surfaces workflow's; first `@dnd-kit/*` install in repo. Continues PR #193 (Steps 6.1.5 → 6.5).
- **Pre-merge gates** (all green; planner-side gate sweep before `gh pr create`):
  - `pnpm check-types` 16/16 (turbo 32/32 cache-miss → all clean).
  - `pnpm lint` 16/16.
  - `pnpm test` 958/958 (zero test file deltas verified via `git diff fcff61cd..HEAD -- '**/*.test.*' '**/*.spec.*'`).
  - `pnpm dep:check` 0 violations / 1153 modules.
  - `pnpm build` 4/4 apps in 1m41s (admin/marketing/platform/storybook).
  - Pre-push hook 32/32 tasks in 1m37s (`dep:check + turbo lint check-types --filter="...[origin/main]"`).
  - Husky pre-commit + commit-msg clean all 9 commits без `--no-verify`.
- **Browser smoke-tests**: 12/12 Step 6.6 + 13/13 Step 6.7 — both green (2026-05-17, user-side).
- **No CI-blocking findings surfaced** during PR review. PR body documented per-step deliverables, architectural decisions (D7 lazy Day + 9 OQ A-G ratifications across 6.6 + 6.7), verification table, 11 deferred carry-forwards, process-rule reversal note (`[[always-via-feature-skill]]` codified mid-batch — Step 7 onwards prompts wrap в `/feature` / `/feature small`).
- **Post-merge cleanup**: `feat/training-domain` deleted on `origin`; recreated locally from fresh `main` (HEAD `376e694f`) per `[[training-domain-workflow]]` convention. Untracked `implementation/step-06.7/prompt.md` (executor docs commit `daf6b293` included only output.md — minor planner-side housekeeping miss) staged into this close-out commit retroactively.
- **Carry-forwards into `main`** (all remain open per their original close-out entries; none blocker for Step 7):
  - React Context для label preload (5-level trigger arrives Step 7.4 BlockList).
  - `@repo/ui` `LabelSelect` `size`/`variant` props extension (Step 7 BlockLabelSelect trigger if needed).
  - ZWS strip on Day/Session notes (domain free-text + coach-owned; revisit on QA report).
  - WeekNotes client `maxLength` cap (Step 5 QA-005 deferred indefinitely).
  - Cross-day Session drag (out per Step 6.1 design).
  - `useBlurCommit` 4th callsite trigger (only if Block adds notes field — verify Step 7 thesis).
  - Tests for new UI components (UI-layer; mirror Step 5/6.x no-test precedent; coverage via browser smoke-tests).

## Step 06.7 — Session body: SessionCard + dnd-kit reorder + Add session + extracts (LabelSelect, useBlurCommit)

- **Date**: 2026-05-17
- **Feature-dev artifacts**: **N/A** — direct prompt execution (last invocation of the direct-exec convention; per `[[always-via-feature-skill]]` saved post-this-step all future prompts wrap в `/feature` or `/feature small`). Intermediate artifacts = `implementation/step-06.7/{prompt,output}.md` + git history `2ab958ea..daf6b293`.
- **Prompt**: `implementation/step-06.7/prompt.md`
- **Output**: `implementation/step-06.7/output.md`
- **Summary**: **Second UI surface workflow's, second production consumer of Step-6.5 hooks** — replace `<Typography>No sessions</Typography>` placeholder в DayRow (line 84-86 post-Step-6.6) реальным `SessionList` с дочерним `SessionCard` per Session. SessionCard содержит dnd-kit `useSortable` drag-handle (`cursor:grab + touchAction:none` per D-4), label Autocomplete, blur-commit notes field, trailing kebab `Menu` → `Delete` через canonical `@repo/ui` `ConfirmationModal` (Menu+Modal как siblings внутри dnd Box per D-3 + plan-action-menu precedent). SessionList wraps в `DndContext` + `SortableContext` (`verticalListSortingStrategy` + Pointer/Keyboard sensors per a11y) + optimistic local `sortedSessions` state с `onError: setSortedSessions(previousOrder)` rollback + `useEffect([sessions])` invalidate-resync (per OQ-D D1). AddSessionButton instant-creates через `useCreateSession.mutate({})` (per OQ-F F1; server stores `{labelId:null, notes:null, order=max+10}`). PlanDetailView добавляет второй `useLabelSearch({level:"SESSION"})` call; SESSION options drillют 4 levels (PlanDetailView → WeekGrid → DayRow → SessionList → SessionCard) per OQ-C C1. **Concurrent cross-package extracts** (per OQ-A A1 + OQ-B B1 — оба triggered satisfied): `packages/ui/src/components/label-select/index.tsx` (73 LOC generic `LabelSelect` Autocomplete<Label>) + `apps/platform/src/lib/hooks/use-blur-commit.ts` (52 LOC byte-equivalent extract). 3 callsites refactor'нуты в Phase 4 commit — WeekNotes (68→36 LOC) + DayNotesField (62→32 LOC) + DayLabelSelect (69→28 LOC); behaviour-preserving (Step 5 + 6.6 smoke-test invariants intact). **First `@dnd-kit/*` install в repo** — 3 deps добавлены к pnpm catalog (`@dnd-kit/core ^6.3.1` + `@dnd-kit/sortable ^10.0.0` + `@dnd-kit/utilities ^3.2.2` per D-1; sortable bumped к ^10 vs prompt's ^8 hypothesis, current stable) + 3 catalog refs в `apps/platform/package.json`. **5 atomic per-layer commits** на `feat/training-domain` (`2ab958ea..70a84707`) per § 7 dependency order: `chore(platform): add dnd-kit deps for session reorder` → `feat(ui): add generic labelselect autocomplete component` → `feat(platform): add useblurcommit hook for shared blur-commit text fields` → `refactor(platform): adopt labelselect+useblurcommit in plan-detail components` (subject shortened 104→76 chars per D-2 commitlint cap fixup без bypass) → `feat(platform): add session body with sessioncard list and dnd-kit reorder` (Phases 5+6+7+8 atomically). Plus `daf6b293 docs(step-06.7): write executor output report`. **19 files / +741/−122 LOC** (8 new + 10 modified + 1 lockfile). Root verification all-green: `pnpm install` OK (+4 packages), `pnpm check-types` 16/16, `pnpm lint` 16/16, `pnpm test` 958/958 (zero test file deltas verified via `git diff cfa4a792..HEAD -- '**/*.test.*'` — counting drift 929→958 от Step 6.6 = counting noise across executor sessions, NOT real test loss), `pnpm dep:check` 0/1153 (2 modules below planner range [1155-1180] per D-7; 0 violations preserved). **Zero § 0 STOP-and-surface escalations** — все verbatim quotes § 0.1-0.9 matched HEAD `cfa4a792` byte-for-byte. **Zero new planner-discipline flavours** — six existing checklists proactively covered все surfaces. **Seventh cleanest run in a row** (Step 6.3 → 6.4 → 6.4.5 → 6.5 → 6.6 → 6.7). Husky pre-commit + commit-msg clean all 6 commits без `--no-verify`.
- **Open questions** (no executor escalations; 7 D-numbered minor justifications в `output.md`):
  - **D-1 (dnd-kit catalog versions)** — prompt § 0.10 G1 предложил `^6 / ^8 / ^3`; § 3 Phase 1 prescribed exact `^6.3.1 / ^10.0.0 / ^3.2.2` (current stable). Executor following Phase 1 directly. `@dnd-kit/sortable` v10 major bump vs hypothesis ^8 — API used in SessionList (`SortableContext` + `useSortable` + `arrayMove` + `sortableKeyboardCoordinates` + `verticalListSortingStrategy`) is byte-compatible с v8 spec; всё green. **Planner-discipline note**: для future external-dep prompts, defer к "current stable on install date" instead of pinning major version в § 0.10 hypothesis — saves a thesis-cycle round-trip when reality drifts.
  - **D-2 (commitlint header overflow на Commit 4)** — original `refactor(platform): adopt labelselect and useblurcommit in week-notes, day-label-select, day-notes-field` = 104 chars vs commitlint cap 100. Executor recreated commit (фейл commit-msg означает commit не создан, поэтому не amend существующего → no `--no-verify`, no `--no-edit` per `[[global-preferences]]`) с subject `refactor(platform): adopt labelselect+useblurcommit in plan-detail components` (76 chars) + затронутые файлы перенесены в body. Family of Step 6.1 PROMPT-001 (commitlint CamelCase reject) — same fix-flow (root-cause-fix-then-recreate). **Planner-discipline note**: для multi-file refactor commit subjects, prefer summarized scope ("in plan-detail components" / "in 3 callsites") to enumerated file list; reserves chars для conventional-commits prefix overhead.
  - **D-3 (SessionCard layout: Menu + ConfirmationModal как siblings внутри dnd Box, не вложены в row Stack)** — отклонение от prompt § 3 Phase 6 sketch (там были вложены в `<Stack>`). Привёл к канону `plan-action-menu.tsx` § 0.6 — portal-elements (Menu, Dialog) висят siblings к trigger-Stack, не дети. Семантически эквивалентно (Menu/Dialog портируются вне DOM tree), но aligned с existing pattern + не trips ESLint `react/no-multi-comp`. Pattern-alignment per `[[scope-via-existing-patterns]]`.
  - **D-4 (drag handle: `cursor:grab + touchAction:none`)** — добавлено вне явного prompt prescription. `touchAction:none` обязателен для PointerSensor работы на touch-устройствах (стандартный dnd-kit gotcha — иначе browser scroll-жест конфликтует с drag). `cursor:grab` — desktop UX affordance. Обе идиоматичны per @dnd-kit docs. Executor domain knowledge applied правильно.
  - **D-5 (lint --fix import reorder в session-card.tsx + session-list.tsx)** — eslint автоматически отсортировал `@dnd-kit/*` поверх `@mui/*`. Family Step 6.4 D-5 / 6.4.5 D-3 / 6.5 D-3 / 6.6 D-2 — lint-staged auto-format pre-ratified.
  - **D-6 (pnpm test 958/958 vs prompt § 5.1 ожидал 929/929)** — counting noise. `git diff cfa4a792..HEAD -- '**/*.test.*' '**/*.spec.*'` returns blank (zero touched test files). Resolved per Step 6.6 housekeeping note: test count summary lines варьируются across executor sessions (включаются/исключаются skipped/dry-run cases differently). Real invariant: zero file deltas → zero behavioural test deltas. Baseline at HEAD `daf6b293` = **958 passing, zero failing**. Adjacent refinement of `[[planner-verbatim-registration]]` already captured Step 6.6.
  - **D-7 (pnpm dep:check 1153 modules vs prompt § 5.1 ожидал [1155-1180])** — 2 modules ниже нижней границы. 0 violations preserved (the critical AC). Slight discrepancy от excluded transient `@dnd-kit/*` peerDeps in depcruise graph (external module accounting). Не critical т.к. invariant = "0 violations" hit. **Planner-discipline note**: future dep:check predictions для new-dep steps — drop the upper bound (transient resolution varies); state "0 violations + N+ modules где N = current baseline + #new-source-files" instead of "[lower, upper] range".
- **Deferred decisions** (7 carry-forwards, все pre-existing per prior steps; no new follow-ups identified):
  - **ZWS / control-char normalization on Session notes** (per OQ-C carry-forward из Step 6.6; Session наследует тот же domain rationale — free-text + coach-owned).
  - **React Context для label preload** — 4-level prop drilling acceptable per OQ-C C1; **5-level trigger arrives в Step 7** с Block surface (PlanDetailView → WeekGrid → DayRow → SessionList → SessionCard → BlockList → BlockCard = 6 levels — Context extraction expected within Step 7 thesis cycle).
  - **Optimistic updates для label/notes mutations** (per Step 6.5 OQ-D — invalidate-only; revisit если UX surfaces flicker).
  - **WeekNotes client `maxLength` cap** (Step 5 QA-005 deferred; не в Step 6.7 scope per § 4 explicit; close separately if Week-side concern surfaces).
  - **Cross-day Session drag** (out per Step 6.1 design — reorder day-scoped; Block-level cross-Session drag = Step 7 concern, if at all).
  - **`@repo/ui` `LabelSelect` props for `size` / `variant` override** — currently hardcoded `size="small" variant="outlined"`; extend на Step 7 BlockLabelSelect trigger if нужны другие variants. Премature extension defer.
  - **Tests для новых components** (UI-layer; mirror Step 5 + 6.5 + 6.6 + 6.7 no-test precedent; coverage via 13-step browser smoke).
- **Analysis/-files touched**: **none** (no Prisma schema change; no domain semantics change; pure UI + dep install + cross-package extract).
- **Smoke-test status**: **passed** (2026-05-17, user-side browser smoke). Все 13 шагов сценария из `step-06.7/output.md` § "Сценарий смоук-теста" зелёные: 7 DayRows с "+ Add session" + zero SessionCards (no `<Typography>No sessions</Typography>`); single `useLabelSearch({level:"SESSION"})` GET preloading 2 labels; instant-create → empty SessionCard render с drag-handle + Autocomplete + multiline notes + kebab; label select PUT + invalidate-refetch + toast; notes blur-commit с trim + null-on-empty; F5 persistence; second card create → drag-reorder с optimistic local + PUT + toast; F5 order persists; kebab→Delete→ConfirmationModal→Delete → DELETE + toast + card removed; network-offline graceful degrade с notifyError + eventual consistency на restore. Zero UI regression — Step 5 calendar viewport + WeekNotes flow + Step 6.6 DayLabelSelect/DayNotesField (post-Phase-4 refactor) все intact под новыми extracts. Step 6.7 fully closed.
- **Process note — direct-exec convention reversed mid-Step**: Step 6.7 prompt was written с "direct prompt execution (no `/feature` wrapper)" per Step 6.4.5 D-1 precedent → repeated через 6.5 / 6.6. User pushback'нул during Step 6.7 execution с честным "почему ты написал делать мимо feature промпта?" — surface'ил scale concern (19 files / cross-package extracts / new `@dnd-kit` dep значимо выигрывают от Stage 5 reviewer + Stage 6 hostile QA). Reverted to "always via /feature or /feature small (planner picks по scope)" — saved as `[[always-via-feature-skill]]` memory entry с branch-cut override instruction (`/feature` default cuts `feat/<slug>` from main — конфликтует с long-lived `feat/training-domain`). **WORKFLOW.md `Per-step cycle` § 4** already aligned ("Each step runs through `/feature` or `/feature small` for trivial 1-3-file no-schema steps"); direct-exec was the explicit deviation, not the norm. From Step 7 onwards prompts wrap в `/feature` (full pipeline для new-domain Block-level work). Not a new planner-discipline flavour (a-f) — это process-level lesson (about how to invoke executor, not what to spec). Saved as procedural memory + cited in next step prompts.

## Step 06.6 — DayRow header reshape: Day label autocomplete + Day notes field

- **Date**: 2026-05-17
- **Feature-dev artifacts**: **N/A** — direct prompt execution per Step 6.4.5 D-1 precedent (`/feature small` would re-derive brief and try to cut `feat/<slug>` from main, incompatible with long-lived `feat/training-domain` per `[[training-domain-workflow]]`); intermediate artifacts = `implementation/step-06.6/{prompt,output}.md` + git history `8c08c218..f11e3e7d`.
- **Prompt**: `implementation/step-06.6/prompt.md`
- **Output**: `implementation/step-06.6/output.md`
- **Summary**: **First production UI surface workflow's since Step 5 plan-detail shell.** Reshape `apps/platform/src/modules/plan-detail/components/day-row.tsx` (49 → 95 LOC) из плоского weekday-name + "No sessions" stub'а в первый consumer трёх Step-6.5 хуков (`useUpdateDayLabel`/`useUpdateDayNotes`/`useLabelSearch`). 2 новых presentational компонента — `DayLabelSelect` (69 LOC, `Autocomplete<Label>` per `apps/admin/src/lib/components/coach-owner-autocomplete/index.tsx` precedent §0.4, slotProps + CircularProgress + conditional-spread `paramsSize/paramsDisabled/paramsFullWidth/paramsId` for `exactOptionalPropertyTypes: true`, server-preloaded options через single `useLabelSearch({level:"DAY"})` в PlanDetailView per OQ-A A1) + `DayNotesField` (62 LOC, multiline `TextField` с blur-commit invariant — verbatim mirror `WeekNotes.commit()` block: `committedRef` + `isFocusedRef` + `useEffect`-sync-when-unfocused + trim + `null`-on-empty + client cap `DAY_CONSTANTS.MAX_NOTES_LENGTH=2000`). DayRow владеет 2 mutation'ами + 3-row stacked layout (weekday-header → responsive `direction={{xs:"column", md:"row"}}` label/notes row → "No sessions" placeholder). WeekGrid signature extended `{ planId, monday, days: DaySlot[], labelOptions, labelOptionsLoading }`; iteration inverted to `dayOfWeekValues.map((dayOfWeek, idx) => { const date = dates[idx]; if (!date) return null; ... })` per § 3 Phase 3 pre-anticipated alternative to satisfy `noUncheckedIndexedAccess: true` без `!` per `[[type-quality]]` (zero non-null assertions). PlanDetailView вызывает `useLabelSearch({level:"DAY"})` единожды на view-mount; drillит options + isLoading + planId + `weekData?.days ?? []` в WeekGrid (single source of truth, дедуп-noise free, TanStack cache `["labels","search","DAY",undefined]` stable through week-navigation). **1 atomic per-layer commit** на `feat/training-domain` (single-package scope = `apps/platform/`, `[[husky-cross-package-squash]]` trigger не сработал per § 0.6 husky pre-check): `8c08c218 feat(platform): reshape day-row header with label autocomplete and notes field` (6 files / +237/−17 LOC). Plus `f11e3e7d docs(step-06.6): write executor output report`. Root verification all-green: `pnpm check-types` 16/16, `pnpm lint` 16/16, `pnpm test` 929/929 (baseline preserved, zero test deltas — see test-count reconcile note below), `pnpm dep:check` 0/1146 (was 0/1144, +2 modules = exact match to 2 new files; planner range `[1146, 1148]` lower bound hit). **Zero § 0 STOP-and-surface escalations** — все verbatim quotes § 0.1-0.7 matched HEAD `50d83f66` byte-for-byte. **Zero new planner-discipline flavours surfaced** — six existing procedural checklists ([[scope-via-existing-patterns]] / [[coach-pov-first]] / [[planner-verbatim-registration]] / [[planner-adversarial-review]] / [[husky-cross-package-squash]] / [[planner-consumer-pattern-read]]) proactively covered все observed paths. **Sixth cleanest run in a row** (Step 6.3 → 6.4 → 6.4.5 → 6.5 → 6.6). Husky pre-commit clean both commits без `--no-verify`.
- **Open questions** (no executor escalations; 5 D-numbered minor justifications в `output.md`):
  - **D-1 (TypeScript narrow в WeekGrid)** — `noUncheckedIndexedAccess: true` (`packages/typescript-config/base.json:21`) делает `dates[idx]: Date | undefined`. Из двух prompt § 3 Phase 3 options — (a) `dayOfWeekValues[idx]!` non-null assertion blocked per `[[type-quality]]`, (b) invert iteration axis so `dayOfWeek` is loop variable — executor выбрал (b): `dayOfWeekValues.map((dayOfWeek, idx) => { const date = dates[idx]; if (!date) return null; ... })`. Branch theoretically unreachable (`getWeekDays` always returns 7 Dates), но TS narrow без `!` — правильная shape. Clean implementation of pre-anticipated alternative; planner-discipline note: § 3 Phase 3 instruction "Pick the approach that types cleanly without `!`" worked as designed — executor not forced to escalate, default chose right.
  - **D-2 (ESLint `curly` auto-fix)** — `pnpm lint --fix` (husky pre-commit lint-staged stage) обернул `if (!date) return null;` → `if (!date) { return null; }` per codebase ESLint config (`curly: all`). Pre-ratified per Step 6.4 D-5 / 6.4.5 D-3 / 6.5 D-3 family (lint-staged auto-format = AST-equivalent transformation, no behaviour delta).
  - **D-3 (`useLabelSearch` location)** — per OQ-A A1 ratification: single hook call в PlanDetailView (`useLabelSearch({level:"DAY"})`), options drilled через WeekGrid → 7 DayRow. TanStack cache `["labels","search","DAY",undefined]` остаётся валиден через week navigation (не перезапрашивается на prev/next click). Single source of truth, чистый DevTools.
  - **D-4 (`React.FC` mixed convention preserved)** — DayRow остался `React.FC<Props>` (mirror existing `WeekGrid`/`WeekNotes`/`WeekNavigator` local convention в папке `plan-detail/components/`); DayLabelSelect + DayNotesField — plain arrow (mirror `coach-owner-autocomplete` precedent § 0.4). Mixed convention acceptable per-folder; не стилистическая регрессия. Minor planner-discipline note: future prompts in `plan-detail/components/` могут explicitly state "match local React.FC convention for DayRow-family files" если строгая single-shape preferred.
  - **D-5 (`disabled || isLoading` vs `disabled === true`)** — destructuring default `disabled = false` гарантирует `boolean` тип; `||` produces `boolean`. Pattern-aligned with `coach-owner-autocomplete:38` precedent. Не нарушает `exactOptionalPropertyTypes: true` т.к. default applied at destructure, не reaching Autocomplete prop boundary as `undefined`.
- **Deferred decisions** (4 carry-forwards, все explicit out-of-Step-6.6 per ratified OQ resolutions; no new follow-ups identified):
  - **ZWS / control-char normalization on Day notes** (per OQ-C C1 ratification). Domain `analysis/artifacts/05-synthesis/domain-model.md §1.1` определяет notes как "free-text"; coach-owned field; ZWS edge rare. Trigger: QA report "notes appear empty but have characters". Implementation when triggered: hoist `normalizeText` helper из `packages/contracts/src/entities/lms/label/label.schema.ts:7` в shared module, apply в DayNotesField `commit()` before trim.
  - **`@repo/ui` LabelSelect extraction** (per OQ-B B1 ratification). Premature at 1 callsite. Trigger: Step 6.7 SessionLabelSelect shows ≥ 90% duplication with DayLabelSelect → extract в `@repo/ui/label-select` с props `{value, options, isLoading, onChange, disabled}` (current DayLabelSelect API stays as the public surface).
  - **WeekNotes ↔ DayNotesField shared blur-commit primitive** — 2 callsites; DRY premature. Trigger: 3rd surface (BlockNotes? — Step 7 candidate). Implementation when triggered: extract `useBlurCommit({value, onCommit})` hook returning `{draft, setDraft, handleFocus, handleBlur}` pair, mirroring shared invariant of both WeekNotes and DayNotesField.
  - **`labelOptions` lazy fetch on Autocomplete open** — Step 6.4 ratified preload server-side (load once on view mount, не on each Autocomplete `onOpen`). Если через 2-3 weeks coach reports slow first render of plan-detail — convert `useLabelSearch` to `enabled: false` + lazy trigger from DayLabelSelect via `onOpen`. Current preload is the explicit coach UX preference per Step 6.4 ratification.
- **Analysis/-files touched**: **none** (no Prisma schema change; no domain semantics change; pure UI surface wrapping existing routes + hooks).
- **Smoke-test status**: **passed** (2026-05-17, user-side browser smoke). Все 12 шагов сценария из `step-06.6/output.md` § "Сценарий смоук-теста" зелёные: 7-row Mon..Sun render с empty Autocomplete + empty TextField + today-circle, label preload через single `useLabelSearch({level:"DAY"})` GET, select+clear roundtrip с invalidate-refetch + toast persistence через F5, notes blur-commit с trim+null-on-empty + breadcrumb-stay-on-clear per D7, network-offline graceful degrade с notifyError + eventual consistency on restore, week-navigation cache preservation. Zero UI regression — Step 5 calendar viewport + WeekNotes flow intact под новым layout. Step 6.6 fully closed.
- **Housekeeping note — Step 6.5 test count reconcile**: my Step 6.5 close-out recorded `pnpm test 958/958` в PLANNING_STATE + IMPLEMENTATION_LOG; Step 6.6 executor recorded `pnpm test 929/929`. `git diff 56f3802e..f11e3e7d -- '**/*.test.*' '**/*.spec.*'` returns **zero touched test files** — discrepancy = counting methodology mismatch between executor sessions, не real test loss. Actual baseline at HEAD `f11e3e7d` = **929 passing tests, zero failing**. Planner-discipline note for future close-outs: ground test counts в final summary line `pnpm test` output, не aggregate from `vitest run --reporter=verbose` which may count skipped/dry-run cases differently. Не a new planner-discipline flavour; minor data-grounding refinement of `[[planner-verbatim-registration]]` (verify at quote-time, не carry from earlier session memory).

## PR #193 merged into `main` (Steps 6.1.5 → 6.5 batch)

- **Date**: 2026-05-16T12:35:00Z
- **Merge commit**: `fcff61cd Merge pull request #193 from maksim-pokhiliy/feat/training-domain`
- **PR URL**: https://github.com/maksim-pokhiliy/the-discipline-program/pull/193
- **Scope**: 32 commits / 117 files / +8624 −261 LOC. Spans Steps 6.1.5 (namespace correction) → 6.2 (7-day shape + day-metadata) → 6.3 (labels-platform) → 6.4 (routes + retry infra) → 6.4.5 (session routes + retry apply) → 6.5 (client API + hooks). Continues PR #192 which batched Steps 6.0 + 6.1.
- **Pre-merge gates**: `pnpm check-types` 16/16, `pnpm lint` 16/16, `pnpm test` 958/958, `pnpm dep:check` 0/1144, `pnpm build` 4/4 apps in 2m32s. Pre-push hook (`dep:check + turbo lint check-types --filter="...[origin/main]"`) — 32/32 successful in 27.85s, zero `--no-verify`.
- **Post-merge cleanup**: `feat/training-domain` deleted on `origin`; recreated locally from fresh `main` (HEAD `fcff61cd`) for Step 6.6 work per `[[training-domain-workflow]]` convention.
- **No CI-blocking findings surfaced** during PR review. PR body documented per-sub-step deliverables, per-layer changes, architectural decisions (D7 lazy Day calendar slot, D8 LMS namespace correction, all OQ-A..G ratifications across the 6 sub-steps), verification table, explicit out-of-scope list (Step 6.6/6.7 UI deferred).
- **Carry-forwards into `main`**: all deferred items from individual sub-steps remain open per their original close-out entries (symbol rename `cms{Label,Exercise}AdminApi → lms*`, `mapToSessionWithLabel` extraction, `retryOnP2034.attempts.max(10)` clamp, GIN expression index on `Label.applicableLevels`). None are blockers for Step 6.6/6.7.

## Step 06.5 — Platform client API + hooks for Sessions / DayMetadata / Labels

- **Date**: 2026-05-16
- **Feature-dev artifacts**: **N/A** — direct prompt execution per Step 6.4.5 D-1 precedent (`/feature small` would re-derive the brief and try to cut `feat/<slug>` from main); intermediate artifacts = `implementation/step-06.5/{prompt,output}.md` + git history `fe6b04bc..56f3802e`.
- **Prompt**: `implementation/step-06.5/prompt.md`
- **Output**: `implementation/step-06.5/output.md`
- **Summary**: Client-layer wrap for the 7 platform HTTP routes shipped в Steps 6.4 + 6.4.5 — 3 client API factories (`createSessionsAPI` / `createDayMetadataAPI` / `createLabelsAPI`) + 6 mutation hooks + 1 query hook + 1 `useWeekMutation<TVars, TResult>` generic helper DRY'ing the 6-fold week-invalidating boilerplate per OQ-A ratification. **3 atomic per-layer commits** on `feat/training-domain` (`fe6b04bc..56f3802e`): `feat(platform): add labels query key and client api endpoints for lms vertical slice` → `feat(platform): add hooks for sessions day-metadata and label search` → `docs(step-06.5):...`. **7 new + 4 edited files**. Phase 1: `labels.search(level?, q?)` namespace added to `platformKeys` + type-only `AppLevelValue` import. Phase 2: 3 endpoint factories + `endpoints/index.ts` +3 alphabetic exports + **`api/index.ts` wired** (mandatory — `createApi(client)` factory enumerates each API explicitly; without this `api.sessions/dayMetadata/labels` undefined at runtime). Phase 3: `useWeekMutation<TVars, TResult>({mutationFn, planId, startDate, successMessage, errorMessage})` helper — `invalidateQueries(platformKeys.weeks.byDate)` + `toast.success` + `notifyError` (mirrors `useUpdateWeekNotes:19-35` pattern, generalized). Phase 4: 3 hook files using the helper — `use-sessions.ts` (4 hooks), `use-day-metadata.ts` (2 hooks), `use-label-search.ts` (1 query hook with object-arg `{level?, q?, enabled? = true}` per OQ-C ratification + default empty arg = preload-no-filter use case); `hooks/index.ts` +4 exports. **Idempotency-Key** confirmed auto-generated by `ApiClient.prepareRequest:111-113` per non-idempotent request (verbatim § 0.4) — zero hook-side work needed per OQ-C-implicit framework resolution. **`extractValidationMessage` confirmed N/A in this repo** (admin-v4-scoped memory entry; verified `grep -rn` zero hits) — pattern is direct `notifyError(error, "Failed to X")` per `@repo/query`. Root verification all-green: `pnpm check-types` 16/16, `pnpm lint` 16/16, `pnpm test` 958/958 (baseline preserved per OQ-B no-test ratification), `pnpm dep:check` 0/1144 (was 0/1137; +7 modules — exact match to 7 new files; planner range `[1140, 1145]` hit). **Zero § 0 STOP-and-surface escalations** — all 8 § 0.x verbatim quotes matched HEAD `08b4c71f` byte-for-byte except for one cosmetic ordering swap in `hooks/index.ts` (resolved per § 0.5 "minimize-diff spirit" instruction without escalation per D-1). Husky pre-commit clean all 3 commits без `--no-verify`.
- **Open questions** (no executor escalations; 3 D-numbered minor justifications):
  - **D-1 (hooks barrel ordering)** — planner § 0.6 verbatim was 2 lines off vs actual `hooks/index.ts` (cosmetic swap of `coach-invite ↔ coach-dashboard`). Executor respected actual file order, inserted 4 new entries at alphabetically-natural positions; spirit-instruction в § 0.5 ("minimize diff") covered the deviation. Adjacent refinement of `[[planner-verbatim-registration]]` (continuation of Step 6.4.5 D-4 lesson): re-`Read` registration files immediately before quoting in § 0, не from few-step-old memory.
  - **D-2 (planner § 5.3 grep counting error)** — planner expected `platformKeys.weeks.byDate` 2 hits в hooks/ post-step; actual 3 (planner missed the second occurrence в existing `use-weeks.ts:27` — `useUpdateWeekNotes.onSuccess` invalidate). Implementation correct; expectation-side off-by-one. Planner-discipline note: when sizing grep regressions, run actual `grep` at prompt-write time vs counting from memory of file shape.
  - **D-3 (lint-staged prettier compact)** — `update` method in `sessions.ts` collapsed multi-line `client.request(...)` to one-liner (fits ≤100 col). `delete` method stayed multi-line (template string slightly longer). Cosmetic, intentional via husky pipeline, no behavior change. Pre-ratified pattern (Step 6.4 D-5 + Step 6.4.5 D-3 same family).
- **Deferred decisions** (carry-forwards only; no new follow-ups identified):
  - **UI consumers** (`DayRow` label autocomplete + notes inline field — Step 6.6 first UI step; `SessionCard` + dnd-kit reorder — Step 6.7 second UI step). Step 6.5 is hook-layer only, no runtime user-visible surface.
  - **Optimistic updates** for the 6 mutations — per OQ-D ratification, all use `invalidateQueries` per Step 6.2 D-4 precedent. Optimistic UI defer until concrete UX feedback в Step 6.6/6.7.
  - **Hook unit tests** — per OQ-B ratification, mirror `use-weeks.ts` no-test precedent. Coverage by api-server integration tests + future UI smoke-tests.
  - **`createCrudHooks` / `useOptimisticMutation` adoption** — don't fit Step 6.5 entities (sessions/day-metadata week-embedded; labels read-only).
  - **Sessions / DayMetadata GET-only hooks** — not needed; both read via `useWeek` 7-day shape (Step 6.2). If a future view wants a single-Session refetch (rare), add then.
  - **Step 6.4 Stage 6 QA INFO-3** (`retryOnP2034.attempts` unbounded) + **INFO-4** (inverted `jitterMsRange`) — single-line clamps if patological caller ever surfaces.
  - **Symbol rename `cms{Label,Exercise}AdminApi` → `lms*`** — Step 6.1.5 carry-forward.
  - **`mapToSessionWithLabel` extraction as named symbol** — Step 6.2 D-2 carry-forward; Step 6.6 WeekGrid trigger.
- **Analysis/-files touched**: **none** (no Prisma schema change; no domain semantics change; pure client-layer wrap of existing routes).
- **Smoke-test status**: **N/A** — hook-layer step, no UI surface. First scenario-based smoke arrives Step 6.6 (DayRow label autocomplete + day notes inline field).

## Step 06.4.5 — Session HTTP routes + `retryOnP2034` apply to `lmsSessionApi.create`

- **Date**: 2026-05-16
- **Feature-dev artifacts**: **N/A** — executor ran prompt brief directly (no `/feature small` pipeline wrapper) per D-1 (rationale below); intermediate artifacts = `implementation/step-06.4.5/{prompt,output}.md` + git history `90bfa3fe..317af4c0`.
- **Prompt**: `implementation/step-06.4.5/prompt.md`
- **Output**: `implementation/step-06.4.5/output.md`
- **Summary**: 3 atomic per-layer commits on `feat/training-domain` (`90bfa3fe..317af4c0`): `feat(api-server): wrap session create transaction with p2034 retry` → `feat(platform): add http routes for session crud and reorder` → `docs(step-06.4.5):...`. **3 new + 2 edited files**. Phase 1: `lmsSessionApi.create`'s `prisma.$transaction(..., Serializable)` wrapped in `retryOnP2034(() => ...)` — the second (and final) production callsite of the helper born in Step 6.4 commit `b0b23ae4` (first was `lmsDayMetadataApi.{setLabel,setNotes}` in `013f8319`). `update`/`delete`/`reorder` byte-identical (single-statement ops or default-isolation array-tx, no P2034 surface). Phase 2: +1 integration case at end of `describe("create")` block in `session/admin.test.ts` — mirror Step 6.2 case 13 pattern (pre-materialize Day via `cleanupRaw.{week,day}.create`, race two concurrent `lmsSessionApi.create` via `Promise.allSettled`, assert at-least-one-fulfilled invariant + `storedCount === fulfilledCount`). **Closes Step 6.1 QA-002 concurrency-repro carry-forward**. Phase 3: 4 verb handlers across 3 new route files — POST `.../weeks/[startDate]/days/[dayOfWeek]/sessions/route.ts`, PUT `.../sessions/reorder/route.ts` (sub-resource action endpoint mirroring `/activate`/`/pause`/`/resume` precedents; uses `createAuthPutByParamHandler` because `createAuthActionHandler` is body-less and no PATCH factory exists in auth land — per OQ-B ratification), multiplexed PUT+DELETE `[planId]/sessions/[sessionId]/route.ts` (id-addressed per Step 6.0 `sessionByIdParamsSchema = { planId, sessionId }` design + `enrollments/[enrollmentId]/route.ts` precedent). Reorder wraps `Session[] → { sessions }` via `.then(...)` chain for `reorderSessionsResponseSchema` ratified Step 6.0 wrap-object shape. Root verification all-green: `pnpm check-types` 16/16, `pnpm lint` 16/16, `pnpm test` 958/958 (+1 net from 957, planner range [957, 959] hit dead-center), `pnpm dep:check` 0/1137 (+3 modules = exactly 3 new route files), `TZ=Asia/Kolkata` session admin 19/19. **Zero § 0 STOP-and-surface escalations** — все 8 § 0.x verbatim quotes matched HEAD `7380eee1` byte-for-byte (single off-by-line-range observation in § 0.1 cited 33-119/48-118 vs actual 38-108 — content verbatim, range is navigation hint only, not invariant; no escalation needed). Husky pre-commit clean all 3 commits без `--no-verify`.
- **Open questions** (no executor escalations; 4 D-numbered minor justifications):
  - **D-1 (executor flow)** — executor ran prompt brief **directly** instead of `/feature small` pipeline wrapper. Rationale: prompt is already self-contained executor brief (§ 0 research-equivalent + § 1 goal + § 2 context + § 3 plan + § 5 acceptance + § 7 commit strategy + § 9 output format). `/feature small` would re-derive the same brief through Research/Implement/Review/Test/Finalize stages and attempt to cut `feat/<feature-slug>` branch from main — both wasteful and incompatible with WORKFLOW.md long-lived `feat/training-domain` convention per `[[training-domain-workflow]]`. Planner-side note: future "narrow scope, no architectural decisions" prompts (like Step 6.4.5) can be marked "direct execution OK; `/feature small` wrapper optional" to give executor explicit license.
  - **D-2 (pattern-align test)** — concurrent-create test added `labelId: null` explicitly to `CreateSessionData` (planner's example showed only `{ notes }`). All 6 existing `create` cases consistently pass both fields. Pattern-match, zero behaviour delta.
  - **D-3 (lint-staged auto-format)** — prettier collapsed multi-line imports in 2 of 3 route files (4-symbol imports fit single-line; 5-symbol stayed multi-line). Pre-ratified in prompt § 3 Phase 3 design note ("if lint or prettier reformats this — let it; AST-equivalent").
  - **D-4 (stale planner test-count)** — prompt § 3 Phase 1 + § 5.4 said "14 existing cases" in `session/admin.test.ts`; factual baseline = 18 (Step 6.2 OQ-C inline cases added 4 cases after Step 6.1 ratification of 14, not propagated into planner's mental model). Net +1 invariant (957 → 958 root) held, gate hit dead-center. **Planner-discipline implication**: when prompt § 5 cites test-case counts, ground in actual `grep -c '^  it('` at prompt-write time, not approximate carry-forward from earlier step summaries. Adjacent refinement of `[[planner-verbatim-registration]]` — test files are a kind of "current state" too. Not a new flavour; absorbed.
- **Deferred decisions** (carry-forwards; no new follow-ups identified):
  - **Step 6.4 Stage 6 QA INFO-3** (`retryOnP2034.attempts` unbounded) + **INFO-4** (inverted `jitterMsRange`) — single-line clamp/validation additions if patological caller ever surfaces. Helper now has 2 production callsites (day-metadata + Session.create); risk surface unchanged.
  - **Symbol rename `cms{Label,Exercise}AdminApi` → `lms*`** — Step 6.1.5 carry-forward, low priority.
  - **`mapToSessionWithLabel` extraction as named symbol** — Step 6.2 D-2 carry-forward; Step 6.6 WeekGrid trigger.
- **Analysis/-files touched**: **none** (no Prisma schema change; no domain semantics change; pure transport + retry-helper application).
- **Smoke-test status**: **N/A** — api-server + route-layer step, no UI surface (per § 4 prompt explicit). UI consumers Step 6.7 (Session body + dnd-kit reorder).

## Step 06.4 — Platform HTTP routes for day metadata + label search, with P2034 retry infrastructure

- **Date**: 2026-05-16
- **Feature-dev artifacts**: `.feature-dev/1778926026/` (`research.md`, `design.md`, `plan.md`, `review.md`, `qa.md` — full `/feature` pipeline)
- **Prompt**: `implementation/step-06.4/prompt.md`
- **Output**: `implementation/step-06.4/output.md`
- **Summary**: Vertical slice — 3 platform HTTP routes (`GET /api/platform/labels/search`, `PUT .../weeks/[startDate]/days/[dayOfWeek]/{label,notes}`) + 4 bundled deferred follow-ups + companion 503-Retry-After infra, shipped as **7 atomic per-layer commits** on `feat/training-domain` (`a45a7dc3..3fde2c26`) per planner § 7.3 dependency order: contracts trim+level → api-routes 503 branch → DAY_OF_WEEK hoist → retryOnP2034 helper → apply-to-day-metadata → label-platform extend → 3 platform routes. Plus `624212e6 docs(step-06.4): write executor output report and step prompt`. **6 new + 10 edited files / +533 −149 LOC**. New `retryOnP2034(fn, opts?)` helper in `packages/api-server/src/utils/` — 2 default attempts + `[50, 200]ms` jitter, exhaustion throws `ServiceUnavailableError({ retryAfter: 5, lastErrorCode: "P2034" })`, non-P2034 errors rethrown immediately. Applied as wrapper around both `lmsDayMetadataApi.{setLabel, setNotes}` `prisma.$transaction(..., Serializable)` calls — `lmsSessionApi.create` deferred to Step 6.4.5 (1-line apply once 6.4.5 lands). `appErrorResponse` in `api-routes/error-handler.ts` gained 4-LOC 503 branch mirroring existing 429 path. `DAY_OF_WEEK_TO_PRISMA` hoisted from 3 callsites (`session/admin.ts`, `day/admin.ts`, `week/admin.ts`) to new `endpoints/lms/_shared/day-of-week.ts` — byte-identical const, no behavioural diff, closes Step 6.2 D-3. `lmsLabelPlatformApi.list` signature `query?: string` → `query?: LabelSearchParams` ({q?, level?}); where-clause conditional-spread `{ ...(q && { nameLower contains }), ...(level && { applicableLevels array_contains }) }`; `LABEL_SEARCH_CAP` 50 → 500 + exported (test references export, no magic-number drift). `labelSearchParamsSchema` in contracts gained `.trim()` transform on `q` + optional `level: appLevelSchema`. **Cleanest run since Step 6.3 — zero § 0 STOP-and-surface escalations, zero new planner-discipline flavours, zero structural deviations**. Root verification all-green: `pnpm check-types` 16/16, `pnpm lint` 16/16, `pnpm test` 957/957 (+20 from 937, planner range 950-960 hit dead-center; breakdown +7 contract / +8 helper / +5 platform), `pnpm dep:check` 0/1134 (was 0/1127, +7 modules per planner expectation). Husky pre-commit clean всех 7 commits без `--no-verify`. Stage 5 reviewer **APPROVED** (0 CRITICAL / 0 WARNING / 1 INFO — D-1 dead-var drop per manifesto 2.2). Stage 6 hostile QA **APPROVED**: 80 attacks enumerated, 0 exploitable, 5 INFO (non-actionable, deferred below).
- **Open questions** (no executor escalations through § 0 protocol — all 8 § 0.x verbatim quotes matched HEAD `d882e2ff` byte-for-byte at research stage; D-numbered minor justifications in `output.md`):
  - **D-1 (Stage 4 self-caught, no escalation)** — prompt § 3 Phase 4 verbatim helper snippet had a `let lastError: unknown` capture inside the loop that was never read. Executor dropped it per manifesto 2.2 (dead code → delete). Behaviour unchanged (`ServiceUnavailableError.details.lastErrorCode: "P2034"` literal still set as planner intended). **Tiny planner-spec defect, not a new discipline flavour** — caught and corrected before commit by executor's manifesto-application pass + Stage 5 reviewer confirmation. Planner-side: spec was over-specified; next prompt for any new utility should mental-trace each declared local against actual use before locking § 3.
  - **D-2 (Phase 5 test framing)** — prompt listed two test names for the cap rewrite that were mechanically identical (501 fixtures + cap assertion). Executor wrote one test combining the framings; net case count `11 → 16 (+5)` hit planner ≥15 target with no coverage loss.
  - **D-3 (Phase 2 test skip confirmed)** — `error-handler.test.ts` does not exist; 4-LOC additive infra verified via integration through retry-exhaustion → 503.
  - **D-4 (CLI correction)** — `apps/platform/package.json` declares `"name": "platform"` (no `@app/` scope); executor used `pnpm --filter platform` instead of `pnpm --filter @app/platform`. Trivial.
  - **D-5 (lint-staged auto-format)** — `npx lint-staged` (husky pre-commit) collapsed verbose 5-line imports in 2-3 files to 1-line, joined single-arg `Promise.all` onto one line. Pure formatting, identical AST.
- **Deferred decisions** (newly identified Stage 6 QA INFO findings + carry-forwards, all non-blocking):
  - **Apply `retryOnP2034` to `lmsSessionApi.create`** — explicit Step 6.4.5 work per OQ-G(i) ratification + § 4 out-of-scope prompt; 1-line wrap once 6.4.5 prompt lands.
  - **QA-INFO-1 (client debounce)** — autocomplete-side debounce for `labels/search` GET endpoint; concern for Step 6.6 UI hook (`useLabelSearch`).
  - **QA-INFO-2 (label drift `applicableLevels=[]`)** — dev-only sanity check; production data should never have empty array (Zod `.min(1)` on create/update). No fix needed unless a malformed row appears.
  - **QA-INFO-3 (`retryOnP2034.attempts` unbounded)** — caller-controlled option; no max-cap. If a future caller passes `attempts: 1_000_000` it would loop forever. Server-only opt with no wire control, low practical risk; add `.max(10)` clamp if it ever becomes a concern.
  - **QA-INFO-4 (inverted `jitterMsRange`)** — `jitterMsRange: [200, 50]` (max < min) makes `range[0] + Math.random() * (range[1] - range[0])` produce values in `(50, 200]` reverse-mapped — degenerate but safe; defaults are correct order so production unaffected.
  - **QA-INFO-5 (inner TOCTOU re-check integration test)** — intra-tx `tx.trainingPlan.findUnique` re-check (Step 6.2 inheritance) is impractical to assert without prisma-client mocking, which the api-server suite avoids per `[[no-tech-debt-in-mocks]]`. Covered indirectly by passing Day endpoint integration suite (15/15).
  - **Idempotency-key reuse with mismatching body** — current `wrapAuthHandler(JSON_CONFIG)` behavior throws `ConflictError("Idempotency-Key reuse with different request body")` (per `idempotency/with-idempotency.ts:54`). Acceptable but undocumented at route level; surface to hook layer (Step 6.5) so clients know the contract.
  - **GIN expression index on `Label.applicableLevels`** — perf opt for catalog > 2k labels; not realistic at current scale (peak ~300 per analysis).
  - **Pagination / cursor on `lmsLabelPlatformApi.list`** — cap 500 covers realistic catalog; revisit if/when peak > 500.
  - **Symbol rename `cms{Label,Exercise}AdminApi` → `lms*`** — Step 6.1.5 carry-forward, low priority.
  - **`?q=` zero-width-character strip** — paranoid; Step 6.3 admin precedent strips only on label create/update, not search. `.trim()` (Phase 1) is the chosen normalization scope.
- **Analysis/-files touched**: **none** (no Prisma schema change; D7 + D8 + applicableLevels semantics all unchanged; cap bump and level-filter are implementation-detail of existing model).
- **Smoke-test status**: **N/A** — api-server + route-layer step, no UI surface (per § 4 prompt explicit + § 5 acceptance criteria). UI consumer + scenario-based smoke-tests arrive in Step 6.6 (DayRow header reshape) and Step 6.7 (Session body + dnd-kit reorder).

## Step 06.3 — `lmsLabelPlatformApi` (read-only platform mirror)

- **Date**: 2026-05-16
- **Feature-dev artifacts**: `.feature-dev/1778922338/` (`research.md` + `review.md`; `/feature small` pipeline)
- **Prompt**: `implementation/step-06.3/prompt.md`
- **Output**: `implementation/step-06.3/output.md`
- **Summary**: Cleanest executor run of the workflow — zero new planner-discipline flavours surfaced; independent reviewer verdict **APPROVED** with zero CRITICAL/WARNING/INFO findings across 6 files / +265 LOC. Shipped `lmsLabelPlatformApi.list(userId, query?)` read-only platform mirror in new `packages/api-server/src/endpoints/lms/label/platform.ts` (`requireCoachLikeRole` outer, optional case-insensitive substring `nameLower contains q.toLowerCase()`, sort `nameLower asc`, hardcoded module-local `LABEL_SEARCH_CAP = 50`). `labelSearchParamsSchema = z.object({ q?: string.min(1).max(200) })` added to existing `lms/label` contract slice; response reuses `getLabelsResponseSchema` (no alias, KISS). Structural symmetry fix: added missing `endpoints/lms/label/index.ts` barrel (asymmetric vs `lms/{session,week,training-plan,plan-enrollment,day}/`), flipped `endpoints/lms/index.ts` `./label/admin` → `./label`. **2 atomic per-layer commits** — additive only, no broken intermediates: `4aca6ea0 feat(contracts): add label search params schema for platform mirror` → `29901fe3 feat(api-server): add lms label platform api with structural label/index barrel`. Plus `c651927b docs(step-06.3): write executor output report`. Root verification all-green: `pnpm check-types` 16/16 (FULL TURBO cached 186ms), `pnpm lint` 16/16, `pnpm test` 937/937 (+11 net new in `platform.test.ts`), `pnpm dep:check` 0/1127. Api-server isolated 538/538 (305.35s serial). Grep regressions all-zero (`cmsLabelAdminApi` intact in admin.ts:2 hits, `lmsLabelPlatformApi` defined 1 hit, `@repo/contracts/cms/label` 0 hits). Husky pre-commit clean both commits without `--no-verify`.
- **Open questions** (3 minor observations, all resolved inline by executor, no escalation):
  - **D-minor-deviation** — `platform.ts` Prisma `findMany` uses `{ ...(query !== undefined && { where: ... }) }` conditional-spread instead of prompt § 3.2.1 verbatim `where: query !== undefined ? {...} : undefined`. Required by `exactOptionalPropertyTypes: true` (`packages/typescript-config/base.json:10`) — TS2379 rejects passing `undefined` to optional property. **Pattern-aligned** with sibling `admin.ts:16-23` (`buildLabelUpdateData`) which uses the same idiom. Independent reviewer confirmed "strictly better than the prompt". Same family as the Step 5 deviation #2 (`description={plan.description ?? undefined}` → conditional-spread). Not a new planner-discipline flavour — the established codebase rule is documented in Step 5 Addendum.
  - **labelSchema shape summary mismatch in prompt § 2** — prompt summary line wrote "id, name, nameLower, applicableLevels, notes, ts" but actual schema uses `createdAt` / `updatedAt` (not `ts`). All actionable sections (§ 3.1/3.2) correctly route through `Label` type via `mapToLabel`. Doc imprecision only; not a blocker. Executor continued without surface.
  - **Optional § 3.1.3 contract tests skipped** per strict prompt reading — only `label.schema.test.ts` exists (Step 4 schema test); `labelSearchParamsSchema` Zod cases will be exercised via HTTP route layer in Step 6.4. Acceptable.
- **Deferred decisions** (from `output.md` § "Что отложено", carried forward):
  - **`endpoints/lms/exercise/index.ts` structural symmetry fix** — same asymmetry as label/ pre-fix (no `index.ts` in folder). 1-commit cleanup in any future Step 6.x close-out batch.
  - **Symbol rename `cmsLabelAdminApi` → `lmsLabelAdminApi`** — carry-forward from Step 6.1.5 deferred. Step 6.3 introduced `lmsLabelPlatformApi` with the correct prefix from start.
  - **`?q=` trim / zero-width-strip normalization** — Step 6.4 HTTP layer concern if needed.
  - **GIN trgm index for `nameLower`** — perf optimization for catalog > 1k labels; not needed at ~100 scale.
  - **`labelSearchResponseSchema` alias** — extract if platform diverges from admin response shape (cursor pagination, slim payload).
  - **Pagination / cursor** — hardcoded `take: 50` deferred indefinitely.
- **Analysis/-files touched**: none.
- **Smoke-test status**: **N/A** — api-server-only step, no user-visible surface yet. Smoke-test arrives in Step 6.6 / 6.7 (UI autocomplete consumers will exercise this through HTTP route Step 6.4 + hook Step 6.5).

## Step 06.2 — `lmsDayMetadataApi` + `getWeekResponseSchema` 7-day shape with embedded `Label`

- **Date**: 2026-05-16
- **Feature-dev artifacts**: `.feature-dev/1778913938/` (`research.md`, `design.md`, `plan.md`, `review.md`, `qa.md` — full `/feature` pipeline)
- **Prompt**: `implementation/step-06.2/prompt.md`
- **Output**: `implementation/step-06.2/output.md`
- **Summary**: D7-ratified `GET /lms/week/{startDate}` surface shipped — `lmsWeekApi.getByPlanAndDate` returns `{ week: Week | null, days: DayWithSessions[7] }` via single Prisma query with nested `include` (N+1 prevention; iterate `dayOfWeekValues` for deterministic Mon..Sun order). Empty slot = `{ dayOfWeek, label: null, notes: null, sessions: [] }`; client never branches on materialization. `lmsDayMetadataApi.{setLabel, setNotes}` side-channel — each `prisma.$transaction(..., Serializable)` with intra-tx plan TOCTOU re-check (mirror `session/admin.ts:50-61`), `applicableLevels.includes("DAY")` validation for `setLabel`, lazy `week.upsert → day.upsert` chain; **OQ-D no-op short-circuit** for `null`-on-unmaterialized. **OQ-C inline Step 6.1 follow-up** — `applicableLevels.includes("SESSION")` enforcement added to `lmsSessionApi.{create, update}` (intra-tx for create, pre-update for update). Consumer adapt: `useUpdateWeekNotes.onSuccess` swap'd `setQueryData` → `invalidateQueries` (would have dropped `days` from cache otherwise). 11 new + 12 modified files; +52 net new test cases (24 contract day-slice + 5 week-api extension + 15 day admin + 5 week admin extension + 4 session admin extension = 53 cases, one offset by a removed Step 6.1.5 test artifact); root verification all-green (check-types 16/16, lint 16/16, root tests 926/926, dep:check 0/1124 modules, `TZ=Asia/Kolkata` day-admin 15/15). **One squashed commit `19c57a66 feat(training-domain): 7-day week response with embedded label + day-metadata side-channel`** per `[[husky-cross-package-squash]]`; pre-commit hooks (secret-check + lint-staged + `turbo check-types --filter="...[HEAD]"`) and commit-msg (commitlint) green without `--no-verify`. Plus `db042e73 docs(step-06.2): write executor output report`.
- **Open questions** (2 escalations during execution, both resolved):
  - **CONTEXT-001 (Phase 3 extension, ratified Inline-fix)** — `apps/platform/src/app/api/platform/training-plans/[planId]/weeks/[startDate]/route.ts:20-22` GET handler manually wrapped `lmsWeekApi.getByPlanAndDate` result in `{ week: ... }`. After § 3.2.3 api returns the full `GetWeekResponse` shape — without unwrap the route would have produced `{ week: { week, days } }`, `getWeekResponseSchema.parse(...)` would fail at runtime. Prompt § 4 claim "handled by the existing route picking up the new GetWeekResponse type" was implicit assumption — handler file not in § 0 verbatim list nor § 3 scope. Executor surfaced via § 0 STOP-and-surface protocol with hypothesis (planner missed substitution, analog of Step 6.1.5 Q2 `@repo/api-server/cms` → `lms` admin-import miss). User ratified inline-fix: 1-line handler signature flip (`async (...) => lmsWeekApi.getByPlanAndDate(...)`); file added 4th in Phase 3 scope. PUT handler verified unchanged (`upsertNotes` still returns scalar `Week` per `updateWeekNotesResponseSchema = weekSchema`). This is the **6th-flavour planner-discipline miss `instinct-consumer-pattern-blindness`** — durable rule [[planner-consumer-pattern-read]] saved post-resolution.
  - **Spec § 3.2.5 case 13 deviation (test rewritten, no escalation)** — Concurrent `setLabel` + `setNotes` on unmaterialized Day was spec'd as "both succeed; last write wins on each field independently; not racing on same field". Actual Postgres SSI behavior: rejects one of the parallel upserts on same `(weekId, dayOfWeek)` unique key with P2034 (serialization failure) — SSI is row-grained, not column-grained, even with disjoint UPDATE columns. Executor rewrote the test to pre-materialize Day via `cleanupRaw.day.create(...)`, then `Promise.allSettled([setLabel(...), setNotes(...)])` with assertions "at least one fulfilled; if both fulfilled — both fields persist". Reflects realistic SSI semantics. Durable rule [[postgres-ssi-upsert-unique-key]] saved post-resolution; **production retry-on-P2034 at HTTP layer deferred to Step 6.4** (same finding bucket as `lmsSessionApi.create` Step 6.1 followups).
- **Deferred decisions** (from `output.md` § "Что отложено"):
  - **`DAY_OF_WEEK_TO_PRISMA` hoist to `endpoints/lms/_shared/day-of-week.ts`** — currently duplicated in 3 callsites (`session/admin.ts`, `day/admin.ts`, `week/admin.ts`). Trigger: Step 6.4 HTTP route layer adds the 4th callsite; DRY at that point.
  - **`mapToSessionWithLabel` extraction as named symbol** — currently inline spread (`{ ...mapToSession(s), label: s.label ? mapToLabel(s.label) : null }`) inside `mapToDaySlot`. Single use site; extract if Step 6.6 WeekGrid wants the same shape.
  - **`upsertNotes` return-type extension** to `DaySlot` or full `GetWeekResponse` — stays scalar `Week`; client invalidate'ит `useWeek` separately.
  - **Memory-hygiene sweep** on `~/.claude/projects/.../memory/` for stale `getWeekResponseSchema` references — n/a (only mention was in workflow planning docs, owned by planner).
  - **`apps/platform/src/lib/api/endpoints/weeks.ts:2` `Week` import** flagged as "appears unused" — actually used (line 8 `updateNotes(...): Promise<Week>`). False positive, no fix.
  - **`Day` auto-cleanup** when `sessions = [] && label = null && notes = null` — explicit out per D7 (breadcrumb policy).
  - **Cross-day session move** in `lmsSessionApi.reorder` — out per Step 6.1 scope.
  - **Production retry-on-P2034** at HTTP layer for `lmsDayMetadataApi.setX` (and the existing `lmsSessionApi.create` carry-forward) — Step 6.4 routes layer.
- **Style deviations / decisions ratified** (`output.md` § "Принятые решения" D-1..D-6):
  - **DAY_OF_WEEK_TO_PRISMA duplicated** (D-3) — see Deferred decisions above.
  - **`mapToSessionWithLabel` inline** (D-2) — see Deferred decisions above.
  - **`invalidateQueries` not partial-merge** in `useUpdateWeekNotes.onSuccess` (D-4) — alternative `(prev) => prev ? {...prev, week} : {...}` requires defensive null handling, couples mutation to query shape; invalidate is simpler, cost is 1 extra HTTP GET on notes-blur (low-frequency).
  - **`mappers/lms/index.ts` strict alphabetical** (D-5) — pre-existing order started with `enum-maps`, but `day.mapper` (added) sorts before. Applied strict alphabetical: `day.mapper`, `enum-maps`, `exercise.enum-maps`, ... Lint accepted.
  - **`getWeekResponseSchema.days: z.array(daySlotSchema).length(7)`** — strict cardinality enforced at schema level; defence-in-depth + self-documenting invariant.
  - **CONTEXT-001 inline-fix** (D-6) — see Open questions above.
  - **Subject reflow** — original prompt § 7 subject was 112 chars; commitlint cap 100. Reflowed to 91 chars without semantic loss; body unchanged.
- **Analysis/-files touched**: none (D7 already ratified; embed unlocked by Step 6.1.5; no schema change).
- **Smoke-test status**: **passed** (2026-05-16, user-side browser smoke). Все 3 шага сценария из `step-06.2/output.md` зелёные: 7-day shape грузится в `GET .../weeks/{monday}` Network response с 7 DayRows; "deload" note blur-commit triggers PUT + auto-refetch через `invalidateQueries`; page refresh persists. Zero UI regression — Step 5 week-notes flow intact under new contract shape. Step 6.2 fully closed.

## Step 06.1.5 — `Label` + `Exercise` namespace move (`cms/*` → `lms/*`)

- **Date**: 2026-05-16
- **Feature-dev artifacts**: `.feature-dev/1778878441/` (`research.md`, `review.md`, `.lock` — `/feature small` pipeline)
- **Prompt**: `implementation/step-06.1.5/prompt.md`
- **Output**: `implementation/step-06.1.5/output.md`
- **Summary**: D8 implementation. Pure path-and-import refactor: `Label`+`Exercise` contracts (14 files / 706 LOC) + api-server endpoints (4 files / 839 LOC) + api-server mappers (2 files / 37 LOC) moved from `cms/*` to `lms/*` namespace; `mappers/cms/enum-maps.ts` (155 LOC) split — CMS-pure (Currency/PriceInterval/BlogCategory/ContactStatus) stays (47 LOC remainder); Exercise enums (Equipment/MovementType/CanonicalCompoundType, both directions) extracted to new `mappers/lms/exercise.enum-maps.ts` (109 LOC). 34 consumer imports updated across `apps/admin/` (27 contract-substring sed + 7 `@repo/api-server/cms` → `@repo/api-server/lms` surgical substitutions). `.dependency-cruiser.cjs` `admin-no-lms` carve-out widened with 4 new `pathNot` entries. Zero Prisma changes; zero analysis-artifacts changes; zero seed changes; zero new tests. 56 files / +173/−159 LOC. Full root verification — `pnpm check-types` 16/16, `pnpm lint` 16/16, `pnpm test` 874/874 (api-server isolated 504/504, same baseline as Step 6.1; contracts 160/160, 2 moved test files unchanged), `pnpm dep:check` 0/1113. 8 grep regressions all-zero. **One squashed commit `5332c034 refactor(training-domain): move label and exercise from cms to lms namespace`** instead of the 4 prompt-prescribed atomic commits — collapsed at executor run-time after `.husky/pre-commit` blocked the first atomic commit attempt (see Open questions Q1 + Lesson-learned addendum below).
- **Open questions** (2 escalations during execution, both resolved):
  - **Q1 / D-OUTPUT-1 (CRITICAL, ratified Squash)** — `.husky/pre-commit` runs `SKIP_ENV_VALIDATION=1 pnpm turbo run check-types --filter="...[HEAD]"` after every `git commit`; rejects intermediate broken-import trees that the prompt's 4-commit strategy required between Phase 1 (contracts move) and Phase 3 (admin imports update). Prompt's note "Root-level `pnpm check-types` will fail until Phase 3 completes. Do not run root-level checks between commits 1 and 3" was operator-directed but the hook is non-discretionary. Executor surfaced via `AskUserQuestion` with two alternatives (deprecation-shim vs squash); user ratified squash. Resolution: one commit `5332c034` with comprehensive body listing all 5 deliverables; per-layer revertability preserved through logical-grouping in commit body (not git-atomicity). `--no-verify` was never on the table per `[[global-preferences]]`.
  - **Q2 / D-OUTPUT-3 (resolved inline, no escalation)** — Phase 2 check-types after api-server move failed because 7 admin route handlers import `cmsLabelAdminApi` / `cmsExerciseAdminApi` from `@repo/api-server/cms` package barrel (`apps/admin/src/app/api/admin/{labels,exercises}/...`), not from contracts. Prompt § 3.3.1 sed sweep covered `@repo/contracts/cms/{label,exercise}` only, not the api-server-package path. Executor added a surgical second sed `@repo/api-server/cms` → `@repo/api-server/lms` filtered by `cms{Label,Exercise}AdminApi` symbol presence (so as not to disturb other CMS consumers like `cmsBlogAdminApi`). Stays within prompt § 5 acceptance criteria upper bound "27-34 admin consumer files" (27 + 7 = 34).
- **Deferred decisions** (3 items per `.feature-dev/1778878441/output.md` § "Что отложено"):
  - **Symbol rename `cms{Label,Exercise}AdminApi` → `lms{Label,Exercise}AdminApi`** — out of scope for pure-refactor step (`prompt § 4` explicitly forbids "Improving moved code along the way"). The moved api-server files export `cmsLabelAdminApi` / `cmsExerciseAdminApi` from `endpoints/lms/`, a semantic mismatch. Mechanical sed in a separate atomic PR: ~7 admin route handlers + 2 api-server self-exports + 4 test files. Schedule: after Step 6.x stabilizes (low priority — runtime is fine).
  - **Memory hygiene sweep** of `~/.claude/projects/.../memory/` for stale `cms/{label,exercise}` refs — flagged as planner-side housekeeping for close-out. **Resolved by planner this turn**: `project_training_domain_workflow.md:14` updated (cms → lms paths + cross-link to `[[husky-cross-package-squash]]` + Step 6.1.5 note). No other matches.
  - **WORKFLOW.md guidance addition** — durable rule for cross-package refactors with intermediate broken trees: choose squash or reorder-with-shim, never `--no-verify`. Planner-side; one-paragraph addition to `implementation/WORKFLOW.md` queued for the next planner-housekeeping touch.
- **Analysis/-files touched**: none (D8 is a code-side namespace correction; `analysis/artifacts/06-formalization/schema.prisma` Label+Exercise blocks already had `@@map("training_*")` — no semantic change).
- **Smoke-test status**: **passed** (2026-05-16, user-side browser smoke). Все 8 шагов сценария из `step-06.1.5/output.md` зелёные: `/admin/labels` list+create+edit roundtrip + `/admin/exercises` list+create+add-demo-URL roundtrip. No UI regression — refactor предсказание "byte-identical runtime" подтверждено. Step 6.1.5 fully closed.

## Step 06.1 — `lmsSessionApi` + `endpoints/lms/_shared/date.ts` extraction

- **Date**: 2026-05-15
- **Feature-dev artifacts**: `.feature-dev/1778843530/` (`research.md`, `design.md`, `plan.md`, `tasks.md`, `review.md`, `qa.md`)
- **Prompt**: `implementation/step-06.1/prompt.md`
- **Output**: `implementation/step-06.1/output.md`
- **Summary**: First api-server slice with **transitive lazy materialization**. `lmsSessionApi.create` runs a single atomic `prisma.$transaction` (Serializable isolation) — intra-tx `plan.deletedAt` + `status` re-check (TOCTOU defence) → `week.upsert` → `day.upsert` → `session.aggregate(_max: order)` → `session.create` with `(maxOrder ?? 0) + 10`. `update` + `delete` use `verifySessionOwnership` (new guard in `authz/guards.ts`, JOIN `session → day → week → plan → creator` with admin/head-coach bypass). `reorder` validates ownership + materialization + complete-set match + foreign-day rejection + per-existence, then `prisma.$transaction(updates[])` with `(i+1)*10` sparse-int renumber. `mapToSession` is plain field copy (7 fields, **no `freezeLoadsAtCreation`** in mapped shape). `resolveWeekStartDate` extracted from `lms/week/admin.ts` to `endpoints/lms/_shared/date.ts` (mandated by 2 callsites now); `week/admin.ts` net `-20 / +1` after the move (2 stranded imports also removed under `--max-warnings 0` lint). 6 commits `a580cf4b..e1f92e06` (5 code + 1 docs) — `refactor → feat → test → fix (QA-001/002/004) → test (QA-003) → docs`. **504/504 tests across 69 files green**, including the new `_shared/date.test.ts` (6 unit cases) and `session/admin.test.ts` (14 integration cases). `TZ=Asia/Kolkata` proof — 14/14 in 14.61s; T17 dev-time revert proved the test is not vacuous (cases #4/5/8/11 fail without the UTC anchor). `pnpm dep:check` clean (0 violations / 1112 modules / 2029 deps); root `check-types` + `lint` 16/16. `freezeLoadsAtCreation` and `Session.name` grep regressions: 0 matches in api-server `src/`.
- **Open questions** (4 escalations during execution, all resolved):
  - **PROMPT-001 (Stage 4)** — commitlint `subject-case` rejected CamelCase identifier `resolveWeekStartDate` in the verbatim commit subject the prompt § 7 prescribed. Ratified via AskUserQuestion: paraphrase to `extract date helpers`; helper names preserved verbatim in the commit body. Mid-flight planner-side correction; commit subject convention updated for future prompts (no CamelCase identifiers in subjects under this commitlint config).
  - **QA-001 (CRITICAL, Stage 6, ratified Fix-now)** — `reorder` validation in prompt § 3.4 specified only "all `orderedIds` belong to that Day"; **missed complete-set requirement**. Subset `[c.id]` on `[a@10, b@20, c@30]` updates only `c` to `order=10`, leaving `a@10` unchanged — two rows with `order=10` (no `@@unique([dayId, order])` constraint at the schema level). Executor added `prisma.session.count({ where: { dayId } })` + length comparison; mismatch → `BadRequestError("orderedIds must include every session in the target day")`. Fixed in `61bb3eeb`; 2 new test cases (subset + non-existent-superset).
  - **QA-002 + QA-004 (WARNING, Stage 6, ratified Fix-both-now)** — prompt § 3.4 specified default Prisma isolation level for `create`'s transaction; concurrent calls could both read `_max(order)=N` and both write `N+10`. Separately, between outer `verifyPlanOwnership` and the transaction body, the plan could be soft-deleted or archived (TOCTOU). Executor upgraded to `Prisma.TransactionIsolationLevel.Serializable` and added intra-tx `tx.trainingPlan.findUnique({ select: { deletedAt, status } })` with re-throwing `NotFoundError` / `ForbiddenError` (wording verbatim from `verifyPlanEditable`). Fixed in `61bb3eeb`.
  - **QA-003 (WARNING, Stage 7, ratified Yes-add-only-this)** — prompt § 3.6 listed ownership-reject tests for `create` but not `delete`. Destructive op without direct ownership-reject coverage = future-refactor risk. Executor added `delete` non-owner integration case (case #14) in `ff8e8033`. Scoped to this one finding, not a broader Must-Test sweep.
- **Deferred decisions** (10 items in `.feature-dev/1778843530/qa.md` § "Что отложено", all INFO-level and routed to Step 6.4 route layer or later):
  - **QA-005 (INFO)** — no upper-bound on `reorder.orderedIds` array length; 10k cuids risks `$transaction` + statement_timeout. Realistic coach ~10 sessions/day; contract-layer `.max(N)` is cross-slice cleanup.
  - **QA-006 (INFO)** — `notes` not API-layer-capped (contract has `.max(2000)` already).
  - **QA-008 (INFO)** — `delete` cascade-to-`PerformedSession` not regression-guarded (only `Block` cascade is tested; `PerformedSession` uses the same `onDelete: Cascade` per `schema.prisma:841`).
  - **QA-010 (INFO)** — `reorder` uses identical error message for week-missing and day-missing; route-layer can disambiguate via context.
  - **QA-011 (INFO)** — TZ test asserts `Week.startDate` UTC components only, not `Day.dayOfWeek` value; defence-in-depth add for later.
  - **QA-012 family (INFO)** — `update`/`delete` on non-existent `sessionId` → `NotFoundError` not tested explicitly; guard exercises path through existing cases (#1, #2, #6, #8, #14).
  - **QA-002 concurrency repro** — integration suite is serial (`fileParallelism: false` per `[[project_api_server_serial_tests]]`); race is not deterministically reproducible at this layer. Behaviour proof requires concurrent execution at the HTTP layer in Step 6.4.
  - **Serializable retry on P2034** — `handlePrismaError` propagates as domain error today; retry with exponential backoff deferred to Step 6.4 HTTP-route layer (where `Retry-After` and idempotency-keys live).
- **Style deviation (ratified Stage 2 design.md § 5.5)** — STYLE-001: `reorder` uses sequential `findUnique` awaits (first `week` by `(planId, startDate)`, then `day` by `(weekId, dayOfWeek)`) rather than the prompt § 3.4 code shape with nested `await` inside an object literal + `?? ""` fallback. Semantics identical; readability strictly better; no magic-string fallback. Planner-side miss documented as engineering-naivety (see Lesson-learned addendum below).
- **Analysis/-files touched**: none (no schema change, no seed change; pure code addition + 2-callsite helper extraction).
- **Smoke-test status**: **N/A** — api-server-only step, no user-visible surface. `lmsSessionApi` callable only from integration tests; HTTP routes / hooks / UI arrive in Steps 6.4–6.7.

## Step 06.0 — Session contract slice + `lms/_shared` namespace

- **Date**: 2026-05-15
- **Feature-dev artifacts**: `.feature-dev/1778832091/` (`research.md`, `design.md`, `plan.md`, `tasks.md`, `review.md`, `qa.md`)
- **Prompt**: `implementation/step-06.0/prompt.md`
- **Output**: `implementation/step-06.0/output.md`
- **Summary**: First atomic sub-step of Step 6.x. Contract-only slice — zero consumers in this step; consumers arrive in Steps 6.1..6.7. Two deliverables: new `packages/contracts/src/entities/lms/_shared/` namespace housing `dayOfWeekSchema` (`as const` tuple + `z.enum`, BLOG_CATEGORY-mirror precedent per Step 3 D11, no `@prisma/client` import) and new `packages/contracts/src/entities/lms/session/` slice (entity schema, create/update/reorder write schemas, two address-param schemas (`byDay` for POST/reorder + `byId` for PUT/DELETE per Q2 hypothesis), 6 wrapper request/response schemas, types, 3 test files). 11 new files + 3 additive edits (`lms/index.ts`, `package.json` exports, `contracts/README.md`); +522/−1; **160 tests across 11 files (baseline 115/8)**. Two regression-guard tests added defensively beyond the spec: `shape.name === undefined` and `shape.freezeLoadsAtCreation === undefined` in both `sessionSchema` and `createSessionSchema`/`updateSessionSchema` — anti-drift guards for the deferred sub-decisions. 4 executor commits + 1 docs commit on `feat/training-domain` (`5e4144bc..71881b75`); all subjects lowercase, body lines ≤150 chars, no `--no-verify` / `--no-edit` / `--no-gpg-sign`; commitlint `subject-case` and `body-max-line-length` failures were root-cause-fixed (no camelCase identifier in subject; body reflowed).
- **Open questions** (4 escalations, all resolved):
  - **CONTEXT-001 (Stage 1, resolved by planner)** — Step 6.0 prompt § 3.3 quoted stale "current state" snippets for `lms/index.ts` and `package.json` exports map; literal application would have silently deleted `./lms/plan-enrollment` (a legitimate sibling entity from `cc48b221`, months pre-Step-5). Executor surfaced per § 0 hard-trigger protocol with a hypothesis. Planner confirmed additive intent, fixed the prompt in `6942b6cd fix(step-06.0): record actual lms registration state in prompt`, recorded the third instinct-specing flavour (`instinct-registration-read`) in the Lesson-learned addendum, and saved the durable rule as `feedback_planner_verbatim_registration` in memory.
  - **QA-001 (CRITICAL, Stage 6, ratified Skip)** — adversarial QA proposed adding `.max(2_147_483_647)` int4 cap to `sessionSchema.order`. Ratified Skip via AskUserQuestion: zero `order`-schema in contracts has such a cap (codebase convention); real risk negligible (server-controlled sparse-int recompute, no user-facing `order` write); single-slice cap would create asymmetry. Captured as future cross-slice cleanup, not Step 6.0 scope.
  - **QA-002 (CRITICAL, Stage 6, ratified Apply)** — adversarial QA flagged that `reorderSessionsSchema` accepted duplicate cuids in `orderedIds`. Ratified Apply via AskUserQuestion: added `.refine((ids) => new Set(ids).size === ids.length, …)` per `cms/label/label.schema.ts:18` pattern. Defence-in-depth on wire-layer — even if a UI-bug duplicates a cuid, the schema rejects before the handler sees it. Sibling slices not touched (point fix, not a codebase-wide convention change). Committed as `44af3f5b fix(contracts): reject duplicate cuids in reorder-sessions schema`.
  - **QA-003 (CRITICAL, Stage 6, ratified Skip)** — adversarial QA proposed tightening `startDate` regex `/^\d{4}-\d{2}-\d{2}$/` to a calendar-validity-aware regex (reject `2026-02-31`). Ratified Skip via AskUserQuestion: the regex is a byte-identical copy of `weekByPlanAndDateParamsSchema:6` (Step 5). Tightening here without tightening `week` would create asymmetry; tightening both is a cross-slice job out of Step 6.0 scope. Server-side parsing rejects invalid dates anyway.
- **Deferred decisions** (from `.feature-dev/1778832091/qa.md` WARNING-level — 10 findings, none blocking):
  - **QA-004..QA-013 (WARNING)** — int4 cap on `order`, reorder-array max-length cap, `notes` max-on-read read-schema (current cap is write-only), Date-roundtrip docs in README, strict-mode-default Zod hint, deep-import lint rule for `lms/_shared/day-of-week` only-vs-barrel, name-shadow risk between contracts `Session` and a hypothetical next-auth `Session`, real-cuid-fixtures instead of cuid-shaped literals in tests, UTF-16-code-unit-vs-grapheme `notes` cap, and 3 more cosmetic findings. All in `.feature-dev/1778832091/qa.md`. Picked up by the planner as separate cleanup if/when a tradeoff materializes; not Step 6.0 scope.
  - **`Session.freezeLoadsAtCreation` (Q10 carry-forward, indefinite)** — not in contract; regression-test guards `shape.freezeLoadsAtCreation === undefined`. Revisited only if a concrete coach use-case for the testing-week toggle materializes.
  - **`Session.name` (carry-forward, indefinite)** — not in contract; regression-test guards `shape.name === undefined`. Revisited only as a schema change (D5-precedent), never as instinct-add.
- **Analysis/-files touched**: none (no schema change; `_shared` and `session` slices are full-scope-Step-2 entities recombined at the contract layer).
- **Smoke-test status**: **N/A** — contract-only step, no user-visible surface, no api-server / platform / UI code. Smoke-tests resume at Steps 6.6 + 6.7.

## Lesson learned (planner discipline)

**Strike pattern surfaced during Step 3** — six (6) deviations between prompt-spec and codebase patterns, all on the same dimension: my prompt encoded "TS best-practice instincts" rather than reading the codebase. Each was caught by the executor at Stage 1 / Stage 2 of `/feature` and resolved via rule 4 ("existing patterns are sacred"):

1. **D2** — server-side pagination spec vs codebase client-side (`z.array(entity)` cap=100 + `DataTable` client-paginate).
2. **D3** — multi-select `DataTableFilter` spec vs codebase single-value primitive (multi extension would touch shared `@repo/ui`).
3. **D4** — `.toISOString()` Date marshalling spec vs codebase raw `Date` + RSC payload.
4. **D7** — backend ILIKE search spec vs codebase client-side `Column.searchValue`.
5. **D10** — `Stack spacing={4}` per global memory vs project codebase `{3}` (41/61 occurrences).
6. **D11** — `z.nativeEnum(Equipment from @prisma/client)` spec vs `.dependency-cruiser.cjs` `contracts-no-prisma` rule + codebase mirror+bridge pattern (per `BLOG_CATEGORY` precedent).

**Rule for Step 4+ prompts**: before specing any cross-package boundary (mapper output, contract schema, API response, client API type, form field, list/filter/search behaviour, sidebar config), the planner **reads 2-3 canonical implementations verbatim** and quotes the pattern in the prompt with file paths + line ranges. No "TS best practice" instincts. Rule 4 supersedes any external convention. The executor's Stage 1 RFC should not be the first time codebase compatibility is checked — the prompt should already align.

**Step 3's `apps/admin/src/modules/exercises/` is now the canonical reference** for catalog-library admin CRUD (Step 4 Label CRUD, future entity catalogs).

**Addendum (Step 4)** — a 7th deviation surfaced, but a _new flavour_: not prompt-vs-codebase misalignment, but **prompt-internal contradiction**. The Step 4 prompt simultaneously specified (a) bounded-`as` mapper handling for `applicableLevels` (correct) and (b) an `enum-maps.ts` AppLevel bridge (wrong — `applicableLevels` is a `Json` column, not an enum column; the bridge would be dead code). Both can't be true. **Rule extension**: prompt self-review must check _internal consistency_ — every field's handling described exactly once, no contradictory instructions across scope-list / phases / ratified-decisions sections.

**Addendum (Step 5 — positive confirmation)** — read-then-spec paid off _at planning time_. The old Step 5 ("Platform plan list / create-plan flow") was a speculative queue entry written by a prior planner who had not read `apps/platform`. Reading platform's conventions verbatim — the explicit precondition for specing this surface — revealed the whole flow already exists as base LMS infrastructure. The step was dropped, not specced. Had the prompt been written from the queue title alone, the executor would have burned a `/feature` session to Stage-1-discover the feature already shipped. **The lesson is not only "read before you spec to get the patterns right" — it is also "read before you spec to confirm the work exists at all."** A queue entry is a hypothesis; the verbatim read is what tests it.

**Addendum (Step 5 — three executor deviations + one carry-forward)** — the re-numbered Step 5 (plan-detail shell, calendar viewport) shipped clean, but the executor caught three planner prompt bugs and one cross-cutting model boundary I had classified incorrectly. All three are read-then-spec failures of subtler kind than Step 3's:

1. **Write-back cache key construction.** The prompt said `useUpdateWeekNotes`'s `onSuccess` should write back via `formatDateParam(week.startDate)`. The executor noticed at L4 that `client.request` in this codebase **does not** coerce the response against the Zod schema (matches `training-plans.ts`), so `week.startDate` is a string at runtime, not a `Date` — `formatDateParam` would have produced a garbage key. Correct fix: write back via the `startDate` string the caller already passed into `mutate`. **Lesson**: when a contract uses `z.coerce.date()`, planner must check whether the client surface actually round-trips through the schema before assuming response fields are `Date`s.

2. **`description` conditional-spread vs `?? undefined`.** The prompt said pass `description={plan.description ?? undefined}`. `exactOptionalPropertyTypes: true` rejects explicit `undefined` — the codebase idiom is conditional-spread (`{...(value !== null && { description: value })}`), visible in `status-chip.tsx` and `InlineEditText`'s own `placeholder` handling. **Lesson**: planner can't reach for `?? undefined` instinctively under `exactOptionalPropertyTypes`; quote the existing pattern from the codebase.

3. **QA-001 (CRITICAL) — `@db.Date` ↔ local-midnight boundary.** The prompt said `Week.startDate`'s wire form was "byte-identical to `boardedAt`". Review classified the construction the same way. **Wrong** — `boardedAt` builds a `Date` from an ISO string (UTC-midnight), while `Week.startDate` builds it via `getMonday` / `parseDateParam`, which call `new Date(y, m, d)` (**local-midnight**). On a positive-UTC-offset server (`TZ=Asia/Kolkata` reproduces it), Prisma 6's `@db.Date` serialization via `toISOString()` shifts a local-midnight `Date` back by one calendar day. Fixed in Step 5 by `resolveWeekStartDate` in `endpoints/lms/week/admin.ts` (re-anchors the Monday to `Date.UTC(...)` before the Prisma boundary, in both `getByPlanAndDate` and `upsertNotes`). **Lesson**: "byte-identical to X" is a constructor-of-the-`Date`-matters claim, not a "same column type" claim — planner must inspect the constructor chain on both sides before reusing prior art.

4. **Hard carry-forward for Step 6+** — any future code path that writes a `Date` into a `@db.Date` column must apply the same UTC-midnight anchor. The first such path is Step 6: `Day` materialization upserts a `Week` row (per D6), so the same `Week.startDate` boundary fires. The Step 6 prompt must spec `resolveWeekStartDate`-style anchoring (reuse the helper directly if Step 6 imports `lmsWeekApi`'s internals, otherwise mirror the pattern). `@repo/shared`'s `getMonday` / `parseDateParam` are left local-midnight on purpose — they're correct for the client-side UI surface; the fix is api-server-boundary-local.

**Addendum (Step 6 thesis — instinct-domain-modeling)** — Step 6 thesis cycle recapitulated the Step 3 strike pattern in a new flavour. The original queue entry "Step 6 — Day-level operations: add/edit/reorder/delete + Day.label autocomplete" was a planner-level domain hallucination: D6 already stated Week is a lazy calendar slot, and the user reframed during thesis review — "ты не можешь редактировать ось времени … тренеру не нужно создавать дни, ему нужно управлять тренировочными сериями". Translated: Day = mirror of Week = lazy calendar slot. No add / remove / reorder day UX. The whole "Day CRUD" framing of the original Step 6 queue entry was instinct-projected from "Week + Day are both entities, both must be CRUDable" — engineering reflex, not coach reality. **Rule extension**: when a domain decision (e.g. D6) declares an entity is a lazily-materialized slot, all sibling entities on the same axis (calendar) inherit the same framing unless analysis explicitly says otherwise. Don't reach for CRUD because schema has the columns.

The same thesis cycle, on the same day, produced two instinct-domain-modeling field-level errors:

1. **Phantom `Session.name`.** I added `name: optional` to the thesis "for flexibility — coach might want 'Morning lifts'." Prisma has **no `name` field** on `Session`. There was no analysis citation; pure UX cargo. User: "зачем нам вообще нужен name для сессии если есть label?" — забей.
2. **Invented `freezeLoadsAtCreation` semantics.** I claimed the flag means "snapshot 1RM so coach doesn't suffer drift after athlete PRs." User opposed: "1RM динамичный, нужен АТЛЕТУ, и снепшот здесь наоборот вреден." A grep of `analysis/artifacts/` showed: the field **does** exist (Q10 / `edge-cases.md §2.4` / `domain-model.md §1169` / `implementation-notes.md §3.8`), but as a **testing-week edge-case toggle**, default false; **default workflow uses live formula resolution (DP2)**. My invented coach motivation was the **opposite** of the real edge case. The user's pushback was correct; my analysis citation was missing because I never opened it.

**Rule extension (the durable form is `[[coach-pov-first]]` in memory)**: before specing any domain field or operation, **cite `analysis/artifacts/` verbatim** for the coach use-case. If no citation, the field/operation is engineer-cargo or genuinely deferred — escalate, don't invent. "Coach might want X" instincts = anti-pattern in this workflow. Every field on every entity in every step prompt's § "Read these first" needs a citation, not just a Prisma line number.

This is the **second wave** of instinct-specing (Steps 3 / 4 / 5 were instinct-pattern-engineering: TypeScript best-practice instincts that diverged from codebase patterns; Step 6 thesis is instinct-domain-modeling: engineering reflexes that diverged from coach reality). Both are caught by **reading first** — but the read targets are different. Codebase patterns: read the canonical implementations. Domain semantics: read `analysis/artifacts/`.

**Addendum (Step 6.0 — instinct-registration-read)** — the **third flavour** of instinct-specing surfaced at executor-launch of Step 6.0: I claimed in § 3.3 that `lms/index.ts` "currently exports `./training-plan` and `./week`" and the `package.json` exports map currently has 2 `./lms*` entries. Actual file had 3 exports (`plan-enrollment`, `training-plan`, `week`) and the map had `./lms` root + 3 sub-entries. `plan-enrollment` is a legitimate sibling in this bounded context (`cc48b221`, months pre-Step 5), not a prior-attempt trace. The executor caught it before producing the silent regression — applying the prompt literally would have deleted `plan-enrollment` from both files. **Rule extension**: for any **registration file** (barrels, `package.json` `exports`, app routers, navigation/sidebar configs, dep-cruiser rule arrays, `pnpm-workspace.yaml`, `turbo.json`) — `Read` the file verbatim at **prompt-write time** (not at thesis time), copy-paste the full current state into the prompt, state additive intent explicitly, and show the final state in full. Memory-based "I know what's there" = anti-pattern. Durable form in memory: [[planner-verbatim-registration]]. Three flavours of read-first now: (a) canonical codebase patterns ([[scope-via-existing-patterns]] and Step 3 strike), (b) domain semantics from `analysis/artifacts/` ([[coach-pov-first]] and Step 6 thesis), (c) registration-file completeness ([[planner-verbatim-registration]] and this Step 6.0 incident). All three caught by reading; all three blown by planner instinct.

**Addendum (Step 6.1 — instinct-engineering-naivety)** — the **fourth flavour**, and the one that landed exactly where I claimed to "hold the line" in the new collaboration rule ([[collaboration-co-ownership-claude-md]] — engineering trade-offs as a strong-opinion area). The Step 6.1 prompt § 3.4 contained code-shaped pseudocode for `lmsSessionApi.create` and `lmsSessionApi.reorder` that the executor's Stage 6 adversarial QA tore through, surfacing three miss'es:

1. **`reorder` subset attack (CRITICAL, QA-001).** Prompt validation specified only "all `orderedIds` belong to the target Day". A subset `{ orderedIds: [c.id] }` on a Day `[a@10, b@20, c@30]` passes that check trivially and then updates `c` to `order=10` — leaving `a@10, b@20, c@10`. Two rows with `order=10`. The schema has no `@@unique([dayId, order])` constraint, so this silently corrupts state. The defence — `prisma.session.count({ where: { dayId } })` with a length-mismatch reject — is one line. I missed it by not asking "what does subset do?" against the validation chain.

2. **`create` race + TOCTOU (WARNING, QA-002 + QA-004).** Prompt specified default Prisma isolation for the `read-then-write` `_max(order)` → `create` chain. Two concurrent `create` calls race: both read `_max=N`, both write `N+10`. Defence: `Prisma.TransactionIsolationLevel.Serializable`. Separately, between the outer `verifyPlanOwnership` and the transaction body, the plan can be soft-deleted or archived; the outer check is stale by transaction-commit time. Defence: intra-tx re-read of `plan.deletedAt` and `plan.status`. Both fixable in 4 lines each; both omitted from the prompt because I drafted them by instinct and did not imagine concurrent / racing callers.

3. **Brittle `await`-in-object-literal (STYLE-001).** Prompt § 3.4 `reorder` had `weekId: ( await prisma.week.findUnique(...) )?.id ?? ""` — `await` inside a `where`-object property slot, with a `?? ""` fallback. Reads as one expression; behaves as a promise-yielding mid-expression resolution with a magic-string fallback that doesn't collapse cleanly. Executor swapped it for two sequential `findUnique`s with explicit `if (!week)` reject — same network shape, same semantics, no magic fallback. I shipped code-smell pseudocode in the prompt because it was the first shape that came to mind for "verify-then-use".

All three are engineering. All three are exactly the area the [[collaboration-co-ownership-claude-md]] rule says I should hold the line on — not because engineering is "given", but because **holding the line requires being correct first**. Instinct-specing in engineering is the same failure as instinct-specing in domain or registration, just better disguised because the code "looks reasonable". The defence is mechanical, not heroic: an **adversarial pass** over every write operation in § 3 against five axes — concurrent, TOCTOU, partial inputs (subset / superset / empty / duplicates), malformed, boundary — before locking the prompt. Each axis takes 30 seconds; together they would have caught all three.

**Rule extension (the durable form is [[planner-adversarial-review]] in memory)**: after § 3 (Scope) is drafted, before § 5 (Acceptance), planner lists every write operation and runs the adversarial sweep, then encodes the defences in-line in § 3 or as regression checks in § 5. The adversarial pass is the planner's pre-emption of the executor's Stage 6 QA. Catch-at-planner = clean per-layer commits; catch-at-QA = `AskUserQuestion` round + retroactive `fix(...)` commits + a planner-discipline lesson like this one.

**Addendum (Step 6.1.5 — instinct-process-blindness)** — the **fifth flavour**, surfaced during a close-out cycle of a pure-refactor sub-step where I had assumed there was zero engineering risk. The Step 6.1.5 prompt § 7 prescribed 4 atomic per-layer commits — contracts move → api-server move + enum-maps split → admin imports update → dep-cruiser carve-out widen — each compiling for its own package but **not** root-wide. The prompt explicitly added a hand-wavy footnote: "Root-level `pnpm check-types` will fail until Phase 3 completes. Do not run root-level checks between commits 1 and 3." The executor read this as a manual operator instruction, started Phase 1 staging, ran `git commit`, and the commit was immediately rejected by `.husky/pre-commit` which runs `SKIP_ENV_VALIDATION=1 pnpm turbo run check-types --filter="...[HEAD]"` on every commit. The "do not run" note was operative for the operator; the hook does not read prompts. The 4-commit strategy was therefore unrealizable without bypassing the hook, which is globally forbidden per `[[global-preferences]]` no-`--no-verify`.

Executor caught it through § 0 escalation protocol, `AskUserQuestion`'d the user with two alternatives (reorder-with-deprecation-shim vs squash-to-1-commit; shim path was simultaneously mentally dismissed as anti-pattern because CMS-shim-importing-from-LMS would break a sibling dep-cruiser rule `contracts-cms-no-lms-coaching-billing`). User ratified squash. Resolution shipped as commit `5332c034` with a comprehensive body documenting all 5 deliverables and the squash reason in one atomic unit.

**Root cause**: planner drafted commit-shape from a "per-layer atomicity preserves revertability and review-clarity" instinct — a generic engineering norm — without checking the **specific** repo's pre-commit hook config. `.husky/pre-commit` is a single-line read, takes 5 seconds, and would have caught the issue at thesis-time. **Step 6.1's PROMPT-001** (commitlint `subject-case` rejecting `resolveWeekStartDate` CamelCase identifier) was the same flavour, narrower scope — I closed it as a one-off and did not extrapolate the rule from "check commitlint config" to "check **all** pre-commit and pre-push hooks before specing commit shape, especially when the strategy plans intermediate cross-package state". Step 6.1.5 shows PROMPT-001 was actually a flavour-family stub, not a one-off.

**Rule extension (the durable form is `[[husky-cross-package-squash]]` in memory, user-saved post-resolution)**: before drafting any commit-strategy in a step's § 7 prompt — especially for **cross-package** refactors where intermediate states can be import-broken — planner reads:

- `.husky/pre-commit` (what runs on every commit)
- `.husky/pre-push` if present (what runs on push)
- `package.json` `husky` field if present (legacy config)
- `turbo.json` pipeline definitions for `check-types`/`lint`/`test` to know what `--filter="...[HEAD]"` actually fans out to

If any hook gates a cross-package check and any planned commit in the strategy leaves a downstream package broken — collapse to one squashed commit, period. Deprecation-shims are anti-pattern (they import in the **wrong** direction per other dep-cruiser rules; see Step 6.1.5 thesis-cycle hypothesis dismissal). `--no-verify` is never on the table.

**Adversarial-pass extension**: `[[planner-adversarial-review]]` covers write-op concurrency / TOCTOU / subset / boundary / malformed. It does **not** cover pipeline integration (hooks, CI gates, intermediate-tree validity). Add a sixth axis to the mental sweep when § 7 is drafted: "**does every intermediate commit in this strategy pass every hook gate?**" If no, collapse.

**Addendum (Step 6.2 — instinct-consumer-pattern-blindness)** — the **sixth flavour**, surfaced via executor § 0 STOP-and-surface during Phase 3 (apps/platform consumer adapt). The Step 6.2 prompt § 4 contained the sentence "HTTP route ... already exists; **only** the response payload schema flips (handled by the existing route picking up the new GetWeekResponse type via getByPlanAndDate return type)" — a one-line implicit assumption that the platform GET route handler at `apps/platform/src/app/api/platform/training-plans/[planId]/weeks/[startDate]/route.ts` is a pass-through over `lmsWeekApi.getByPlanAndDate`. The handler file was not in § 0 verbatim list nor § 3 scope. Actual handler at lines 20-22 was wrapping the api result in `{ week: ... }` (manual envelope, pre-Step-6.2 shape baked into the route layer). After § 3.2.3 made the api return the full `GetWeekResponse`, the un-touched handler would have produced `{ week: { week, days } }` — double wrap, `getWeekResponseSchema.parse(...)` rejection at runtime. Executor caught it before staging, surfaced via `AskUserQuestion` with the hypothesis "planner missed substitution, analog of Step 6.1.5 Q2 admin-import miss". User ratified inline-fix: 1-line handler signature flip (`async (...) => lmsWeekApi.getByPlanAndDate(...)`); route file added 4th to Phase 3 scope; CONTEXT-001 documented in `output.md`. PUT handler in the same file verified unchanged (`upsertNotes` still returns scalar `Week` per `updateWeekNotesResponseSchema = weekSchema`).

**Root cause**: planner wrote "handled by the existing route picking up the new type" without **reading** the actual route handler file. Same failure mode as Step 6.1.5 Q2 (`@repo/api-server/cms` → `lms` substitution in admin routes that planner's Phase 3.1 sed sweep didn't cover because § 3.3.1 only specified `@repo/contracts/cms/X` paths). Same flavour family as the earlier 5 — assume vs read — narrower scope (one file, one line), but identical procedural failure.

**Rule extension (the durable form is [[planner-consumer-pattern-read]] in memory, saved post-resolution)**: for any change to a contract response shape, planner reads, **verbatim at prompt-write time**:

1. Every HTTP route handler that calls the touched api method (`apps/*/src/app/api/.../route.ts`). Handlers may manually wrap, transform, or schema-validate; cascades into double-wrap / parse-fail if not unwrapped.
2. Every client API endpoint (`apps/*/src/lib/api/endpoints/*.ts`) typed against the contract.
3. Every client hook (`apps/*/src/lib/hooks/use-*.ts`) consuming via `useQuery`/`setQueryData`/`invalidateQueries`. Cache shape may be coupled to old shape (Step 6.2's other consumer fix — `useUpdateWeekNotes.onSuccess` swap from `setQueryData` to `invalidateQueries` — would have been a CONTEXT-002 if not pre-emptively in § 3.3.1).
4. Every downstream mapper / wrapper bridging layers.

Sixth procedural checklist alongside [[planner-verbatim-registration]] / [[planner-adversarial-review]] / [[husky-cross-package-squash]] / [[scope-via-existing-patterns]] / [[coach-pov-first]]; none is optional.

**Adjacent finding: Postgres SSI test-spec mismatch** — Step 6.2 § 3.2.5 case 13 spec'd concurrent `setLabel` + `setNotes` on unmaterialized Day as "both succeed; last write wins on each field independently". Reality: Postgres Serializable SSI rejects one of the parallel upserts on same `(weekId, dayOfWeek)` unique key with P2034 — SSI is row-grained, not column-grained, even with disjoint UPDATE columns. Executor rewrote the test to pre-materialize the Day then run concurrent UPDATEs via `Promise.allSettled` with "at least one fulfilled" assertion. **Not** a planner-discipline miss per se — the assumption was that disjoint columns under Serializable would coexist; the failure mode is Postgres-specific knowledge. Captured as project memory [[postgres-ssi-upsert-unique-key]] for future concurrent-mutation specs (Block + Schema + SchemaRow in Step 7+); **production retry-on-P2034 at HTTP layer deferred to Step 6.4** (same bucket as the `lmsSessionApi.create` Step 6.1 carry-forward).

Six flavours of planner-discipline miss now (read-then-spec for (a)-(d) + (f), verify-then-spec for (e) — same root: planner instinct uncalibrated by repo-specific facts), in order of surface: (a) canonical codebase patterns ([[scope-via-existing-patterns]], Step 3 strike), (b) domain semantics from `analysis/artifacts/` ([[coach-pov-first]], Step 6 thesis), (c) registration-file completeness ([[planner-verbatim-registration]], Step 6.0 CONTEXT-001), (d) engineering correctness under adversarial input ([[planner-adversarial-review]], Step 6.1 QA-001/002), (e) commit-strategy correctness under live hook config ([[husky-cross-package-squash]], Step 6.1.5 D-OUTPUT-1), (f) consumer-pattern completeness for contract response shape changes ([[planner-consumer-pattern-read]], Step 6.2 CONTEXT-001). The collaboration rule ([[collaboration-co-ownership-claude-md]]) sets the stance; these six are the procedural checklists. None is optional.

---

## Step 05 — Plan-detail shell (calendar viewport)

- **Date**: 2026-05-15
- **Feature-dev artifacts**: `.feature-dev/1778776411/` (`research.md`, `design.md`, `plan.md`, `tasks.md`, `review.md`, `qa.md`)
- **Prompt**: `implementation/step-05/prompt.md`
- **Output**: `implementation/step-05/output.md`
- **Summary**: First real training-domain surface in `apps/platform`. Replaced the `"Coming soon"` stub at `/coach/plans/[planId]` with a **calendar viewport** over plan weeks per **D6** (Week = lazily-materialized calendar slot, not a managed entity). Shipped a read-mostly `Week` vertical slice — contract slice (`lms/week`), `lmsWeekApi` (`getByPlanAndDate` returns `Week | null`, no 404 on unmaterialized slot; `upsertNotes` is the only write, materializes the row), `mapToWeek`, platform GET+PUT route (no POST/DELETE, no list endpoint), `useWeek` / `useUpdateWeekNotes`. Added `@repo/ui` `InlineEditText` primitive (later refactored to always-edit `InputBase`) + strictly-additive `PageHeader` extension (`editable`, `description`, `onTitleCommit`, `onDescriptionCommit`); built the `plan-detail` module (`PlanDetailView`, `WeekNavigator`, `WeekGrid`, `DayRow`, `WeekNotes`); resolved OQ-D (create-plan now lands the coach in `/coach/plans/<newId>`, not back on the list). 12 executor commits `2845244a..42787606`, 16/16 type-check/lint, 0 dep:check violations, 810 tests green (3 new test files). 5 user-directed post-validation tweaks (sibling-session direct commits, Step 4 precedent): `11a7c463` (`InlineEditText` edit mode → chromeless `InputBase`), `8cc8cf43` (`WeekNavigator` two-row reflow + MUI X `DatePicker` + "Today" folded into picker action-bar + `DateLocalizationProvider` client wrapper, since the root `layout.tsx` is a server component), `27be221c` (smoke-test sync), `e600c23c` (drop the display↔edit toggle from `InlineEditText` — always-edit semantics), `9d9389bd` (`WeekNotes` rendered as a plain `TextField`, not `InlineEditText`). Two further planner-side cosmetic commits at close-out (`3dc40feb` editable `PageHeader` h2/body1; `169d5086` `WeekNavigator` `alignItems="flex-end"` consolidation).
- **Open questions** (3 executor-caught planner-prompt bugs, all resolved):
  - **L4 write-back key (deviation #1)** — `useUpdateWeekNotes` writes back via the caller's `startDate` string, not `formatDateParam(week.startDate)` (the latter would produce a garbage key because `client.request` does not validate response). Single-line why-comment in `use-weeks.ts`.
  - **`description` conditional-spread (deviation #2)** — `PlanDetailView` uses `{...(plan.description !== null && { description: plan.description })}` because `exactOptionalPropertyTypes: true` rejects explicit `undefined`; matches the codebase idiom.
  - **QA-001 (deviation #3, CRITICAL)** — `Week.startDate` is re-anchored to `Date.UTC(...)` at the api-server boundary via `resolveWeekStartDate`. Proven with `TZ=Asia/Kolkata pnpm --filter @repo/api-server test src/endpoints/lms/week/admin.test.ts` (green; pre-fix the value-assertion failed `expected 17 to be 18`). See the "Lesson learned" Step 5 addendum for the full diagnosis and the Step 6 carry-forward.
- **Deferred decisions** (from QA / Review):
  - **QA-003 (WARNING)** — `useUpdateWeekNotes` has no `invalidateQueries`/`mutationKey`. Accepted as-is: `setQueryData` with the authoritative server response is correct for a single-user blur-commit surface; two rapid saves on the same week would briefly desync the cache but heal on the next fetch.
  - **QA-005 (WARNING)** — `InlineEditText` has no client-side `maxLength` cap; over-length pastes trigger a server-400 → toast → lost edit. Follow-up; coach still sees an error toast (not silent).
  - **QA-002 (INFO)** — no dedicated `parseDateParam` / `getMonday` unit tests in `@repo/shared`; the reject path is covered indirectly through `lmsWeekApi`. Pre-existing surface, out of step ownership.
  - **QA-007 / QA-008 / QA-009 (INFO)** — `notes` UTF-16-code-unit cap (codebase-wide), 404-vs-403 oracle in `verifyPlanOwnership` (codebase-wide, pre-Step-5), `PlanDetailView` swallows `useWeek` errors (narrow: healthy plan-GET + failed week-GET → empty notes panel without error-toast). All pre-existing or completeness-level.
  - **Review WARN-2 (INFO)** — the single code comment in `use-weeks.ts` is ~147 chars; cosmetic.
  - **QA-001 (CRITICAL — carry-forward to Step 6+)** — `Day` materialization upserts a `Week` row (D6) and re-enters the same `@db.Date` ↔ local-midnight boundary. Step 6 prompt must spec `resolveWeekStartDate`-style UTC anchoring. The deferred default for any future step writing a `Date` into a `@db.Date` column is **the same**: anchor to `Date.UTC(...)` at the api-server boundary; leave `@repo/shared` local-midnight helpers untouched (correct for the client surface).
- **Analysis/-files touched**: `analysis/artifacts/05-synthesis/domain-model.md` §1.0 (Week — prose clarified to the lazily-materialized calendar-slot framing under D6; no schema change, no `06-formalization/` touch).
- **Smoke-test status**: **passed** (2026-05-15) on the final post-validation HEAD (`169d5086` after the close-out cosmetic commits). All 15 scenario steps green: 7-row Mon–Sun layout with today's highlight circle, prev/next/today/jump-to-date via the MUI `DatePicker`, `?week=` survives refresh, week-notes upsert materializes the row and clears with `null`, inline title/description edit (empty-title reverts; empty-description saves as `null`), create-plan lands in `/coach/plans/<newId>` on the current calendar week. Step 5 fully closed.

## Step 05 — Plan list / create-plan flow — DROPPED (already implemented)

- **Date**: 2026-05-14
- **Feature-dev artifacts**: none — no `/feature` run; no executor session.
- **Prompt**: none written.
- **Output**: none.
- **Summary**: The queued Step 5 ("Platform plan list / create-plan flow — replace/verify `apps/platform/src/modules/plans/` scaffolding") was found, on a verbatim read of `apps/platform`, to be already fully implemented as **pre-existing base LMS infrastructure** — not workflow scaffolding. Present and functional: `modules/plans/` (`PlansView`, `PlansListSection` with URL-synced status tabs, `PlanCard`, `PlanActionMenu`, `CreatePlanDialog`), `lib/hooks/use-training-plans.ts` (full CRUD via `createCrudHooks` + status mutations), `lib/api/endpoints/training-plans.ts`, `lib/api/keys.ts`, all `app/api/platform/training-plans/...` routes (list/create/getById/update/delete/activate/archive/restore), `packages/contracts/.../lms/training-plan/` (full contracts), `packages/api-server/.../training-plan.ts` + `training-plan.mapper.ts`, `lib/config/navigation.ts` (`/coach/plans` present), seed (`coach@thedisciplineprogram.com` / `password12345` + 4 plans across all statuses). The training-domain workflow's only change to this surface was the `weeks Week[]` back-relation on `TrainingPlan` (Steps 1-2), which touches neither the contract, the create flow, the mapper, nor any plans UI. There was nothing to spec. User decision (2026-05-14): drop the step, renumber old Steps 6-12 → 5-11.
- **Open questions**: two side-observations surfaced during the read, both recorded in `PLANNING_STATE.md` (one in the new Step 5 description, one in § "Deferred sub-decisions"):
  - **Create-plan redirect** — `createCrudHooks` is configured `redirectTo: "/coach/plans"`, so creating a plan returns the coach to the list rather than landing them in the new plan. Hypothesis: once plan-detail is non-stub, create should land in `/coach/plans/[newId]`. Folded into the new Step 5 (plan-detail shell) scope.
  - **No plan edit/rename UI** — `useUpdateTrainingPlan` / `applyTrainingPlanUpdate` / `updateTrainingPlanSchema` / PUT route all exist, but `PlanActionMenu` has no edit affordance. Hypothesis: plan-metadata editing belongs in the plan-detail header, not a list-level dialog. Deferred; revisit during the plan-detail shell.
- **Deferred decisions**: none.
- **Analysis/-files touched**: none.
- **Smoke-test status**: not applicable (no code change). The existing plan list / create flow has not been independently smoke-tested against the post-Step-2 reseeded DB; that verification folds naturally into the new Step 5 smoke-test (navigating plans → plan-detail is a precondition for testing the plan-detail shell).

## Step 04 — Admin Label CRUD

- **Date**: 2026-05-14
- **Feature-dev artifacts**: `.feature-dev/1778744240/` (research, design, plan, tasks, review, qa)
- **Prompt**: `implementation/step-04/prompt.md`
- **Output**: `implementation/step-04/output.md`
- **Summary**: Second admin catalog-library CRUD — Label. Structural mirror of Step 3 Exercise module, adapted from 10-field Exercise to 3-field Label (`name`, `applicableLevels`, `notes`). One new pattern — `applicableLevels` multi-value checkbox widget (`FormGroup` + 3 `Checkbox`), `Controller` subscribed to `fieldState` with `FormControl error` + `FormHelperText` (direct copy of the Step 3.1 `placeholderFlag` fix — regression guard for the silent-submit bug). 9 commits `6a8b2302..252d7323`. No schema change (Label + AppLevel shipped in Step 2). 786 tests passing (+33: 22 contract unit + 11 api-server integration). Review APPROVE 0/0/0, QA A−. `enum-maps.ts` NOT touched (DEC-7).
- **Open questions** (3 escalations, all resolved):
  - **Trigger-2 (memory prior-attempt vocab)** — `feedback_discipline_db_non_prod.md` had a stale ADR-0037/plan-editor breadcrumb. Resolution: surface-only (per Step 1 precedent); planner cleaned the memory file in parallel (removed breadcrumb + corrected stale `db:reset` definition that wrongly included `&& prisma db seed`). Re-grep of memory dir confirmed zero remaining stale refs.
  - **enum-maps.ts dead-code** — prompt specified an AppLevel bridge in `enum-maps.ts`, but `applicableLevels` is a `Json` column (mirrors Exercise `aliases Json` — no bridge). Planner-side prompt error (internal contradiction — see Lesson learned addendum). Resolution: skip `enum-maps.ts` (DEC-7); append-only edits 8 not 9.
  - **QA-001 (duplicate applicableLevels)** — `["DAY","DAY"]` passed write+read schemas; UI checkboxes can't produce dups but direct API / edit-reseed bypass that. Fixed (`4c9c922a`): field-level `.refine` uniqueness on `applicableLevelsSchema`.
- **Deferred decisions** (from QA):
  - QA-002 (`notes: ""` not normalized to `null`) — codebase-wide pattern shared with Exercise; fix codebase-wide or leave, not in isolation.
  - QA-003 (`as AppLevelValue[]` mapper narrowing) — ratified pattern (mirrors Exercise `aliases`); guarded by integration test asserting read-path throws `ZodError` on malformed data.
  - Separate sidebar icon for "Labels" — currently shares `"exercises"` icon; ~2-line `icon-map.ts` addition, out of Step 4 scope.
- **Analysis/-files touched**: none (no schema change).
- **Smoke-test status**: **passed** (2026-05-14). 9-step scenario green, including the step-4 regression guard (empty `applicableLevels` surfaces an inline error, not a silent no-op). Two user-directed post-acceptance tweaks committed via a separate session: `6f4b033f` (distinct sidebar icon for Labels — closes the deferred DEC-5 shared-icon item) and `b7defec9` (label form split into two cards — supersedes DEC-3 flat-single-card, user preference). Step 4 fully closed.

## Step 03 — Admin Exercise CRUD (with Phase 0 D5 refinement)

- **Date**: 2026-05-13
- **Feature-dev artifacts**: `.feature-dev/1778666831/` (research.md, design.md, plan.md, review.md, qa.md, tasks.md)
- **Prompt**: `implementation/step-03/prompt.md`
- **Output**: `implementation/step-03/output.md`
- **Summary**: Phase 0 ratified D5 (`Exercise.defaultDemoUrl String?` → `defaultDemoUrls String[]`) across analysis-artifacts + real schema; `db:reset+seed` clean. Phases 1-7 shipped first user-visible admin CRUD: contracts (mirrored enums per dep-cruiser `contracts-no-prisma` rule), backend handlers with `P2002`/`P2003` intercepts, 4 admin routes, client API + TanStack hooks, admin module (form split into 4 sub-cards + 2 helpers per `react/no-multi-comp`), 3 pages, new `Library` sidebar group. Stage 5/6 hardening: NFKC normalize + zero-width strip, http(s)-only URL scheme refine, length/array caps (`MAX_URL_LENGTH=2048`, `MAX_NOTES_LENGTH=10_000`, `MAX_ARRAY_LENGTH=20`), cross-field placeholder consistency refine. 12 commits on `feat/training-domain` (HEAD `51302f93`). 753 tests passing (+25); type-check/lint/dep:check all 16/16 green.
- **Open questions**:
  - **Memory hygiene** flagged by planner this turn — `feedback_localized_helper.md` is admin-v4 cross-pollution; `feedback_pattern_compliance.md` Stack spacing should note discipline-specific `{3}` override. Both pending user approval.
  - **Browser smoke-test** scenario documented in `step-03/output.md` §"Сценарий смоук-теста"; awaiting user execution.
- **Deferred decisions** (from executor's Stage 6 QA):
  - **QA-008** server-side pagination — defer until library > ~500 entries; cross-cutting refactor on all admin CMS endpoints simultaneously.
  - **QA-010** `canonicalCompoundType × placeholderFlag` dual-encoding collapse — long-term schema migration (drop flag, derive on read).
  - **QA-011** `ConfirmationModal` stuck open on delete-error — project-wide pattern in `useDeleteConfirmation`; fix once for all modules.
  - **QA-012** Server-validation errors not inline on input — project-wide; centralized fix in `createCrudHooks` `setError` chain.
  - **`aliases Json? → String[]`** schema migration — bounded `as string[] | null` narrowing in mapper this step; future symmetry with `defaultDemoUrls`.
  - **`defaultLoad` UI surface** — nullable in schema; no form input this step; future schema-extension step.
  - **Multi-select `DataTableFilter` primitive** — `@repo/ui` extension; needed by plan-builder picker (Step 9+).
  - **`stress-final.md` / `00-meta/phase-06-prompt.md` stale `defaultDemoUrl` mentions** — out-of-scope per prompt §2/§3; paper trail in output.md.
  - **Production rollout workflow** — separate ADR when prod migrates off Neon dev.
- **Analysis/-files touched**: `06-formalization/{schema.prisma, implementation-notes.md, er-final.md}` (Phase 0 D5 refinement only).
- **Smoke-test status**: passed (2026-05-13). 10 of 11 scenario steps green on first run; step 8 (cross-field refine UX) silently failed — refine fired at resolver but Switch field did not surface error. Fixed inline via commit `919b836d fix(exercise): surface cross-field refine error on placeholder flag toggle` (`classification-card.tsx` placeholderFlag Controller now subscribes to `fieldState` and renders error in `FormHelperText`, matching the `canonicalCompoundType` Select pattern in the same file). Re-test passed both directions (PLACEHOLDER+off and ATOMIC+on). Step 3 fully closed.

## Step 02 — Prisma Schema Port + Archetype Seed

- **Date**: 2026-05-13
- **Feature-dev artifacts**: `.feature-dev/1778644165/` (`research.md` + `review.md`)
- **Prompt**: `implementation/step-02/prompt.md`
- **Output**: `implementation/step-02/output.md`
- **Summary**: Training-domain срез из `analysis/artifacts/06-formalization/schema.prisma` ported into `packages/api-server/prisma/schema.prisma` — 11 enums + 14 models + 3 back-relations on existing `User`/`TrainingPlan`. `OneRMRecord.valueKg` = `Decimal @db.Decimal(6, 2)`. All `@@map("training_*")`. 34 canonical archetypes seeded (split into 4 family files per ESLint `max-lines: 300`). Exercise/Label tables empty per D4. `prisma format/validate/generate` ✓, `db:reset + db:seed` ✓, 16/16 workspaces type-check + lint clean, 728/728 tests passed.
- **Open questions**:
  - **`db:reset` script ≠ auto-seed** in this repo. Real script = `prisma db push --force-reset && tsx scripts/apply-sql-checks.ts`. Executor compensated by calling `db:seed` separately. Implication for future prompts: when planner says "db:reset" assume it needs explicit `db:seed` follow-up.
  - **`archetypeParamsSchema` source = `types.ts` `ArchetypeParams` discriminated union** (not `implementation-notes.md` — that file covers only 2/34 archetypes in full template form). Acceptable derivation per Step 1 ratification; not fabrication.
- **Deferred decisions**:
  - `Exercise` / `Label` seed — Step 3 / Step 4 implement via admin UI (D4).
  - `Week / Day / Session / Block / Schema / SchemaRow` content seeding — out of scope per D3 (full-schema port ≠ full content seed).
  - `OneRMRecord / PerformedSession*` seeding — out of scope (athlete-flow out).
  - `citext` migration for case-insensitive uniques — explicit defer; current lowercase-mirror pattern stays.
  - Versioned migrations directory — per ADR-0019, not maintained during workflow.
- **Analysis/-files touched**: none (Step 2 is pure port, no model refinement).
- **Smoke-test status**: passed (db:reset + db:seed, table populations verified: archetypes=34, exercises=0, labels=0, training\_\*=0, users=13, lms_training_plans=4).

## Step 01 — Model Ratification

- **Date**: 2026-05-12
- **Feature-dev artifacts**: N/A (no `/feature`; pure docs/spec edit)
- **Prompt**: `implementation/step-01/prompt.md`
- **Output**: `implementation/step-01/output.md`
- **Summary**: Applied D1-D4 ratifications across 6 analysis-artifacts. `schema.prisma` got `Week` model + `DayOfWeek` enum + stub `User`/`TrainingPlan`, `Day` rewritten (drop `order`, add `weekId`+`dayOfWeek`), `Athlete` model removed, FK `athleteId→userId` on `OneRMRecord`/`PerformedSession`. ER-diagrams synced; `implementation-notes.md` got dated §0 with ratifications + OPEN-items closed. `types.ts` minimal cleanup. Validated via `prisma format`.
- **Open questions**:
  - **Memory entries про прошлые попытки** (ADR-0037/0041/0042/0043 + feedback_coach_always_edit_mode) — executor surfaced без halt; cleanup requested via user approval (this conversation turn).
  - **Order semantics divergence** — pre-existing inconsistency between `er-final.md §5 #7` (sparse 10/20/30, Phase 4 Q6 ratified) and `PLANNING_STATE.md` deferred-default (sequential 1,2,3). Resolved: `PLANNING_STATE.md` reverts to **sparse 10/20/30** per Phase 4 Q6. No analysis-artifact edit needed (Phase 4 Q6 was already ratified). To be reflected in Step 2 seed.
- **Deferred decisions**:
  - Pseudocode rewrite of `implementation-notes.md §3.5 resolveDualValue` / §3.8 resolveHrZoneToBpm — requires finalized AthleteProfile columns (Phase 8+).
  - Real Prisma client regeneration — happens in Step 2 (port to `packages/api-server/prisma/schema.prisma`).
  - 149-exercise / 19-label starter-pack delivery format (CSV import in admin? on-demand seed command? manual entry?) — Step 2+ decides.
- **Analysis/-files touched**:
  - `analysis/artifacts/06-formalization/schema.prisma`
  - `analysis/artifacts/06-formalization/er-final.md`
  - `analysis/artifacts/06-formalization/implementation-notes.md`
  - `analysis/artifacts/06-formalization/types.ts`
  - `analysis/artifacts/05-synthesis/domain-model.md`
  - `analysis/artifacts/05-synthesis/er-diagram.md`
- **Smoke-test status**: N/A (no UI/runtime impact).
