# implementation/ — training-domain pre-pivot execution log (history)

> ⛔ **SUPERSEDED — history only. Do NOT plan off this dir or resume from it.** Active work + the live board live in `initiatives/plan-editor-compose/` (resume there, or `/initiative-resume`). The still-live distillate was migrated out (2026-06-03):
>
> - The **9 planner-discipline checklists** (was `WORKFLOW.md` "Lessons learned") → `docs/planner-discipline.md`.
> - The **live AlternatingGroup domain facts** (D14 / D-A5 / D-A6.1 / C-A1) → `initiatives/plan-editor-compose/decisions.md` (D-AG-FACTS).
>   Everything else here (the queue, the per-step `prompt`/`output`, the step log) is frozen at step 9.2 as a record. Grep results from this dir are stale by default — prefer the initiative + `docs/`.

The journal of the **two-session planner/executor workflow** that built the training-domain model through step 9.2. As of 2026-06-02 it is **superseded**, retained as history:

- **The two-session model is dropped** → see `initiatives/README.md` (single session + `/feature`/ultracode). `WORKFLOW.md` here is the old process spec, kept for history.
- **Archetype-era steps are superseded** by the compose pivot (ADR-0037): step-01/02 (the 34-archetype seed) and step-08.x (archetype contracts / picker / per-archetype forms / `AlternatingGroup`). The **`step-09.x` `SchemaRow` leaf editors survive** — reused by the compose constructor.
- **Live state moved** → `initiatives/plan-editor-compose/`. The `state/` + `log/` here are frozen at step 9.2.

Per-file audit 2026-06-02: zero garbage, nothing to delete; superseded files kept as history (no per-file edits).
