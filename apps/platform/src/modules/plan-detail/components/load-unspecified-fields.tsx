"use client";

import { Typography } from "@mui/material";

export const LoadUnspecifiedFields = () => {
  return (
    <Typography variant="caption" color="text.secondary">
      Load left unspecified.
    </Typography>
  );
};
