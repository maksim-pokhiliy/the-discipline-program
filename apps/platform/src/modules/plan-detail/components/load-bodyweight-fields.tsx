"use client";

import { Typography } from "@mui/material";

export const LoadBodyweightFields = () => {
  return (
    <Typography variant="caption" color="text.secondary">
      Bodyweight — no load value.
    </Typography>
  );
};
