# mobile-publish — state (the board)

**Updated:** 2026-06-27 — **Publish-syntax v3 DONE (structure-faithful projection, in PR `feat/mobile-publish-structure`) → the Day→legacy-mobile projection now renders EVERY composition type and honors BOTH grouping layers.** Three drops fixed: **RC-1** `count`'s "N rounds" was lost on every header-present strength block (`count` was missing from `STRUCTURE_APPEND_KINDS` — a one-token fix that KEEPS the `.has()` gate so `once`/`benchmark` stay excluded); **RC-2** `SchemaGroup` parallel tracks rendered as unrelated blocks; **RC-3** `RowGroup` separator labels (`super-set`, `1st/2nd/3rd round:`) were silently dropped. The fix reuses the platform SSOT groupers `buildBlockItems`/`buildRowItems` (the EXACT functions `build-session-detail.ts` walks → published text groups identically to the platform athlete view, zero drift); new `projection/format-schema-group.ts` holds the wrapper formatter. Projection-only — no schema/migration/contract/legacy change; output type unchanged. **D-17 ratified** (supersedes the D-13 §2 "structure-drop is correct" stance; RFC-local DR D-1..D-5 — `·` separator · orthogonal composition-vs-row-group layers · wrapper-as-own-entry · SSOT-grouper reuse · row-group label = normalized `notes[0]` — in `design-publish-syntax.md` §v3). **Byte-identical for ungrouped/already-correct (Denys LIVE in prod):** structural, not luck — `order: asc` include + same-`order` grouper sort + `@@unique` no-ties; only the three bugged classes change bytes (the intended self-healing content-hash churn, D-9 owned-day PUT / D-4 unowned conflict-guard). **Review APPROVE (0 CRITICAL/0 WARNING) · QA B (0 CRITICAL):** QA-001 (wrapper counted empty members) + QA-002 (colon-only `notes[0]` → bare `:`) fixed pre-merge; QA-003/QA-004 + REV-003/REV-004 → **MP-17..MP-20**. **Verify green:** isolated projection suite 38/38 (+12 new) · check-types · lint · dep:check direction safe. **P1 (#317/#318) + P2a/P2b stay LIVE/merged; publish-syntax v2 (D-13, #319) extended, not replaced; the athlete-facing twin MP-14 unchanged. Next = P3 (Hardening) or pause.**

## Board

| #   | Phase                           | Status  | Pointer                                                                                 |
| --- | ------------------------------- | ------- | --------------------------------------------------------------------------------------- |
| 0   | Local legacy harness            | ✅ done | `legacy-contract.md` (verified live); `tdp/local/docker-compose.yml` — stack UP         |
| 1a  | Connector server foundation     | ✅ done | MERGED **#317**; D-6…D-9; `endpoints/coaching/mobile-publish/`                          |
| 1b  | Coach UI (connect/link/publish) | ✅ done | MERGED **#318**; D-10/11/12; +additive `GET`/`DELETE /links` routes                     |
| 1c  | Publish syntax v2 (readability) | ✅ done | MERGED **#319**; D-13; `projection/format-legacy-schema.ts`                             |
| 1d  | Publish syntax v3 (structure)   | ✅ done | PR `feat/mobile-publish-structure`; D-17; SSOT-grouper reuse + `format-schema-group.ts` |
| 2a  | Individual server foundation    | ✅ done | PR `feat/mobile-publish-p2a`; D-14/D-15; channel-aware seam + athletes proxy            |
| 2b  | Individual coach UI             | ✅ done | PR `feat/mobile-publish-p2b`; D-16; `IndividualLinksSection` + grouped publish          |
| 3   | Hardening                       | ⏳ next | `plan.md` P3 — disconnect (MP-12), token-refresh UX, P1b/P2b polish (MP-13/16)          |

## Next action

**▶ Publisher structure-fidelity (v3) DONE — next = P3 (Hardening) or pause.** The Day→legacy-mobile projection now renders every composition kind + honors schema/row groups (PR `feat/mobile-publish-structure`, D-17). The Individual channel stays end-to-end (server P2a #320 + coach UI P2b, D-16; MP-1 closed). P3 candidates (`plan.md` P3): disconnect (`DELETE /connections`, MP-12 — blocked on the D-11 cascade-semantics decision), token-expiry/refresh UX (MP-4 — reconnect side already shipped P1b), publish history/audit, delete-propagation (MP-3), plus the deferred P1b/P2b polish (MP-13 / MP-16). Optional P2-tail: weight-baking (MP-2 — `athleteId` is stored → a pure `renderRowLine` ctx follow-up, NOT taken in P2b).

**v3 go-live:** pure projection — **NO migration, NO new env var, NO server-contract change.** Merge the PR → Vercel redeploys → the next coach publish renders full structure; owned days self-heal (content-hash differs → PUT/`updated`, D-9), unowned stay conflict-guarded (D-4). The owner render-and-show gate (D-13 §8) governs WHEN Denys re-publishes a real week through v3 (text diff vs live) — not a PR blocker.

**P2b go-live:** pure coach UI — **NO migration, NO new env var, NO server change** (P2a already shipped the pipe). Merge this PR → Vercel redeploys → Denys can link athletes + publish per-athlete. The P2a migration already rode `db-migrate.yml` at the #320 merge; the `mobile_link_channel_key_xor` prod-snapshot dry-run + the first individual prod-publish smoke stay owner steps (`prod-data-inviolable`). Carried from P1a (still owner-side): confirm `MOBILE_PUBLISH_ENCRYPTION_KEY` + `LEGACY_MOBILE_API_BASE_URL` are set in Vercel. Re-run the gated integration anytime: `RUN_LEGACY_INTEGRATION=1` + the harness up (seed a `user_plan_id=2` athlete, see `journal.md`) + dev `DATABASE_URL` exported (sourced from `packages/api-server/.env`, wins over `.env.test`).

## Open decisions awaiting ratification

- **(none open)** — D-1…D-17 ratified. **v3 ratified D-17** (structure-faithful projection — render all composition kinds + honor schema/row groups; supersedes the D-13 §2 structure-drop stance). P2a ratified D-14 (Individual identity model) + D-15 (Individual DELETE+POST). **P2b ratified D-16** (Individual coach UI shape: OQ-1..4 + the Gate-B graceful-degrade refinement of design §6.2). The disconnect cascade-semantics call (warn-and-cascade vs restrict-if-linked) is a P3 decision (MP-12), not now.

## Gotchas a resuming session must know

- **P1a env var:** `MOBILE_PUBLISH_ENCRYPTION_KEY` (base64 32-byte, `openssl rand -base64 32`) + `LEGACY_MOBILE_API_BASE_URL` — both in `.env.example`; set them in dev `.env` + Vercel before the endpoints work. The cipher fails closed at boot on a bad key.
- **The local harness is UP** — `docker compose -f ~/projects/contrib/tdp/local/docker-compose.yml {ps,down,up -d --wait}`. Backend `localhost:8080/api/v1`; DB host `5433`. Seeded ADMIN `admin@tdp.local` / `Admin123!` (role ADMIN, plan General, level Pro=id 2). Data EPHEMERAL — re-seed recipe in `journal.md` (incl. a `user_plan_id=2` Individual athlete for the P2a integration).
- **WSL→Docker cred-bypass:** `export DOCKER_CONFIG=~/projects/contrib/tdp/local/.docker`.
- **Auth header = RAW token** (no `Bearer `) — the adapter (`rest-adapter.ts`) sends the bare `accessToken`.
- **POST is insert-only → 409**; GENERAL publish emulates upsert (GET→POST/409→PUT/skip). Skip compares to LIVE legacy content (D-9), so an out-of-band iOS edit of an owned day is re-published, not silently skipped. Unowned day + `overwriteUnowned:false` → `conflict` (never clobbered). **INDIVIDUAL "replace" = DELETE+POST** (the legacy individual PUT is broken — D-15); same decision logic, different write mechanism via `ChannelProgramOps`.
- **Token TTL = 1 month, no refresh** → reconnect ~monthly; api-server emits a `MOBILE_RECONNECT_REQUIRED` signal on a legacy 401 / decrypt-fail (now a `@repo/contracts` constant, D-12). P1b shipped the reconnect UX (MP-4): a proactive amber expiry nudge on the connect section + an inline `ConnectMobileModal` reconnect CTA from the link manager + publish modal (`isReconnectRequired` detects it). **Disconnect (`DELETE /connections`) is NOT built** — deferred to P3 on a cascade-semantics decision (MP-12, D-11); the connect section shows "Reconnect", never "Disconnect".
- **The iOS app has in-app `CreateProgram`** — a published day can collide with a coach-typed one → the overwrite-guard (D-4/D-9) surfaces it as `conflict`.
- **Legacy is SEPARATE infra** — consume its REST API as ADMIN only; never touch its code/DB. Legacy prod DB inviolable; P1a/P2a only ever talk to localhost:8080.

## Live carry-forwards (see `deferred.md`)

~~MP-1 (individual identity picker)~~ **CLOSED (P2b, D-16 — end-to-end)** · MP-2 (bake resolved weights, P2-tail; athleteId now stored) · **MP-14 (`cap`→`AMRAP` in the SHARED athlete-facing formatter — owner decision; projection twin shipped #319)** · **MP-15 (Individual DELETE+POST non-atomicity — documented residual, P2-tail/P3)** · MP-3 (delete-propagation, P3) · MP-4 (token-expiry reconnect UX — **reconnect side shipped P1b**; expiry nudge + inline reconnect CTA) · ~~MP-5 (Week.startDate=Monday)~~ **CLOSED (verified)** · MP-6 (level-name drift — mitigated, read live always) · ~~MP-7 (placement)~~ **CLOSED (D-6)** · MP-8 (no legacy unique — mitigated; residual legacy TOCTOU) · MP-9 (token key rotation, P3) · MP-10 (week-publish resilience/latency, P3) · MP-11 (row notes athlete-facing? owner call) · **MP-12 (disconnect `DELETE /connections` — DEFERRED P3, cascade-blast-radius blocker, D-11)** · **MP-13 (P1b deferred polish — QA-004/007/011/020, all recoverable)** · **MP-16 (P2b deferred polish — QA-02/03-delete/04/06/09/10/11/12/13 + REV-I3/I4, all recoverable)** · **MP-17 (v3 unnamed row-group blank-line asymmetry — owner KEPT the D-5 default at Gate B; no prod data, Denys's groups all named)** · MP-18 (v3 `count` digit-collision collapse — CLOSED, design-intended non-issue) · MP-19 (v3 headerless `once:` — OPEN, pre-existing, LOW) · MP-20 (v3 multi-element `notes` — CLOSED, `notes[0]`-only SSOT parity) · MP-NORTH-STAR (repoint iOS → platform API — future initiative).
