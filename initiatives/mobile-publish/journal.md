# mobile-publish — journal (append-only)

## 2026-06-25 — Founded; direction ratified; P0 started

- Received Vladyslav's two repos (iOS app + Spring backend); cloned both fully to `~/projects/contrib/tdp/` (`mobile-ios` on `develop`, `mobile-backend` on `master`). Platform monorepo left in place.
- Read both ends: the legacy plan model (per-day `general_programs` / `individual_programs` with a free-text `dailyProgram` JSONB), the full auth/role/token stack, and our platform's plan domain (`TrainingPlan→Week→Day→Session→Block→Schema→SchemaRow`). Verified wire contract → `legacy-contract.md`.
- Ratified with the owner the **Connector push-projection** direction (D-1…D-5): legacy untouched; per-coach legacy auth (store token, not password); link = channel+key (General first); idempotent upsert + overwrite-guard; lossy text projection via `build-session-detail`. The connector's manual, coach-driven link dissolves the cross-system identity blocker (owner: user-bases "probably the same, but we don't know"; delivers both by-level and by-user; legacy stays frozen — its dev is busy).
- Owner note: the mobile app has SEVERAL admins → connections are per-coach, each connecting as himself (folded into D-2).
- Started P0: wrote `tdp/local/docker-compose.yml`; kicked off the legacy backend docker build as the publish test-harness.

## 2026-06-25 — P0 DONE (legacy harness up + contract verified live)

- **Infra fixes:** (1) WSL→Docker cred-bypass — `credsStore: desktop.exe` not on the non-interactive PATH → a `{"auths":{}}` config at `tdp/local/.docker` + `DOCKER_CONFIG`. (2) `postgres:alpine` now = PG18+ whose data-dir layout breaks a `/var/lib/postgresql/data` mount → pinned `postgres:16-alpine`, dropped the named volume (ephemeral harness), added a healthcheck + `up --wait`. (yapster on 5432 left alone — legacy DB is on 5433.)
- **Stood up:** db + backend healthy; applied `schema.sql` (7 tables + seeds: levels Scaled/Pro/Advanced, plans General/Individual, roles USER/ADMIN); seeded one enabled ADMIN by direct SQL (bcrypt via `htpasswd`).
- **Contract verified by curl, all green:** `signin` (→ token, role ADMIN, plan General, exp ~1 mo) → `POST /generalProgram` **200** (ADMIN gate passes, `dailyProgram` JSONB round-trips intact) → `GET /generalProgram?trainingLevelId=&scheduledDate=` 200 → duplicate POST **409** "already exists" (insert-only) → `PUT` 200 (republish in place) → **athlete route** `GET /program?userId=&scheduledDate=` 200 returns the published day (the exact call the iOS app makes). **Zero deltas** from the source-read contract; enrichment: dup = HTTP 409; auth header = RAW token (no `Bearer`).
- **Next:** P1 (connector + General publish) via `/feature`, plan-first. Proposed split P1a (server/integration foundation) → P1b (coach UI). See `state.md` Next action.

### Re-seed recipe (harness data is ephemeral — after any recreate)

```
export DOCKER_CONFIG=~/projects/contrib/tdp/local/.docker
docker compose -f ~/projects/contrib/tdp/local/docker-compose.yml up -d --wait
docker exec -i tdp_legacy_db psql -U tdp -d tdp < ~/projects/contrib/tdp/mobile-backend/backend/src/main/resources/schema.sql
HASH=$(docker run --rm httpd:alpine htpasswd -nbBC 10 x 'Admin123!' | cut -d: -f2)
docker exec tdp_legacy_db psql -U tdp -d tdp -c "INSERT INTO users (is_enabled,username,password,user_role_id,user_plan_id,training_level_id) VALUES (true,'admin@tdp.local','$HASH',2,1,2) ON CONFLICT (username) DO UPDATE SET password=EXCLUDED.password,is_enabled=true,user_role_id=2,user_plan_id=1,training_level_id=2;"
# + an Individual-plan (user_plan_id=2) athlete for the P2 individual integration:
docker exec tdp_legacy_db psql -U tdp -d tdp -c "INSERT INTO users (is_enabled,username,password,user_role_id,user_plan_id,training_level_id,first_name,last_name) VALUES (true,'athlete@tdp.local','$HASH',1,2,2,'Test','Athlete') ON CONFLICT (username) DO UPDATE SET is_enabled=true,user_plan_id=2,training_level_id=2,first_name='Test',last_name='Athlete';"
# if a prior run left stale program rows (→ first publish returns `conflict` instead of `created`), clear them:
docker exec tdp_legacy_db psql -U tdp -d tdp -c "TRUNCATE general_programs, individual_programs RESTART IDENTITY;"
# signin → raw token in Authorization header:
curl -s -X POST localhost:8080/api/v1/auth/signin -H 'Content-Type: application/json' -d '{"username":"admin@tdp.local","password":"Admin123!"}'
```

## 2026-06-25 — P1a DONE (server/integration foundation; via `/feature`)

Built the full connector server spine on `feat/mobile-publish-p1a` (19 commits, all green: check-types 16/16, lint 16/16, dep:check 0, 61 api-server unit tests + 1002 contracts). Ratified **D-6…D-9**.

- **Shipped:** 3 Prisma models + 1 additive migration (gated; authored offline via `migrate diff`, owner-approved); AES-256-GCM token cipher + `@repo/env` `MOBILE_PUBLISH_ENCRYPTION_KEY`; the legacy REST adapter (`infrastructure/legacy-mobile/`, raw-token header, 404→null/409→propagate, transport/5xx→502, timeout→504); the `Day → dailyProgram` projection; the idempotent publish service; 5 coach-gated endpoints (`POST/GET /connections`, `GET /training-levels`, `POST /links`, `POST /publish`). No UI (P1b).
- **D-6 placement:** `endpoints/coaching/mobile-publish/` + `infrastructure/legacy-mobile/`; Coaching→LMS allowed, no new context/rule (MP-7 closed). api-server gained a `@repo/api-client` dep (the legacy adapter reuses `ApiClient`; owner-approved, lockfile + dep-graph updated).
- **D-8 projection seam (the big Gate-A call):** reading every formatter showed the platform's row-text family was already UI-free → extracted to a shared `@repo/contracts/lms/row-text` `renderRowLine`; the platform UI now delegates to it → **true single-function SSOT** (owner chose option (b) over a copied renderer). Honors D-5's intent (published text == platform display); a parity test enforces it. The 9-ALT copy + MP-10-consolidation path was avoided.
- **Spec corrections (code won over the brief):** `trainingNumber` = 1-based session index (not `Session.order`, steps of 10); rest-day = the `Label.rest` boolean (not a name string); UTC date-param via a centralized `utils/date-param.ts` (not `formatCalendarDate`/`formatDateParam`); `legacyUserName` = the login email (signin returns no person name).
- **Review + QA fix-loop (2 CRITICAL + 3 WARNING fixed):** per-day publish failures now isolated (`failed` action, results never discarded — QA-002); the 409-race re-runs the overwrite-guard so a raced iOS day is never blindly clobbered (SEC-001); `skipped` now compares to LIVE legacy content, detecting out-of-band iOS edits (CORR-001, refines D-9); env var renamed feature-scoped (DX-001); `startDate` format-validated (QA-004/5). Correction to the design's §8 claim: double-click isn't deduped via an Idempotency-Key (no client sends one in P1a) — convergence is via the per-(linkId,date) upsert + the legacy 409→PUT fallback.
- **Carry-forwards:** MP-5 closed (startDate=Monday verified), MP-7 closed (D-6), MP-6 mitigated (read levels live), MP-8 mitigated (residual legacy TOCTOU documented), +MP-9 (key rotation), +MP-10 (week-publish resilience). Docs: 5 READMEs synced + a new `docs/runbooks/mobile-publish-legacy-connector.md`; no CHANGELOG/ADR (none warranted — initiative-scoped).
- **Live-validated (this session):** the migration was applied to dev (neondb, `migrate deploy`) and the gated T16 ran **green (7/7)** against the local harness — connect→listLevels(Pro id 2, live)→link→publish(created)→republish(skipped)→edit(updated), with 404→null confirmed (CORR-002). The live run caught **4 wire bugs invisible to the mocked unit tests** (the whole point of the integration): signin body must be `{username}` not `{email}`; the base URL must include the `/api/v1` version prefix (the legacy prefix, `/dev-api/v1` for the dev backend — part of the base, not a hardcode); the `generalProgram` write body needs a nested `trainingLevel:{id}` not a flat `trainingLevelId` (a flat field → 403); the response `trainingLevel.name` is nullable on a write echo. All fixed + `rest-adapter.test` updated to pin the real wire shape.
- **Before prod:** set `MOBILE_PUBLISH_ENCRYPTION_KEY` + `LEGACY_MOBILE_API_BASE_URL` (WITH `/api/v1`) in Vercel; the migration rides `db-migrate.yml` post-merge.
- **Next:** P1b — coach UI on the 5 endpoints (UI-first on mocks).

## 2026-06-25 — PR #317 review addressed (8 findings)

An external review of #317 raised 8 findings (concentrated in the publish state machine). Disposition:

- **#1 (blocking, fixed):** a content-identical but UNOWNED legacy row (e.g. one we POSTed whose response timed out → retry → 409) was reported `conflict` and the ledger never claimed → the day became a permanent conflict. Fixed: `decidePublishAction` now checks `contentMatches` BEFORE the unowned-guard, and `publishDay`/`resolveRace` CLAIM the ledger (upsert) on a content-match skip when `!isOwned`. Pinned by new decide + publish-day tests.
- **#2 (blocking, fixed):** a legacy 401 mid-publish was swallowed into `action:"failed"`; the per-day catch now rethrows it as the `MOBILE_RECONNECT_REQUIRED` signal (fail-fast — a dead token fails every day the same way).
- **#4 (fixed, OWNER CONFIRM → MP-11):** the projection published `row.notes` into the legacy free-text, but the platform's own SessionCard hides notes (`buildSummary` → `notes:[]`) → published ≠ displayed + a possible leak of coach cues to athletes' phones. Applied the safe default (stop publishing notes, matching the platform display); flagged MP-11 for the owner.
- **#5 / #7 (fixed):** legacy writes (POST/PUT) no longer auto-retry (a timed-out write may have committed → retrying risks a double-insert, the mechanism behind #1); extracted `toWriteBody` to dedupe the create/put wire body (its shape already needed a live-found fix once).
- **Held the line (not bugs):** #3 — `MobilePublishedDay.contentHash` is NOT dead state; it's the ledger fingerprint for the P3 overwrite-guard ("ours vs a day Denys typed"), and the legacy GET is needed anyway (row id + drift detect), so a stored-hash compare wouldn't save a round-trip. #6 — 502 (legacy responded with an error) vs 504 (legacy didn't respond) are meaningfully distinct upstream failures HTTP separates on purpose (alerting matches `502|504` in one rule); unifying loses diagnostics. #8 — that's MP-10 (P3), already documented.
- **Verification:** all unit tests + the gated T16 re-ran green (7/7); check-types 16/16 · lint · dep:check 0.

## 2026-06-25 — P1b DONE (coach UI; via `/feature`)

Built the 3 coach surfaces on the P1a endpoints on `feat/mobile-publish-p1b` (11 commits). Ratified **D-10/D-11/D-12**.

- **Shipped (3 surfaces):** (C) **Connect** — `MobileAppSection` + `ConnectMobileModal` on `/coach/profile` (legacy email/password → `POST /connections`; connected identity + role + expiry; a proactive amber "Reconnects in N days" / "Expired" nudge near/past expiry → inline reconnect). (D) **Link** — an inline "Mobile publishing" strip (`EnrollmentsStrip` template, between the enrollments strip and `WeekNavigator`) + `ManageMobileLinksModal` (linked-level list with per-row danger-confirm delete + an unlinked-level `Select`/Add); "Publishes to: …" joins `legacyLevelId→name`, falling back to a count when levels are loading/errored (OQ-3). (E) **Publish** — "Publish this week" → `PublishWeekModal`: a per-link `Promise.allSettled` loop (`usePublishMobile().mutateAsync`), results grouped by level as `StatusChip`s (`PUBLISH_RESULT_CHIPS`, 5 actions), any `conflict` → a nested warning `ConfirmationModal` → on confirm re-run with `overwriteUnowned=true` (D-4, never auto), a reconnect rejection → an inline `ConnectMobileModal` CTA.
- **Client layer:** the `mobile` endpoint slice (`createMobileAPI`), `platformKeys.mobile.*`, the `use-mobile-publish` hooks (queries `useMobileConnections`/`useTrainingLevels(enabled)`/`useMobileLinks(planId)`; mutations connect/createLink/deleteLink/publish), the shared `isReconnectRequired(error)` helper, fixtures (`lib/mobile.fixtures.ts`) + the chip config. An extracted `PublishResultsPanel` keeps one-component-per-file.
- **D-12 constant promotion:** `MOBILE_RECONNECT_REQUIRED` moved to `@repo/contracts/coaching/mobile-publish` (`mobile-publish.constants.ts`); api-server `reconnect-signal.ts` imports + re-exports it (behavior-preserving, byte-identical) so its importers resolve unchanged; the client detects it via `isReconnectRequired`.
- **Additive server addendum (D-11):** `listLinks(userId, planId)` (scoped `findMany` by `{ planId, connection:{ coachProfileId } }` after `verifyPlanOwnership`) + `deleteLink(userId, linkId)` (`verifyMobileLinkOwnership` then delete) services; `GET /links?planId=` (alongside the existing `POST`) + `DELETE /links/[id]` routes; the `getMobileLinksQuery`/`deleteMobileLinkParams` contract schemas. Unlink IN scope; **disconnect (`DELETE /connections`) DEFERRED to P3** on the cascade-blast-radius decision (both `MobilePublishLink.connection` and `MobilePublishedDay.link` are `onDelete: Cascade` → a disconnect cascade-wipes every link + the whole ledger; MP-12, D-11).
- **Review + QA fix-loop (2 CRITICAL + a D-4 erosion fixed):** the hostile QA found the publish state machine lacked a synchronous in-flight lock. Fixed in `b8619002`: **QA-001** (close-mid-publish repopulated a closed modal AND a reopen fired a SECOND concurrent publish — orphaned confirm + double legacy write) and **QA-002** (a double-click on "Overwrite & publish" spawned TWO parallel overwrite loops clobbering a coach-authored day) — both fixed via a synchronous in-flight lock + a `runId` epoch gating the post-await applies (a stale run's results are dropped); **QA-006** (the D-4 erosion: `overwriteUnowned` was sticky across a reconnect → a post-reconnect re-run silently re-overwrote without a fresh confirm) — fixed by resetting overwrite intent so any still-present conflict re-prompts. UX polish in `aa3ec837` (e.g. the "1 days" plural, results keyed by id not name — QA-027/QA-032). The idempotency reality (every non-GET call mints a fresh `crypto.randomUUID()` key, so no client/server dedup protects publish) is why the synchronous lock was the right fix, not idempotency-key reuse.
- **Deferred polish (MP-13, all recoverable, no data loss):** QA-004 (re-publish after Cancel needs an in-panel affordance — owner UX call), QA-007/QA-020 (snapshot `links` on modal open to avoid mid-publish desync/404), QA-011 (coordinate the dual conflict+reconnect CTAs), the residual close+reopen double legacy round-trip (idempotent-safe per D-9). Recorded in `deferred.md`.
- **Tests (in this PR):** 7 files — the mandatory `PublishResultsPanel` (5 actions) + conflict→confirm→`overwriteUnowned:true` and conflict→cancel→no-write component tests; regression guards for QA-001/002/006 (single overwrite run, no stale-close double-publish, reconnect without silent re-overwrite); the `use-mobile-publish` hook + `isReconnectRequired` truth-table + connect/link/strip state tests (**42 platform tests green**); plus the gated `links` service-unit test (authored — the api-server run stays gated, owner approval).
- **Next:** P2 — Individual publish (the per-athlete identity picker, MP-1).

## 2026-06-25 — P1 MERGED + go-live (step close-out)

- **P1a (#317) + P1b (#318) merged to main → P1 (connector + General publish) is COMPLETE end-to-end.** A coach connects the mobile app from his profile (legacy login → encrypted token), links a plan to a training Level, and publishes a week → legacy `general_programs` → his athletes see it in the iOS app. Owner manually validated P1b ("works great").
- **Go-live (owner, this session):** generated `MOBILE_PUBLISH_ENCRYPTION_KEY` (`openssl rand -base64 32`), setting it + `LEGACY_MOBILE_API_BASE_URL=https://thedisciplineprogram.com/api/v1` in Vercel + merged both PRs. The P1a migration (3 additive `app_mobile_*` tables) rode `db-migrate.yml` at the #317 merge.
- ⚠ **The prod encryption key was pasted into chat** → regenerating it NOW is free (zero coaches connected = zero tokens encrypted with it); after a coach connects, rotation costs a reconnect (MP-9). Owner's call.
- **Confirm next session:** the prod build greened with the env set + a deliberate first prod-publish smoke (the first real publish writes to inviolable legacy-prod — pick a real week or a throwaway date to clean up).
- **Next:** P2 (Individual channel, the per-athlete identity picker MP-1) — or pause. Owner is sending coach Denys a "how to use it" note to start pushing real weeks.

## 2026-06-26 — Publish-syntax v2 (D-13; coach readability feedback; via `/feature small`)

- **Trigger.** First prod publishes landed (P1 merged: #317 P1a + #318 P1b). Денys's feedback: the synced text doesn't show the EMOM/AMRAP/round-count "tab" and lacks brackets/separators — it scans as a wall of text vs. how he writes by hand.
- **Diagnosis (code + prod).** `projectBlock` rendered ONLY `schema.rows` via the flat `renderRowLine` and dropped the entire Schema structural layer (`header`/`composition`/`intensity`); `parity.test.ts` ASSERTED the drop as correct. Verified on **prod** (read-only): our Neon — 66/66 schemas have `composition` + `intensity`, 59/66 a `header` (the dropped layer ≈ 100% of his authoring; kinds in use: count 25, ladder 8, interval 7, once 3, cadence 1, timeCap 1, benchmark 13). Legacy prod (ADMIN REST GET, 395 manual entries) decoded his house style: one `exercises[]` = one schema = `header\n\nreps-first movement lines` with `[ ]` notes, `|`/`/` separators, intensity once. Live levels drifted to `Scaled/Pro/Advanced/Functional Bodybuilding` (MP-6 — read live). Found our own flat publishes at legacy ids 571/573/575/576 (Advanced, the days he screenshotted).
- **D-13 (RATIFIED).** New schema-aware assembler `projection/format-legacy-schema.ts` composing the shared `@repo/contracts` element formatters (no re-implementation); SSOT reframed to the element level, line/schema assembly is a legacy-specific view. Projection-only — no schema/migration/contract/legacy change; re-publish self-heals owned days (D-9 PUT), conflict-guard protects coach-authored days.
- **AMRAP correction (owner).** `timeCap` repetition = AMRAP, not `cap` → the projection labels it `AMRAP 20’` (header that is the label's leading token collapses, no `AMRAP · AMRAP`); the shared-formatter twin for athlete-facing surfaces is deferred MP-14.
- **Tests.** Rewrote `projection/{parity,project-day}.test.ts` (they had locked the drop) → 22 unit tests asserting the structured output + element-formatter parity + intensity-once + AMRAP-not-cap + notes/side/tempo surfacing (review TEST-001). Worktree run: `SKIP_ENV_VALIDATION=1 DATABASE_URL=… pnpm --filter @repo/api-server exec vitest run …/projection/`; `check-types`/`lint` clean. Review-light: B+, 0 CRITICAL.
- **Open (owner-facing):** per-element wording (`RX M:18 F:14`→`18/14`, `@…% of 1RM`→`from 1 RM`) deferred to Денys's reaction to the live iOS render (D-13 element-level decision); MP-14 (shared `cap`→`AMRAP`).
- **PR #319 review follow-up (fixed in-PR).** REVIEW-1: the append heuristic gated on "header has no digit" → a name header with an unrelated number ("Part 2") suppressed the whole cadence/AMRAP/ladder label → replaced with keyword-containment (append unless the header already conveys the label's lead token). REVIEW-3: `formatCapSummary`/`formatBenchmarkSummary` ran only in the headerless branch → a coach header dropped a cross-cutting cap/benchmark ("fran" lost `cap 5’`) → cap+benchmark now appended in every branch. REVIEW-4: added wiring coverage (range reps, alternating side, row rest, own rpe/hr/numeric-pace, single-axis/absolute loads) → 26 projection units. REVIEW-2 (idempotency on multiline): **validated** — a byte-exact POST→GET round-trip through the legacy harness (the prod Spring clone) preserved `\n\n`/brackets/`·`/`’`/`×` exactly (190==190 bytes), so `contentMatches` still skips an unchanged re-publish (no churn of live iOS data).

## 2026-06-26 — P2a DONE (Individual channel server/integration; via `/feature`)

Built the Individual publish channel's server/integration half on `feat/mobile-publish-p2a`. Ratified **D-14** (identity model) + **D-15** (DELETE+POST replace); carried **MP-15** (non-atomicity). (Merged main's publish-syntax v2 #319 in — D-13/MP-14 are its, renumbered this side D-13→D-14, D-14→D-15, MP-14→MP-15 to resolve the parallel-session collision.)

- **Pre-spec verify (the load-bearing find).** Source-traced the legacy `IndividualProgram` controller/service/DTO → **the legacy Individual PUT is broken**: `IndividualProgramDTO` has `@JsonIgnoreProperties(value="id", allowGetters=true)` (General's DTO doesn't), so the PUT body `id` is dropped on deserialization → `updateIndividualProgram(null, …)` → `findById(null)` throws (a slip-through would INSERT a dup). So republish-in-place CANNOT be a PUT — it's **DELETE `/individualProgram/{id}` + POST** (D-15). Also confirmed: the individual write body is FLAT `userId` (mirror of General's nested `trainingLevel:{id}`), the athlete `id` is a `Long` (→ `legacyUserId Int`), `GET /user?userPlanId=2` returns `List<UserRequestDTO>`. Folded into `legacy-contract.md`.
- **Schema (additive, prod-safe).** `MobilePublishLink` +`legacyUserId Int?` +`athleteId String?` (FK→`User`, Cascade) + `legacyLevelId` relaxed nullable + the per-plan bijection uniques (NULLS-DISTINCT) + the raw-SQL `mobile_link_channel_key_xor` CHECK. Authored OFFLINE (`migrate diff` git-old→current) as **two** folders — `…_enum` (`ALTER TYPE ADD VALUE 'INDIVIDUAL'` alone) then `…_link` (cols+uniques+FK+CHECK) — split because a CHECK referencing the new enum value can't run in the ADD-VALUE transaction (FLAG-2). Applied to dev; existing GENERAL rows satisfy the CHECK.
- **The seam.** `publish-day` read only `.id/.isRestDay/.dailyProgram` from legacy rows → extracted a unified `LegacyProgramRow` + an injected `ChannelProgramOps {getProgram, createProgram, replaceProgram}` built in `publish.ts` from `link.channel`. GENERAL `replaceProgram`=PUT; INDIVIDUAL=DELETE-then-POST (new row). `decidePublishAction`/projection/`recordPublishedDay`/the 5 D-9 unit cases stay **byte-identical** (the GENERAL regression guard); `buildChannelOps` fails closed on a null key.
- **Contracts (flat-additive, FLAG-1).** Widened `mobileLinkSchema` (channel enum, nullable level/user/athlete) + `createIndividualMobileLinkSchema` + a `z.union([individualArm, generalArm])` request (NOT `discriminatedUnion` — the GENERAL wire has no `channel` discriminator; individual-first). The widening broke `MobileLink.legacyLevelId` to `number|null` → minimal null-guards in the 3 live General components + a `channel==="GENERAL"` filter so an individual link never leaks as "Level null".
- **Endpoints.** A live athletes proxy (`GET /api/platform/mobile/athletes` → `getIndividualAthletes` → legacy `GET /user?userPlanId=2`, MP-6, coach-gated + rate-limited, mirrors `training-levels`); `POST /links` takes the union via `createIndividualLink`. `createIndividualLink` **upserts keyed on `athleteId`** (D-14/OQ-1), `update` re-points legacyUserId — re-pick updates in place; a second athlete on a taken legacy user hits the unique cleanly (NOT a silent steal — the Review/QA refinement over the design's legacyUserId-key, QA-04/05).
- **Review + QA.** Caught 1 CRITICAL — a RED contracts test (`mobile-link.schema.test.ts` baseEntity missed the new required-nullable keys; I'd committed contracts on check-types+lint **without running the package test** — lesson: run the package `test`, not just types — saved to memory). Fixed + the upsert-key refinement + mock fidelity. QA's sharpest (QA-01): the DELETE+POST window can destroy an overwritten-confirmed athlete day on POST-failure → documented MP-15 (no cheap atomic fix; Individual isn't live; integration proves the happy path).
- **Gated proof (owner gave standing test approval — runs at discretion now).** Dev migration applied; **api-server mobile units 92✓**; the **live integration green (14✓)** against the harness — the GENERAL block (no regression) AND the new INDIVIDUAL block, which **DELETE+POST-proves D-15**: publish (created) → republish (skipped) → edit→republish (`updated` + a NEW legacyRowId ≠ the create id). One stumble: a prior session left 33 stale `general_programs` rows → the GENERAL block first failed `conflict` (unowned existing rows); truncating the legacy program tables → clean slate → 14✓ (recipe extended above).
- **Next:** P2b — the Individual coach UI (athlete picker + link-modal individual mode + grouped publish + client wiring).
