# athlete-core — journal (append-only)

## 2026-06-17 — founded (planner session, parallel to primitive-v2)

Mined the Phase-3 athlete domain in parallel with the `primitive-v2` Step-0 thread.

- **Parallelism resolved first.** Phase 3 builds on the frozen skeleton + 6 repetition kinds (stable). The only cross-thread dependency is the byProfile nested shape — isolated as a tail. The "everything is in flight" worry was an over-read of the charter scope list; the owner corrected it ("это до-дизайн, каркас не меняется") and the spec read confirmed it.
- **Domain mined with the owner** (athlete + coach, first-person authority). Caught and resolved the plan-as-train clash: the recorded Denys principle ("поезд не ждёт", make-up = anti-pattern) is the BILLING layer, not navigation — **D-LAYERS**. Verified the recorded memory verbatim BEFORE folding; nearly overwrote Denys's principle, the check saved it. The memory was then refined (scope-split), not erased.
- **Ratified:** D-LAYERS, D-STATS (statistics decompose; law differs training vs benchmark), D-RESULT-TYPES (six canonical CrossFit result types), D-PUBLISH (plan-level draft/published), D-DATE-THREAD (optional hide-past).
- **New scope surfaced:** benchmarks as a subsystem (any-schema→benchmark, catalog, admin CRUD, profile linkage), templates (block/schema reusables, value-copy on insert), the publish mechanism. Recorded as scope expansion vs the roadmap's "seed ≥25".
- **Founded** the initiative (charter/plan/state/decisions/deferred/discovery). `ACTIVE` stays `primitive-v2`. The designer/UI wave is owner-run, out of planner scope.

**NEXT:** contract-shape spec (plan step 0).

## 2026-06-17 — primitive-v2 closed, repo synced, tail resolved

- Owner merged PR #282 (the primitive-v2 reshape, one wave) and dropped the branch; that session had already done the spec re-freeze + close-out (`docs(primitive-v2): re-freeze spec + initiative close-out`).
- Synced local `main` (fast-forward 07cbcaad..458d5190), pruned the deleted remote, deleted three merged local branches (`feat/primitive-v2-reshape`, `fix/gated-suite-failures`, `fix/timed-test-ui-papercuts`). Local repo clean on `main`.
- Read the final primitive (`reshape-design.md` + `load.ts`): byProfile is now `axes[]` (1–2) + `cells[]` (cartesian, coords-per-axis); intensity trinity (row/schema/block, render overlay row>schema>block); cross-cutting `composition.cap`; interval `work/off {value,unit}`. **Key:** the design doc routes the byProfile + %-of-1RM **resolver to Phase 3** — so it is athlete-core scope, not a wait.
- **Switched `initiatives/ACTIVE` → `athlete-core`** (primitive-v2 closed). Updated charter/plan/state/deferred: no open cross-thread waits; resolver folded into block 1; waves kept coarse per owner directive.

**Still NEXT:** contract-shape spec (plan step 0) — now with the final primitive in hand.

## 2026-06-17 — block 1 (data core) SHIPPED via `/feature` (full)

Executor `/feature` run on `block-1-feature-prompt.md` — the athlete-core data floor. ONE coarse wave; 6 prod commits on `feat/athlete-core-data-core` (+ test/docs/close-out).

- **Built:** Prisma reshape (drop `PerformedExerciseInstance` + the `OneRMRecord`/`PerformedSession` uniques; `startedAt/completedAt → performedAt`; new `PerformedSchemaResult`; `+PlanEnrollment.hidePastBeforeBoarding`; `+AthleteProfile.profileSelections`); 4 contract entities (`result` VO + `one-rm-record` + `performed-session` + `performed-schema-result`); mappers/handlers; 3 athlete write routes (`api/platform/athlete/...`); the pure `resolveLoad` + records/PR derivation libs (fetch-then-compute, N+1-guarded); the coach-metrics migration (`→ performedAt`, window Map holds MAX); the green benchmark chip + editor section.
- **9 build decisions ratified → `decisions.md`:** D-DIST-UNITS, D-TICK-DERIVED, D-PROFILE-SELECTIONS (overrides contract-shapes §1.3 "no storage" — doc fixed), D-RESULT-RELATION, D-RESOLVED-SHAPE, D-SCOPE-PUBLISH (publish OUT of block 1).
- **Process:** research caught a blast-radius the spec understated — the `PerformedSession` reshape fans out through `PerformedByKey` to ~15 coach-metrics files (folded into the wave, migrated semantically). 3 implement waves (Prisma∥Contracts; api-server; platform), per-wave checkpoint commits, orchestrator verified each via `git diff` + check-types/lint (never agent self-report). Review+QA = 0 CRITICAL; 4 integrity WARNINGs proactively fixed (schema↔session link, result-type match, last-activity clamp, result `@@unique`).
- **Gated suite caught one bug** (as intended): a Wave-2 gap — `mapToAthleteProfile` gained `profileSelections` but `admin-user-view.mapper.test.ts`'s loosely-typed fixture omitted it (check-types blind to the loose helper) → `parse(undefined)`. Fixed the fixture (prod always sends null). Reseeded DB, suite GREEN (962 tests).
- **Close-out** (`closeout-before-pr`): decisions/deferred/contract-shapes/plan/state/journal advanced; landed IN the feature PR.

**NEXT:** block 2 — Athlete UX (owner-run UI): plan-as-train view, logging-30s, records/PR, profile + the set-1RM / pick-profile affordances the resolver's `unresolved` shapes feed.

## 2026-06-18 — block 2 screen 1 (Plan Timetable) SHIPPED via `/feature` (full, autonomous)

Executor `/feature` run on `block-2-screen-1-feature-prompt.md` — the athlete plan-timetable on REAL data (the first of block 2's 4 athlete screens). Owner delegated ALL gate decisions ("принимай решения сам, run fully without me"); no blocking gates — every gate self-resolved with logged rationale. 13 commits on `feat/athlete-plan-timetable`.

- **Built (cross-layer slice):** a new `plan-timetable` derived-view contract + read endpoint (`GET /api/platform/athlete/plan-timetable`, `withAthleteAuth`) over a PURE `buildPlanTimetable` (no N+1: 1 enrollment + 1 performed + 1 user query) + a TanStack hook + a mobile-first MUI timetable module (16 files, zero-hex, one-component-per-file) + the athlete nav extension (Records item + `leaderboard` icon + placeholder route) + the page wire. Faithful to the owner-approved Claude Design prototype, built native via theme tokens (no transplanted HTML).
- **8 decisions ratified → `decisions.md` (D-TT-\*):** MULTIPLAN (list endpoint + switcher), SLOT-MODEL (slot/card decoration split for N-session days), SERVER-COMPUTES (view presentation-only), DATES-ABSOLUTE (the tz fix), NO-COACHING-EDGE (overrode the design's backward-import — the `api-server-lms-no-coaching` dep-cruiser rule would have failed pre-push `dep:check`), SHELL, DESKTOP (responsive column, not the prototype's aside rail).
- **Review + QA = 0 CRITICAL.** Both independently flagged a device-tz off-by-one in the date display (reproduced "Jun 14–20" vs "Jun 15–21" under LA) — fixed at the source: the server emits an absolute `dayOfMonth`, the client formats the week-range in UTC + reads the weekday from the `dayOfWeek` enum. Also fixed QA-004 (planId-keyed switcher state vs array-index), QA-005 (default to the first NON-empty plan), and removed dead code. Commit `6c97dcdc`.
- **Tests 70/70 green:** contract 16 + builder 24 (pure, tz-stable across UTC±) + presentation 13 + component 8 + integration 9 (live dev DB, self-fixturing, no-N+1 verified by `vi.spyOn`). No destructive `db:reset` needed — the slice is additive (no schema change) and the integration test creates + cleans its own data.
- **Process:** 8 task-commits in dependency waves (contract → builder/nav → endpoint/hook → ui → route/page), each orchestrator-verified via `git diff` + check-types/lint (never agent self-report). The D-A12 backward-import override proved load-bearing — it aligns with an existing dep-cruiser rule that pre-push enforces.
- **Close-out** (`closeout-before-pr`): decisions/deferred/state/journal/plan advanced; landing IN the feature PR.

**NEXT:** block 2 screen 2 — the session / workout view (read-only blocks→schemas→rows, the `ResolvedLoad` 4-variant render, the benchmark green chip + "Log result" / "Mark completed"). The timetable card-tap stub (`onOpenSession`) is the entry point.

## 2026-06-18 — block 2 screen 2 scope EXPANDED (owner) + resolver-home blocker found (planner pre-design verify)

**Owner directive (intent):** the screen-2 `/feature` run must close every blocker that exists or surfaces along the way — the 1RM / benchmark / profile holes — in ONE run, cross-domain work included, so the athlete training screen ends up FULLY working. Per `scope-not-deciding-criterion` ("скоуп не решающий критерий").

**Wave-2 scope (re-set):** a session/workout view that works end-to-end — read-only plan content + all 4 `ResolvedLoad` states with the resolve-holes closed INLINE (missing_one_rm → set-1RM affordance; missing_profile_pick → pick-profile affordance) + logging (Mark completed / benchmark Log result, 6 typed forms).

**Planner verify (read source verbatim) — the floor block-1 already laid (NOT blockers):** all write routes exist — `POST performed-sessions`, `POST performed-sessions/[id]/result`, `POST one-rm-records`, `PUT athlete/profile` (takes `weightKg`+`profileSelections`); the pure `resolveLoad` (4-variant `ResolvedLoad`) + `loadAthleteLoadContext`; app hooks `useCreatePerformedSession`/`useCreatePerformedSchemaResult`/`useOneRMRecords`+`useCreateOneRMRecord` (built, not yet in the `lib/hooks` barrel). So most "cross-domain" work is already done — wave 2 is mostly the read endpoint + UI + wiring.

**The one architectural blocker found:** the resolver family (`resolve-load`/`load-records`/`derive-records`/`athlete-records.types`) lives in `endpoints/coaching/athlete-records/`, but every athlete read-endpoint (incl. the shipped timetable) lives in `endpoints/lms/`, and `api-server-lms-no-coaching` (dep-cruiser, pre-push) FORBIDS `endpoints/lms → endpoints/coaching`. The resolver's only consumer today is the `coaching/index.ts` barrel re-export (no real prod caller — grep-confirmed). **Planner call (ratify at the prompt gate): move the resolver family → `endpoints/lms/`** — aligns with D-TT (athlete endpoints = lms), keeps block-3 coach-metrics legal (coaching→lms is a forward edge), dep-cruiser-clean. The one `contracts/coaching/athlete-profile` import (`profileSelectionsSchema`) from an lms endpoint stays — allowed (the bans are endpoints→endpoints and contracts-lms→contracts-coaching; neither matches an lms-endpoint→coaching-contract edge).

**Wave-2 builds:** [ARCH] resolver→lms move · [READ] session-detail aggregate endpoint (session→blocks→schemas[composition+benchmark]→rows, server-side per-row load resolve; mirrors `SCHEMA_BODY_INCLUDE`+blocks) + contract + app hook · [UI] session view (4 resolve states, green benchmark chip, primary action) · [LOG] Mark-completed + benchmark Log-result (6 typed forms) wiring the existing hooks (+ barrel) · [1RM] inline set-1RM · [PROFILE] inline pick-profile (+ a new athlete-profile app hook over the existing GET/PUT) · [NAV] `onOpenSession` no-op → route `/athlete/session/[sessionId]` · [DATA] a dev fixture plan (percentage+byProfile+benchmark) for the walkthrough.

**Border (planner rec, pending owner retort):** the Records/PR-history screen + the standalone Profile-management screen (block-2 screens 3-4; own nav, "Coming soon") stay a SEPARATE next wave — inline set-1RM/pick-profile close the resolve holes on the session screen itself, so they aren't needed for "the training screen works". The benchmark/template/profile CATALOG (admin CRUD, fusion form) stays the deferred library wave; free-string profile axes suffice. Plan publish stays its own wave (D-SCOPE-PUBLISH).

**NEXT:** await the screen-2 Claude Design from owner → review → assemble the executor `/feature` prompt with all of the above (+ D-TT-DATES-ABSOLUTE for any dates, tokens-not-hex, lms↛coaching).

## 2026-06-18 — block 2 screen 2 (Session / Workout View) SHIPPED via `/feature` (full)

Executor `/feature` run on `block-2-screen-2-feature-prompt.md` — the athlete training screen, fully working end-to-end in ONE cross-layer slice (the owner's expanded scope: close the resolve-holes INLINE, cross-domain work included). 11 commits on `feat/athlete-session-view` (off `main`); 94-file diff, 0 CRITICAL after the fix wave.

- **Built (cross-layer slice):** [ARCH] the load-resolver family moved `coaching/athlete-records/` → `lms/athlete-records/` (a pure `git mv` + 2 barrel edits — the session-view endpoint is lms and `api-server-lms-no-coaching` forbids lms→coaching). [READ] a new `session-detail` derived-view contract + a `GET /api/platform/athlete/sessions/[sessionId]` endpoint over a pure builder (IDOR-guarded default-deny → `NotFound` on every miss incl. before-boarding; N+1-budgeted — one subtree fetch + one ctx fetch + one performed fetch, per-model spies pin it; server-computes the `ResolvedLoad` per row). [HOOK] the read hook + client + an athlete-profile GET/PUT hook, barrel-wired. [UI] the `athlete-session/` module (one-component-per-file, zero-hex): the read surface (header → blocks → schema cards → rows) with all **4 `ResolvedLoad` states** + **6 typed result forms** + **inline set-1RM / pick-profile editors** + the green benchmark chip + `<DisplayNumber>`. [LOG] Mark-completed (ordinary) + benchmark Log-result. [NAV] `onOpenSession` → `/athlete/session/[sessionId]` under the `(secondary)` padded `AthleteShell`. The resolve-holes close in place (a `%` → kg on set-1RM; a `byProfile` cell → kg on pick, remembered). Faithful to the owner-approved Claude Design prototype, native via theme tokens.
- **13 decisions ratified → `decisions.md` (D-RESOLVER-LMS … D-SD-ROUTE-PADDED):** RESOLVER-LMS (whole-dir move), SD-CONTRACT (bespoke Prisma-free view + `resolvedLoadSchema` — shipped as `z.union` not `discriminatedUnion`, a parity type-test guards equivalence), VIEW-SHAPE (embedded raw VOs + server `resolvedLoad`), SD-SERVER-RESOLVE, SD-DATES (absolute `dayOfWeek`+`dayOfMonth`, no today-detection → Today pill deferred), FMT-HOME (the ~6 pure formatters COPIED into the module, NOT the barrel — Gate-A OQ-3 override), SD-BADGE-TEXT (`format-composition-summary` wording; CrossFit vocab deferred OQ-1), SD-GROUP-LABELS (data-derivable only), SD-SHEET (mobile bottom Drawer / desktop rail), SD-NUMERIC (module-local `<DisplayNumber>`, no repo variant), **SD-LOG-ATOMIC** (the QA-001 fix — supersedes the planned D-SD-LOG-MULTISCHEMA), VIEW-RESULT (`existingResult` = latest, not best-of — R3), SD-ROUTE-PADDED.
- **Review + QA → one CRITICAL, fixed.** QA-001 (CRITICAL): the planned two-step benchmark log (create performed-session, then loop `POST …/[id]/result` per schema) could ORPHAN a persisted performed-session + partial results on a mid-loop failure — the session would show `done` while a schema had no result, silently, on the marginal gym connection this screen targets. **Fixed atomically (`254e3fe3`):** `createPerformedSessionSchema` gained `results: z.array(...)` (+ a dup-`plannedSchemaId` reject); the server commits the performed-session + all schema results in ONE `prisma.$transaction`; the client posts once. Verified as-built (`confirm()` does one `mutateAsync({...results})`; the loop is gone). Also fixed (`14557f2b`) QA-002 (inline editors double-submittable — threaded the right `isPending`), QA-003 (profile PUT-per-tap + lost-pick race), QA-006 (read guard now filters soft-deleted/archived plans). Review = A− APPROVE (zero `any`/`as`/`!`, default-deny IDOR, tz-stability structural not careful, N+1 spy-asserted, zero hex).
- **Deferred items recorded → `deferred.md`:** the pure-formatter lift to `@repo/contracts/lms/_shared/format/` on a 3rd consumer · a repo-wide `numeric` typography variant (2nd consumer now) · the optional full-screen "calm confirmation" overlay (shipped toast + done-flip instead) · CrossFit-vocab badges · **FUNC-002** (rows inside a "choose one" row-group don't expose their inline resolve affordances — prototype-faithful compact line, uncommon; known limitation) · the Today pill · the now-UI-unused standalone `…/[id]/result` route + `useCreatePerformedSchemaResult` hook (candidate cleanup; still valid block-1 API) · the `composeSlotTitle`/title-fallback dedup · the coach-side `DayRowHead` date-tz fix (repo-wide pass).
- **Process:** dependency waves (resolver-move → contract/endpoint/route → hooks → UI → wire → fix), each orchestrator-verified via `git diff` + check-types/lint/dep:check (never agent self-report). Gate-A resolutions: OQ-2 dropped the dev-fixture task (owner builds the walkthrough plan by hand via the coach platform), OQ-3 = copy-not-barrel, OQ-1 = `format-composition-summary` wording, R3 = latest-result. `db:reset` world, no migration files; the slice is additive (the contract `results[]` field is optional/backwards-compatible).
- **Close-out** (`closeout-before-pr`): decisions (13) / deferred / state / journal / plan advanced; landing IN the feature PR.

**NEXT:** block 2 screens 3-4 — the standalone Records / PR-history screen (best-of per 1RM + per benchmark; the block-1 derive/records libs' UI; D-VIEW-RESULT deferred PR computation to here) + the standalone Profile-management screen (bodyweight + remembered profile picks; the athlete-profile hook already exists). Not on the "training screen works" critical path — screen 2 closed the resolve-holes inline. Then block 3 (coach honest-metrics) · the catalog library wave · publish · leaderboard.

## 2026-06-19 — block 2 screen 2 in-schema benchmark logging RE-HOMED (athlete-owned append history) via `/feature` (full)

Executor `/feature` run on `feat/athlete-session-view` (dirty tree = baseline; one working-tree commit after the owner's manual test — NO checkpoint commits). **Escalated from `/feature small` → FULL at Gate A** when the owner's domain ruling forced a Prisma model re-home (a `small` slice can't own a schema change + a new endpoint + a completion refactor). The screen-2 logging shape from D-SD-LOG-ATOMIC (logging folded into the completion transaction) was reworked: **logging a benchmark result and completing a workout are now TWO INDEPENDENT EVENTS**, and the result is re-homed to an athlete-owned, append-only model.

- **Built (cross-layer slice, ~34 files):** [PRISMA] re-home `PerformedSchemaResult` → athlete-owned **`BenchmarkResult`** (`userId, plannedSchemaId, result, recordedAt`; no `performedSessionId`, no `@@unique`; `@@index([userId, plannedSchemaId, recordedAt])`); `PerformedSession` lost its `results` relation. [CONTRACTS] full rename `performed-schema-result` → `benchmark-result` (dir + `@repo/contracts` `package.json` exports entry + barrels + the reshaped entity, `sessionId` params — OQ-D full rename). [API-SERVER] `mapToBenchmarkResult`; a NotFound session-reachability guard (`_shared/session-access.ts`); a **new athlete-owned result service** (append create + `load`→`OneRMRecord` atomic in one `prisma.$transaction`, `retryOnP2034`); the **completion refactor** (dropped the `results[]` validate+write loop, kept the Forbidden guard); `validateBenchmarkResultForSession` relocated + the dead standalone service removed; `session-detail` `existingResult` re-pointed to `BenchmarkResult` latest (done/completedAt unchanged); `derive-records` seed date field `performedAt`→`recordedAt`. [ROUTE] new `POST /api/platform/athlete/sessions/[sessionId]/result` (sessionId-keyed, IDOR→NotFound); old `performed-sessions/[performedSessionId]/result` route removed. [CLIENT/HOOK] `api.benchmarkResults.create(sessionId, data)` + the revived `useLogBenchmarkResult` (invalidate detail + oneRMRecords, **NOT** the timetable — the log doesn't flip the tick). [UI] a new `<BenchmarkLogPanel>` (in-schema logPrompt → typed editor → logged strip, "Log again" appends a new attempt) + a new `<BenchmarkStatusStrip>` read-only echo; the completion rail/sheet/done **decoupled** (no more result-form collection, Mark Completed never gated on logging); `use-session-logging` hub re-wired (`confirm()` posts no `results`).
- **Decisions ratified → `decisions.md` (5):** **D-BR-OWNED-HISTORY** (athlete-owned append-only re-home; decoupled; supersedes D-SD-LOG-ATOMIC's batching while preserving its atomicity invariant; retires D-RESULT-RELATION's `@@unique` — the QA-001 orphan class dissolves), **D-BR-COMPLETION-PURE** (completion = the pure tick, coach-metrics ZERO files changed, `done = performed.length > 0` unchanged), **D-BR-1RM-ATOMIC** (`load` writes `OneRMRecord` in the same tx; new sessionId-keyed endpoint; old standalone endpoint removed, hook/validator/contracts revived), **D-1RM-LATEST** (the owner live-test ruling — working-weight `%` resolution reads the LATEST `OneRMRecord` by `recordedAt` to reflect current form, NOT the all-time MAX; benchmark records/PR stay best-of via `derive-records` — two laws, two layers; a code change in `load-records.ts`), **D-DUAL-RENDER-KEPT** (the logged result intentionally shows on BOTH the schema card and the completion echo, "как в прото" — ratified, not a double-render bug; closes QA-005).
- **Review + QA → review APPROVE (0 CRITICAL), QA score C (the 2 "CRITICAL" were test-file red owned by the test task, not product bugs).** Fixes applied (all green): QA-003 (`load.kg` bound — max 9999.99 + 2dp), QA-004 (`existingResult` deterministic tiebreak `orderBy recordedAt,id`), QA-006 (write guard `isBeforeBoarding` parity with the read guard), QA-007 (a single `recordedAt` instant in the tx), QA-010 (clear the draft on a successful log). Owner-driven live-test fixes: the `completedAt` string→Date coercion at the api-client boundary (deferred: adopt `responseSchema` parse repo-wide), and the **D-1RM-LATEST** resolution ruling (MAX→latest in `load-records.ts`).
- **Tests:** platform athlete-session interaction + presentation tests GREEN (asserting the in-schema log call args, decoupled completion with NO `results`, always-enabled Mark Completed, the read-only echo, `load-records` latest-not-MAX). api-server integration tests for the new service + the 3 affected red files WRITTEN but **GATED** (serial ~10 min, live Neon — owner-run manual gate per `api-server-serial-tests`).
- **Process:** built strictly L1→L8 by dependency (Prisma re-home → contracts → api-server services → route → client/hook → UI re-wire → tests → close-out); a cohesive migration shipped as ONE feature (a split would land a renamed-but-unconsumed model — `split-server-ui-visible-progress`). `db:reset` world, no migration files (ADR-0019); the owner runs `db:reset` at test time (OQ-E). Gate-A resolutions: OQ-A="Log again", OQ-B=delete dead `areBenchmarksReady`/`buildBenchmarkResults`, OQ-C=defer (keep `results .optional()`), OQ-D=full rename incl. package.json exports.
- **Close-out** (`closeout-before-pr`): decisions (5) / deferred / state / journal advanced; landing IN the single working-tree commit (incl. the pre-existing visual-redesign baseline already in the dirty tree — these docs are additive on top).

**NEXT (unchanged):** block 2 screens 3-4 — the standalone Records / PR-history screen (now reading the shipped athlete-owned `BenchmarkResult` history; best-of per 1RM + per benchmark; D-VIEW-RESULT deferred PR computation to here) + the standalone Profile-management screen. Then block 3 (coach honest-metrics) · the catalog library wave · publish · leaderboard.
