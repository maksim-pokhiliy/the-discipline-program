# athlete-core — state (the board)

**Updated:** 2026-06-19. **Block 2 — Athlete UX wave DONE: all 4 screens shipped.** Screen 3 (Athlete Records) on `feat/athlete-records` (#288 merged): the standalone `/athlete/records` screen — a server-derived `records-view` GET + native MUI module + route swap, the 1RM section (per movement: best + recent-change delta + date-of-best → trend chart + full history with per-row source chips) + the benchmark section (per `plannedSchemaId`: direction-aware best + PR + faithful subline → attempt history + trend); the block-1 `derive-records` lib got its FIRST production caller; the format lift landed (result formatters → `_shared`, composition → `composition`); `background.sunken` token; READ slice, no schema change (D-RV-\*). Screen 4 (Athlete Profile) on `feat/athlete-profile` (PR #286): bodyweight + height as hero-stat twins + the remembered profile-picks overview/clear (D-PROF-\*), then ENRICHED v2 — avatar upload (`User.image` via the shared Blob infra + `session.update` so the header refreshes) + an editable gender/health "Details" card (D-AV-\*/D-FIELDS-\*). Screen 2 (Session/Workout View) shipped + extended 2026-06-18/19; screen 1 (Plan Timetable) 2026-06-18; block 1 (data core) 2026-06-17 (#283). **NEXT: block 3 — coach honest-metrics.**

## Board

| #   | Step                                             | Status                    | Pointer                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --- | ------------------------------------------------ | ------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0   | Design lock + contract-shape spec                | ✅ done                   | `decisions.md` + `contract-shapes.md` + `block-1-feature-prompt.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 1   | Data core (coarse wave)                          | ✅ **DONE** (feature PR)  | `decisions.md` D-DIST-UNITS…D-SCOPE-PUBLISH · `.feature-dev/1781712323/` (research/design/plan)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2   | Athlete UX (coarse wave)                         | ✅ **DONE** · screens 1-4 | **Plan Timetable** (`feat/athlete-plan-timetable`, D-TT-\*) + **Session/Workout View** (`feat/athlete-session-view`, D-SD-\* + D-BR-\* + D-1RM-LATEST) + **Athlete Records** (`feat/athlete-records`, D-RV-\*) + **Profile** (`feat/athlete-profile`, D-PROF-\* + v2 avatar/fields/height-twin D-AV-\*/D-FIELDS-\*) ALL SHIPPED — the training screen works end-to-end, Records surfaces best-of per 1RM + per benchmark (direction-aware, per-`plannedSchemaId`, trend + history), Profile edits bodyweight/height (hero-stat twins) + picks + avatar + gender/health. 1RM resolution=latest, records=best-of. NEXT: block 3 |
| 3   | Coach honest-metrics                             | ⬜ pending                | derived fields + reconcile cron; on the migrated `performedAt` coach-metrics                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| —   | Library wave (profiles + benchmarks + templates) | 🅿️ deferred               | one catalog pass; profile-catalog re-homes `profileSelections` free-string keys; fusion-form gated                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

## Next action

**▶ Block 3 — coach honest-metrics (derived fields + reconcile cron on the migrated `performedAt`).** ALL FOUR athlete-UX screens are shipped — timetable, session/workout view (+ in-schema benchmark logging), Records (`feat/athlete-records`, D-RV-\*), and Profile (`feat/athlete-profile`, D-PROF-\* + v2 avatar/fields/height-twin D-AV-\*/D-FIELDS-\*). Block 2 (Athlete UX) is DONE. Block 3 sits on the migrated `performedAt` coach-metrics (the window Map holds MAX `performedAt`); the roadmap's `/athletes` derived wire-up.

**Remaining waves after block 3** (unchanged, deferred): the benchmark/profile/template CATALOG library wave (free-string axes suffice until then; the profile-type catalog also re-homes `profileSelections` AND would wire the typed `gender` field into byProfile resolution — D-FIELDS-GENDER-INERT) · plan publish / version-gate (D-SCOPE-PUBLISH, gate #2) · cross-athlete leaderboard · the systemic image-URL blob-host hardening (D-AV-IMAGE-BLOB-WRITE deferred part).

Screens 1-4 are DONE: screen 1 (Plan Timetable) on `feat/athlete-plan-timetable`; screen 2 (Session/Workout View) on `feat/athlete-session-view` — read endpoint + builder + session-detail contract + hooks + UI (4 load states + 6 result forms + inline set-1RM/pick-profile) + route, then EXTENDED with the in-schema benchmark-logging re-home (D-BR-\* + D-1RM-LATEST); screen 3 (Athlete Records) on `feat/athlete-records` — the `records-view` server-derived GET + builder + Prisma-free contract triplet + hook + native MUI module (1RM + benchmark sections, trend chart, history, Update-1RM modal) + route swap (D-RV-\*); screen 4 (Athlete Profile) on `feat/athlete-profile` — bodyweight + height as hero-stat twins (a shared `BodyStatCard`), the remembered profile-picks overview/clear, avatar upload (`User.image` via the shared Blob infra + `session.update` for the header), and an editable gender/health Details card (D-PROF-\* + v2 D-AV-\*/D-FIELDS-\*); `gender` is a stored attribute, NOT a resolver input (D-FIELDS-GENDER-INERT). The resolver family + derive libs are lms-local (D-RESOLVER-LMS); the `derive-records` lib got its first production caller in screen 3. Logging is **decoupled from completion** and the result re-homed to the athlete-owned `BenchmarkResult` (D-BR-OWNED-HISTORY); the result formatters were lifted to `@repo/contracts/lms/_shared` + the composition formatters to `@repo/contracts/lms/composition` (D-RV-4), and screen 3 added the `background.sunken` palette token (D-RV-6).

## Resolved this wave (no longer open)

- **byProfile cell-pick** — RESOLVED (D-PROFILE-SELECTIONS): pick-once-remembered, stored in `AthleteProfile.profileSelections` (free-string-keyed), resolved render-time; re-homes to a catalog profile-type id in the library wave. (Superseded contract-shapes §1.3 "no storage" — doc fixed.)
- **D-PUBLISH performed-vs-published seam** — RESOLVED (D-RESULT-RELATION): a recorded result pins its schema (`onDelete: Restrict`).

## Live carry-forwards (see `deferred.md`)

Publish / version-gate wave (D-SCOPE-PUBLISH — gate #2) · input-sanity bounds mini-pass (future-date / valueKg max / profileSelections cardinality — low severity) · per-exercise actuals (post-MVP) · profile-type catalog (re-homes `profileSelections`; wires `gender` into resolution) · leaderboard · systemic image-URL blob-host hardening (coach + user write paths; D-AV-IMAGE-BLOB-WRITE deferred part) · `gender` → resolver wiring · coach header `session.update`.

## Gotchas a resuming session must know

- **Block 1 was the DATA FLOOR (backend + contracts); block 2 screens 1-4 ship the athlete UI** — the timetable + the fully-working training screen (4 `ResolvedLoad` states + inline set-1RM/pick-profile + benchmark logging) + the standalone Records screen + the Profile screen. The block-1 records/derive libs are SURFACED (screen 3 is `derive-records`'s first production caller). All athlete-UX surfaces are now shipped; block 2 (Athlete UX) is DONE.
- **coach-metrics was MIGRATED** (`startedAt/completedAt → performedAt`): the window Map holds **MAX** `performedAt` per (session,user); `isSessionCompleted` = a row exists; `coach-athletes` last-activity = `_max(performedAt)`. Behavior-preserving + repeat-correct.
- **`db:reset` world** (ADR-0019) — block 1 reshaped the schema destructively (dropped `PerformedExerciseInstance` + the uniques); no migration files. A resuming session re-applies via `db:reset` + `db:seed`. (Screen 4 v2 added NO schema change — `User.image` already existed.)
- **Build decisions ratified** in `decisions.md`: block 1 = D-DIST-UNITS … D-SCOPE-PUBLISH (9); screen 1 = D-TT-\*; screen 2 = D-RESOLVER-LMS … D-SD-ROUTE-PADDED (13) + the re-home D-BR-OWNED-HISTORY / D-BR-COMPLETION-PURE / D-BR-1RM-ATOMIC / D-1RM-LATEST / D-DUAL-RENDER-KEPT (5); screen 3 (Records) = D-RV-1 … D-RV-15 (15, incl. the format lift D-RV-4 + the QA-001/QA-002 fixes D-RV-14/D-RV-15); screen 4 (Profile) = D-PROF-\* (screen-4) + v2 D-AV-\*/D-FIELDS-\* (incl. D-FIELDS-HEIGHT-TWIN superseding the design's height-in-Details, D-FIELDS-GENDER-INERT, D-AV-IMAGE-BLOB-WRITE). Don't re-litigate.
- **The benchmark result was RE-HOMED off the performed-session** (D-BR-OWNED-HISTORY, supersedes D-SD-LOG-ATOMIC's batching, retires D-RESULT-RELATION's `@@unique`): it's now an athlete-owned, append-only `BenchmarkResult` (`userId, plannedSchemaId, result, recordedAt`; no `performedSessionId`, no unique). **A re-log is a NEW row** (append, never overwrite — D-STATS history); `existingResult` = latest by `recordedAt`. **Logging is decoupled from completion** — it does NOT flip the done tick. Completion is the pure tick (`done = performed.length > 0` unchanged; coach-metrics ZERO files changed — D-BR-COMPLETION-PURE). A `load` log writes `OneRMRecord` in the same tx (D-BR-1RM-ATOMIC). `db:reset` re-applies the model change (no migration files).
- **1RM resolution = LATEST, records/PR = best-of** (D-1RM-LATEST — TWO laws, two layers). `load-records.ts` builds `currentOneRMByExercise` from `OneRMRecord` ordered `recordedAt desc` → the **latest** 1RM per exercise drives working-weight `%` resolution (reflects current form — detraining / de-load / peak); `derive-records.ts` (`deriveBestResult`/`isNewPR`) stays **best-of** for records/PR. Don't "fix" the resolution to MAX — it's a deliberate owner ruling. NB: the `gender` field is read NOWHERE in the resolver (only `weightKg` + `profileSelections`) — D-FIELDS-GENDER-INERT.
- **The resolver family + derive libs are `endpoints/lms/athlete-records/`** (moved coaching→lms in screen 2, D-RESOLVER-LMS — `api-server-lms-no-coaching` forbids the lms→coaching edge). The `derive-records` lib (`deriveBestResult`/`isNewPR`/`buildResultSeries`) now HAS its production caller — the screen-3 `records-view` builder (`endpoints/lms/records-view/`, D-RV-1) reads the athlete-owned `BenchmarkResult` history through it (best-of/direction-aware). `derive-records`'s `scoreVector` was extended to normalize `distance` to meters for comparison (D-RV-15, QA-002 — also corrects screen-2's PR badge).
- **The result/composition pure-formatters were LIFTED to `@repo/contracts`** (D-RV-4, advancing D-FMT-HOME): `formatResult` + the new `formatResultParts` → `@repo/contracts/lms/_shared`; `formatRepetitionLabel`/`formatCompositionSummary`/`formatRestSpec` → `@repo/contracts/lms/composition` (NOT `_shared` — that would make a `_shared ⇄ composition` barrel cycle). Screen-2 re-pointed; the coach `plan-detail/lib/format-*` copies were intentionally LEFT (carry-forward to fully close D-FMT-HOME). Reuse the lifted homes, don't re-copy.
- **Screen 3 server-emits what the client would otherwise re-derive** (D-RV-12/13/14): a numeric `scalar` per benchmark series point (the chart plot value, `scoreOf` stays server-side), each 1RM point's `source` (per-row provenance chips), and a server-authoritative `isBest` flag (the client marks the PR row by `isBest`, NOT by `recordedAt` equality — fixes QA-001: same-day midnight-UTC 1RM logs collide on the instant). Don't push these back to the client.
- **Avatar = `User.image` (shared coach/athlete); screen 4 reads it from the profile QUERY + `session.update` for the header** (D-AV-CONTRACT-FLAT / D-AV-READ-FROM-QUERY / D-AV-SESSION-UPDATE). The shared `/upload/image` route is now `withAuthenticated` (any authenticated user — D-AV-AUTH-BROADEN); `ProfileAvatar` lives in `apps/platform/src/lib/components` (D-AV-LIFT). The athlete image WRITE is blob-host-restricted (D-AV-IMAGE-BLOB-WRITE); the SYSTEMIC hardening (coach + user write paths + read schema) is deferred.
