# athlete-core — state (the board)

**Updated:** 2026-06-19. **Block 2 screen 3 (Athlete Records) SHIPPED** on `feat/athlete-records`: the standalone `/athlete/records` screen — a server-derived `records-view` GET + native MUI module + route swap, surfacing the 1RM section (per movement: best + recent-change delta + date-of-best → trend chart + full history with per-row source chips) and the benchmark section (per `plannedSchemaId`: direction-aware best + PR + faithful subline → attempt history + trend). Best-of/direction-aware/per-`plannedSchemaId`; the block-1 `derive-records` lib got its FIRST production caller. The format lift landed (result formatters → `_shared`, composition formatters → `composition`); refinements: server-authoritative `isBest`/`scalar`/per-row `source`, distance normalized to meters for comparison. READ slice — no schema change. Screen 2 (Session/Workout View) shipped + extended 2026-06-18/19 (`feat/athlete-session-view`); screen 1 (Plan Timetable) 2026-06-18 (`feat/athlete-plan-timetable`); block 1 (data core) 2026-06-17 (#283). **NEXT: block 2 screen 4 — the standalone Profile-management screen** (bodyweight + remembered profile picks; the athlete-profile GET/PUT hook already exists from screen 2).

## Board

| #   | Step                                             | Status                              | Pointer                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --- | ------------------------------------------------ | ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0   | Design lock + contract-shape spec                | ✅ done                             | `decisions.md` + `contract-shapes.md` + `block-1-feature-prompt.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 1   | Data core (coarse wave)                          | ✅ **DONE** (feature PR)            | `decisions.md` D-DIST-UNITS…D-SCOPE-PUBLISH · `.feature-dev/1781712323/` (research/design/plan)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2   | Athlete UX (coarse wave)                         | 🔄 **IN PROGRESS** · screens 1-3 ✅ | **Plan Timetable** (`feat/athlete-plan-timetable`, D-TT-\*) + **Session/Workout View** (`feat/athlete-session-view`, D-SD-\* + D-BR-\* + D-1RM-LATEST) + **Athlete Records** (`feat/athlete-records`, D-RV-\*) SHIPPED — the training screen works end-to-end (4 `ResolvedLoad` states + inline set-1RM/pick-profile + **in-schema benchmark logging: athlete-owned append history, decoupled from completion**) and the standalone Records screen surfaces best-of per 1RM + per benchmark (direction-aware, per-`plannedSchemaId`, trend chart + history). 1RM resolution=latest, records=best-of. NEXT: screen 4 — standalone Profile-management |
| 3   | Coach honest-metrics                             | ⬜ pending                          | derived fields + reconcile cron; on the migrated `performedAt` coach-metrics                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| —   | Library wave (profiles + benchmarks + templates) | 🅿️ deferred                         | one catalog pass; profile-catalog re-homes `profileSelections` free-string keys; fusion-form gated                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |

## Next action

**▶ Block 2 screen 4 — the standalone Profile-management screen.** The last remaining athlete-UX surface (own nav, currently "Coming soon" — Profile under `/athlete/profile`). NOT on the critical path for "the training screen works" — screen 2 closed the 1RM / profile resolve-holes INLINE on the session view, so set-1RM and pick-profile already work without a dedicated screen. **Profile** = bodyweight edit + the remembered profile picks; the athlete-profile GET/PUT hook already exists from screen 2 (`AthleteProfile.weightKg` + `profileSelections`). Build on the now-established patterns: server-computes / view presentation-only (D-TT-SERVER-COMPUTES / D-SD-SERVER-RESOLVE), tz-stable dates if any (D-SD-DATES / D-RV-9), no lms→coaching edge (D-RESOLVER-LMS), faithful-to-prototype via theme tokens, `(secondary)` padded `AthleteShell` (D-SD-ROUTE-PADDED) unless it wants full-bleed. Reuse the lifted formatters (`@repo/contracts/lms/_shared` + `…/composition`, D-RV-4) and the module-local `<DisplayNumber>` idiom (D-RV-5) rather than re-copying.

**Remaining waves after screen 4** (unchanged, deferred): block 3 coach honest-metrics (derived fields + reconcile cron on the migrated `performedAt`) · the benchmark/profile/template CATALOG library wave (free-string axes suffice until then) · plan publish / version-gate (D-SCOPE-PUBLISH, gate #2) · cross-athlete leaderboard.

Screens 1-3 are DONE: screen 1 (Plan Timetable) on `feat/athlete-plan-timetable`; screen 2 (Session/Workout View) on `feat/athlete-session-view` — the read endpoint + builder + the session-detail contract + hooks + the UI module (4 load states + 6 result forms + inline set-1RM/pick-profile editors) + the route, then EXTENDED with the in-schema benchmark-logging re-home (D-BR-\* + D-1RM-LATEST); screen 3 (Athlete Records) on `feat/athlete-records` — the `records-view` server-derived GET + builder + the Prisma-free contract triplet + hook + the native MUI module (1RM + benchmark sections, trend chart, history, Update-1RM modal) + the route swap (D-RV-\*). The resolver family + derive libs are lms-local (D-RESOLVER-LMS); the `derive-records` lib got its FIRST production caller in screen 3. Logging is **decoupled from completion** and the result re-homed to the athlete-owned `BenchmarkResult` (D-BR-OWNED-HISTORY); the result formatters were lifted to `@repo/contracts/lms/_shared` + the composition formatters to `@repo/contracts/lms/composition` (D-RV-4), and screen 3 added the `background.sunken` palette token (D-RV-6).

## Resolved this wave (no longer open)

- **byProfile cell-pick** — RESOLVED (D-PROFILE-SELECTIONS): pick-once-remembered, stored in `AthleteProfile.profileSelections` (free-string-keyed), resolved render-time; re-homes to a catalog profile-type id in the library wave. (Superseded contract-shapes §1.3 "no storage" — doc fixed.)
- **D-PUBLISH performed-vs-published seam** — RESOLVED (D-RESULT-RELATION): a recorded result pins its schema (`onDelete: Restrict`).

## Live carry-forwards (see `deferred.md`)

Publish / version-gate wave (D-SCOPE-PUBLISH — gate #2) · input-sanity bounds mini-pass (future-date / valueKg max / profileSelections cardinality — low severity) · per-exercise actuals (post-MVP) · profile-type catalog (re-homes `profileSelections`) · leaderboard.

## Gotchas a resuming session must know

- **Block 1 was the DATA FLOOR (backend + contracts); block 2 screens 1-3 now ship the athlete UI** — the timetable + the fully-working training screen (4 `ResolvedLoad` states + inline set-1RM/pick-profile + benchmark logging) + the standalone Records screen. The block-1 records/derive libs are now SURFACED (screen 3 is `derive-records`'s first production caller); the only remaining athlete-UX surface is screen 4 (Profile).
- **coach-metrics was MIGRATED** (`startedAt/completedAt → performedAt`): the window Map holds **MAX** `performedAt` per (session,user); `isSessionCompleted` = a row exists; `coach-athletes` last-activity = `_max(performedAt)`. Behavior-preserving + repeat-correct.
- **`db:reset` world** (ADR-0019) — block 1 reshaped the schema destructively (dropped `PerformedExerciseInstance` + the uniques); no migration files. A resuming session re-applies via `db:reset` + `db:seed`.
- **Build decisions ratified** in `decisions.md`: block 1 = D-DIST-UNITS … D-SCOPE-PUBLISH (9); screen 1 = D-TT-\* (timetable, incl. the live-walkthrough D-TT-DESKTOP-3PANE / MUILAB-TIMELINE / NAV-MODEL / DOTS-SCALE revisions); screen 2 = D-RESOLVER-LMS … D-SD-ROUTE-PADDED (13, incl. D-SD-LOG-ATOMIC); the screen-2 **re-home** added D-BR-OWNED-HISTORY / D-BR-COMPLETION-PURE / D-BR-1RM-ATOMIC / D-1RM-LATEST / D-DUAL-RENDER-KEPT (5); screen 3 (Records) = D-RV-1 … D-RV-15 (15, incl. the format lift D-RV-4 + the QA-001/QA-002 fixes D-RV-14/D-RV-15). Don't re-litigate; screen 4 builds to them.
- **The benchmark result was RE-HOMED off the performed-session** (D-BR-OWNED-HISTORY, supersedes D-SD-LOG-ATOMIC's batching, retires D-RESULT-RELATION's `@@unique`): it's now an athlete-owned, append-only `BenchmarkResult` (`userId, plannedSchemaId, result, recordedAt`; no `performedSessionId`, no unique). **A re-log is a NEW row** (append, never overwrite — D-STATS history); `existingResult` = latest by `recordedAt`. **Logging is decoupled from completion** — it does NOT flip the done tick. Completion is the pure tick (`done = performed.length > 0` unchanged; coach-metrics ZERO files changed — D-BR-COMPLETION-PURE). A `load` log writes `OneRMRecord` in the same tx (D-BR-1RM-ATOMIC). `db:reset` re-applies the model change (no migration files).
- **1RM resolution = LATEST, records/PR = best-of** (D-1RM-LATEST — TWO laws, two layers). `load-records.ts` builds `currentOneRMByExercise` from `OneRMRecord` ordered `recordedAt desc` → the **latest** 1RM per exercise drives working-weight `%` resolution (reflects current form — detraining / de-load / peak); `derive-records.ts` (`deriveBestResult`/`isNewPR`) stays **best-of** for records/PR. Don't "fix" the resolution to MAX — it's a deliberate owner ruling.
- **The resolver family + derive libs are `endpoints/lms/athlete-records/`** (moved coaching→lms in screen 2, D-RESOLVER-LMS — `api-server-lms-no-coaching` forbids the lms→coaching edge). The `derive-records` lib (`deriveBestResult`/`isNewPR`/`buildResultSeries`) now HAS its production caller — the screen-3 `records-view` builder (`endpoints/lms/records-view/`, D-RV-1) reads the athlete-owned `BenchmarkResult` history through it (best-of/direction-aware). `derive-records`'s `scoreVector` was extended to normalize `distance` to meters for comparison (D-RV-15, QA-002 — also corrects screen-2's PR badge).
- **The result/composition pure-formatters were LIFTED to `@repo/contracts`** (D-RV-4, advancing D-FMT-HOME): `formatResult` + the new `formatResultParts` → `@repo/contracts/lms/_shared`; `formatRepetitionLabel`/`formatCompositionSummary`/`formatRestSpec` → `@repo/contracts/lms/composition` (NOT `_shared` — that would make a `_shared ⇄ composition` barrel cycle). Screen-2 re-pointed; the coach `plan-detail/lib/format-*` copies were intentionally LEFT (carry-forward to fully close D-FMT-HOME). Reuse the lifted homes from screen 4 on, don't re-copy.
- **Screen 3 server-emits what the client would otherwise re-derive** (D-RV-12/13/14): a numeric `scalar` per benchmark series point (the chart plot value, `scoreOf` stays server-side), each 1RM point's `source` (per-row provenance chips), and a server-authoritative `isBest` flag (the client marks the PR row by `isBest`, NOT by `recordedAt` equality — fixes QA-001: same-day midnight-UTC 1RM logs collide on the instant). Don't push these back to the client.
