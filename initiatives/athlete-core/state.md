# athlete-core — state (the board)

**Updated:** 2026-06-19. **Block 2 screen 2 (Session / Workout View) SHIPPED + then EXTENDED** on `feat/athlete-session-view`: the fully-working athlete training screen (read-only plan content + all 4 `ResolvedLoad` states + inline set-1RM/pick-profile) now ALSO ships **in-schema benchmark logging** — an athlete-owned, append-only `BenchmarkResult` history, logged in place per schema, **decoupled from completion** (a re-log is a new attempt, never an overwrite; logging never flips the done tick). A `load` log writes `OneRMRecord` atomically and resolves the % rows below it; the **1RM-latest resolution rule** landed (working-weight `%` resolves off the LATEST 1RM, records/PR stay best-of). Completion is now the pure tick (coach-metrics ZERO files changed). Screen 1 (Plan Timetable) shipped 2026-06-18 (`feat/athlete-plan-timetable`); block 1 (data core) shipped 2026-06-17 (#283). **Screen 4 (Athlete Profile) SHIPPED 2026-06-19** on `feat/athlete-profile` (D-PROF-\*) — bodyweight edit + the remembered profile-picks overview/clear on the existing contract (UI + route-swap, NO server change). **NEXT: block 2 screen 3 — the standalone Records / PR-history screen** (in the parallel `feat/athlete-records` session; reads the shipped `BenchmarkResult` history).

## Board

| #   | Step                                             | Status                                                       | Pointer                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --- | ------------------------------------------------ | ------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0   | Design lock + contract-shape spec                | ✅ done                                                      | `decisions.md` + `contract-shapes.md` + `block-1-feature-prompt.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 1   | Data core (coarse wave)                          | ✅ **DONE** (feature PR)                                     | `decisions.md` D-DIST-UNITS…D-SCOPE-PUBLISH · `.feature-dev/1781712323/` (research/design/plan)                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2   | Athlete UX (coarse wave)                         | 🔄 **IN PROGRESS** · screens 1-2,4 ✅ · screen 3 in parallel | **Plan Timetable** (`feat/athlete-plan-timetable`, D-TT-\*) + **Session/Workout View** (`feat/athlete-session-view`, D-SD-\* + D-BR-\* + D-1RM-LATEST) SHIPPED — the training screen works end-to-end (4 `ResolvedLoad` states + inline set-1RM/pick-profile + **in-schema benchmark logging: athlete-owned append history, decoupled from completion**). 1RM resolution=latest, records=best-of. NEXT: screens 3-4 — standalone Records/PR-history (reads the shipped `BenchmarkResult` history) + Profile-management |
| 3   | Coach honest-metrics                             | ⬜ pending                                                   | derived fields + reconcile cron; on the migrated `performedAt` coach-metrics                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| —   | Library wave (profiles + benchmarks + templates) | 🅿️ deferred                                                  | one catalog pass; profile-catalog re-homes `profileSelections` free-string keys; fusion-form gated                                                                                                                                                                                                                                                                                                                                                                                                                     |

## Next action

**▶ Block 2 screen 3 — the standalone Records / PR-history screen.** Screen 4 (Profile-management) SHIPPED 2026-06-19 on `feat/athlete-profile` (D-PROF-\*) — bodyweight edit + the remembered profile-picks overview/clear, UI + route-swap on the existing contract. Records (screen 3) is in flight in the parallel `feat/athlete-records` session. Both were the two remaining athlete-UX surfaces (own nav, were "Coming soon" — Records under `/athlete/records` with the `leaderboard` icon; Profile under `/athlete/profile`, now live). They are NOT on the critical path for "the training screen works" — screen 2 closed the 1RM / profile resolve-holes INLINE on the session view, so set-1RM and pick-profile already work without dedicated screens. Screens 3-4 are the dedicated homes: **Records/PR** = best-of per 1RM + per benchmark (the records/derive libs are block-1 deliverables waiting for their UI; D-STATS best-of→profile, D-VIEW-RESULT deferred PR computation to here) — **their data producer now exists**: the athlete-owned, append-only `BenchmarkResult` history shipped this wave, readable as a full per-attempt series via `@@index([userId, plannedSchemaId, recordedAt])`, with the `derive-records` seed re-pointed to its `recordedAt` field (NB: D-1RM-LATEST — Records show best-of, while working-weight `%` resolution reads the LATEST 1RM; the two layers diverge by design); **Profile** = bodyweight edit + the remembered profile picks (the athlete-profile GET/PUT hook already exists from screen 2). Build on the now-established patterns: server-computes / view presentation-only (D-TT-SERVER-COMPUTES / D-SD-SERVER-RESOLVE), tz-stable dates (D-TT-DATES-ABSOLUTE / D-SD-DATES), no lms→coaching edge (resolver + derive libs are now lms-local per D-RESOLVER-LMS), faithful-to-prototype via theme tokens, `(secondary)` padded `AthleteShell` (D-SD-ROUTE-PADDED) unless a screen wants full-bleed.

**Remaining waves after 3-4** (unchanged, deferred): block 3 coach honest-metrics (derived fields + reconcile cron on the migrated `performedAt`) · the benchmark/profile/template CATALOG library wave (free-string axes suffice until then) · plan publish / version-gate (D-SCOPE-PUBLISH, gate #2) · cross-athlete leaderboard.

Screens 1-2 are DONE: screen 1 (Plan Timetable) on `feat/athlete-plan-timetable`; screen 2 (Session/Workout View) on `feat/athlete-session-view` — the read endpoint + builder + the session-detail contract + hooks + the UI module (4 load states + 6 result forms + inline set-1RM/pick-profile editors) + the route, then EXTENDED with the in-schema benchmark-logging re-home (D-BR-\* + D-1RM-LATEST). The resolver family + derive libs are now lms-local (D-RESOLVER-LMS). Logging is now **decoupled from completion** and the result re-homed to the athlete-owned `BenchmarkResult` (D-BR-OWNED-HISTORY): a new `POST /api/platform/athlete/sessions/[sessionId]/result` endpoint + `useLogBenchmarkResult` hook; the old standalone `performed-sessions/[id]/result` route + hook were REMOVED (the deferred candidate-cleanup, realized as the revive) — see `deferred.md`.

## Resolved this wave (no longer open)

- **byProfile cell-pick** — RESOLVED (D-PROFILE-SELECTIONS): pick-once-remembered, stored in `AthleteProfile.profileSelections` (free-string-keyed), resolved render-time; re-homes to a catalog profile-type id in the library wave. (Superseded contract-shapes §1.3 "no storage" — doc fixed.)
- **D-PUBLISH performed-vs-published seam** — RESOLVED (D-RESULT-RELATION): a recorded result pins its schema (`onDelete: Restrict`).

## Live carry-forwards (see `deferred.md`)

Publish / version-gate wave (D-SCOPE-PUBLISH — gate #2) · input-sanity bounds mini-pass (future-date / valueKg max / profileSelections cardinality — low severity) · per-exercise actuals (post-MVP) · profile-type catalog (re-homes `profileSelections`) · leaderboard.

## Gotchas a resuming session must know

- **Block 1 was the DATA FLOOR (backend + contracts); block 2 screens 1-2 now ship the athlete UI** — the timetable + the fully-working training screen (4 `ResolvedLoad` states + inline set-1RM/pick-profile + benchmark logging). The remaining no-UI block-1 deliverables are the records/derive libs (their UI is screens 3-4).
- **coach-metrics was MIGRATED** (`startedAt/completedAt → performedAt`): the window Map holds **MAX** `performedAt` per (session,user); `isSessionCompleted` = a row exists; `coach-athletes` last-activity = `_max(performedAt)`. Behavior-preserving + repeat-correct.
- **`db:reset` world** (ADR-0019) — block 1 reshaped the schema destructively (dropped `PerformedExerciseInstance` + the uniques); no migration files. A resuming session re-applies via `db:reset` + `db:seed`.
- **Build decisions ratified** in `decisions.md`: block 1 = D-DIST-UNITS … D-SCOPE-PUBLISH (9); screen 1 = D-TT-\* (timetable, incl. the live-walkthrough D-TT-DESKTOP-3PANE / MUILAB-TIMELINE / NAV-MODEL / DOTS-SCALE revisions); screen 2 = D-RESOLVER-LMS … D-SD-ROUTE-PADDED (13, incl. D-SD-LOG-ATOMIC); the screen-2 **re-home** added D-BR-OWNED-HISTORY / D-BR-COMPLETION-PURE / D-BR-1RM-ATOMIC / D-1RM-LATEST / D-DUAL-RENDER-KEPT (5). Don't re-litigate; screens 3-4 build to them.
- **The benchmark result was RE-HOMED off the performed-session** (D-BR-OWNED-HISTORY, supersedes D-SD-LOG-ATOMIC's batching, retires D-RESULT-RELATION's `@@unique`): it's now an athlete-owned, append-only `BenchmarkResult` (`userId, plannedSchemaId, result, recordedAt`; no `performedSessionId`, no unique). **A re-log is a NEW row** (append, never overwrite — D-STATS history); `existingResult` = latest by `recordedAt`. **Logging is decoupled from completion** — it does NOT flip the done tick. Completion is the pure tick (`done = performed.length > 0` unchanged; coach-metrics ZERO files changed — D-BR-COMPLETION-PURE). A `load` log writes `OneRMRecord` in the same tx (D-BR-1RM-ATOMIC). `db:reset` re-applies the model change (no migration files).
- **1RM resolution = LATEST, records/PR = best-of** (D-1RM-LATEST — TWO laws, two layers). `load-records.ts` builds `currentOneRMByExercise` from `OneRMRecord` ordered `recordedAt desc` → the **latest** 1RM per exercise drives working-weight `%` resolution (reflects current form — detraining / de-load / peak); `derive-records.ts` (`deriveBestResult`/`isNewPR`) stays **best-of** for records/PR. Don't "fix" the resolution to MAX — it's a deliberate owner ruling.
- **The resolver family + derive libs are now `endpoints/lms/athlete-records/`** (moved coaching→lms in screen 2, D-RESOLVER-LMS — `api-server-lms-no-coaching` forbids the lms→coaching edge). The derive/records libs' production caller is screens 3-4 (Records/PR), a ratified scope boundary like `hidePastBeforeBoarding`; their data producer — the athlete-owned `BenchmarkResult` history — now exists.
