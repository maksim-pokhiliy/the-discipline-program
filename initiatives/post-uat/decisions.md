# post-uat — decisions

Status: `RATIFIED` / `OPEN` / `SUPERSEDED`.

- **D-1 — Umbrella structure: one registry, execution follows code ownership.** RATIFIED 2026-07-27. `post-uat` holds the single accountable registry for ALL UAT feedback (PU-01..17). Items whose home initiative already owns the surface execute THERE with a pointer here: PU-08 → `mobile-publish` MP-22 (keeps the projection/publish discipline + MP-numbering in one place). Payments (PU-14) becomes its own initiative once the brief lands — a purchase/billing arc must not run as a side-item of a bug-grind. Rationale: the corpus arrived as one list and needs one place that answers "is everything dispositioned?", while execution context (gotchas, sacred rules, deferred history) lives where the code lives.

- **D-2 — Admin access model = two accounts; HEAD_COACH capability upgrade DROPPED.** RATIFIED 2026-07-27 (by owner action + feedback round). The owner created a separate ADMIN account for Denys: console work runs under ADMIN, the platform account stays HEAD_COACH. The sec-001 strict guard stays untouched; the old Fix B (capability rules letting HEAD_COACH manage non-admins) is dropped. Remaining scope: **Fix A** (soft-delete list-brick — now URGENT, since an ADMIN delete is actually possible), **Fix A′** (hide user-mutation controls from HEAD_COACH sessions — dead 403 buttons must go), **PU-07** email edit under ADMIN. Rationale: the workaround already ships the outcome operationally; capability engineering would duplicate it for marginal benefit. Revisit a single-account model only if two-account juggling starts to hurt.

  - **D-2 addendum — investigation outcome 2026-07-27 (Fix-A executor run).** The list-brick that made Fix A urgent **does not exist**. Soft-delete filtering is central (`db/client.ts` `SOFT_DELETE_MODELS` + the `$extends` block), confirmed at runtime and pinned by a mutation test; details in `triage.md` § PU-06 "Investigation outcome". Consequences: Fix A re-scoped to regression guards only (no production diff), and the standing **"no user deletion in the prod admin" freeze is LIFTED**. Fix A′ is confirmed and ships with one scope correction: the UI hides exactly what 403s — Delete, the role-change interaction, the edit-form Save — while **Create User and Resend invite stay visible and working**, because `createUser` (`users-admin.ts:49`) and `resendInvite` (`:181`) carry no actor guard. That asymmetry is safe by contract, not by accident: `createUserSchema.role = z.enum([UserRole.ATHLETE, UserRole.COACH])` (`user.schema.ts:47`), so a HEAD_COACH cannot mint an ADMIN through create — there is no privilege-escalation path here, and the question does not need re-litigating. The guards themselves remain untouched, as ratified above.

- **D-3 — Contact-notification channels: email now AND resurrect the Telegram bot (supersedes the roadmap "Telegram = v1.1+" cut).** RATIFIED 2026-07-27 (owner: "да"). The ~12-line `/contact` email notify (pre-approved in `pre-launch/marketing-lead-capture-feature-prompt.md:189`) + the verbatim git resurrection of the bot (`38f4a304^`) + prod env verification (`RESEND_API_KEY`/`EMAIL_FROM` are optional-and-silently-skipped — possibly never set → lead emails may have never fired). Template quality spun out as **PU-17** (Claude Design pass) per the same feedback round.

- **D-4 — PU-05 mitigation = advisory + audit, NOT a hard reps↔load validation.** OPEN. **Recommendation:** editor warning on cardio-unit rows with loads (escalated copy for byProfile) + a report-only audit script for coach re-authoring; never disable/auto-clear. Rationale: loads on cal/m rows are legitimate (vest, farmer carry) — a hard block destroys real authoring and is unmigratable; the defect is a missing per-gender VOLUME dimension. Owner context (27.07): the real direction is the profiling redesign (PU-13) — the advisory is the interim so new mis-encoded rows don't accumulate while that design is pending. Awaiting an explicit yes/no.

- **D-5 — PU-04 correction model = append-correction, not history editing.** RATIFIED 2026-07-27 (owner: "да, добавим возможность поправить"; mechanism per recommendation). v1 re-opens the 1RM prompt on resolved percentage rows (additive `exerciseId` on rowView); a correction APPENDS a new record — latest-wins is already the resolution law and the Records history stays honest. A true PATCH/DELETE on 1RM history stays a separate product decision.

- **D-6 — 1RM movement-picker scope (PU-01).** OPEN (born from the owner's design-check 27.07: "там только движения, которые тренер пометил как бенчмарк — или нет?"). Verified answer: **no** — the list is scoped to the athlete's OWN `OneRMRecord` rows (coach influence is indirect: `% of 1RM` prompts and LOAD-benchmark side-effect appends create records); a fresh athlete has a permanently empty picker and can never proactively log a first 1RM from Records. Fork: **(a)** open a read-only movement catalog to athletes (union with own records; coach-only guard intact) — **recommended**: records are athlete-owned by ratified stance (athlete-core), proactive logging is a legitimate flow, and Tetiana's screenshot is exactly this dead end; **(b)** declare record-scope intended → replace the dead autocomplete with an honest empty-state ("1RMs appear here once a workout asks for one"). Awaiting the owner's pick.
