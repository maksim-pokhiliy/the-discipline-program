# post-uat — decisions

Status: `RATIFIED` / `OPEN` / `SUPERSEDED`.

- **D-1 — Umbrella structure: one registry, execution follows code ownership.** RATIFIED 2026-07-27. `post-uat` holds the single accountable registry for ALL UAT feedback (PU-01..16). Items whose home initiative already owns the surface execute THERE with a pointer here: PU-08 → `mobile-publish` MP-22 (keeps the projection/publish discipline + MP-numbering in one place). Payments (PU-14) becomes its own initiative once the brief lands — a purchase/billing arc must not run as a side-item of a bug-grind. Rationale: the corpus arrived as one list and needs one place that answers "is everything dispositioned?", while execution context (gotchas, sacred rules, deferred history) lives where the code lives.

- **D-2 — HEAD_COACH capability in admin (PU-06 Fix B / PU-07 access).** OPEN. The sec-001 blanket `requireAdminStrict` blocks Denys (HEAD_COACH) from ALL user mutations while the UI shows him the controls. **Recommendation:** allow HEAD_COACH to manage non-admin users (update incl. email, delete) with explicit denials preserving the sec-001 intent — target role is ADMIN → 403; requested role ADMIN → 403; self role-change → 403. Restores consistency with create (already HEAD_COACH-allowed) and gives the operator his tool. Alternative (weaker): keep ADMIN-only and hide the controls for HEAD_COACH — honest UI, but every user-management request then routes through Maksim. The 3 pinned test assertions (`users-admin-actor-role.test.ts:79/128/141`) get rewritten deliberately, not deleted.

- **D-3 — Contact-notification channels: email now AND resurrect the Telegram bot (supersedes the roadmap "Telegram = v1.1+" cut).** OPEN. The 2026-02-25 removal (`38f4a304`) was a deliberate MVP scope cut; the owner promised the bot to Denys in the UAT chat (16.07: "можно воскресить этот бот", "имейл, телега, что угодно"). **Recommendation:** ratify both in W4 — the ~12-line `/contact` email notify is already pre-approved in `pre-launch/marketing-lead-capture-feature-prompt.md:189`; the bot is a verbatim git resurrection + 3 mechanical adaptations. Acceptance must include the prod env verification (`RESEND_API_KEY`/`EMAIL_FROM` are optional-and-silently-skipped — possibly never set → lead emails may have never fired).

- **D-4 — PU-05 mitigation = advisory + audit, NOT a hard reps↔load validation.** OPEN. **Recommendation:** editor warning on cardio-unit rows with loads (escalated copy for byProfile) + a report-only audit script for coach re-authoring; never disable/auto-clear. Rationale: loads on cal/m rows are legitimate (vest, farmer carry) — a hard block destroys real authoring and is unmigratable against existing rows; the defect is a missing per-gender VOLUME axis (PU-13), not a validation gap. Projection untouched (published spreads are unit-less and read correctly).

- **D-5 — PU-04 correction model = append-correction, not history editing.** OPEN. **Recommendation:** v1 re-opens the 1RM prompt on resolved percentage rows (additive `exerciseId` on rowView) and a correction APPENDS a new record — latest-wins is already the resolution law and the Records history stays honest. A true PATCH/DELETE on 1RM history is a separate product decision (athlete-owned append-only history is a ratified athlete-core stance); revisit only if athletes demand hard deletion.
