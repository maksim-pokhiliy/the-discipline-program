# plan-editor-compose — plan

Phased. Each code step ships via `/feature` (UI-first where it has UI); the mechanical sweep via an ultracode workflow. Budget ≤1 `/feature` per session → spans sessions.

| #         | Step                                                                                                                                                     | Gate                                                                                                | Status                                                                                                                                                                                                                                                                                                                |
| --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 10.0      | Algebra spec (`algebra-spec.md`)                                                                                                                         | review                                                                                              | ✅ DONE (ratified 2026-06-02)                                                                                                                                                                                                                                                                                         |
| 10.1      | Compose constructor **prototype on mocks** — tree canvas + node-inspector (axes) + reuse `step-09.x` row-editors + **duplication** (week/day/block/node) | coach-walkthrough: expressiveness + ergonomics (charter)                                            | ✅ SHIPPED (PR `feat/compose-prototype-10-1`, 41 files additive); walkthrough PASSED                                                                                                                                                                                                                                  |
| 10.2      | Contracts + schema cut — axes replace `archetypeId`; `kind`/`family` computed-on-read; drop Archetype table/union/picker contracts                       | `db:reset`, not migration (non-prod, ADR-0019)                                                      | ✅ DONE (2026-06-03, `feat/compose-contracts-10-2`) — **Option C**: axis contract + additive `Schema.composition` FROZEN; literal drop/removal deferred to 10.4 (husky/phasing — see journal); db:reset gated                                                                                                         |
| 10.3      | Backend + seed-as-compositions — endpoints/mappers; derived-label; seed fixture = primitive trees, not a catalog                                         | seed = composition trees                                                                            | ✅ DONE (2026-06-03, `feat/compose-backend-10-3`) — backend wired (mapper read + projection/tree-validation obligation #1 + endpoint dual-write), `deriveCompositionLabel` computed-on-read, seed = 5 gauntlet trees; api-server 811/811 green. **QA-001 write-time collision guard carried to 10.4 (hard blocker).** |
| 10.4      | Mechanical sweep — remove ~18 forms + picker + 34-way render switches (`formatSchemaHeader`/`formatArchetypeParams`); render → derived-label             | **ultracode workflow**; starts only after 10.1 walkthrough passed **and** 10.2 schema frozen (OQ-4) | pending                                                                                                                                                                                                                                                                                                               |
| 5 (later) | Scoring/execution layer — make the `scoring` axis live + conditional scoring + parallel-track interleave                                                 | separate initiative                                                                                 | OUT of scope                                                                                                                                                                                                                                                                                                          |

Open design details deferred to their step (see `algebra-spec.md` §9): ladder placement (repetition-axis value vs row rep-scheme), `arrangement:parallel` shape, axis persistence (columns vs Json), derived-label algorithm, super-set row grouping, `trailingConnector` fold.

**10.1 Gate-A resolutions feeding 10.2 (DECISIVE, four-projection — see `journal.md` 2026-06-02 + memory `compose-four-projection`):** `interval` is a CONFIRMED `repetition` primitive (freeze it). `ladder` DECOMPOSES into two primitives — round-counter (container axis) vs rep-scheme (row `RepNotation`); 10.2 MUST split it (the listed "ladder placement" detail above is now a mandated decomposition, not an open choice). Plus: axis-input validation is owed to 10.2 (zod on the contract; the prototype defers it, proven inert).
