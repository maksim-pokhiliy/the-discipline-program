# plan-editor-compose — journal

Append-only.

## 2026-06-02 — pivot decided, initiative stood up

- Reviewed the 34-archetype model end-to-end (schema + `analysis/` + UI + blast radius). Diagnosis: structural backbone is sound (passed a blind "Gauntlet" stress test); the Archetype catalog + picker-first UX are the overkill — and the same problem as the flee-to-Sheets risk.
- Ratified **compose-only** (ADR-0037): archetype emergent, ~8 composable primitives, acceptance = expressiveness not coverage. OQ-1..4 resolved — drop stored discriminators (`kind`/`family` computed-on-read, never denormalized); `AlternatingGroup` → `arrangement:parallel`; `scoring` present-but-inert; mechanical sweep = gated ultracode workflow.
- Algebra spec written + ratified (step 10.0 → `algebra-spec.md`).
- Dropped the two-session planner/executor workflow → this `initiatives/` system (single session + `/feature`/ultracode for code). See `initiatives/README.md`.
- Doc audit (10-agent workflow, 215 files): **zero garbage / duplicates / delete-candidates.** Reorg = supersede markers + ADR + roadmap entry; **no deletes, no moves** (deep coupling: living-mirror protocol, read-only-forever corpus). Dropped the living-mirror protocol (live schema = single source of truth).
- Next: step 10.1 — compose constructor prototype on mocks.

## 2026-06-02 — step 10.1 shipped (compose constructor prototype on mocks)

- Ran via `/feature` (full). Branch `feat/compose-prototype-10-1` off `main`. **41 new files, purely additive — zero existing files modified** (the `*-schema-form.tsx` / `archetype-*` cut set is untouched, to be removed in 10.4). 55 tests; check-types + lint green.
- **What shipped.** `/coach/compose-prototype` route: a recursive tree canvas (nest/add/reorder/**duplicate** node + week/day/session/block, local state, dnd per-level) + ONE parameterized node-inspector (the four orthogonal axes — NOT 18 bespoke forms) + the `step-09.x` leaf row editors reused verbatim via `RowPayloadFormDispatch` + the draft→contract converters (no persistence hooks). Offline own-`QueryClient` shell seeds the exercises so `ExercisePicker` works with zero network. `compose/compose-tree.types.ts` = the keystone draft for the 10.2 contract.
- **Gate A — two DECISIVE four-projection resolutions** (criterion: a primitive is legitimate iff it means the same thing across SET / EXECUTE / RENDER / ANALYTICS; otherwise it's a name collision, the §A.4 `window` class):
  - `interval{workMin,offMin,count}` — **CONFIRMED as a `repetition` primitive** (work:rest duty-cycle is one concept across all four projections; ≠ `cadence` start-gun). 10.2 freezes it. The old "ask the coach, work-window vs timeCap-window" framing is withdrawn.
  - `ladder` — **DECOMPOSES into two primitives**: round-counter ladder (container axis, shared steps; Fran 21-15-9 over two movements) vs rep-scheme ladder (per-row reps notation, likely the existing `RepNotation` VO; Block C 21-15-9 ‖ 9-15-21). Proven by an analytics-projection divergence (one field, two computation paths). **10.2 MUST split it**; the prototype deliberately keeps container-`ladder` (renders fine, no analytics ⇒ collision dormant). Recorded in memory `[[compose-four-projection]]`.
- **Review (B, 0 CRITICAL) + QA (0 CRITICAL).** Fixed two clear bugs inline (`31d4d3b4`): block-root container card showed a no-op delete + dead drag handle (root is select/rename/extend; duplication is block-level); `axes-summary` mislabeled `range_min` rest as seconds.
- **Defer ratified (Gate B).** Axis-input validation (NumberField 0/neg, window HH:MM, rest refine-invalid, count<1) → 10.2's zod on the real contract (single home; piecemeal UI clamps = throwaway + inconsistent). **Safe because Stage-7 safety-probes proved malformed input is stored-but-INERT** — no tree corruption, no render crash. User condition: if a future test shows malformed input actually breaks the tree/UI, that's a blocker, not defer-class.
- **Commits:** `1c4634ea` keystone types · `1412331b` mock seed · `3710f750` tree-ops · `b38295f6` canvas/shell/route/affordances · `966ed48d` inspector + leaf editor · `31d4d3b4` review/QA fixes · `69778cf4` tests · `9e05c7aa` test-timeout stabilization · `ee870364` route-reachability fix.
- **Two post-ship corrections (surfaced opening the route):** (1) the 6 full-view interaction smokes timed out under the FULL test suite's CPU contention (passed isolated) — added an `initialProgram` test-seam (default `MOCK_SEED`) and pointed the seed-agnostic ones at a 1-block fixture (~10× lighter), not a timeout bump. (2) The route was **unreachable** — the platform's edge auth/role gate lives in `apps/platform/src/proxy.ts` (the modern-Next replacement for `middleware.ts`, which the research missed by grepping only `middleware.ts`); it redirects an authenticated user to their `ROLE_HOME` for any path not under it, so `/compose-prototype` bounced coaches to `/coach`. Moved the page to `/coach/compose-prototype` (inherits the coach nav chrome) — no auth/proxy change. See memory `[[platform-proxy-role-gate]]`.
- **Next:** coach-walkthrough gate (expressiveness B/C/D + ergonomics, on the route) → then 10.2 (contracts + schema cut, carrying the obligations in `state.md`).

## 2026-06-03 — step 10.2 frozen (axis contract + additive schema; Option-C expand-then-contract)

- Ran via `/feature` (full). Branch `feat/compose-contracts-10-2` off `main`. **Additive only — Option C.** 16 files (`@repo/contracts` axis contract + `_shared` lift + one additive Prisma column + a `.claudeignore` docs-unignore chore); **1023 contracts tests**; monorepo check-types + lint green (husky 16/16 on every commit). The 10.1 coach-walkthrough PASSED before this step.
- **What froze.** `@repo/contracts/lms/composition` (new subpath): four orthogonal axes — `repetition` (8 variants incl. frozen `interval{workMin,offMin,count}`) · `arrangement` (ordered/parallel/superset) · `scoring` (6, INERT) · `rest` (=`RestSpec`) — + `compositionSchema` flat bundle + a recursive `composeNode`/`composeContainer`/`composeRow` **read-projection** wired to the REAL `_shared`+`schema-row` VOs. zod on EVERY axis (the deferred-from-10.1 validation, now homed; window regex tightened to real-clock — the legacy `/^\d{1,2}:\d{2}$/` accepted `99:99`). `exactOrRangeSchema` **lifted to `_shared`** (keystone-trap fix: the contract never imports the UI `CountOrRangeValue`). Additive nullable **`Schema.composition Json?`** (prisma validate/generate green; **`db:reset` GATED — not run; additive nullable is forward-compatible, so the schema is frozen-and-green without it**).
- **Persistence = flat per-node, NOT the recursive blob (keystone-trap #2).** `Schema.composition` stores ONE node's axes; nesting stays on the sacred `parentSchemaId` graph; the mapper assembles the recursive projection (exactly like `schemaWithBodySchema` today).
- **Ladder split (mandated) RESOLVED + the four-projection invariant now ENFORCED.** Round-counter = container `repetition:ladder`; rep-scheme = the existing **`SchemaRow INNER_LADDER_MARKER`** — NOT a new `RepNotation` variant (keeps the sacred leaf untouched; the planner's "likely RepNotation" hypothesis was overridden in favour of the existing pattern; user-ratified at Gate A). Review+QA found the contract **structurally ADMITTED the collision** (a container with BOTH `repetition:ladder` and an `INNER_LADDER_MARKER` child) — the exact thing the split exists to kill; the four-projection test masked it by only building Fran XOR Block C. **Fixed:** a `composeContainerSchema.superRefine` forbids the fused shape + a negative test. **HANDOFF: the invariant lives on the TREE PROJECTION** (the flat per-node `compositionSchema` can't see children) → **10.3's mapper MUST validate assembled trees through `composeContainerSchema`**, not just per-node `compositionSchema.parse`.
- **AlternatingGroup → `arrangement:parallel` fold TARGET frozen** (no Prisma removal): `{interleaveOrder, tracks:[{childSchemaId, setEnumeration?, pairedWithRowId?}]}` + a field-by-field old→new map (design §9); `direction` = derived-not-stored (OQ-1). Verified the §5 data (`setEnumeration`/`pairedWithInnerRowId`) lives in `Schema.archetypeParams` Json today, not the `AlternatingGroup` model.
- **Gate-B decision: `.strict()` on the whole axis contract** (user-ratified, reversing the design's non-strict choice) — unknown keys REJECT, not silent-strip, so a 10.3 mapper typo on an optional field (`cadence.totalMin`, track `setEnumeration`/`pairedWithRowId`, `scoring.condition`) fails loud; the §5 rename-slip (`pairedWithInnerRowId`) is now a clean strict reject. Two existing strip-asserting tests flipped to reject.
- **Gauntlet traversal pinned at the contract level (charter §7 expressiveness acceptance):** B/D/A/E assembled as composed `composeContainerSchema` structures + all validate (C = the four-projection test). **Block A's `rest: until_recovery` is the one expressiveness gap** — `restSpecSchema.duration` is mandatory, so until-recovery round-trips only via a sham `{value:1,unit:"sec"}` + `qualifier:"until_recovery"` (QA-003, pinned executably).
- **Option-C deviation (LOUD — inherited by 10.3/10.4).** The prompt's literal "drop the `Archetype` table + `Schema.archetypeId` + `archetypeParams`" and "`kind`/`family` — no stored column (grep proves it)" are **NOT done in 10.2.** Rationale (re-grounded on merits after the user flagged husky is a _delivery_ gate, not a scope authority): the destructive cut adds ZERO contract value (the frozen contract is byte-identical either way) while breaking ~40 consumers whose healing IS 10.3 (api-server rewire + seed-as-compositions) + 10.4 (UI sweep); expand-then-contract = manifesto §2.8 + the initiative's own phasing (OQ-4 gates the sweep on "schema frozen"). 10.2 satisfies the RESTATED acceptance: **no NEW stored kind/family column; the axis contract derives them; the legacy `Archetype`/`archetypeId`/`archetypeParams`/`Schema.kind`/`AlternatingGroup`/`alternatingGroupId`/`trailingConnector` are RETAINED and scheduled for 10.4 removal.**
- **Carried for 10.3/10.4:** (1) mapper validates assembled trees via `composeContainerSchema` (ladder invariant); (2) the §6 scoring-inert _consumer-execution_ source-scan now lives ONLY in the 10.1 UI guard (10.2's contract guard is the stronger deep-structural "no function members") — **re-home the source-scan to the api-server consumers in 10.3 before 10.4 deletes the UI guard** (TEST-001); (3) decide `rest: until_recovery` (the `restSpecSchema.duration`-mandatory gap — sham-duration convention vs an optional-duration VO change, QA-003); (4) optional mapper-side recursion-depth bound (QA-002 — `safeParse` throws `RangeError` ~1000 deep; harmless given the flat persisted shape + shallow real trees).
- **Commits:** `4e904ee6` .claudeignore chore · `4bc9b028` axis contract · `9ba79de7` contract tests · `30d7702e` prisma column · `641ebdf9` ladder-invariant + superset-uniqueness · `23a59e69` `.strict()` · `4ce2f634` gauntlet/strict/recursion/until-recovery tests.
- **Next:** step 10.3 — backend + seed-as-compositions (endpoints/mappers, derived `kind`/`family` label computed-on-read, seed fixtures = primitive trees).
