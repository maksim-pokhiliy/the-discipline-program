# plan-editor-compose — journal

Append-only.

## 2026-06-02 — pivot decided, initiative stood up

- Reviewed the 34-archetype model end-to-end (schema + `analysis/` + UI + blast radius). Diagnosis: structural backbone is sound (passed a blind "Gauntlet" stress test); the Archetype catalog + picker-first UX are the overkill — and the same problem as the flee-to-Sheets risk.
- Ratified **compose-only** (ADR-0037): archetype emergent, ~8 composable primitives, acceptance = expressiveness not coverage. OQ-1..4 resolved — drop stored discriminators (`kind`/`family` computed-on-read, never denormalized); `AlternatingGroup` → `arrangement:parallel`; `scoring` present-but-inert; mechanical sweep = gated ultracode workflow.
- Algebra spec written + ratified (step 10.0 → `algebra-spec.md`).
- Dropped the two-session planner/executor workflow → this `initiatives/` system (single session + `/feature`/ultracode for code). See `initiatives/README.md`.
- Doc audit (10-agent workflow, 215 files): **zero garbage / duplicates / delete-candidates.** Reorg = supersede markers + ADR + roadmap entry; **no deletes, no moves** (deep coupling: living-mirror protocol, read-only-forever corpus). Dropped the living-mirror protocol (live schema = single source of truth).
- Next: step 10.1 — compose constructor prototype on mocks.

## 2026-06-02 — step 10.1 shipped (compose constructor prototype on mocks)

- Ran via `/feature` (full). Branch `feat/compose-prototype-10-1` off `main`. **41 new files, purely additive — zero existing files modified** (the `*-schema-form.tsx` / `archetype-*` cut set is untouched, to be removed in 10.4). 55 tests; check-types + lint green.
- **What shipped.** `/coach/compose-prototype` route: a recursive tree canvas (nest/add/reorder/**duplicate** node + week/day/session/block, local state, dnd per-level) + ONE parameterized node-inspector (the four orthogonal axes — NOT 18 bespoke forms) + the `step-09.x` leaf row editors reused verbatim via `RowPayloadFormDispatch` + the draft→contract converters (no persistence hooks). Offline own-`QueryClient` shell seeds the exercises so `ExercisePicker` works with zero network. `compose/compose-tree.types.ts` = the keystone draft for the 10.2 contract.
- **Gate A — two DECISIVE four-projection resolutions** (criterion: a primitive is legitimate iff it means the same thing across SET / EXECUTE / RENDER / ANALYTICS; otherwise it's a name collision, the §A.4 `window` class):
  - `interval{workMin,offMin,count}` — **CONFIRMED as a `repetition` primitive** (work:rest duty-cycle is one concept across all four projections; ≠ `cadence` start-gun). 10.2 freezes it. The old "ask the coach, work-window vs timeCap-window" framing is withdrawn.
  - `ladder` — **DECOMPOSES into two primitives**: round-counter ladder (container axis, shared steps; Fran 21-15-9 over two movements) vs rep-scheme ladder (per-row reps notation, likely the existing `RepNotation` VO; Block C 21-15-9 ‖ 9-15-21). Proven by an analytics-projection divergence (one field, two computation paths). **10.2 MUST split it**; the prototype deliberately keeps container-`ladder` (renders fine, no analytics ⇒ collision dormant). Recorded in memory `[[compose-four-projection]]`.
- **Review (B, 0 CRITICAL) + QA (0 CRITICAL).** Fixed two clear bugs inline (`31d4d3b4`): block-root container card showed a no-op delete + dead drag handle (root is select/rename/extend; duplication is block-level); `axes-summary` mislabeled `range_min` rest as seconds.
- **Defer ratified (Gate B).** Axis-input validation (NumberField 0/neg, window HH:MM, rest refine-invalid, count<1) → 10.2's zod on the real contract (single home; piecemeal UI clamps = throwaway + inconsistent). **Safe because Stage-7 safety-probes proved malformed input is stored-but-INERT** — no tree corruption, no render crash. User condition: if a future test shows malformed input actually breaks the tree/UI, that's a blocker, not defer-class.
- **Commits:** `1c4634ea` keystone types · `1412331b` mock seed · `3710f750` tree-ops · `b38295f6` canvas/shell/route/affordances · `966ed48d` inspector + leaf editor · `31d4d3b4` review/QA fixes · `69778cf4` tests · `9e05c7aa` test-timeout stabilization · `ee870364` route-reachability fix.
- **Two post-ship corrections (surfaced opening the route):** (1) the 6 full-view interaction smokes timed out under the FULL test suite's CPU contention (passed isolated) — added an `initialProgram` test-seam (default `MOCK_SEED`) and pointed the seed-agnostic ones at a 1-block fixture (~10× lighter), not a timeout bump. (2) The route was **unreachable** — the platform's edge auth/role gate lives in `apps/platform/src/proxy.ts` (the modern-Next replacement for `middleware.ts`, which the research missed by grepping only `middleware.ts`); it redirects an authenticated user to their `ROLE_HOME` for any path not under it, so `/compose-prototype` bounced coaches to `/coach`. Moved the page to `/coach/compose-prototype` (inherits the coach nav chrome) — no auth/proxy change. See memory `[[platform-proxy-role-gate]]`.
- **Walkthrough PASSED (2026-06-02, owner-tested on `/coach/compose-prototype`).** Expressiveness (Gauntlet B/C/D assemble by free nesting) + ergonomics (duplication) both held. The smoke test (expectation-vs-reality, block by block) surfaced a third defect beyond the route: **row cards rendered movement names without numbers** — reps/load were intact in the Row (visible in the inspector) but the card summary dropped them, so every block read as contentless (the four-projection READ leak; a render defect in `row-summary`, NOT the model/seed). Fixed in PR #235: `row-summary` renders reps + load primary (compound `5 Pull-up + 10 Dip`; atomic `name · load · reps`; `implicit` reps hidden so container-driven schemes aren't duplicated), and the seed labels structural containers (`Min 1-4`, parallel tracks) + gives conditioning rows real loads. The walkthrough-on-mocks earned its keep: caught an unreachable route and unreadable cards before any backend.

## 2026-06-03 — step 10.2 frozen (axis contract + additive schema; Option-C expand-then-contract)

- Ran via `/feature` (full). Branch `feat/compose-contracts-10-2` off `main`. **Additive only — Option C.** 16 files (`@repo/contracts` axis contract + `_shared` lift + one additive Prisma column + a `.claudeignore` docs-unignore chore); **1023 contracts tests**; monorepo check-types + lint green (husky 16/16 on every commit). The 10.1 coach-walkthrough PASSED before this step.
- **What froze.** `@repo/contracts/lms/composition` (new subpath): four orthogonal axes — `repetition` (8 variants incl. frozen `interval{workMin,offMin,count}`) · `arrangement` (ordered/parallel/superset) · `scoring` (6, INERT) · `rest` (=`RestSpec`) — + `compositionSchema` flat bundle + a recursive `composeNode`/`composeContainer`/`composeRow` **read-projection** wired to the REAL `_shared`+`schema-row` VOs. zod on EVERY axis (the deferred-from-10.1 validation, now homed; window regex tightened to real-clock — the legacy `/^\d{1,2}:\d{2}$/` accepted `99:99`). `exactOrRangeSchema` **lifted to `_shared`** (keystone-trap fix: the contract never imports the UI `CountOrRangeValue`). Additive nullable **`Schema.composition Json?`** (prisma validate/generate green; **`db:reset` GATED — not run; additive nullable is forward-compatible, so the schema is frozen-and-green without it**).
- **Persistence = flat per-node, NOT the recursive blob (keystone-trap #2).** `Schema.composition` stores ONE node's axes; nesting stays on the sacred `parentSchemaId` graph; the mapper assembles the recursive projection (exactly like `schemaWithBodySchema` today).
- **Ladder split (mandated) RESOLVED + the four-projection invariant now ENFORCED.** Round-counter = container `repetition:ladder`; rep-scheme = the existing **`SchemaRow INNER_LADDER_MARKER`** — NOT a new `RepNotation` variant (keeps the sacred leaf untouched; the planner's "likely RepNotation" hypothesis was overridden in favour of the existing pattern; user-ratified at Gate A). Review+QA found the contract **structurally ADMITTED the collision** (a container with BOTH `repetition:ladder` and an `INNER_LADDER_MARKER` child) — the exact thing the split exists to kill; the four-projection test masked it by only building Fran XOR Block C. **Fixed:** a `composeContainerSchema.superRefine` forbids the fused shape + a negative test. **HANDOFF: the invariant lives on the TREE PROJECTION** (the flat per-node `compositionSchema` can't see children) → **10.3's mapper MUST validate assembled trees through `composeContainerSchema`**, not just per-node `compositionSchema.parse`.
- **AlternatingGroup → `arrangement:parallel` fold TARGET frozen** (no Prisma removal): `{interleaveOrder, tracks:[{childSchemaId, setEnumeration?, pairedWithRowId?}]}` + a field-by-field old→new map (design §9); `direction` = derived-not-stored (OQ-1). Verified the §5 data (`setEnumeration`/`pairedWithInnerRowId`) lives in `Schema.archetypeParams` Json today, not the `AlternatingGroup` model.
- **Gate-B decision: `.strict()` on the whole axis contract** (user-ratified, reversing the design's non-strict choice) — unknown keys REJECT, not silent-strip, so a 10.3 mapper typo on an optional field (`cadence.totalMin`, track `setEnumeration`/`pairedWithRowId`, `scoring.condition`) fails loud; the §5 rename-slip (`pairedWithInnerRowId`) is now a clean strict reject. Two existing strip-asserting tests flipped to reject.
- **Gauntlet traversal pinned at the contract level (charter §7 expressiveness acceptance):** B/D/A/E assembled as composed `composeContainerSchema` structures + all validate (C = the four-projection test). **Block A's `rest: until_recovery` is the one expressiveness gap** — `restSpecSchema.duration` is mandatory, so until-recovery round-trips only via a sham `{value:1,unit:"sec"}` + `qualifier:"until_recovery"` (QA-003, pinned executably).
- **Option-C deviation (LOUD — inherited by 10.3/10.4).** The prompt's literal "drop the `Archetype` table + `Schema.archetypeId` + `archetypeParams`" and "`kind`/`family` — no stored column (grep proves it)" are **NOT done in 10.2.** Rationale (re-grounded on merits after the user flagged husky is a _delivery_ gate, not a scope authority): the destructive cut adds ZERO contract value (the frozen contract is byte-identical either way) while breaking ~40 consumers whose healing IS 10.3 (api-server rewire + seed-as-compositions) + 10.4 (UI sweep); expand-then-contract = manifesto §2.8 + the initiative's own phasing (OQ-4 gates the sweep on "schema frozen"). 10.2 satisfies the RESTATED acceptance: **no NEW stored kind/family column; the axis contract derives them; the legacy `Archetype`/`archetypeId`/`archetypeParams`/`Schema.kind`/`AlternatingGroup`/`alternatingGroupId`/`trailingConnector` are RETAINED and scheduled for 10.4 removal.**
- **Carried for 10.3/10.4:** (1) mapper validates assembled trees via `composeContainerSchema` (ladder invariant); (2) the §6 scoring-inert _consumer-execution_ source-scan now lives ONLY in the 10.1 UI guard (10.2's contract guard is the stronger deep-structural "no function members") — **re-home the source-scan to the api-server consumers in 10.3 before 10.4 deletes the UI guard** (TEST-001); (3) decide `rest: until_recovery` (the `restSpecSchema.duration`-mandatory gap — sham-duration convention vs an optional-duration VO change, QA-003); (4) optional mapper-side recursion-depth bound (QA-002 — `safeParse` throws `RangeError` ~1000 deep; harmless given the flat persisted shape + shallow real trees).
- **Commits:** `4e904ee6` .claudeignore chore · `4bc9b028` axis contract · `9ba79de7` contract tests · `30d7702e` prisma column · `641ebdf9` ladder-invariant + superset-uniqueness · `23a59e69` `.strict()` · `4ce2f634` gauntlet/strict/recursion/until-recovery tests.
- **Next:** step 10.3 — backend + seed-as-compositions (endpoints/mappers, derived `kind`/`family` label computed-on-read, seed fixtures = primitive trees).

## 2026-06-03 — step 10.3 done (backend wiring + seed-as-compositions; Option-C dual-write live)

- Ran via `/feature` (full). Branch `feat/compose-backend-10-3` **stacked on `feat/compose-contracts-10-2`** (10.2 not merged — 10.3 needs its frozen contract + the `Schema.composition` column). 8 commits, 33 files; monorepo check-types 16/16 + lint green on every commit; `dep:check` clean (1715 modules); **first `db:reset`+`db:seed` since 10.2 added the column** → full api-server suite **811/811 green** (85 files, 612s serial) + contracts **1267** green.
- **What shipped (the four deliverables).** (1) **Derived label** — `deriveCompositionLabel(composition): { kind, family }` pure fn in `@repo/contracts/lms/composition/composition-label.ts`, switch over the axis discriminants per algebra-spec §2.5 (priority arrangement→ladder→cadence/interval/window→timeCap→count/range→flat; scoring NEVER alters the label; compose-native 9-kind/7-family enum, NOT the 34 archetype names — re-imposing them = masked-archetype regression §2.4). **Computed-on-read, NO Prisma column** (a `composition-label-not-stored.test.ts` greps `model Schema` to prove it — OQ-1). (2) **Read wiring** — `mapToSchema` parses `Schema.composition` (null-guarded, mirrors `trailingConnector`) + computes `label`; the **tree-validation obligation #1** lives in a new `compose-projection.mapper.ts` — projects the live `SchemaWithBody` → the frozen `.strict()` `ComposeNode` shape (drops the 7 legacy SchemaRow + 11 legacy Schema fields, lifts `composition ?? {}`), runs `composeContainerSchema.safeParse`, and throws **`InternalServerError`(500, DbCorruption)** on the ladder collision (NOT a bare ZodError→400 — the AppError surfaces correctly through the un-try/catch'd week read); hooked per top-level subtree in `block.mapper`. (3) **Write** — create/update dual-write `composition` (`toInputJson`/`Prisma.JsonNull`, mirrors `trailingConnector`); composition is OPTIONAL on `createSchemaSchema`, archetype stays REQUIRED (Option-C FK reality). (4) **Seed-as-compositions** — 5 gauntlet axis kinds (count+until_recovery-sham, ladder, cadence, interval, timeCap+amrap) attached ADDITIVELY to existing archetype nodes in `week-1-{monday,tuesday,wednesday}.ts` (kept the 34-archetype catalog + coverage cells green); new `composition.present` coverage cell (`{ not: Prisma.AnyNull }`, required 5). Plus the **§6 scoring-inert source-scan re-homed** from the (10.4-doomed) UI guard to `scoring-inert-consumers.test.ts` scanning `src/mappers/lms/` + `src/endpoints/lms/schema/`.
- **Gate A — 5 decisions ratified** (all per recommendation): DEC-1 dual-write (composition optional / archetype required); DEC-2 pure label fn in contracts, threaded into the read response as a nullable read-only `Schema.label`, compose-native vocabulary; DEC-3 Gauntlet-port seed (additive, NOT full per-node derivation); DEC-4 keep the `until_recovery` sham `{value:1,unit:"sec"}`; DEC-5 new api-server source-scan guard.
- **Gate B — Review A (0 CRIT/0 WARN/4 INFO) + QA C.** User ratified **defer QA-001 → 10.4**. QA-005 (seed interval count 3→6) fixed inline (`30da9577`).
- **Deviation (LOUD — for the 10.4 planner).** The plan estimated "~19-21 files; `mapToSchema` is the sole `Schema` producer." Reality: the **required-nullable** `composition`+`label` on the read `Schema` broke **13 OTHER `Schema`-literal producers** — all TEST fixtures (2 contracts + 11 platform plan-detail), fixed mechanically with `composition: null, label: null` (they already carried `intensity: null`/`trailingConnector: null` — same nullable-VO convention). Kept `.nullable()` (matches the codebase's intensity/trailingConnector convention + the manifesto two-state encoding); did NOT weaken to `.optional()` to dodge the churn (that = letting husky/cone-pain dictate the contract design). Total ~34 files. Not a scope/design change — the additive field rippled to literal-producers exactly as a contract field does.
- **Carried to 10.4 (HARD BLOCKER + flagged WARNINGs).** **QA-001 (CRITICAL, deferred):** the ladder-collision invariant is enforced ONLY at read; the sibling `schema-row/admin.ts` create (outside 10.3 scope) is unguarded, so a coach can POST an `INNER_LADDER_MARKER` row to a `repetition:ladder` ATOMIC container (the seed ships one: `BLOCK_LADDER_DESC_WK1_MON`) → the next `getByPlanAndDate` 500s the WHOLE week (no per-block try/catch). Reachable via DIRECT API only (no composition-write UI in 10.3). **10.4 MUST add write-time subtree re-validation on schema-row create/update + schema update (re-fetch parent container + children, run `assertComposeTreeValid`, reject 400) BEFORE the compose-write UI ships** — and flip the pinned read-500 test (`week/admin.test.ts`) to assert a write-time 400. Flagged WARNINGs (no 10.3 action, recorded): QA-002 (read assembly has no per-block isolation — one bad node 500s the week), QA-003 (corrupt-column → misleading 400 not 500 on the week read; the day path already wraps via `handlePrismaError` — mirror it), QA-004 (superset/parallel axis refs validated for format/distinctness but NOT existence — inert until the arrangement axis is consumed), the `until_recovery` sham (`value:1` not pinned to `qualifier` — a `restSpecSchema.superRefine` would close it), QA-006 (the `required:5` coverage cell is brittle — add per-structure assertions). The §6 source-scan now also lives on the api-server; 10.4 deletes the UI `compose/` guard freely.
- **Commits:** `95232358` label fn · `c93b6a21` contract+mapper threading (+13 fixture sweep) · `bb115634` projection + tree-validation + no-DB test · `1cbf1175` endpoint dual-write · `be595c51` seed-as-compositions · `623876c5` scoring-guard re-home + no-stored-label guard · `30da9577` seed interval-count fix · `e698354e` round-trip/collision/seed-read-through tests.
- **Next:** step 10.4 — the mechanical sweep (remove `Archetype`/`archetypeId`/`archetypeParams`/`Schema.kind`/`AlternatingGroup`/`trailingConnector`, the 18 bespoke forms + picker + the 34-way `formatSchemaHeader`/`formatArchetypeParams` switches, render → the derived label) **AND** the QA-001 write-time guard. Ultracode workflow; starts now that 10.1 walkthrough passed + the 10.2 schema is frozen (OQ-4) + 10.3 has wired the backend.

## 2026-06-03 — 10.4 recon + initiative-process overhaul (NO product code)

- **10.4 RECON+PLANNING pass.** 5 parallel read-only agents mapped the blast radius across 3 apps + packages + seed + the write-guard surface; load-bearing claims verified directly. Promoted durably to `10-4-recon.md` (removal surface file:line, the 3 non-mechanical mines, the write-guard design, the seed redesign, the green-keeping sequence). Three mines verified: (1) `Schema.kind` is consumed by non-archetype write-guards (`assertParentKindForRow`/`assertSubSchemaInvariants` + ownership guards) — dropping it is a behavior change, not mechanical; (2) the production compose-write UI does not exist — the 10.1 prototype is walkthrough-validated UX but does NOT persist (zero mutation wiring, local types ≠ frozen contract, no converter) → removing archetype removes the only authoring path; (3) the AlternatingGroup fold + the `stagedProgram`/`slotSpec` coverage cells have no 1:1 composition home. **Conclusion:** 10.4 is a 3-phase arc (S1 guard+expand → S2 productionize-prototype → S3 mechanical sweep), not a pure sweep. 3 forks surfaced + recorded OPEN in `decisions.md`: D-10.4-1 (arc), D-10.4-2 (drop `kind`), D-10.4-3 (S2 scope). **No code written — planning only.**
- **Initiative-process overhaul (the session's main deliverable).** Diagnosed why the system was used badly: the new `initiatives/` system collapsed the old `implementation/` 6-file board (queue/decisions/deferred/next-action/rules) into one narrative `state.md`, losing the structured tracking; AND the richest reasoning lived in gitignored `.feature-dev/` + external chats, never promoted to durable docs; AND nothing enforced the close-out. Evidence: this session the user had to hand-feed context 3× (docs → web-Claude export → prototype correction). **Fix shipped:** restored a lean board (`decisions.md` + `deferred.md` + `state.md`-as-board), wrote the promotion discipline + close-out protocol into `initiatives/README.md`, added a SessionStart hook that force-loads the active initiative board, added `/initiative-close` + `/initiative-resume` skills, added the process rule to project `CLAUDE.md`, migrated the 9 planner-discipline lessons to `docs/planner-discipline.md` + the live AlternatingGroup facts to `decisions.md` D-AG-FACTS, and banner-quarantined `implementation/` as superseded history. The `_template/` gained the two new board files.
- **Retroactive backfill.** 10.0–10.3 brought into the new system: their ratified decisions promoted to `decisions.md` (D-PIVOT/INTERVAL/LADDER/CADENCE/EMOM-SLOT/PERSIST/STRICT/DUALWRITE/LABEL/SEED/UNTILREC/SCORING-INERT), their carry-forwards to `deferred.md`, all phases on the `state.md` board. The orphaned web-Claude UX rules (EMOM row-as-minute, container-vs-row invariant) finally got a durable home (D-EMOM-UX, D-CONTAINER-VS-ROW) — they were the smoking-gun gap.
- **Next:** ratify D-10.4-1/2/3 → launch S1 (`/feature small`: QA-001 write-guard + QA-003 fold + nullable-archetype expand). See `state.md` Next action.

## 2026-06-03 — session close-out (process overhaul merged; durability spot-check passed)

- **Merged:** PR #239 (process overhaul) landed on `main` (`21fac0eb`). The new system is live: SessionStart hook force-loads the board, `/initiative-resume` + `/initiative-close` skills, `decisions.md` + `deferred.md` boards.
- **Durability spot-check (user-requested, passed).** Verified the Gauntlet (the canonical acceptance fixture + its primitive decomposition) is durable in THREE places independent of the doomed prototype: `algebra-spec.md §3` (full decomposition, axis vocabulary), `composition-gauntlet.test.ts` (executable B/D/A/E — survives the sweep), `ADR-0037`. The prototype `buildGauntletWeek()` mock-seed is the weakest home (dies in S3) but the others hold. The original "8 primitives" naming evolved into the ratified 4-axis algebra (lineage, not loss).
- **Promoted:** `D-PHASE5-SCORING` — the scoring/execution layer as the product differentiator (the chat's strongest strategic insight) was thin in the docs; now captured durably (rationale + content + design-seed) so the future phase-5 initiative starts with the full picture, not a one-line pointer.
- **State:** 10.4 stays PLANNING, blocked on D-10.4-1/2/3. Recon is DONE + durable (`10-4-recon.md`) — the next session does NOT re-recon; it ratifies the 3 forks then fires S1.
- **Next session:** resume → ratify D-10.4-1/2/3 (recs in `decisions.md`; only D-10.4-1 gates S1) → `/feature` S1 (QA-001 write-guard + QA-003 fold + nullable-archetype expand). A start-prompt was handed to the user.

## 2026-06-04 — 10.4 S1 shipped (write-guard + QA-003 fold + nullable-archetype expand)

- **Ratified** (session start): D-10.4-1 (the arc S1→S2→S3) + D-10.4-2 (drop-`kind` direction — S1 makes it nullable, S3 drops). D-10.4-3 (S2 scope) left OPEN with the leaning recorded (full four-projection; parallel/superset last) → ratify at S2 kickoff.
- **Ran via `/feature` (full).** Research right-sized small→**full** (the nullable-expand is a cross-package contract swing, not additive). Branch `feat/compose-s1-write-guard-nullable-expand` off `main`. **4 commits, husky cone green per commit (turbo check-types 16/16); api-server 813/813 + contracts 1269 green after `db:reset`+`db:seed`.** PR **#241**.
  - `8a3a6b84` C1 — `assertComposeTreeValidForWrite` (400) + 3 write hooks (schema-row create in-tx-rollback / update defensive / schema update-of-composition), reusing a shared pure `buildSchemaWithBody` (DRY-extracted from `block.mapper`).
  - `762838f0` C2 — QA-003: week read wrapped in `handlePrismaError` (corrupt composition → 500 DbCorruption, not a misleading 400); 3 pinned week tests flipped/re-pointed; dead `attachLadderCollision` removed.
  - `ade5dbb8` C3 — nullable-expand (one squashed atomic): Prisma `kind/archetypeId/archetypeParams`+relation nullable; `createSchemaSchema` triad optional + all-present-or-all-absent `superRefine` (base-object pattern); `mapToSchema` narrow-guard (option b); ownership guards `| null`; the two kind-assertions null-tolerant; `assertArchetypeConsistency` caller-gated; R9 + alt-group archetype-null guards.
  - `683581ab` test — Hook-3 composition-update→ladder→400 integration test (the only reachable hole, was uncovered).
- **Gate-A — D-10.4-S1-RS:** the read-shape fork → **option (b)** (keep `schemaSchema` non-null; narrow in `mapToSchema`; defer the read-shape widen + ~6 platform null-guards to S2). Research/design surfaced it as the #1 fork; on the adversarial pass I corrected the plan's `updateSchemaSchema = createSchemaSchema.partial()` (breaks once `superRefine` makes it a `ZodEffects`) → base-object pattern.
- **Gate-B — 1 CRITICAL, owner-overrode.** Review + QA (independent, offline-proven) both flagged: the optional triad makes composition-only creates reachable-but-poison (persist → read 500s → week bricks; the write-guard also inverts to 500 on a composition-only parent). I recommended a ~10-line handler gate, held the line once (week-bricking poison + guard-inversion ≠ UI lag). **Owner override:** migration errors are normal mid-process, code is LOCAL, won't merge broken, "это ПРОЦЕСС, не результат" — do NOT gate. Recorded as **D-10.4-S1-INT** (accepted poison-intermediate); S2 closes it (DR-1). Deferred WARNINGs: QA-103 (Hook-3 TOCTOU), REVIEW-004 (`marshalNullableJson` dup → S3).
- **Carry-forwards:** QA-001 + QA-003 → CLOSED. New **DR-1** (HARD → S2: widen read-shape + null-guard ~6 platform consumers + enable the composition-only handler, before composition-only writes), QA-103, REVIEW-004.
- **Process note:** corrected a stale memory — husky **pre-push runs only the cone** (dep:check + lint + check-types), NOT the api-server suite; the ~10-min `vitest run` is a separate GATED manual run.
- **Next:** S2 — productionize the 10.1 compose-write prototype (`/feature` full, UI-first), discharging DR-1. Ratify D-10.4-3 at kickoff. Coach re-walkthrough gate.

## 2026-06-04 — 10.4 S2 Run-1 shipped (productionized the compose-write prototype; DR-1 CLOSED)

- **Ratified at kickoff:** D-10.4-3 (S2 scope) — full four-projection-faithful; parallel/superset + QA-004 sequenced LAST (→ Run-2); the split decision delegated to the `/feature` research stage.
- **Research reframed DR-1 (the win):** S1 already shipped the WRITE half — the Prisma triad is already nullable + the handler writes `?? null`, so a composition-only `Schema` already PERSISTS; the only blocker was the READ (`mapToSchema` 500-guard + the non-null contract). So DR-1 = drop the guard + widen 3 contract fields + null-guard the consumers — no Prisma migration, handler-enable confirm-only. Verdict **SPLIT**: Run-1 (DR-1 + converter + persistence + mount + simple-axes UX) this session; Run-2 (parallel/superset + QA-004) next.
- **Owner directive (mid-run):** aggressive in-place migration — no preventive guards, no hiding, broken OLD/peripheral UI acceptable, "важен результат, не процесс." Floor = compile+lint green per commit (husky stays, no bypass). Drove dropping Task 8 (hide parallel/superset) + cheapest-compiling null-guards on the old archetype read sites. Recorded as D-10.4-S2.
- **Ran via `/feature` full.** Branch `feat/compose-s2-run1` off `main`. **5 code/test commits + 1 chore + this docs commit; the owner's full root suite was green (3656/3658 — the 2 fails = a PRE-EXISTING load-flake in `row-editor-modal.test.tsx`, pass 25/25 isolated, not in this diff → FLAKE-001).** PR **#242**.
  - `f01995db` DR-1 — widen the read triad nullable + drop the `mapToSchema` narrow-guard + fix `schemaSchemaWithInvariants` + null-guard consumers. **Recon under-counted: 6 named + 17 `*-schema-form` extractors = 24 files** (widening `archetypeParams` broke every per-archetype param extractor); full-graph check-types 16/16.
  - `cef57ca2` compose-core — pure converter (`composeRootToCreatePlan` → `CreateSchemaPlanNode` plan-tree; assemble axes → `compositionSchema.safeParse` as the sole judge, no arrangement special-casing) + persistence cascade (sequential parent-first, stop-on-failure) + `deriveMinuteView` + `shouldBeContainer`. 38 tests.
  - `d955d2c8` mount + render — `ComposeEditorDrawer` + `AddComposeBlockButton` at the block-card seam (real catalog via prop; `/coach` authed, no route change) + composition-only read render (axes summary + EMOM MIN n) + container↔row inspector hint.
  - `c9f929a4` fix — composition-only card header derives from the axes summary (Review WARNING-2: was blank).
  - `45165ded` test — gated api-server composition-only round-trip (`week/admin`, PASSED in the owner's full-suite run) + null-triad read parse + header derive + converter bounds.
- **Gate A:** approved. Corrected the persistence-location drift (brief said `@repo/query`; the real hooks are app-local on `useWeekMutation`); resolved cascade partial-failure as **DR-2** (sequential, leave-partial, no bulk endpoint → **DR-3** deferred); resolved the FK crux (composition-only writes `archetypeId:null`, inert `Restrict`).
- **Gate B:** Review (B+, 0 CRITICAL) ∥ QA (B, 0 CRITICAL); both verified the round-trip + the closed poison + the backwards-compat grep (24 consumers; admin/marketing zero). Only WARNING-2 (blank header) fixed inline; the rest carried (QA-007 double-submit, QA-008 NaN, REVIEW-005 clone).
- **Acceptance:** owner coach re-walkthrough on the dev server — **PASSED** ("технически всё работает, полировка UX потом"). DR-1 poison CLOSED.
- **Side-quests:** dropped the redundant pre-commit `turbo check-types` (kept on pre-push) — `chore(husky)` `7acd2132`; commits are fast now. Discussed domain-level speedups (worktree-parallel waves, S3-as-`Workflow`, property-based round-trip test, faster test-DB harness) — the property test slotted into Run-2.
- **Next:** S2 Run-2 — parallel/superset authoring + two-phase ref persist + QA-004 + the property-based round-trip test; worktree-parallel waves; consider collapsing Run-2 + S3 into one session. See `state.md` Next action.

## 2026-06-04 — 10.4 S2 Run-2 shipped (parallel/superset authoring + two-phase ref persist + QA-004 + property test)

- **Ran via `/feature` (full).** Branch `feat/compose-s2-run2` off `main`. **5 commits; check-types (platform/api-server/contracts) + lint + `dep:check` green; ungated platform suite 143/143** (compose module 16 files, was 12/105). PR **#244**. The gated api-server QA-004 cases were authored but UNRUN (owner skipped the gated run — run before merge). Discharges D-10.4-3's parallel/superset obligation → **S3 unblocked.**
- **What shipped (the four deliverables).** (1) **Arrangement authoring UI** — draft `ArrangementAxis` → discriminated union (parallel payload, refs as `NodeId`); a ref-wiring inspector (selection-over-existing): `ParallelArrangementFields` (interleaveOrder + per-child track toggles + `setEnumeration` via `StepArrayFields` + `pairedWithRowId` select) + `SupersetArrangementFields` (pair builder over DIRECT rows). (2) **Two-phase ref persist** — the converter strips arrangement from phase-1 (Omit-guarantee), carries it on a `deferredArrangement` side channel with draft refs + a pre-network cardinality/ref-resolvability `ConvertIssue` check; the cascade collects `draftNodeId→cuid` (containers + rows) then phase-2 `useUpdateSchema`s the WHOLE composition with resolved cuids (Prisma wholesale-replace). `cloneNode` remaps arrangement refs (duplicate-and-tweak stays working). (3) **QA-004** `assertArrangementRefsInScope` — existence/scope-only, zero extra queries (shape/cardinality already enforced by the QA-001 guard). (4) **Property round-trip test** (fast-check, 300 runs) + cascade phase-2 + clone-remap + convert/resolve units.
- **Gate A — D-10.4-S2-R2 ratified.** Adopted three orchestrator positions: selection-over-existing UX; `deferredArrangement` side-channel + phase-2 whole-composition; BOTH remap-on-clone AND reject-on-convert. The design SHARPENED QA-004 to existence/scope-only (the brief implied a cardinality check too, but `assertComposeTreeValidForWrite → composeContainerSchema.safeParse` already 400s on shape/cardinality/format — verified). `fast-check` added to the catalog (`^4.8.0`, confirm-gated, pre-authorized at Gate A).
- **Gate B — Review B (→A) ∥ QA B− (→ test gap); both 0 CRITICAL.** Correctness core verified by both (Omit-guarantee, resolver fail-loud, QA-004 layering + zero-query + depth, phase-2 whole-composition, clone remap, frozen-contract diff = 0). Fix-loop closed 4 normal-authoring/latent findings: **QA-201** (superset offered grandchild rows client-side but server checks DIRECT → partial save; aligned client+converter to DIRECT rows per algebra §3.E), **QA-202** (non-positive `setEnumeration` → a pre-network ConvertIssue), **QA-207** (shared `EMPTY_PAIR` ref → fresh literal), **QA-205** (double-submit → a sync `isSubmittingRef` latch; deeper server idempotency stays deferred, QA-007). Deferred: QA-101 (same-track `pairedWithRowId` accepted server-side — inert, client-caught), QA-204 (multi-container partial-wiring → DR-3).
- **Process notes.** Worktree caveat (corrected the prior gotcha): a fresh pnpm worktree has NO `node_modules`, so an agent can't self-verify check-types/lint — ran Wave A (api-server) ∥ Wave B (platform) as parallel agents on the SAME checkout (file/package-disjoint, orchestrator commits) instead. `compose-arbitraries.ts` split across two files for the 300-LOC ceiling. commitlint's lowercase-subject + ≤150-char-body rules forced several commit-message retries (added to the board gotchas).
- **Commits:** `5638e8ab` feat author + two-phase persist · `4712ccfe` feat qa-004 existence/scope check · `387176f0` test qa-004 gated cases · `79124c87` fix superset-direct-rows + 3 review fixes · `508a9c3e` test run-2 coverage + fast-check dep · + this docs close-out.
- **S3 merge decision (Gate C):** recommended NOT to fold S3 into this session — land Run-2, leave S3 (the mechanical sweep Workflow + gated `db:reset` + full suite) as the clean next-session pickup (risk retired; the owner skipped even the gated QA-004 run, signalling budget conservation). Budget ≤1 `/feature` full/session intact either way.
- **Next:** S3 — the mechanical archetype sweep (ultracode `Workflow`); run the gated QA-004 cases before merging #244. See `state.md` Next action.

## 2026-06-04 — S3 A+B (UI half: render-flip + authoring delete)

- **Vehicle.** S3 opened with an ultracode `Workflow` discover fan-out (6 read-only agents, 534K tok, re-verifying every removal site vs current post-S2 code) — NOT a separate recon session. The discover paid off big: it corrected the recon's "mechanical sweep" framing in two load-bearing ways (the card render-flip was UNDONE work, not done; the seed is 64-node domain re-authoring, not mechanical) and found a whole dead alt-group subsystem + a ~2× larger coverage gate. See `decisions.md` D-10.4-S3.
- **Phase A (`0fe3e6c3`) — render-flip + alt-group fold, done direct (delicate wiring, not fan-out).** Cards (`schema-card{,-head,-meta}`) source label/header/meta from `deriveCompositionLabel` + `formatCompositionSummary` + a slimmed `formatSchemaHeader(schema)`; `schema-archetype-tag` → `schema-composition-tag`. Alt-group block-render → flat list (parallel/superset via the composition label, no AccentGroupCard accent — deferred, S3-ACCENT). Per-schema EDIT button removed (compose is create-only). REVIEW-005 partial (2 const maps → `lib/compose-axis-labels.ts`; full dedup blocked by `compose-tree.types` divergence). Rewrote schema-card/block-card/block-card-body/format-schema-header tests. cone + platform 1116 green.
- **Phase B (`25c7080f`) — authoring delete, delegated to one implement-agent + verified.** Deleted ~51 files (18 forms + registry/dispatch + picker + editor-modal + schema-editor-types + use-archetypes + use-alternating-groups + endpoints + 5 routes); pruned barrels/keys; slimmed catalog-provider (drop `archetypeById`) + schema-form-utils (drop `parseArchetypeParams` + its dead helper tree — the agent's beyond-brief call, verified correct by the cone). Independently re-verified: cone green, platform **982** green, api-server/contracts UNTOUCHED, zero residual archetype refs.
- **Paused at the owner-sanctioned A-B / C-E split.** Discover confirmed Phase C is the largest, most domain-sensitive task (the arrangement back-patch re-key + ~20 structural conversions) + ends in the gated DB run — recommended fresh focus over a rushed tail. Owner agreed: close the session, hand off C-E. Full Phase-C spec promoted to `phase-c-seed-conversion.md`.
- **Next:** S3 Phase C — seed composition-native, via `/feature` full (domain re-authoring, NOT Workflow), `phase-c-seed-conversion.md` as design input. Then D (mechanical contract/api-server removal) + E (Prisma + `db:reset` + gated suite, owner pre-authorized). NO fresh recon. See `state.md` Next action.

## 2026-06-05 — S3 Phase C done (seed composition-native; committed, green)

- Ran via `/feature` (full) on `feat/compose-s3`. Commit **`ff8b0392`** (54 files, +2018/−3776). api-server **check-types + lint + dep:check green** = the Phase-C ceiling. Seed RUNTIME validation (`db:reset` + the gated ~10-min suite) is **Phase E** — NOT run. Budget intact (one `/feature` full this session; D+E next).
- **What shipped.** `CanonicalSchemaNode` flipped composition-native (drop archetype/kind/alternatingGroupRef; `composition` required); 63 call-sites across **15** plan-synthetic files converted; the **NET-NEW two-phase arrangement back-patch** (`backPatchComposition`/`resolveArrangement` + a child-schema ref map in `ref-resolver`; refId→cuid via `cuidFromSeed`; strip-arrangement-at-create → register children → whole-composition patch; mirrors S2-R2); `trailingConnector` dropped (emit + cells + the leftover `connector:` marker notes cleaned); coverage gate → per-axis cells (SCOPED to emitted kinds); seed-test archetype assertions stripped; archetype machinery deleted (catalog + 34 helpers + alt-group emit plumbing + `trailing-connector.{ts,test.ts}`); `buildComposeNode` moved into `builder/compose/` → **zero `archetype` paths remain in the seed**.
- **Aggressive no-bridge** (owner directive, [[aggressive-migration-no-bridge]]): NO green-per-commit transitional bridge — direct flip, intermediate tree globally check-types-RED, only the integrated end-state green. INFRA agent (engine) → 7 parallel CONVERT agents (15 files, file-disjoint) + 1 COVERAGE agent → orchestrator integrated + verified + committed (agents wrote + self-reported, did NOT commit, per [[verify-implementer-report]]).
- **Back-patch crux — ALT-1** (vs the platform's `deferredArrangement` side-field): refIds live INSIDE `composition.arrangement` as cuid-format strings (`z.string().cuid()` infers `string` → type-checks; cuid-format passes the Phase-E parse; mirrors the existing super-set `schemaRows` pattern). Less machinery, no new type field. QA empirically ran `canonicalSeedSchema.safeParse(SYNTHETIC_DEMO_PLAN)` → **PARSE OK**; every refId cuid-format + same-block + childSchemaId↔child-refId matched.
- **program/slot DEFERRED (DEFER-001).** The frozen contract + Prisma + S2 UI have NO row-level home for `StagedProgram`/`SlotSpec` (verified: consumed only by `archetype-params.schema.ts`; no SchemaRow column; platform `ComposeProgram` = tree-of-weeks naming collision). algebra §1's "program is a sacred Row-VO" was WRONG — it was always an archetype param. The 5 named-program + the emom-slot nodes → flat `{}` (exercise rows survive); the staged-program (wave/drop_set/cluster) + the 1 grouped-slot span deferred → Phase D/E when the row payload gains the field. Single-minute slots lose nothing (minute = child position, D-EMOM-UX). Dropped the 5 `stagedProgram.*`/`slotSpec.*` coverage cells (honest coverage, [[no-list-caps-honest-counts]]).
- **Review + QA (Gate B) caught 2 gated-run-burners** (both invisible to check-types, both would abort the Phase-E ~10-min run): (1) `rowKind.INNER_LADDER_MARKER` coverage cell unsatisfiable — the parallel conversion dropped ALL marker rows (0 producers) → `report.satisfied===report.total` throws; the design's coverage redesign enumerated emitted _axis_ kinds but missed STRUCTURAL `ROW_KINDS`; (2) the kept QA-006 test asserted `absentComposition > 0`, now always 0 (composition required). Both fixed + amended into `ff8b0392`. The gate earned its keep.
- **Design corrections (accuracy).** Spec's 18 files/64 nodes → really **15 files/63 call-sites** (sub-schema-coverage emits 22 slots from 1 buildSlot). Coverage total: design said 112→78 (a family-undercount); real `COVERAGE_CELLS.length` is **196→161**, and NO test hard-codes it (`.length` auto-tracks). The back-patch was DEFINITIVELY NET-NEW (3 confirmations). design §8 OQ-THEN ("leave connector notes un-stripped, harmless") was WRONG — post-extractor-removal the marker would store as notes cruft → cleaned (correction).
- **Carry-forwards** (→ `deferred.md`): DEFER-001 (program/slot row home → D/E + re-add the 5 cells); `coverage-matrix.md` stale archetype-era SSOT doc (superseded by the per-axis COVERAGE_CELLS code → update/delete at D or close-out); REVIEW-004 (marshalNullableJson → D, unchanged); QA-006 RESOLVED in C (re-floor `composition.present`→40).
- **Next:** Phase D (mechanical — `/feature small` or direct): contract + api-server archetype removal (ONE squash) + D-10.4-2 (`kind` strip + abolish `assertParentKindForRow`/`assertSubSchemaInvariants`) + REVIEW-004. Then Phase E (direct, gated): Prisma drops + `db:reset` + `db:generate` + the GATED full api-server suite (validates the Phase-C seed — QA pre-verified it parses + every cell is satisfiable). Then `/initiative-close` for the whole S3 (docs in the S3 PR, push behind the PR). See `state.md` Next action.
