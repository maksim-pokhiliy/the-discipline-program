# plan-editor-compose — state

**Updated:** 2026-06-02

**Where we are.** Step **10.1 shipped + walkthrough passed** — compose constructor **prototype on mocks** (PRs #234 + #235, merged to `main`). The `/coach/compose-prototype` route (moved under `/coach` so the `proxy.ts` role-gate allows it — see journal) renders a recursive tree canvas (nest/add/reorder/**duplicate** node + week/day/session/block) + ONE parameterized node-inspector (the four orthogonal axes `repetition`/`arrangement`/`scoring`-inert/`rest`, NOT per-archetype) + reused `step-09.x` leaf row editors. `compose/compose-tree.types.ts` is the **keystone draft** that seeds the 10.2 contract. 41 new files, **purely additive** (zero existing files modified); 55 tests; check-types + lint green. Pivot/spec/initiative-system context unchanged (ADR-0037; algebra-spec 10.0).

**Next action — step 10.2 (contracts + schema cut).** The coach-walkthrough gate **PASSED** (owner-tested on `/coach/compose-prototype`, 2026-06-02): expressiveness (Gauntlet B/C/D assemble by free nesting) + ergonomics (duplication). The walkthrough also surfaced + fixed two defects (PR #235): the route was unreachable (`proxy.ts` role-gate — moved under `/coach`), and row cards showed movement names without numbers (row-summary now renders reps + load — the four-projection READ leak). Now: **10.2** — refine `compose/compose-tree.types.ts` into `@repo/contracts/lms`, cut the Prisma schema (drop `Archetype` table + `archetypeId` FK + `archetypeParams` union; axes persistence; `kind`/`family` computed-on-read, never stored), per the DECISIVE obligations below. Gate: `db:reset`, not migration (non-prod, ADR-0019). Run via `/feature` — its Research stage IS the recon; the DECISIVE design calls (ladder split, axis persistence, archetype-drop sequencing) surface at Gate A for ratification, as the 10.1 interval/ladder calls did.

**Open decisions** (made, overridable): no-delete / no-move reorg; living-mirror protocol dropped (live Prisma + `@repo/contracts` = single source of truth); `scoring` axis present-but-inert (type + test enforced — Stage-7 guard passes). **Gate-A four-projection resolutions (DECISIVE, 2026-06-02):** `interval` is CONFIRMED as a `repetition` primitive; `ladder` DECOMPOSES into two primitives (round-counter = container axis vs rep-scheme = row `RepNotation`) — 10.2 MUST split it; the prototype deliberately keeps container-`ladder`. **Axis-input validation DEFERRED to 10.2** (zod on the real contract = the single declarative home; Stage-7 safety-probes proved malformed cadence/interval/window/rest input is stored-but-INERT — no tree corruption, no UI crash, so the defer is safe). See journal + memory `[[compose-four-projection]]`.

**For 10.2 (carried obligations).** Freeze `interval` as a primitive; **split `ladder`** (run the Fran-vs-Block-C four-projection test — chosen placement must express BOTH with no analytics collision); add zod axis validation; resolve the 14 DRAFT items (interval already resolved, ladder mandated; the rest: rest-scope, parallel shape, superset, scoring-condition, EMOM slotSpec, count_times_reps, timeCap VO, cadence reconciliation, staged-program row, upper-tree, trailingConnector, derive-label). Mine `analysis/artifacts/03-content/` (VO analysis) + `algebra-spec.md`; ignore the archetype-taxonomy framing.

**Deferred.** The `analysis/` archetype artifacts carry surviving backbone/VO reasoning alongside cut archetype content.
