# plan-editor-compose — state

**Updated:** 2026-06-03

**Where we are.** Step **10.2 frozen** — the `@repo/contracts/lms/composition` axis contract + an additive nullable `Schema.composition Json?` column (branch `feat/compose-contracts-10-2`, 9 commits — 8× 10.2 contract + 1 carried unrelated fix: a `day-row` test date-flake frozen-clock fix, one PR per user; **1023 contracts tests**, monorepo check-types/lint green; not merged — 10.3 stacks on this branch). The 10.1 coach-walkthrough PASSED; 10.2 lifted the keystone `compose-tree.types.ts` draft into the real zod-validated contract: four orthogonal axes (`repetition` 8-variant incl. frozen `interval` · `arrangement` ordered/parallel/superset · `scoring` 6 INERT · `rest`=`RestSpec`) + a flat `compositionSchema` bundle + a recursive read-projection wired to real VOs; zod on every axis; `exactOrRangeSchema` lifted to `_shared`. **`.strict()`** on the whole contract (Gate-B ratified — unknown keys reject). Ladder split DONE + the four-projection invariant ENFORCED (container `repetition:ladder` vs row `INNER_LADDER_MARKER`; a `composeContainerSchema.superRefine` forbids the fused collision). AlternatingGroup→`arrangement:parallel` fold TARGET frozen (no Prisma removal). scoring inert (deep structural guard). **`db:reset` GATED — not yet run** (additive nullable = forward-compatible; schema frozen-and-green offline via prisma validate/generate).

**Scope note — Option C (expand-then-contract).** 10.2 ADDED only; it removed NOTHING. The destructive cut (`Archetype` table, `Schema.archetypeId`, `archetypeParams`, `Schema.kind`, `AlternatingGroup`, `alternatingGroupId`, `trailingConnector`) is RETAINED and moves to **10.4** (the mechanical sweep, where the ~40 consumers die atomically with the symbols — husky blocks a broken intermediate tree; expand-then-contract = manifesto §2.8 + OQ-4). The prompt's literal "cut" + "no stored kind/family column" are satisfied in 10.4, not 10.2.

**Next action — step 10.3 (backend + seed-as-compositions).** Endpoints/mappers read/write `Schema.composition` (`compositionSchema.parse` on read, `toInputJson` on write); the derived `kind`/`family` label algorithm (computed-on-read, NEVER stored — OQ-1); seed fixtures = primitive trees, not a catalog. **Inherited obligations from 10.2 (see journal 2026-06-03):** (1) the mapper MUST validate assembled trees through `composeContainerSchema` — the ladder-collision invariant lives on the tree projection, not the flat per-node bundle; (2) re-home the §6 scoring-inert _consumer-execution_ source-scan to the api-server consumers before 10.4 deletes the 10.1 UI guard; (3) decide `rest: until_recovery` (the `restSpecSchema.duration`-mandatory gap — sham-duration convention vs a VO change); (4) optional mapper-side recursion-depth bound.

**Open decisions** (made, overridable): Option-C expand-then-contract (two-way door — 10.4 can still true-remove); `.strict()` axis contract (Gate-B); ladder rep-scheme home = existing `INNER_LADDER_MARKER` (not a new `RepNotation` variant); `interval` frozen as a `repetition` primitive; `kind`/`family` computed-on-read, never a stored column. See journal 2026-06-03 + memory `[[compose-four-projection]]`.

**Deferred.** The `analysis/` archetype artifacts carry surviving backbone/VO reasoning alongside cut archetype content.
