# profile-axis-catalog — plan

3 vertical waves (each a visible slice, not server-then-UI), each ships via `/feature`, each owner-smoke-tested on dev. Budget ≤1 `/feature`/session. Sequential: W2 needs W1's catalog, W3 needs W2's binding. Intermediate-red between waves on dev is OK (owner rule — `migration-intermediate-velocity`).

| #   | Wave                          | Covers                                                                                                                                                                                                                                                                                                                                                                                                                           | Gate                                                                                                                                                                                              | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --- | ----------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Catalog + admin**           | `ProfileAxis` Prisma model + migration · contract (entity + CRUD api) · CRUD endpoints · admin CRUD module (labels/exercises pattern)                                                                                                                                                                                                                                                                                            | admin creates/edits/deletes axes; api-server suite green; NO load-VO/resolver/athlete touch                                                                                                       | ✅ merged — PR #304 (D-4/D-5; migration `20260622113153_add_profile_axes` authored offline via `migrate diff`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2   | **Coach binding** (sacred-VO) | plan-editor-compose decision + four-projection re-check FIRST → byProfile axis → `{kind:catalog,axisId}\|{kind:human,attribute}` · resolver branch (catalog→selections, human→`gender` col) · find-or-create in coach load-editor · migrate existing loads                                                                                                                                                                       | D-3 ratified in plan-editor-compose; coach authors load by catalog axis / gender; existing byProfile loads migrated                                                                               | ✅ **DONE — built (#309), reverted on incident (#311), RE-APPLIED + prod-confirmed (#313 → `a7ac9a63`).** Shipped WITH the PAC-1 backfill (13/13 loads → design-A, one transaction + re-validation) + a prod-snapshot dry-run (D-8); Option A cutover (PAC-19), zero-traffic window. Catalog = 2 axes (system `Gender` + plain `level`); gender resolves from the typed column (owner smoke). Design D-7 unchanged. **Cut realized:** W2 = resolver READ + inline-picker WRITE by `axisId`; **W3** = the profile-card write + the bulk `profileSelections` migration (PAC-2)                                                            |
| 3   | **Athlete two-layer profile** | **W3-core (D-9):** athlete-gated catalog READ (the coach read is coach-gated) · EXTEND `ProfilePicksCard` → catalog-backed curated picker (re-label `axisId`→label, proactive pick-groups over `binding=null`, write `profileSelections[axisId]`, hide bound, tolerate orphan) · **PAC-2** `profileSelections` key migration (D-8). **W3.1 fast-follow (D-10):** PAC-17 inline gender-set from the session (typed-column write). | athletic card shows catalog axes + re-labels picks; athlete sets level proactively; gender hidden from the card (stays in details); PAC-2 dry-run clean vs prod snapshot; resolver/VO diff = ZERO | ✅ **DONE 2026-06-24** — W3-core merged **#314** (`feat/profile-axis-w3-core`): D9-1 the catalog-backed re-labelling picker (`ProfilePickRow`→`ProfilePickGroup`, closes PAC-12) · D9-2 the athlete-gated read (`GET /api/platform/athlete/profile-axes`, `binding=null`, coach auth UNTOUCHED) · D9-3 the PAC-2 script (transform + backfill, dev-dry-run clean). Guardrails ZERO-diff; NO Prisma migration; api-server tests green via CI. **W3.1 / PAC-17 merged #315** (`InlineGenderPicker` + the typed-column write). Owner prod-confirmed. **Residual:** the optional PAC-2 prod `--write` cutover (re-probe → dry-run → write). |

**Sequencing rationale.** Vertical, not horizontal — each wave is something the owner can see and smoke (W1 in admin, W2 in the coach editor, W3 in the athlete profile). Server-then-UI would leave nothing visible after a run. W1 is deliberately the LOW-RISK slice (isolated new entity, zero sacred-VO radius); the sacred-VO change is quarantined to W2 behind its ratification gate.

**W1 design details LOCKED here** (not silently deferred):

- `ProfileAxis { id @cuid, key @unique, label, values String[], createdAt, updatedAt }`.
- Values = controlled `String[]` ON the axis (NO `ProfileAxisValue` table yet — PAC-4).
- GLOBAL catalog (no owner/coach FK — single-coach project — PAC-5).
- Admin module MIRRORS the existing labels/exercises CRUD (find that module, match its pattern — don't invent).
- **The lms resolver does NOT read `ProfileAxis`** — resolution needs only the load VO's axisId + cells + the profile's selections, so there is no `lms → coaching/admin` boundary breach (`api-server-lms-no-coaching` stays green). W1 doesn't touch the resolver anyway; this is the design invariant that keeps W2/W3 clean.

**Resolved in W2** (was "deferred to W2"): VO shape `label`+`values` DENORMALIZED (`four-projection-recheck.md` §5/Part B); find-or-create UX = FULL inline create-with-values (D-6 / R3); the migration is SPEC'd (probe `training_schema_rows` + the backfill map — deferred.md PAC-1), an owner-gated prod run. **REVISED to design A (D-7, 2026-06-23):** the `kind` union is SUPERSEDED by a flat `binding`-discriminated object (`{axisId,label,values,binding}`); kind-first authoring → ONE catalog picker (the system Gender axis badged "Profile attribute"); the backfill map now targets the system Gender `axisId` for a legacy `name∈{gender,sex}` (not a removed `human` arm).

**Resolved in W3 (planner session 2026-06-24 → D-9 / D-10):**

- **The curated-picker UX is now LEGITIMATE** — `athlete-core/D-PROF-SELECTIONS-HONEST` cut it "for lack of a catalog"; W1/W2 shipped the catalog → the named condition is fulfilled (D-9). The athletic card EXTENDS `ProfilePicksCard` (re-label + proactive pick-groups), not a rebuild. The UX itself is designed on MOCKS first (athlete-POV), owner-approved before wiring.
- **Bound axes HIDDEN — confirmed** (D9-1): `binding != null` (gender) axes never render on the athletic card (gender lives in the details card, no dup), mirroring the inline-picker's `binding===null` filter; W3-core's athlete READ also server-filters `binding=null`.
- **The catalog-read auth gap** (NOT seen pre-recon): the coach `GET /api/platform/profile-axes` is coach-gated → the athlete would 403 → W3-core adds a dedicated athlete read (D9-2).
- **PAC-17 split out** to a fast-follow (D-10) — distinct surface (session), decision-first DISCHARGED (supersedes D-7's "no inline pick" + the bound-arm four-projection note); typed-column write-affordance, NOT `profileSelections`; no `ResolvedLoad` change.
