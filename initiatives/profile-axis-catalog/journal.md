# profile-axis-catalog — journal

Append-only. One entry per session/step.

## 2026-06-22 — founded; W1 prompt prepared (paper-only session)

- **Founded** the initiative — pulls the **profile-type-catalog** carry-forward out of `athlete-core` (its deferred "library wave": re-home `profileSelections` + wire typed `gender` into resolution, named in `D-FIELDS-GENDER-INERT`) into its own 3-wave initiative. Set `initiatives/ACTIVE` → `profile-axis-catalog`.
- **Design dialogue** (this session): diagnosed that the real defect is axes have NO identity (string-join footgun + typed `gender` invisible to the resolver) — NOT a lack of coach freedom (free-form axis names already ship in `load-by-profile-fields.tsx`). Owner caught the ontology smell in the first "gender-as-bound-axis" sketch → ratified the two-category split (D-1), the catalog-with-find-or-create (D-2), and the discriminated-union axis (D-3, gated to W2).
- **Waves** ratified: W1 catalog+admin (no sacred-VO touch) · W2 coach binding (sacred-VO, decision-first) · W3 athlete two-layer profile. Owner GO on the slicing; ≤1 `/feature`/wave, owner-smoke per wave.
- **W1 feature-prompt PREPARED** (`w1-feature-prompt.md`) for a separate EXECUTOR session — this planning session is paper-only (owner: «волны будем запускать в отдельной сессии-исполнителе, здесь только бумажки»). The prompt is the self-contained W1 brief (scope + read-list + hard guardrails + acceptance + owner-smoke).
- **Research basis** (read this session): `contracts/lms/_shared/load.ts` (the `byProfile` VO — axes `{name,values}` + cartesian `cells`), `endpoints/lms/athlete-records/resolve-load.ts` + `load-records.ts` (resolver reads ONLY `{weightKg, profileSelections}` — gender disconnected, confirmed), `AthleteProfile` schema, the full write-back flow (`inline-profile-picker.tsx` → `pickProfile` → `profileSelections` mutation), `load-by-profile-fields.tsx` (free-text authoring), athlete-core decisions (`D-FIELDS-GENDER-INERT` / `D-PROF-SELECTIONS-HONEST`), `plan-editor-compose/D-PERSIST` (sacred VO).

## 2026-06-22 — W1 SHIPPED (catalog + admin) via `/feature`

- **Built W1** on `feat/profile-axis-catalog-w1` (full `/feature` pipeline, executor session): `ProfileAxis` model (`@@map("app_profile_axes")`) · contract `@repo/contracts/coaching/profile-axis` (6-file entity + CRUD api; `normalizedString` for key/label; values `.min(1).max(50)` + a `load.ts`-mirrored uniqueness `superRefine` on create AND `.partial()` update) · api-server `profileAxisAdminApi` (FLAT `endpoints/coaching/profile-axis.ts` + `mappers/coaching/profile-axis.mapper.ts`, zero-cast passthrough) · admin "Profile Axes" CRUD module (mirror of labels/exercises, `@repo/ui` `TagsInput` for `values`). Commits: founding · `feat(db)` · `feat(contracts)` · `feat(api-server)` · `feat(admin)` (+ dep-cruiser carve-out) · `test(coaching)` · `docs(bounded-contexts)` · this close-out.
- **Decisions ratified (Gate A):** D-4 (case-sensitive `key`, no `keyLower`) · D-5 (`coaching/` placement + a file-precise carve-out in `admin-coaching-only-via-user-detail-route` admitting the 3 admin routes — the literal mirror of how labels/exercises were admitted via `admin-no-lms`; the named `api-server-lms-no-coaching` needed no edit, it holds for free).
- **Review + QA:** 0 code-defect CRITICALs. Code A-grade — mapper zero-cast; the `superRefine` dedups POST-normalize (NFKC + zero-width strip), STRONGER than the `load.ts` rule it copies; auth airtight on all 3 routes × all verbs (`withAdminAuth` default-deny + rate-limit); every guardrail git-verified (zero diff to `load.ts`/resolver/athlete-profile/`load-by-profile-fields.tsx`, seed untouched, no `lms→catalog` import, no `gender` link). Contract suite 907 green (incl. added normalization-dedup + update-conflict cases). MUI per-icon (admin `next build` EXIT 0).
- **Carry-forwards → deferred.md:** PAC-8 (W2 validation-law reconciliation: catalog NFKC + case-sensitive key vs `load.ts`) · PAC-9 (axis-delete referential policy) · PAC-10 (future `library` bounded context) · PAC-11 (`TagsInput` raw-vs-NFKC dedup mismatch).
- **OPEN — the one mechanical step:** the Prisma migration file is NOT authored. `prisma migrate dev` needs `SHADOW_DATABASE_URL`, absent in the local `.env`; owner approved pausing at Gate A. The model is in `schema.prisma` + `db:generate`'d (type-checks green), but the migration FILE + owner-smoke are pending the shadow URL — until then the admin module 500s (P2021) on a fresh DB.
- **Next:** land the migration + owner-smoke → W1 PR → W2 (D-3 gate first).

## 2026-06-22 — W1 migration authored + PR #304 opened (post-Gate-A)

- **SHADOW_DATABASE_URL wired** (gitignored `api-server/.env`, a SEPARATE Neon branch ≠ dev) → migration `20260622113153_add_profile_axes` authored offline via `prisma migrate diff` (memory `offline-prisma-migration-authoring`). **Supersedes the prior entry's "migration file NOT authored / pending SHADOW_DATABASE_URL" Gate-A note.**
- **PR #304 opened** (`feat/profile-axis-catalog-w1`) — CLEAN / MERGEABLE, all checks green (Build · dep:check · Format · Lint · Tests 6m28s · Type check · Vercel ×3 · claude-review). gh-verified (co-owner check, not on report alone).
- **Remaining = owner-side:** local smoke (`db:reset` → admin :3002 → Profile Axes CRUD + dup-key 409) → merge (#304 → migration auto-applies to prod via `db-migrate.yml`; the PREVIEW DB has NO auto-migrate — Profile Axes 500s there until the schema is pushed) → gated api-server suite (~10 min, on owner ok). Then W2 (D-3 gate first).

## 2026-06-22 — W2 SHIPPED (coach binding — the sacred-VO wave) via `/feature`

- **Gate FIRST (sacred-VO, decision-first).** Discharged the D-3 ratification gate before any VO code: the four-projection re-check PASSED (`four-projection-recheck.md` — the free-string axis is a latent NAME-COLLISION; the union is the lens-MANDATED split on `kind`, structurally identical to `D-LADDER`; each arm is four-projection-invariant) + the plan-editor-compose `D-BYPROFILE-AXIS` cross-ref RATIFIED (the sacred-VO sign-off, recorded in `plan-editor-compose/decisions.md`). D-3 → RATIFIED. Commit `67d9f143`.
- **Built W2** on `feat/profile-axis-catalog-w2` (full `/feature` pipeline, executor session) — 6-commit build arc after the gate: `a4592076` contract union (`byProfileAxisSchema` discriminated on `kind`; catalog arm denormalizes `axisId`(cuid)+`label`+`values`, human arm const-locked to `GENDER_AXIS_COORDS`, lms-local; superRefine kind-guarded + the new distinct-axes rule) · `3cefc3cc` resolver branch by `axis.kind` (catalog→`profileSelections[axisId]`, human→typed `gender` via a LOCAL map, two unresolved arms, `gender` added to ctx via one `select` column / zero extra queries) · `18769df5` the new platform `ProfileAxis` GET+CREATE chain (mirrors the lms-label chain; shared `createProfileAxisRow`; dual role-gate) · `c98092c7` kind-first find-or-create coach authoring (FULL inline create-with-values — R3) · `ae950c80` athlete picker+write re-keyed by `axisId` + the gender steer (no inline pick for the human arm) · `e81ebb47` coach-prose for the union. Then `cc5994ed` review/qa fixes. (+ the Stage 7-8 test commit + this close-out docs commit.)
- **Decisions ratified (Gate A).** D-3 (the union — gate-discharged) + **D-6** (the W2 execution calls): the 4 forks decided — DR-1 two distinct unresolved arms (`missing_profile_pick` inline-fixable vs `missing_profile_attribute` profile-steer; catalog-first on a mixed load) + `axisNames`→`axisLabels` (no external consumer — apex is the separate legacy Spring app); DR-2 the platform chain mirrors lms-label + a shared `createProfileAxisRow` (rule-of-two) + dual role-gate, no dep-cruiser edit; DR-3 the unbound axis = empty-`axisId` in the live `Load`, rejected on save (compose draft→strict precedent, zero blast radius); DR-4 the migration is SPEC-ONLY, owner-gated. **R3** (owner-approved Gate-A scope expansion) = FULL inline find-or-create (a values form, not pick-only; binds the server response, no NFKC divergence). The denormalized snapshot is WHY PAC-8/PAC-9 closed.
- **Review + QA.** Review STRONG — 0 real CRITICAL (the lone CODE-001 was a stray untracked QA-probe file in `contracts/`, deleted — not a design defect); the architecture matches gate §5/§6/§9 + DR-1..DR-4 faithfully; the four-projection guardrails hold by construction (dep:check green, 2054 modules; resolver imports no `coaching/*`, no `ProfileAxis` read; `axis.name`/`axisNames` fully gone; gender stays typed). QA block-B — 0 CRITICAL (the wrong-weight-to-athlete class is closed: stale picks validated against the snapshot, gender map exhaustive over `MALE|FEMALE`, human arm strips arbitrary values, distinct-axes + cartesian-cover fire). **7 WARNINGs fixed** (CODE-002/QA-001 all-human cell-miss now steers; CODE-003 composite picker key; CODE-004 create-form remount; CODE-005 create-error surfacing; TEST-001/QA-003/QA-004/QA-005 the test gaps — authz, inline-picker, inline-create round-trip).
- **Carry-forwards → deferred.md.** PAC-7/PAC-8/PAC-9 → CLOSED (the gate + the denormalize/bind-by-`axisId` design). PAC-1 → SPEC'd, owner-gated (the probe `training_schema_rows` + the backfill map). New: PAC-12 (W2↔W3 intermediate-red — owner-accepted), PAC-13 (QA-006 `regenerateCells` kg-carry, low), PAC-14 (the GATED api-server tests must run green before merge).
- **Pending (owner-side).** The GATED api-server suite (~10 min serial, live Neon — the added authz + resolver cases) + owner-smoke on dev platform → merge. Then W3 (athlete two-layer profile + the bulk `profileSelections` migration). Optionally the PAC-1 prod probe (owner-gated; backfill only on a non-empty result).

## 2026-06-23 — W2 REVISED to design A (gender = a system bound axis) via `/feature` (worktree)

- **Why.** Owner smoke on the W2 UNION found a real hole: the `{kind:"human"}` arm was bypassable — a coach could create a `{kind:"catalog"}` axis literally named "Gender" (M/F) and assign THAT; it resolved from `profileSelections` (a manual pick), ignored the typed `gender` column, and a `gender=MALE` athlete got "pick your gender" (values mismatched too: catalog `M`/`F` vs the human arm's `Male`/`Female`). Two parallel authoring paths for gender = the D-1 disease via the catalog door. The four-projection re-check had PASSED the union but MISSED the AUTHORING (coach-SETS) projection's dual path.
- **Gate FIRST (sacred-VO again, design-first).** Re-ran four-projection on the new shape — `four-projection-recheck.md` Part B (the union analysis kept as superseded Part A) covering the single-path-authoring fix; ratified **D-7** (supersedes D-3's union shape); revised the plan-editor-compose `D-BYPROFILE-AXIS` cross-ref. Commit `d1abd33b` — docs landed BEFORE any code (guardrail).
- **Design A.** gender stops being a parallel union arm → becomes ONE protected catalog row distinguished by a `binding` enum. byProfile axis = flat `{ axisId, label, values, binding:"GENDER"|null }` (the `kind` union + the `human` arm removed). `binding=null` → plain pick (`profileSelections[axisId]`); `binding="GENDER"` → reads the typed `gender` via the LOCAL coord map, NO pick. Single-path by construction: `binding` not API-settable (absent from create/update form schemas) + `@@unique([binding])` (Postgres: many NULLs, one GENDER) + the protected migration-inserted system row (edit/delete → `ForbiddenError` 403). A coach-named plain "Gender" axis is `binding=null` — can't read the column, not a gender differentiation.
- **Built** on `feat/profile-axis-catalog-w2` (full `/feature`, isolated git worktree): 6 commits after the gate — `feat(db)` (`5d99e35b`) enum+column+`@@unique`+migration `20260623083123_add_profile_axis_binding` with the system-row INSERT (cuid `cgender0…`, values `[Male,Female]`, authored offline via `migrate diff` because `migrate dev` aborts non-interactively on the unique-constraint warning; `ON CONFLICT DO NOTHING`) · `feat(contracts)` (`1b2e1245`) flat VO + `binding` on the read schema only (local literal — lms↛coaching) + union fixtures migrated · `feat(ui)` (`53271dcb`) `CreatablePicker` optional `renderOption` (backward-compat) · `feat(api-server)` (`404619a4`) mapper passthrough + `assertNotProtected` (403) + resolver branch on `binding` · `feat(platform)` (`f4337deb`) ONE picker + the "Profile attribute" badge + the athlete inline-picker `binding===null` filter + `makeCatalogAxisDraft`→`makeAxisDraft` · `feat(admin)` (`c560974f`) system row read-only (System chip, no edit/delete/link). Then `test(contracts)` (`5b6c2e7c`) the ungated coords invariant (REVIEW-001). + this close-out docs commit.
- **Decisions ratified.** D-7 (design A — supersedes D-3; D-6 snapshot note revised: the snapshot carries `binding`, not `kind`). DR-A1 (rename `makeCatalogAxisDraft`→`makeAxisDraft`). `ForbiddenError` 403 for protected-row edit/delete. Existence mechanism = migration-insert (the only one reaching prod via `migrate deploy`).
- **Review + QA.** Review APPROVE (A), QA A — **0 CRITICAL** both. Single-path survived every attack (4 independent guards: form-schema strip, update-builder field-pick, `superRefine` distinct-by-`axisId`, DB `@@unique([binding])`). The exact bug is dead + pinned (`resolve-load.test.ts`: MALE resolves the Male cell with empty `profileSelections`). REVIEW-001 (ungated `GENDER_AXIS_VALUES`/`COORDS` pin) landed. **QA-W001 held the line — NOT applied**: the `GENDER_TO_COORD[gender]` undefined-vs-null asymmetry is unreachable (Postgres enum) + degrades safely; the proposed `?? null` is dead code (lint) or weakens `Record<Gender,string>` exhaustiveness (which catches the real future risk — a 3rd gender). INFOs noted, non-blocking.
- **Verification.** check-types 16/16 · lint 16/16 · dep:check 0 (lms↛coaching held) · platform vitest 1267 · contracts 923 · ui 267. **GATED api-server suite: 1163/1164 passed — ALL design-A cases green** (resolver-on-binding matrix, CRUD 403 protection, the 2nd-GENDER-impossible, the system-row-`values` invariant). The lone failure is `cms/pages/public.test.ts > getHomePage` ("expected null not to be null") — **PRE-EXISTING, design-A-unrelated**: not in this diff (CMS domain); deterministic in isolation; a seed↔CMS-read drift surfaced by the gated run on a fresh `db:reset` (`ensureSection` find-or-create doesn't repair an existing mismatched home hero section + a blanket `try/catch` returns `seeded=true` on partial). Flagged for a separate `/fix`; does NOT block design A.
- **Pending (owner-side).** Owner-smoke on dev (the exact failed scenario — see state.md Next action) → merge PR #309 (force-updated to design A). Then W3.
- **Pushed + CI GREEN (2026-06-23).** Fast-forward push `8c1e7f8e..eea2f3a7` updated PR #309 (the 9 revision commits) + the PR body to design A. **PR #309 CI is fully green** — Build · Dependency boundary check · Format · Lint · Tests (6m44s) · Type check · Vercel ×3. The local CMS `getHomePage` failure did NOT reproduce in CI → CONFIRMED a dev-DB residual-state artifact (a pre-existing home hero section that `ensureSection` find-or-create skips), NOT a regression and NOT a real pre-existing CI failure → no `/fix` needed. Worktree note: mid-run the gitignored `node_modules` + `.env` were cleaned (untracked-file cleanup), so the FIRST push's pre-push hook failed with `depcruise: not found` — fixed by re-running `pnpm install` + re-copying the env files, then the push passed clean (no `--no-verify`). Remaining = owner-smoke + merge.
- **Post-review fix (2026-06-23).** Owner's LOCAL review of #309 caught the migration INSERT was idempotent only on `binding` (`ON CONFLICT ("binding")`) — a pre-existing coach `key='gender'` row (binding=null) would hit the `key` unique constraint (`app_profile_axes_key_key`), which the `ON CONFLICT` target doesn't cover → unhandled `unique_violation` → `migrate deploy` aborts (release blocked) / `db:reset` fails. Fixed → `ON CONFLICT DO NOTHING` (no target — catches the binding, key, AND id conflicts). Verified: `db:reset` re-applies clean (system row lands exactly once) + a dup `key='gender'` INSERT now no-ops without throwing (`dupKeyInsertThrew:false`, still 1 row). Residual → **PAC-16** (low): a pre-migration coach `key='gender'` would make the system INSERT silently no-op (caught by owner-smoke; post-migration the `key @unique` blocks the dup — the window is pre-migration only, empty on prod).
- **Dropped the `claude-code-review.yml` auto-review workflow** (per owner — it rubber-stamps every PR "all OK / no bugs", ~100 PRs; owner reviews locally now). The on-demand `@claude` bot (`claude.yml`, mention-triggered only) is KEPT — it's not the auto-rubber-stamper.
