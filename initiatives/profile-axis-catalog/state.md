# profile-axis-catalog — state (the board)

**Updated:** 2026-06-24 — **INITIATIVE COMPLETE + CLOSED.** All 3 waves merged + prod-confirmed: **W1** (catalog + admin, #304) · **W2** (coach binding, design A, re-applied #313) · **W3** = **W3-core** (the catalog-backed athlete picker + the athlete-gated read + the PAC-2 script, #314) + **W3.1 / PAC-17** (inline gender-set from the session, #315). Owner verified on prod: works. Decisions D-1…D-10 ratified + acted. The charter's acceptance is met end-to-end: a coach CRUDs axes · authors a byProfile load by a catalog axis OR the system gender · an athlete with gender resolves a gendered load with NO manual pick · an athlete proactively sets his classification from the catalog in his profile (and a new catalog axis surfaces there live) · gender is inline-settable from the workout (writes the profile) · no free-string axis-name join remains in resolution. **One residual (OPTIONAL, owner-gated, NON-blocking):** the PAC-2 prod rekey `--write` cutover — a low-urgency cleanup of any pre-W2 free-string `profileSelections` keys (the script is built + dev-dry-run clean; **RE-PROBE prod first** — the prior `1 profile with selections / 0 inert-risk` snapshot predates prod growth).

**W2 (design A) is RE-APPLIED, MERGED (#313), and PROD-CONFIRMED working.** The #309→#311 incident is closed at the root: this time the VO cut shipped WITH the **PAC-1 data-migration** + a prod-snapshot dry-run (the **D-8** law). The 13 prod `byProfile` loads were rewritten `{name,values}`→`{axisId,label,values,binding}` (gender coords remapped `m/f`→`Male/Female`; the owner-identified `CAL` folded into the system Gender axis), one transaction + post-write re-validation 13/13; the catalog now holds exactly **2 axes** — system `Gender` (`cgender000000000000000000`, `binding=GENDER`) + plain `level` (`cmqrp2xdy0000sot4u5l3z5wq`, `binding=null`). Owner smoke confirmed: gender resolves from the typed column (no manual pick), design-A loads resolve. Cutover = Option A maintenance window (PAC-19); the ~64s deploy→backfill window saw zero traffic → zero real 500s. **W1 + W2 DONE; next = W3.** (Tangential earlier this day: observability fix **PR #312** — `fa357632`, prod-confirmed; memory `[[nextjs-serverless-di-observability]]`.)

## Board

| #   | Step                                    | Status    | Pointer                                                                                                                                                                                                             |
| --- | --------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0   | Found + ontology lock                   | ✅ done   | charter · decisions D-1/D-2 · journal 2026-06-22                                                                                                                                                                    |
| 1   | Catalog + admin                         | ✅ merged | W1 — PR #304 (model+contract+endpoints+admin+carve-out+tests+docs; migration `20260622113153_add_profile_axes`)                                                                                                     |
| 2   | Coach binding (sacred-VO)               | ✅ done   | W2 design A — built (#309), reverted on incident (#311), **RE-APPLIED + prod-confirmed (#313 → `a7ac9a63`)** with the PAC-1 backfill (D-8). 2 axes live                                                             |
| 3   | Athlete two-layer profile (W3-core)     | ✅ merged | D-9 — PR **#314**: catalog-backed `ProfilePicksCard` picker + athlete-gated read (D9-2) + PAC-2 script (dev-dry-run clean). Guardrails zero-diff; NO migration. Residual: the optional PAC-2 prod `--write` cutover |
| 3.1 | Inline-set gender from session (PAC-17) | ✅ merged | D-10 — PR **#315**: `InlineGenderPicker` + gender coord map; the session steer → an inline pick writing the typed `gender` column. Prod-confirmed                                                                   |

## Next action

**▶ NONE — the initiative is COMPLETE + CLOSED.** All 3 waves merged + prod-confirmed. The only residual is OPTIONAL + owner-gated + non-blocking: the **PAC-2 prod rekey `--write` cutover** (a low-urgency cleanup of any pre-W2 free-string `profileSelections` keys; the script is built + dev-dry-run clean — **RE-PROBE prod first**, the prior `1 profile with selections / 0 inert-risk` snapshot predates prod growth; decide **PAC-21** the orphan-name-key create-path at that point). Future axis work (PAC-3 masters/age, PAC-4 a value table, PAC-5 per-coach scoping, …) lives in `deferred.md` for if/when Denys needs it — out of this initiative's scope.

## Open decisions awaiting ratification

- **(none open.)** D-1…D-8 ratified + acted. **D-9 (W3-core shape) ACTED — merged #314. D-10 (PAC-17 + the bound-arm four-projection note) ACTED — merged #315.** PAC-19 RESOLVED. D-3 SUPERSEDED. All four-projection gates DISCHARGED (`four-projection-recheck.md` for W2; D-10's bound-arm EXECUTE note for PAC-17). No OPEN decisions remain. The PAC-2 prod rekey cutover (+ PAC-21 the orphan-name-key create-path) is the one residual owner-gated OPS step — optional, non-blocking, deferred.

## Live carry-forwards (see deferred.md)

**Residual (owner-gated, OPTIONAL, non-blocking):** PAC-2 the prod rekey `--write` cutover — script BUILT + dev-dry-run clean; **re-probe prod first** (the prior `1 profile with selections / 0 inert-risk` snapshot predates prod growth) + decide PAC-21 (orphan-name-key create-path: co-owner rec = FLAG, don't auto-create) + QA-304 (find-or-create outside the `$transaction`, low) at the cutover.
**Open (low / later — out of this initiative's scope):** PAC-3 masters/age · PAC-4 `ProfileAxisValue` table · PAC-5 per-coach scoping · PAC-6 cells UX · PAC-10 `library` context · PAC-11 `TagsInput` dedup · PAC-15 admin protected-row edit-VIEW · PAC-16 migration system-row skip edge · PAC-21 orphan-name-key create-path (ties to the PAC-2 cutover).
**Closed:** PAC-1 (the byProfile backfill — DONE at the #313 cutover, 13/13) · PAC-7 (four-projection gate + cross-ref) · PAC-8 (single NFKC law) · PAC-9 (tolerate-orphan) · PAC-12 (W3-core D9-1 re-labels the card `axisId`→`label`; the resolver reads `axisId`) · PAC-13 (kg-carry) · PAC-14 (W2 gated tests — green via CI at #313) · **PAC-17 (D-10 inline gender-set — built + merged #315)** · PAC-18 (orphans reconciled) · PAC-19 (Option A executed) · **PAC-20 (W3-core gated api-server tests — green via CI at #314)**.

## Gotchas a resuming session must know

- **W1 + W2 are LIVE on prod.** The `byProfile` VO is the flat design-A `{axisId,label,values,binding}`; the resolver branches on the snapshot's `binding` (`GENDER`→typed column, `null`→`profileSelections[axisId]`). Catalog = 2 axes (system `Gender` + plain `level`). Do NOT reopen D-7.
- **The system Gender row is now legit** (`cgender000000000000000000`, protected — admin shows it read-only via the design-A code). PAC-18 is closed (no longer an orphan: the migration is in the repo + applied; the row is the real system axis).
- **W3-core is MERGED (#314)** on `feat/profile-axis-w3-core`: the athlete-gated read + the catalog-backed `ProfilePicksCard` picker + the PAC-2 script. Guardrails ZERO-diff; NO Prisma migration (binding/enum/`profileSelections` all pre-existed). The picker re-labels `axisId`→catalog `label`, renders proactive pick-groups over `binding=null` axes, writes `profileSelections[axisId]` via the existing PUT, HIDES bound gender, tolerates orphans. `ProfilePickRow` was DELETED → `ProfilePickGroup` + a LOCALIZED `ProfileOptionButton` (a byte-identical copy of the athlete-session one — a rule-of-two `@repo/ui` hoist candidate, left as-is).
- **The athlete catalog read exists now** — `GET /api/platform/athlete/profile-axes` (`withAthleteAuth` + rate-limit, `binding=null` server-filter, `label asc`; `profileAxisPlatformApi.listForAthlete`, NO in-method role gate — the route auth IS the gate, D9-2/DR-2). The COACH endpoint (`GET /api/platform/profile-axes`, coach-gated) is UNTOUCHED — the diff to `endpoints/coaching/profile-axis.ts` is purely additive. Do NOT broaden the coach endpoint's auth.
- **PAC-2 is BUILT but the prod `--write` cutover is NOT run (residual, optional).** The transform (`utils/profile-selections-rekey.ts`) + the script (`scripts/backfill-profileselections-rekey.ts`) are authored; dev dry-run CLEAN. The prod-snapshot DRY-RUN + `--write` are OWNER-gated (D-8). **NB on the "1 athlete" figure:** the W2 re-apply `auditProfileSelections` probe found **1 PROFILE carrying non-empty `profileSelections`** (gender-keyed → 0 inert-risk) — that is NOT the prod athlete count (there are MORE athletes total). Post-W2 picks are `axisId`-keyed (fine); only pre-W2 free-string keys need rekeying, a small + bounded set — but **RE-PROBE before the cutover** (prod has grown since that snapshot). **OPEN at the cutover (PAC-21):** the create-path for an ORPHAN name-key (no matching catalog axis) find-or-creates with `values:[name]` (a placeholder) — co-owner rec: FLAG for owner review, don't auto-create (the catalog is coach-owned, D-2). Plus QA-304 (low): find-or-create writes axes OUTSIDE the `$transaction`.
- **The W3-core api-server tests ran GREEN via CI at #314 (PAC-20 closed)** — the `listForAthlete` endpoint test + the PAC-2 transform unit test (the root `vitest.config` runs the api-server project in CI). (The QA-301 fix lives here: zod `z.string().cuid()` is a LOOSE regex that misclassified real axis names — "Conditioning"/"Competition" — as cuids; the classifier uses a strict `/^c[a-z0-9]{24}$/`.)
- **PAC-17 is MERGED (#315).** `InlineGenderPicker` + a `gender-coord-map.ts` (the `Male/Female`→`MALE/FEMALE` map) + the flipped `schema-row.tsx` `case "profile_attribute"` (steer → an inline pick writing the typed `gender` column via `pickGender`). NO `ResolvedLoad`/contract change (the picker reads `row.load`). The pick writes the typed column, NEVER `profileSelections` (the four-projection safety, D-10). Note: the build refactored the "binding registry" to a plain gender coord map (`4d1bc399`) — same single-decision-point safety, less ceremony for a one-attribute case.
- **gender stays a typed column** (D-1) — the bound axis READS it, never stores a gender value in the catalog/`profileSelections`.
- **The backfill script is one-time + idempotent** (`packages/api-server/scripts/backfill-byprofile-reapply.ts`): re-running skips already-migrated loads. The original 13 loads were backed up at cutover (gitignored scratch). Not needed again.
