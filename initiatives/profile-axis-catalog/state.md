# profile-axis-catalog — state (the board)

**Updated:** 2026-06-22. **W2 (coach binding — the sacred-VO wave) SHIPPED pending owner-smoke** on `feat/profile-axis-catalog-w2` — the `byProfile` axis is now the discriminated union (`{kind:catalog,axisId,label,values} | {kind:human,attribute:gender}`); the resolver branches by kind (catalog→`profileSelections[axisId]`, human→typed `gender` via a local map, two distinct unresolved arms); the coach gets kind-first FULL inline find-or-create over a new platform `ProfileAxis` GET+CREATE chain; the athlete inline picker + write re-keyed by `axisId` (human arm = no pick, a profile steer); `axisNames`→`axisLabels`. Gate DISCHARGED: D-3 RATIFIED (`four-projection-recheck.md` PASSED + plan-editor-compose `D-BYPROFILE-AXIS` cross-ref). Review STRONG (0 real CRITICAL — the lone CODE-001 was a stray probe file, deleted), QA block-B (0 CRITICAL); 7 WARNINGs fixed. **Two owner-side steps pending:** (1) the GATED api-server suite (~10 min, authz + resolver tests batched), (2) owner-smoke on dev — then merge. **W1 (catalog + admin) is MERGED** (PR #304). The ontology stays RATIFIED (D-1/D-2); W2 execution calls ratified as D-6.

## Board

| #   | Step                      | Status       | Pointer                                                                                                                                                                                                                                                 |
| --- | ------------------------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0   | Found + ontology lock     | ✅ done      | charter · decisions D-1/D-2/D-3 · journal 2026-06-22                                                                                                                                                                                                    |
| 1   | Catalog + admin           | ✅ merged    | W1 — PR #304 merged (model+contract+endpoints+admin+carve-out+tests+docs; migration `20260622113153_add_profile_axes`)                                                                                                                                  |
| 2   | Coach binding (sacred-VO) | ✅ shipped\* | W2 on `feat/profile-axis-catalog-w2` (union VO + resolver branch + platform ProfileAxis chain + kind-first inline find-or-create + athlete picker re-key + steer); **\*pending GATED api-server run + owner-smoke → merge**; D-3 RATIFIED, D-6 ratified |
| 3   | Athlete two-layer profile | ⬜ pending   | curated picker + write-back by kind + selections migration                                                                                                                                                                                              |

## Next action

**▶ Owner: GATED api-server run → owner-smoke on dev → merge the W2 PR; then W3.** W2 is built + reviewed + QA'd on `feat/profile-axis-catalog-w2` (commit arc: `67d9f143` gate docs · `a4592076` contract union · `3cefc3cc` resolver+gender ctx · `18769df5` platform profile-axis chain · `c98092c7` kind-first authoring · `ae950c80` athlete picker+steer · `e81ebb47` coach-prose · `cc5994ed` review/qa fixes · + the Stage 7-8 test commit + this close-out docs commit). Remaining (owner-side): (1) the GATED api-server suite (~10 min serial, live Neon — memory `api-server-serial-tests`) with the added authz + resolver cases (PAC-14) on your ok; (2) owner-smoke on dev platform (port 3001 → a plan → load editor → By profile → Training axis: pick/create an axis, fill cells; Athlete attribute: gender auto-seeds; athlete session resolves a gendered load with NO pick + a catalog pick inline) → merge. **Then W3 (athlete two-layer profile): athletic-card curated picker over the catalog + write-back by kind + the bulk `profileSelections` key migration (PAC-2).** Optionally run the PAC-1 prod probe before/after merge (owner-gated; author the backfill only if non-empty).

## Open decisions awaiting ratification

- **(none open.)** D-3 is RATIFIED — the gate is DISCHARGED (`four-projection-recheck.md` PASSED + the plan-editor-compose `D-BYPROFILE-AXIS` cross-ref RATIFIED). D-6 (the W2 execution calls — two unresolved arms, shared create fn, empty-`axisId` draft, full inline find-or-create) is RATIFIED. W3 may surface new decisions (the curated-picker UX; confirming human-attribute axes are hidden from the athletic card).

## Live carry-forwards (see deferred.md)

**OPEN:** PAC-1 byProfile-load migration (SPEC'd W2 — owner-gated prod probe + backfill) · PAC-2 `profileSelections` key migration (W3; W2 already re-keyed the resolver read + inline-picker write) · PAC-3 masters/age as a human attribute (default: custom axis) · PAC-4 `ProfileAxisValue` table (`String[]` now) · PAC-5 per-coach axis scoping (global now) · PAC-6 full-cartesian cells UX · PAC-10 future `library` bounded context (low) · PAC-11 `TagsInput` raw-vs-NFKC dedup (low — W2 reused it in the create-form) · **PAC-12 W2↔W3 intermediate-red (name-keyed selections inert; profile card shows raw cuids until W3 — owner-accepted) · PAC-13 QA-006 `regenerateCells` kg-carry on rebind (low) · PAC-14 the GATED api-server authz/resolver tests must run green before merge (owner batch)**.
**CLOSED (W2):** PAC-7 (four-projection gate — PASSED + cross-ref) · PAC-8 (single NFKC law via snapshot + bind-by-`axisId`) · PAC-9 (tolerate-orphan via denormalization).

## Gotchas a resuming session must know

- **gender is NOT absorbed** (D-1) — it stays a typed column with 7 coach-facing read sites; the catalog holds training-classification axes ONLY. The human arm (`{kind:human,attribute:gender}`) reads the typed column; gender is NEVER written into `profileSelections`, never an inline pick. The human/training split IS the point.
- **The resolver never reads `ProfileAxis`** — resolution uses the load VO (the denormalized `axisId`+`label`+`values` snapshot + `cells`) + the profile's `profileSelections`/`gender`; the catalog GET fires only in the coach editor. This keeps the lms→coaching boundary clean (dep:check green) and the athlete hot path query-free. The boundary const `GENDER_AXIS_COORDS` is lms-local in `load.ts`; the resolver uses a LOCAL gender→coord map (NOT `mappers/coaching` — `api-server-lms-no-coaching`).
- **W2↔W3 intermediate-red is LIVE + owner-accepted** (PAC-12) — after W2, legacy `profileSelections` keyed by axis NAME are inert (the resolver reads by `axisId`); they resolve to `missing_profile_pick` until re-picked inline or W3-migrated. The profile card (`ProfilePicksCard`) renders raw `axisId` cuids until W3 re-labels. Dev is near-empty (seeds zero selections) so this is negligible in practice; no corruption (keys coexist, resolver reads only `axisId`).
- **The PAC-1 migration is SPEC-ONLY** (deferred.md PAC-1 detail) — dev has ZERO byProfile loads; the prod probe + backfill are OWNER-GATED (probe table = `training_schema_rows`, NOT `app_schema_rows`). Do NOT connect to prod from this work; author the script only on a non-empty probe.
- **The api-server suite is GATED** (PAC-14, memory `api-server-serial-tests`) — ~10 min serial against live Neon, owner-approved batch; NOT pre-push (cone-only). The W2 authz (`profileAxisPlatformApi` role-gate + conflict + ordering) + the resolver edge cases (all-human cell-miss, stale-name-key) live there — run green before merge. Do NOT read green check-types as "feature verified."
- **athlete-core's deferred profile-type catalog is PROMOTED here** — the gender→resolver wiring (`D-FIELDS-GENDER-INERT`) is W2; the curated-picker (`D-PROF-SELECTIONS-HONEST` unblock) is W3.
