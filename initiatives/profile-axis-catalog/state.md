# profile-axis-catalog — state (the board)

**Updated:** 2026-06-24 (W3-core BUILT — executor session). **W3-core (D-9) is BUILT** on `feat/profile-axis-w3-core` (6 commits, full `/feature`): the catalog-backed curated athletic-card picker (re-labels `axisId`→catalog `label`, closes PAC-12's render half; proactive pick-groups over `binding=null` axes; writes `profileSelections[axisId]`; HIDES bound gender; tolerates orphans) + the athlete-gated catalog read (`GET /api/platform/athlete/profile-axes`, `binding=null` server-filter, the COACH endpoint auth UNTOUCHED) + the PAC-2 `profileSelections` key migration (authored, dev-dry-run clean). check-types/lint/dep:check(0)/platform-vitest(1280) GREEN; **guardrails ZERO-diff** (git-verified); NO Prisma migration. **PENDING owner-gated:** the gated api-server test run (`listForAthlete` + the PAC-2 transform tests — written, not run) + the **PAC-2 prod-snapshot dry-run + cutover** (D-8). **Next = those owner-gated steps, THEN the W3.1/PAC-17 executor session** (`w3-pac17-feature-prompt.md`, D-10). This was a PER-WAVE update; the FULL `/initiative-close` runs after PAC-17.

**W2 (design A) is RE-APPLIED, MERGED (#313), and PROD-CONFIRMED working.** The #309→#311 incident is closed at the root: this time the VO cut shipped WITH the **PAC-1 data-migration** + a prod-snapshot dry-run (the **D-8** law). The 13 prod `byProfile` loads were rewritten `{name,values}`→`{axisId,label,values,binding}` (gender coords remapped `m/f`→`Male/Female`; the owner-identified `CAL` folded into the system Gender axis), one transaction + post-write re-validation 13/13; the catalog now holds exactly **2 axes** — system `Gender` (`cgender000000000000000000`, `binding=GENDER`) + plain `level` (`cmqrp2xdy0000sot4u5l3z5wq`, `binding=null`). Owner smoke confirmed: gender resolves from the typed column (no manual pick), design-A loads resolve. Cutover = Option A maintenance window (PAC-19); the ~64s deploy→backfill window saw zero traffic → zero real 500s. **W1 + W2 DONE; next = W3.** (Tangential earlier this day: observability fix **PR #312** — `fa357632`, prod-confirmed; memory `[[nextjs-serverless-di-observability]]`.)

## Board

| #   | Step                                    | Status    | Pointer                                                                                                                                                                                                                                                                   |
| --- | --------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0   | Found + ontology lock                   | ✅ done   | charter · decisions D-1/D-2 · journal 2026-06-22                                                                                                                                                                                                                          |
| 1   | Catalog + admin                         | ✅ merged | W1 — PR #304 (model+contract+endpoints+admin+carve-out+tests+docs; migration `20260622113153_add_profile_axes`)                                                                                                                                                           |
| 2   | Coach binding (sacred-VO)               | ✅ done   | W2 design A — built (#309), reverted on incident (#311), **RE-APPLIED + prod-confirmed (#313 → `a7ac9a63`)** with the PAC-1 backfill (D-8). 2 axes live                                                                                                                   |
| 3   | Athlete two-layer profile (W3-core)     | 🟡 BUILT  | D-9 built on `feat/profile-axis-w3-core` (6 commits): catalog-backed picker + athlete-gated read (D9-2) + PAC-2 authored (dev-dry-run clean). Green + guardrails zero-diff; NO migration. **Pending owner-gated:** the api-server test run + the PAC-2 prod cutover (D-8) |
| 3.1 | Inline-set gender from session (PAC-17) | ⬜ next   | D-10: decision-first DISCHARGED · typed-column write-affordance (NOT `profileSelections`) · `w3-pac17-feature-prompt.md` · **the next executor session** (after W3-core's owner-gated steps)                                                                              |

## Next action

**▶ The W3-core owner-gated steps, THEN the W3.1/PAC-17 executor session.** W3-core (D-9) is BUILT on `feat/profile-axis-w3-core` (the athlete-gated read + the catalog-backed picker + the PAC-2 migration; green + guardrails zero-diff). Before/at merge: **(1) the gated api-server test run** (~10 min serial, live Neon — the new `listForAthlete` endpoint test + the PAC-2 `profile-selections-rekey` transform unit test are WRITTEN, NOT RUN; memory `api-server-serial-tests`) · **(2) the PAC-2 prod-snapshot DRY-RUN + the `--write` cutover** (D-8 / `prod-data-inviolable` — the script is authored + dev-dry-run clean; on prod today 1 athlete gender-keyed only → 0 inert-risk → drops one dead gender key; **DECIDE at the cutover the create-path for an orphan name-key — PAC-21**). **Then** the **W3.1/PAC-17** fast-follow executor session (`w3-pac17-feature-prompt.md`, D-10) — inline gender-set from the session (typed-column write-affordance). After PAC-17 → the FULL `/initiative-close`.

## Open decisions awaiting ratification

- **(none open.)** D-7 (design A) RATIFIED + re-applied. D-8 (re-apply law) ACTED. D-9 (W3-core shape) **ACTED — W3-core BUILT this session** (`feat/profile-axis-w3-core`). D-10 (PAC-17 + four-projection note) RATIFIED, the build pending (W3.1). PAC-19 RESOLVED. D-3 SUPERSEDED. The four-projection gates are DISCHARGED (`four-projection-recheck.md` for W2; D-10's bound-arm EXECUTE note for PAC-17). **OWNER-gated steps left:** (a) the W3-core gated api-server test run (PAC-20) · (b) the PAC-2 prod-snapshot DRY-RUN + cutover (D-8), incl. the create-path-for-an-orphan-name-key decision (PAC-21). The W3-core athletic-card UX MOCKS were owner-shown during the build.

## Live carry-forwards (see deferred.md)

**W3-core owner-gated (D-9 BUILT, these remain):** PAC-2 the `profileSelections` bulk key migration — BUILD DONE (transform + script, dev-dry-run clean), the prod DRY-RUN + cutover OWNER-gated (D-8) · PAC-20 the gated api-server test run (`listForAthlete` + the PAC-2 transform tests — written, not run) · PAC-21 the PAC-2 create-path for an orphan name-key + QA-304 (an OPEN question for the cutover; co-owner rec = FLAG, don't auto-create). **W3.1 (D-10):** PAC-17 inline-set bound gender attribute (the next executor session, decision-first DISCHARGED).
**Open (low / later):** PAC-3 masters/age · PAC-4 `ProfileAxisValue` table · PAC-5 per-coach scoping · PAC-6 cells UX · PAC-10 `library` context · PAC-11 `TagsInput` dedup · PAC-15 admin protected-row edit-VIEW · PAC-16 migration system-row skip edge.
**Closed:** PAC-1 (the byProfile backfill — DONE at the #313 cutover, 13/13) · PAC-7 (four-projection gate + cross-ref re-added) · PAC-8 (single NFKC law) · PAC-9 (tolerate-orphan) · PAC-12 (W3-core — D9-1 re-label closes the raw-cuid render; PAC-2 authored; 0 prod inert-risk) · PAC-13 (kg-carry) · PAC-14 (the W2 gated tests — ran green via CI at #313; W3-core's are PAC-20) · PAC-18 (orphans reconciled — the migration is back in the repo + applied; the system Gender row is now the legit live axis) · PAC-19 (Option A executed).

## Gotchas a resuming session must know

- **W1 + W2 are LIVE on prod.** The `byProfile` VO is the flat design-A `{axisId,label,values,binding}`; the resolver branches on the snapshot's `binding` (`GENDER`→typed column, `null`→`profileSelections[axisId]`). Catalog = 2 axes (system `Gender` + plain `level`). Do NOT reopen D-7.
- **The system Gender row is now legit** (`cgender000000000000000000`, protected — admin shows it read-only via the design-A code). PAC-18 is closed (no longer an orphan: the migration is in the repo + applied; the row is the real system axis).
- **W3-core is BUILT (not yet merged)** on `feat/profile-axis-w3-core` (6 commits): the athlete-gated read + the catalog-backed `ProfilePicksCard` picker + the PAC-2 migration. Green; guardrails ZERO-diff; NO Prisma migration (binding/enum/`profileSelections` all pre-existed). The picker re-labels `axisId`→catalog `label`, renders proactive pick-groups over `binding=null` axes, writes `profileSelections[axisId]` via the existing PUT, HIDES bound gender, tolerates orphans. `ProfilePickRow` was DELETED → `ProfilePickGroup` + a LOCALIZED `ProfileOptionButton` (NOT imported from athlete-session).
- **The athlete catalog read exists now** — `GET /api/platform/athlete/profile-axes` (`withAthleteAuth` + rate-limit, `binding=null` server-filter, `label asc`; `profileAxisPlatformApi.listForAthlete`, NO in-method role gate — the route auth IS the gate, D9-2/DR-2). The COACH endpoint (`GET /api/platform/profile-axes`, coach-gated) is UNTOUCHED — the diff to `endpoints/coaching/profile-axis.ts` is purely additive. Do NOT broaden the coach endpoint's auth.
- **PAC-2 is BUILT but NOT yet run on prod.** The transform (`utils/profile-selections-rekey.ts`) + the script (`scripts/backfill-profileselections-rekey.ts`) are authored; dev dry-run CLEAN (0 selections). The prod-snapshot DRY-RUN + the `--write` cutover are OWNER-gated (D-8). On prod today: 1 athlete, gender-keyed only → 0 inert-risk → the cutover just drops one dead gender key. **OPEN at the cutover (PAC-21):** the create-path for an ORPHAN name-key (no matching catalog axis) currently find-or-creates with `values:[name]` (a placeholder) — co-owner rec: FLAG for owner review rather than auto-create (the catalog is coach-owned, D-2). Plus QA-304 (low): find-or-create writes axes OUTSIDE the `$transaction`.
- **The gated api-server tests for W3-core are written, NOT run (PAC-20)** — the `listForAthlete` endpoint test + the PAC-2 transform unit test (~10 min serial, live Neon). Must run green before/at merge. (The QA-301 fix lives here: zod `z.string().cuid()` is a LOOSE regex that misclassified real axis names — "Conditioning"/"Competition" — as cuids; the classifier uses a strict `/^c[a-z0-9]{24}$/`.)
- **PAC-17 needs NO `ResolvedLoad`/contract change** (D-10 correction): the inline gender picker reads the bound axis's `values`+`axisId` from `row.load`, exactly as the plain picker reads `load.axes`. The inline gender pick MUST write the typed `gender` column, NEVER `profileSelections` (the four-projection safety).
- **gender stays a typed column** (D-1) — the bound axis READS it, never stores a gender value in the catalog/`profileSelections`.
- **The backfill script is one-time + idempotent** (`packages/api-server/scripts/backfill-byprofile-reapply.ts`): re-running skips already-migrated loads. The original 13 loads were backed up at cutover (gitignored scratch). Not needed again.
