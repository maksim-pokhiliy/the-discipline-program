# profile-axis-catalog — state (the board)

**Updated:** 2026-06-24 (W3 PLANNED — planner session). **W3 is now fully planned + decisioned: split into W3-core** (the catalog-backed athletic-card picker + an athlete-gated catalog read + the PAC-2 `profileSelections` migration) **+ W3.1/PAC-17** (inline gender-set from the session, fast-follow). Decisions **D-9** (W3-core shape) + **D-10** (PAC-17 + its four-projection note) RATIFIED; executor prompts committed (`w3-feature-prompt.md`, `w3-pac17-feature-prompt.md`). The recon surfaced one gap the prior waves left: the catalog read is COACH-gated → W3-core adds an athlete read. **Next = the W3-core executor session.**

**W2 (design A) is RE-APPLIED, MERGED (#313), and PROD-CONFIRMED working.** The #309→#311 incident is closed at the root: this time the VO cut shipped WITH the **PAC-1 data-migration** + a prod-snapshot dry-run (the **D-8** law). The 13 prod `byProfile` loads were rewritten `{name,values}`→`{axisId,label,values,binding}` (gender coords remapped `m/f`→`Male/Female`; the owner-identified `CAL` folded into the system Gender axis), one transaction + post-write re-validation 13/13; the catalog now holds exactly **2 axes** — system `Gender` (`cgender000000000000000000`, `binding=GENDER`) + plain `level` (`cmqrp2xdy0000sot4u5l3z5wq`, `binding=null`). Owner smoke confirmed: gender resolves from the typed column (no manual pick), design-A loads resolve. Cutover = Option A maintenance window (PAC-19); the ~64s deploy→backfill window saw zero traffic → zero real 500s. **W1 + W2 DONE; next = W3.** (Tangential earlier this day: observability fix **PR #312** — `fa357632`, prod-confirmed; memory `[[nextjs-serverless-di-observability]]`.)

## Board

| #   | Step                                    | Status         | Pointer                                                                                                                                                                      |
| --- | --------------------------------------- | -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0   | Found + ontology lock                   | ✅ done        | charter · decisions D-1/D-2 · journal 2026-06-22                                                                                                                             |
| 1   | Catalog + admin                         | ✅ merged      | W1 — PR #304 (model+contract+endpoints+admin+carve-out+tests+docs; migration `20260622113153_add_profile_axes`)                                                              |
| 2   | Coach binding (sacred-VO)               | ✅ done        | W2 design A — built (#309), reverted on incident (#311), **RE-APPLIED + prod-confirmed (#313 → `a7ac9a63`)** with the PAC-1 backfill (D-8). 2 axes live                      |
| 3   | Athlete two-layer profile (W3-core)     | ⬜ next        | D-9: catalog-backed athletic-card picker + athlete-gated read (D9-2) · re-label `axisId`→label (PAC-12) · PAC-2 `profileSelections` migration (D-8) · `w3-feature-prompt.md` |
| 3.1 | Inline-set gender from session (PAC-17) | ⬜ fast-follow | D-10: decision-first DISCHARGED · typed-column write-affordance (NOT `profileSelections`) · `w3-pac17-feature-prompt.md`                                                     |

## Next action

**▶ W3-core executor session** (gate-light — no sacred-VO touch; the design is fully decisioned in **D-9**). Run `w3-feature-prompt.md`: (1) an athlete-gated catalog READ (`GET /api/platform/athlete/profile-axes`, `binding=null` server-filter — the coach endpoint is COACH-gated, the athlete would 403) · (2) EXTEND `ProfilePicksCard` into the catalog-backed curated picker — re-label `axisId`→catalog `label` (closes PAC-12 render), proactive pick-groups over `binding=null` axes, write `profileSelections[axisId]` via the existing PUT, HIDE bound (gender) axes, tolerate orphans · (3) **PAC-2** the bulk `profileSelections` key migration (D-8: probe→idempotent backfill→prod-snapshot dry-run→gated cutover). UI on MOCKS first (athlete-POV), owner-approve before wiring. **Then** the **W3.1/PAC-17** fast-follow (`w3-pac17-feature-prompt.md`, D-10) — inline gender-set from the session (typed-column write-affordance). After PAC-17 → `/initiative-close`.

## Open decisions awaiting ratification

- **(none open.)** D-7 (design A) RATIFIED + re-applied. D-8 (re-apply law) ACTED. **D-9 (W3-core shape) + D-10 (PAC-17 + four-projection note) RATIFIED this session.** PAC-19 RESOLVED. D-3 SUPERSEDED. The four-projection gates are DISCHARGED (`four-projection-recheck.md` for W2; D-10's bound-arm EXECUTE note for PAC-17). The only OWNER-gated step left is the PAC-2 prod-snapshot DRY-RUN + cutover (inside the W3-core executor run, D-8) — and the W3-core athletic-card UX MOCK approval (athlete-POV, before wiring).

## Live carry-forwards (see deferred.md)

**W3-core (D-9):** PAC-2 `profileSelections` bulk key migration (under D-8) · PAC-12 the `ProfilePicksCard` raw-cuid render + intermediate-red (closed by D9-1 re-label + PAC-2; on prod **0 inert-risk** — the one athlete's selections are gender-keyed, resolved from the typed column). **W3.1 (D-10):** PAC-17 inline-set bound gender attribute (fast-follow, decision-first DISCHARGED).
**Open (low / later):** PAC-3 masters/age · PAC-4 `ProfileAxisValue` table · PAC-5 per-coach scoping · PAC-6 cells UX · PAC-10 `library` context · PAC-11 `TagsInput` dedup · PAC-15 admin protected-row edit-VIEW · PAC-16 migration system-row skip edge.
**Closed:** PAC-1 (the byProfile backfill — DONE at the #313 cutover, 13/13) · PAC-7 (four-projection gate + cross-ref re-added) · PAC-8 (single NFKC law) · PAC-9 (tolerate-orphan) · PAC-13 (kg-carry) · PAC-18 (orphans reconciled — the migration is back in the repo + applied; the system Gender row is now the legit live axis) · PAC-19 (Option A executed).

## Gotchas a resuming session must know

- **W1 + W2 are LIVE on prod.** The `byProfile` VO is the flat design-A `{axisId,label,values,binding}`; the resolver branches on the snapshot's `binding` (`GENDER`→typed column, `null`→`profileSelections[axisId]`). Catalog = 2 axes (system `Gender` + plain `level`). Do NOT reopen D-7.
- **The system Gender row is now legit** (`cgender000000000000000000`, protected — admin shows it read-only via the design-A code). PAC-18 is closed (no longer an orphan: the migration is in the repo + applied; the row is the real system axis).
- **PAC-2 is the live W3 debt.** The resolver reads `profileSelections[axisId]`; legacy name-keyed picks are inert until W3 re-keys them. On prod today: only 1 athlete, gender-keyed only → 0 inert-risk. A NEW non-gender pick made before W3 would be inert — W3-core closes this.
- **The catalog read is COACH-gated** (`GET /api/platform/profile-axes` = `withCoachAuth` + `requireCoachLikeRole`, built for the coach load-editor). The athlete profile screen runs under `withAthleteAuth` → it would 403 on that endpoint. **W3-core adds a dedicated athlete read** (`GET /api/platform/athlete/profile-axes`, `binding=null` server-filter; D9-2) — do NOT broaden the coach endpoint's auth.
- **PAC-17 needs NO `ResolvedLoad`/contract change** (D-10 correction): the inline gender picker reads the bound axis's `values`+`axisId` from `row.load`, exactly as the plain picker reads `load.axes`. The inline gender pick MUST write the typed `gender` column, NEVER `profileSelections` (the four-projection safety).
- **gender stays a typed column** (D-1) — the bound axis READS it, never stores a gender value in the catalog/`profileSelections`.
- **The backfill script is one-time + idempotent** (`packages/api-server/scripts/backfill-byprofile-reapply.ts`): re-running skips already-migrated loads. The original 13 loads were backed up at cutover (gitignored scratch). Not needed again.
