# profile-axis-catalog — deferred

Carry-forwards with disposition + status. `OPEN` (live) · `SCHEDULED` (assigned to a step) · `CLOSED` · `DROPPED`.

| ID     | One-liner                                                                                                                                                                                                                                                                                      | Disposition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Status                      |
| ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------- |
| PAC-1  | Existing `byProfile` loads use free-string axis `name`; need migration to the flat `{axisId,label,values,binding}` shape                                                                                                                                                                       | **⚠️ ESCALATED — this UNRUN migration CAUSED the #309→#311 prod incident** (the VO was cut without it → pre-W2 loads 500'd). NOW MANDATORY before re-apply: run it + a prod-snapshot DRY-RUN before merge (D-8). Probe SQL + backfill map below                                                                                                                                                                                                                                                                           | OPEN (re-apply gate)        |
| PAC-2  | `profileSelections` keys are free strings; re-home to `axisId`                                                                                                                                                                                                                                 | **BUILD DONE (W3-core, D9-3): the transform `utils/profile-selections-rekey.ts` + the script `scripts/backfill-profileselections-rekey.ts` are authored; dev dry-run CLEAN (0 selections).** The prod-snapshot DRY-RUN + the `--write` cutover are the remaining OWNER-gated step (D-8 / `prod-data-inviolable`). NB: W2 already re-keyed the resolver READ + the inline-picker WRITE by `axisId`; this is the bulk legacy-key migration. Backfill map below                                                              | OPEN (owner — prod cutover) |
| PAC-3  | masters/age as a human attribute (dob → auto age-group resolution)                                                                                                                                                                                                                             | default: masters is a CUSTOM catalog axis; promote to a human attribute only if Denys runs age-resolution                                                                                                                                                                                                                                                                                                                                                                                                                 | OPEN                        |
| PAC-4  | `ProfileAxisValue` as its own table                                                                                                                                                                                                                                                            | deferred — `String[]` on the axis until cardinality / per-value metadata demands it                                                                                                                                                                                                                                                                                                                                                                                                                                       | OPEN                        |
| PAC-5  | Per-coach axis scoping                                                                                                                                                                                                                                                                         | global catalog now (single-coach); revisit at multi-tenant                                                                                                                                                                                                                                                                                                                                                                                                                                                                | OPEN                        |
| PAC-6  | Full-cartesian `cells` authoring burden grows with axis-value count                                                                                                                                                                                                                            | existing `loadSchema` constraint, not new; watch the coach UX as level gains values                                                                                                                                                                                                                                                                                                                                                                                                                                       | OPEN                        |
| PAC-7  | plan-editor-compose four-projection re-check + cross-ref decision for the VO change                                                                                                                                                                                                            | DONE — `four-projection-recheck.md` (PASSED) + the plan-editor-compose `D-BYPROFILE-AXIS` cross-ref (RATIFIED)                                                                                                                                                                                                                                                                                                                                                                                                            | CLOSED                      |
| PAC-8  | W2 validation-law reconciliation: catalog value-normalization (NFKC+strip, stricter than `load.ts` bare-trim) + key case-folding (`key` case-sensitive; a picker keyed on `axis.key` risks a `level`/`Level` fork)                                                                             | RESOLVED by the denormalize + bind-by-`axisId` design: catalog values are a snapshot of the already-NFKC'd catalog values (single law); binding by `axisId` (cuid) makes `level`/`Level` two visible rows, never a silent fork — no `keyLower` needed (gate §7)                                                                                                                                                                                                                                                           | CLOSED                      |
| PAC-9  | Axis-delete referential policy — once a load references an axis (no FK per D-2), delete silently orphans the reference                                                                                                                                                                         | RESOLVED = TOLERATE-ORPHAN: the load denormalizes label+values+cells, so a deleted axis leaves the referencing load fully rendering + resolving off its snapshot; no restrict-on-reference, no reference-scan (gate §7)                                                                                                                                                                                                                                                                                                   | CLOSED                      |
| PAC-10 | Dedicated `library`/`catalog` bounded context for admin-managed global catalogs (labels/exercises/modifiers/profile-axes split across lms/coaching today)                                                                                                                                      | deferred — research Option 2; W1 used a file-precise dep-cruiser carve-out (D-5) instead; revisit if catalog count / cross-cutting grows                                                                                                                                                                                                                                                                                                                                                                                  | OPEN (low)                  |
| PAC-11 | `TagsInput` widget dedups raw strings, the contract dedups NFKC → a normalization-equal value pair passes the widget then rejects on submit without pinpointing                                                                                                                                | minor UX; shared `@repo/ui` leaf (also exercises). **W2 reused `TagsInput` in the inline create-form** → still live, still low                                                                                                                                                                                                                                                                                                                                                                                            | OPEN (low)                  |
| PAC-12 | W2↔W3 intermediate-red: legacy name-keyed `profileSelections` are inert until re-picked/W3-migrated; the profile card (`ProfilePicksCard`) renders raw `axisId` cuids until W3 re-labels                                                                                                      | **CLOSED (W3-core).** D9-1 re-labels the card (`axisId`→catalog `label`, `c8f2dc23`/`43035471`) → no more raw cuid; PAC-2 (the keys) authored + dev-dry-run clean. On prod 0 inert-risk (the one athlete's selections are gender-keyed, resolved from the typed column). The data half fully drops only at the PAC-2 prod cutover, but the render is fixed and there is nothing inert to migrate on prod today.                                                                                                           | CLOSED (W3-core)            |
| PAC-13 | QA-006 residual: `regenerateCells` carries kg across a rebind-to-different-axis when coord value-strings collide (stale pre-fill on a freshly-switched dimension)                                                                                                                              | minor; coach reviews the grid before save + the snapshot/superRefine prevent an invalid load reaching the athlete — never silently delivered. Optionally clear kg on a `changeKind`/rebind                                                                                                                                                                                                                                                                                                                                | OPEN (low)                  |
| PAC-14 | The GATED api-server authz/resolver tests (`profileAxisPlatformApi` role-gate + conflict + ordering; resolver all-human cell-miss + stale-name-key) must run green before the W2 merge                                                                                                         | **CLOSED.** These were the W2 cases; they ran green via CI at the #313 re-apply (the root `vitest.config.ts` runs the `api-server` project — journal 2026-06-24). The W3-core gated tests are tracked separately as PAC-20.                                                                                                                                                                                                                                                                                               | CLOSED                      |
| PAC-20 | The GATED api-server tests for W3-core (`listForAthlete` endpoint + the PAC-2 `profile-selections-rekey` transform unit test) are WRITTEN (`58743c3d`) but NOT RUN                                                                                                                             | owner-gated (~10 min serial, live Neon — memory `api-server-serial-tests`; like PAC-14 was for W2). Must run green before/at the W3-core merge. Platform vitest is already GREEN (1280); this is only the api-server slice.                                                                                                                                                                                                                                                                                               | OPEN (owner)                |
| PAC-21 | PAC-2 create-path for an ORPHAN name-key (a legacy name with NO matching catalog axis): find-or-create writes `values:[name]` — a PLACEHOLDER (the created axis gets its own NAME as its only value, not the picked value). Plus QA-304: find-or-create writes axes OUTSIDE the `$transaction` | **OPEN question for the PAC-2 cutover.** UNREACHED on current prod (gender-only selections). **Recommendation (co-owner): FLAG an orphan name-key for owner review rather than auto-create** — the catalog is coach-owned (D-2); auto-creating a single-value axis from an athlete pick is backwards — OR fix the create to use the picked value(s). Owner decides at the cutover. **QA-304** (low): a failed out-of-transaction create orphans benign `binding=null` rows (deletable, PAC-9) — document for the cutover. | OPEN (owner — cutover)      |

## Detail on the live ones

**PAC-1 (the migration — SPEC'd W2, owner-gated run).** W2 flipped the `byProfile` VO to the union; legacy free-string loads (`axes:[{name,values}]`) are now invalid until backfilled (aggressive cut, no both-shapes bridge — owner rules `aggressive-migration-no-bridge` / `migration-intermediate-velocity`). **Dev has ZERO byProfile loads** (`seed.ts` = users+profiles only, memory `seed-architecture`), so dev needs no backfill — the intermediate-red is the accepted aggressive path. The **prod** probe + backfill are an OWNER-GATED step (do NOT connect to prod from this work; author the script only on a non-empty probe).

- **Probe (owner runs against prod).** Correct table = `training_schema_rows` (verified `schema.prisma:696` — research's `app_schema_rows` was WRONG; the `load` Json column lives on `training_schema_rows`):
  ```sql
  SELECT id, load FROM training_schema_rows WHERE load->>'kind' = 'byProfile';
  ```
  Prisma equivalent: `prisma.schemaRow.findMany({ where: { load: { path: ["kind"], equals: "byProfile" } } })`.
- **Backfill map (per legacy load's axis `{name, values}`) — REVISED for design A (D-7); the union arms below are superseded by the flat `{axisId,label,values,binding}` snapshot.**
  - `name` ∈ {gender, sex} (owner judgment) → snapshot the SYSTEM Gender row: `{ axisId:"cgender000000000000000000", label:"Gender", values:["Male","Female"], binding:"GENDER" }` (verify the legacy values map to Male/Female; the athlete then resolves from the typed column, no pick).
  - else → find-or-create a `ProfileAxis` by `key`/`label` matching `name` (create from `{ key:name, label:name, values }` if none — created rows are `binding=null`) → `{ axisId, label, values, binding:null }` (snapshot from the catalog row).
- **Properties.** Idempotent (run once); tolerate-orphan (PAC-9 — no FK; the snapshot keeps the load functional). Because the catalog denormalizes label+values+cells, the rewrite is purely additive. The matching W3 step re-keys `profileSelections` (PAC-2).

**PAC-2 (the `profileSelections` key migration — W3-core, D9-3, under D-8).** Legacy `profileSelections` keys are free-string axis NAMES; the resolver + the W2 inline-picker read/write by `axisId`. Re-home the keys. This is a PROD data-migration → **D-8 / `prod-data-inviolable`: probe prod → idempotent backfill → DRY-RUN vs a prod snapshot → gated cutover. NO "merge then fix" (the #309 lesson).** **BUILT in W3-core:** a pure transform `packages/api-server/src/utils/profile-selections-rekey.ts` + the script `packages/api-server/scripts/backfill-profileselections-rekey.ts` (probe → audit → dry-run by default → `--write` backup + `$transaction` + re-validate; REFUSES to write on any flag). The byProfile template's `auditProfileSelections` pre-flight informed it. Dev dry-run CLEAN (0 selections). The prod dry-run + the `--write` cutover are OWNER-gated.

- **Target.** `AthleteProfile.profileSelections` (Json on `app_athlete_profiles`). Probe: `prisma.athleteProfile.findMany({ select: { id:true, gender:true, profileSelections:true } })`, filter non-empty.
- **Backfill map (per key in each profile's `profileSelections`).**
  - key is already a cuid (`axisId`) → **already migrated, SKIP** (idempotent re-run).
  - key ∈ gender tokens {gender, sex, …} → **DROP** (gender is intrinsic — it lives in the typed `gender` column, D-1; this selection is DEAD, the resolver reads the typed column for `binding=GENDER`). **Guard:** only drop when the typed `gender` is already set; if `gender=null` AND a gender-keyed selection exists, do NOT silently drop — FLAG it in the dry-run for owner review (no data loss).
  - else (a real training-axis name) → find-or-create a `ProfileAxis` by `key`/`label` matching the name (per-name, like the byProfile backfill) → re-key `{ axisId: value }`, value preserved.
- **Properties.** Idempotent · dry-run by default · `--write` backs up originals + one transaction + post-write re-validation · tolerate-orphan (PAC-9).
- **Prod reality today** (journal 2026-06-24): 1 athlete, gender-keyed only → **0 inert-risk** → the cutover just drops one dead gender key. Near-empty — but authored with the FULL D-8 discipline (this is exactly the "near-empty bet" that caused #309).
- **OPEN at the cutover — the create-path for an ORPHAN name-key (→ PAC-21).** The `else` (real training-axis name) branch does find-or-create; when there is NO matching catalog axis it CREATES one with `values:[name]` — a placeholder (the new axis gets its own NAME as its only value, NOT the picked value). UNREACHED on current prod (gender-only). **Co-owner recommendation: FLAG such an orphan name-key for owner review rather than auto-create** (the catalog is coach-owned, D-2; auto-creating a single-value axis from an athlete pick is backwards), OR fix the create to use the picked value(s). Owner decides at the cutover. **QA-304** (low): find-or-create writes axes OUTSIDE the `$transaction`, so a failed write orphans benign `binding=null` rows (deletable, PAC-9).

(PAC-7 — the four-projection gate — is now CLOSED: `four-projection-recheck.md` PASSED + the plan-editor-compose `D-BYPROFILE-AXIS` cross-ref RATIFIED.)

**PAC-3 (age/masters — the one genuinely-ambiguous attribute).** dob is human, masters-division is a coach classification. Owner default (2026-06-22): keep masters a custom catalog axis; do NOT add a `dob` human attribute until auto age-resolution is actually needed. Revisit if Denys runs age-based load routinely.

**PAC-8 / PAC-9 (W1 → W2 hand-off — CLOSED by the W2 design).** Both dissolved as a BENEFIT of the denormalize + bind-by-`axisId` design (gate §7), not by adding machinery:

- **PAC-8** — the byProfile load no longer free-types axis values; catalog values are a snapshot of the catalog's already-`normalizedString` (NFKC + zero-width-strip) values, so the catalog's NFKC law is the single law (`load.ts`'s bare `.trim()` is defensive only). The find-or-create picker binds by `axisId` (cuid), NOT `key`/`label` string → a `level`/`Level` split is two visible, distinct rows the coach chooses between, never a silent fork. No `keyLower` needed (D-4 carry-forward closed).
- **PAC-9** — TOLERATE-ORPHAN. No FK (D-2). Because the load denormalizes label+values+cells, a deleted axis leaves the referencing load fully RENDERING (snapshot label) and RESOLVING (cells + selection by `axisId`); the only losses are re-pickability + an orphan-labeled analytics group. No restrict-on-reference, no reference-scan-on-delete.

## Closed history

- **PAC-7** — CLOSED 2026-06-22 (W2 gate). `four-projection-recheck.md` PASSED + plan-editor-compose `D-BYPROFILE-AXIS` cross-ref RATIFIED.
- **PAC-8** — CLOSED 2026-06-22 (W2). Single NFKC law via the catalog snapshot; bind-by-`axisId` kills the case-fold fork (no `keyLower`).
- **PAC-9** — CLOSED 2026-06-22 (W2). Tolerate-orphan; denormalization makes delete intrinsically safe.

## 2026-06-23 — design A revision (W2) deltas

The W2 union (D-3) was REVISED to design A (D-7): gender = a `binding`-bound system catalog row; the byProfile axis is a flat `{axisId,label,values,binding}`. Carry-forward changes:

- **PAC-1** — backfill map REVISED above (the union arms → the flat snapshot; `name∈{gender,sex}` → the system Gender `axisId`). Still OPEN, owner-gated prod run.
- **PAC-13** — CLOSED 2026-06-23. QA confirmed `replaceDimension` passes empty `previousCells`, so no stale kg carries across a dimension rebind; the snapshot+superRefine also prevent an invalid load reaching the athlete. No code change needed.
- **PAC-14** — the GATED api-server suite (now the resolver-on-binding + CRUD-protection + system-row-`values` invariant cases) must run green before the merge. Owner-batch, ~10 min serial.
- **PAC-15** — NEW (low). The admin protected-row affordance blocks the LIST (System chip, no edit/delete, no edit-link); a hand-typed `/profile-axes/<id>` still loads an editable form that the server `assertNotProtected` 403s on save. Making the edit VIEW itself read-only is W3-adjacent polish (the server 403 is the hard backstop).
- **QA-W001 (resolved, not carried).** `GENDER_TO_COORD[gender]` returns `undefined` (not `null`) for an out-of-enum gender — DELIBERATELY not "fixed": the state is unreachable (Postgres enum) and degrades safely (cell-miss → attribute steer); the proposed `?? null` is dead code (lint) or weakens `Record<Gender,string>` exhaustiveness (which catches the real future risk — a 3rd gender added to the enum). TS-exhaustiveness is strictly better.
- **PAC-17** — **→ DECISION RECORDED as D-10 (2026-06-24 planner session); status SCHEDULED (W3.1 fast-follow).** Today a bound axis whose attribute is UNSET (gender=null) shows a "set your sex in your profile" STEER — no inline pick (D-7 D7-4). Owner wants the athlete to set it INLINE from the session — the SAME pick UX as a catalog axis — but the pick WRITES THE TYPED COLUMN (`AthleteProfile.gender`), not `profileSelections` (gender stays intrinsic → D-1 holds; only the write entry-point changes). Generalize a registry `binding → { typed column, value vocabulary, update mutation }`; the inline picker gains a second write-path (typed-column vs `profileSelections[axisId]`, chosen by `binding`). **Decision-first gate DISCHARGED in D-10:** supersedes D-7's D7-4 "no inline pick" + the four-projection note on the bound-arm EXECUTE projection (the inline pick is a typed-column WRITE-AFFORDANCE — EXECUTE invariant unchanged; D-1 holds). **CORRECTION (recon this session): NO `ResolvedLoad` arm change needed** — the picker reads the bound axis's `values`+`axisId` from `row.load` (it carries `["Male","Female"]`), exactly as `InlineProfilePicker` reads `load.axes`; the older "the arm must carry values+axisId" was wrong. **Scope = a fast-follow mini-`/feature` AFTER W3-core** (distinct surface = the athlete session, zero shared code with the athletic card; `w3-pac17-feature-prompt.md`).
- **PAC-16** — NEW (low; from the post-merge-prep local review). The migration system-row INSERT is `ON CONFLICT DO NOTHING` (won't abort on a pre-existing `key='gender'` coach row — the original `ON CONFLICT ("binding")` would have aborted `migrate deploy` on the `key` unique constraint), but it then SILENTLY skips inserting the system Gender axis → no bound gender. Window = **pre-migration only** (post-migration the `key @unique` blocks a coach `key='gender'` create → 409); **empty on prod** (W1 smoke created `level`, not `gender`); caught by owner-smoke ("system Gender axis exists + protected?"). If it ever bites: delete the colliding coach axis + re-run the migration (or insert the system row). A stronger fix = reserve `key='gender'` in the CRUD create path — deferred (not worth the scope; `@unique` already covers the realistic post-migration case).

## 2026-06-24 — W2 reverted (prod incident) deltas

W2 (design A) was merged (#309) then REVERTED (#311) — the VO cut shipped WITHOUT PAC-1, so the coach's pre-W2 `byProfile` loads 500'd (D-8). New / changed carry-forwards:

- **PAC-18 — NEW (prod schema + data orphans from the forward-only migration).** Migration `20260623083123_add_profile_axis_binding` ran on prod at the #309 merge (`db-migrate.yml` on `201b82a8` = success) and the revert deleted the FILE but did NOT (and `migrate deploy` cannot) undo it. So **prod physically carries, with NO source file in the repo:** the `ProfileAxisBinding` enum, the `binding` column on `app_profile_axes`, the `app_profile_axes_binding_key` unique index, the `_prisma_migrations` applied record, and the SYSTEM Gender row (`id cgender000000000000000000`, `key gender`, `binding GENDER`, `values [Male,Female]`). **Harmless to the reverted code** (Prisma selects named columns; an extra NULL-binding column + multi-NULL-tolerant unique index don't interfere; admin plain-axis creates set `binding=NULL`). **BUT the system Gender row is now VISIBLE + EDITABLE in the W1 admin Profile Axes** (the reverted code has no `binding`-protection). Disposition: **leave it — do NOT delete it from admin** (its INSERT is already applied, won't re-run → the re-apply needs it). Re-confirm the prod `_prisma_migrations` + row state at re-apply (owner probe; planner work does NOT connect to prod). Status **OPEN (re-apply)**.
- **PAC-19 — NEW, OPEN DECISION (the re-apply ordering gate — owner decides before the re-apply session).** The re-apply flips the VO back to design-A as a hard cut. There is NO 500-free ordering by code alone: old code 500s on the new shape, new code 500s on the old shape, and deploy + data-migration are not atomic. Decide ONE: **(A) maintenance window** — brief read-block / accept a short 5xx blip while deploy + `migrate deploy` + the PAC-1 backfill run as one coordinated step (simplest; single-coach, low traffic — likely fine); or **(B) read-tolerant transitional `loadSchema`** — a temporary VO that accepts BOTH `{name,values}` and `{axisId,…}` on READ so old loads render through the deploy→backfill gap, then tighten in a follow-up (no blip, more code, a second PR). `aggressive-migration-no-bridge` does NOT decide this — it is a dev-velocity rule; this is a PROD-500 question governed by `prod-data-inviolable`. Status **OPEN**.
- **PAC-7 (plan-editor-compose cross-ref) — RE-OPENED for the re-apply.** The `D-BYPROFILE-AXIS` sacred-VO sign-off in `plan-editor-compose/decisions.md` was reverted with #311 (the byProfile VO is currently back to `{name,values}`, so the cross-ref is intentionally NOT restored now — it would contradict the live code). Re-lands with the re-apply. The `four-projection-recheck.md` verdict itself is restored + STANDS (this board sync).
- **PAC-12 (W2↔W3 intermediate-red) — DORMANT.** Moot until the re-apply re-lands W2 (the resolver is back to the legacy name-keyed read).

## 2026-06-24 — W2 RE-APPLIED (#313): closures

W2 design-A re-landed + prod-confirmed (PR #313 → `a7ac9a63`) with the PAC-1 backfill; owner smoke confirms it works. Carry-forward dispositions:

- **PAC-1 — CLOSED.** The byProfile backfill ran at the #313 cutover: 13/13 loads rewritten to design-A, one transaction + post-write re-validation; gender coords remapped (`m/f`→`Male/Female`), `CAL`→system Gender (owner). `packages/api-server/scripts/backfill-byprofile-reapply.ts`.
- **PAC-7 — CLOSED.** The plan-editor-compose `D-BYPROFILE-AXIS` cross-ref re-landed with the #313 code; `four-projection-recheck.md` verdict stands.
- **PAC-18 — CLOSED.** Orphans reconciled: the migration is back in the repo + still applied on prod (byte-identical → `migrate deploy` no-op, green); the system Gender row is now the legit live system axis (design-A code renders it read-only/protected in admin). No longer an orphan.
- **PAC-19 — CLOSED.** Option A (maintenance window) executed: deploy-first → READY → immediate backfill `--write`. The ~64s window saw zero traffic → zero real 500s.
- **PAC-12 — still W3 (resolved on current prod data).** The resolver reads `profileSelections[axisId]`; on prod today the one athlete's only selections are gender-keyed (resolved from the typed column) → 0 inert-risk. W3's PAC-2 re-keys the profile-card writes; a NEW non-gender pick made before W3 would be inert until then.
