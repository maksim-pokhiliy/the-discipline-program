# compose-hardening — journal

Append-only. One entry per session/step.

## 2026-06-05 — initiative opened from the state-of-the-feature audit

- **Origin.** Mid-UX-polish session, owner stopped and asked for the real picture: "рванул полировать UX, а в коде, может, бардак и непонятны границы — кто за что отвечает." Ran an independent 13-agent audit (Workflow `wf_fc0a986c-5ae`; 6 layer-mappers + 7 adversarial verifiers; 588s; 514 tool-uses) over the merged `plan-editor-compose` (PR #245), deliberately re-verifying the prior `theory-vs-code-reconciliation.md` rather than trusting it.
- **Verdict.** Model / contract / persistence production-grade, zero drift (re-confirmed). Authoring half-built: create-only (T0-1) + ⅓ algebra unauthorable (T0-2 program/slot; conditional-scoring). "Bardak" not supported; "UX-polish premature" supported — a readiness boundary, not messy code. Owner's instinct to stop was right, the stated reason wrong.
- **NEW finds beyond the prior reconciliation** (the payoff of an independent fan-out, not a re-run): T1-1 (`assertArrangementRefsInScope` create-skip), T1-2 (`mapScoring` condition-drop), T2-7 (BlockEditor terminology), T3-API-1 (SCAN_ROOTS gap), T3-RD-1 (empty scoring-only header), T3-SEED-2 (duplicate `blockInstanceRef`), T3-SEED-3/4 (EMOM-20/22, block-020 stub), T3-RD-3 (module leak), T3-CT-3/4 (literal-match guard, 7/8 range).
- **Created the initiative** (analysis + docs only, no code): `charter`, `audit-findings` (full evidence + verifier verdicts + carry-over map), `deferred` (Tiers 0–3, ~26 finds), `decisions` (D-1 RATIFIED + 4 OPEN: D-EDIT/D-ONTOLOGY/D-SCORING-RENDER/D-MARKER), `plan`, `state`. Set `initiatives/ACTIVE` → `compose-hardening`; pointed memory.
- **Next.** Ratify D-EDIT + D-ONTOLOGY; recommended first cut = T0-1 edit-mode. No code until ratified.

### Close-out verification (spot-check vs live code, owner: "проверь себя 10 раз")

Re-verified the load-bearing + all NEW findings against current source (not agent reports):

- **Confirmed exact:** T0-1 (`compose-editor-drawer.tsx:80` `emptyBlockProgram`); T0-2 (`phase-7-blocks.ts:141-169` snatch wave = 1 row + composition `{}`, stages 80/90 gone; `schema-row.schema.ts` payload = 9 variants, no `program`/`stagedProgram`/`slotSpec`); T1-2 (`compose-to-create-requests.ts:82-97` `mapScoring`, no `condition`); T1-3 (`schema.mapper.ts:41` `subSchemas: []`); T2-1 (`format-composition-summary.ts:54-55`); T3-API-1 (`scoring-inert-consumers.test.ts:11-13` SCAN_ROOTS = mappers/lms + endpoints/lms/schema, schema-row absent); T3-CT-3 (`composition.schema.ts:241` string-literal match); **T3-SEED-2** (`block-008` dup: `week-1-friday.ts:46` + `week-2-monday-strength.ts:24` — my first grep was a false alarm, the dup is real); T3-SEED-3 (`sub-schema-coverage.ts:32` `EMOM_MINUTES=22`, `:62` `BLOCK_EMOM_20_SUBSCHEMAS` → 22-minute block named "20").
- **Corrected one:** **T1-1.** Initial wording claimed "the ladder-collision check runs on create." Source (`admin.ts:121-131`) shows create does **no** composition validation at all; the asymmetry is the arrangement-ref scope check (update-only, `:157-158`), and the normal S2 cascade wires arrangement via a _guarded phase-2 update_ — so it's an API-level asymmetry, not a normal-UI hole. Fixed in `audit-findings.md` + `deferred.md`.
- **Not individually re-verified** (carry agent `file:line`, low-stakes cosmetics — flagged in `state.md`): T3-SEED-4 (`block-020` stub), T3-RD-1 (empty scoring-only header), T3-API-2 (reorder atomicity), the duplicate-formatter line numbers. Re-verify before acting on any single one.

## 2026-06-06 — T0-1 edit-mode shipped (`/feature` full)

- **Ran `/feature` (full) on T0-1** (compose edit-mode) end-to-end: Research → Design+Plan → Gate A → Implement → Review∥QA → Test∥Docs → close-out. Branch `feat/compose-edit-mode` off `main` (owner picked "docs→main, feat from main" at Gate A; the 2 initiative-docs commits were rebased onto `main` first so the feat PR diff stays clean).
- **Ratified at Gate A: D-EDIT + D-SCORING-RENDER** (see `decisions.md`). Research resolved the open sub-questions on live code: per-node `updateSchema` (not subtree-replace), axis-only scope (structural deferred), the real drawer (not the mock prototype). Keystone confirmed: stored `arrangement` refs are real cuids = the subtree's `schema.id`/`row.id`, so branding them as draft `NodeId` round-trips arrangement with zero remap → the inverse adapter was the entire blocker; no Prisma/contract/endpoint change.
- **Shipped (5 commits):** `990b7164` inverse adapter · `1de6c958` edit-persist diff + per-node update hook · `b528e784` drawer mode + entry button + axis-only guards · `7380801a` fix (lock header read-only + hide upper-row Duplicate in edit-mode — QA-101/102) · `792888ea` test coverage (per-axis round-trips, edit latch, divergence matrix).
- **Hardened beyond the design** (orchestrator Gate-A catch + review): scoring-verbatim invariant (edit never routes stored `scoring` through the lossy `mapScoring` → `condition` survives byte-for-byte; closes T1-2 for the edit path); structural-divergence guard as a typed `DiffResult` refusal (fail-closed on any add/remove/reparent/root-sibling — defense-in-depth behind the UI affordance-disabling); graceful range-refuse.
- **Review B / QA zero-CRITICAL** — data-loss spine verified independently from both sides. Fixed before Gate B: QA-101 (phantom upper-row Duplicate) + QA-102 (in-drawer header silent-drop → read-only) + an `isScoringEditable→isCreateMode` naming cleanup. Deferred (→ `deferred.md`): QA-103, REV-W2, QA-201, QA-302.
- **Verification:** check-types + lint clean; full platform project suite **89 files / 1031 tests green** (~40 new). FROZEN contract + inert tripwire untouched; archetype not reintroduced.
- **Next.** Merged via PR #247 (`809a819a`). Then 0b (program/slot, T0-2) — ratify **D-ONTOLOGY** first.

## 2026-06-06 — D-ONTOLOGY ratified (decision session, no code)

- **Scope.** Decision-only session (owner directive): resolve the program/slot ontology gating 0b/T0-2. No `/feature`, no FROZEN-contract code — ratify first (the ontology touches the FROZEN contract; that's a decision, not a research side-effect).
- **Method = verify-then-spec on live code, not agent reports.** Read both VOs (`staged-program.ts`, `cap-spec.ts`), both FROZEN contracts (`schema-row.schema.ts` 9-variant payload union, `composition.schema.ts` 4-axis `.strict()`), the Prisma models (`RowKind` enum, `composition Json?`, `parentSchemaId`), the seed flatten (`phase-7-blocks.ts`, `week-2-monday-strength.ts`, `week-1-friday.ts`), the form registry, and `composition-gauntlet.test.ts`.
- **Live-code findings that reframed the board's question:** (a) zombie confirmed — repo-wide grep: 0 production consumers of `stagedProgram`/`slotSpec` (only def + enum + 06-formalization quarantine + tests). (b) The board's "Prisma column" is wrong — `rowPayload`/`composition` are `Json`, so (i) would add a `RowKind` enum value, (ii)/(iii) zero Prisma. (c) **The gauntlet itself flattens the wave** (`stagedProgram` validated only standalone, "off the projection", `:242`; the real `waveContainer` uses `count:3` + flat rows) and **expresses EMOM via nested `window` sub-containers** (Gauntlet B) — so `slotSpec` is redundant even in the legitimacy test. (d) `useComposeProgram.addRow`+`reorderChildren` already exist → multi-row authoring already works; the seed flatten was a seed-author shortcut.
- **Owner's four-projection ruling (coach authority, asked not invented):** Q1 SET = "separate sets" (→ a stage is a row, not an atomic unit — killed my (i) leaning + the audit's untested atomic-unit premise); Q2 ANALYTICS = per-stage (carried by row `load`/`reps`); Q3 EMOM = window-centric (→ delete `slotSpec`); **classification = needed** (badge + analytics grouping).
- **Ratified (D-ONTOLOGY):** stages = sibling rows; classification = a **thin `programKind` on `composition`** (M1, group-level classifier — chosen over an M2 marker-row on the D-LADDER precedent: group/structural → container axis, per-row → marker; + analytics-grouping robustness + cluster-count co-location); **DELETE both fat VOs**, keep `stagedProgramKindSchema` (now consumed). **Gate-A on the sacred `composition` module — authorised by the ratification.** Disposes T3-CT-1 + seed-flatten. NOT (ii) — M1 carries only the KIND label, not the per-movement stages, so no heterogeneous-container ambiguity.
- **Promoted:** `decisions.md` (D-ONTOLOGY OPEN→RATIFIED, full reasoning + blast radius + M1-vs-M2 + why-not-(i)/(ii)/(iii)); board + `deferred.md` (T0-2/T3-CT-1 → SCHEDULED 0b). Fixed the stale "PR pending" → "merged #247" across state/journal/plan.
- **Open for 0b design** (non-blocking): rehome/delete `STAGE_INDICATORS` (`explode`/`without_weight`) orphaned by the `stageSchema` delete; decide a `programKind` movement-homogeneity `superRefine`.
- **Next.** 0b via `/feature` (UI-first, like T0-1): `programKind` authoring affordance + render badge on mock → contract (`composition` += `programKind`, delete VOs) + seed re-author.

## 2026-06-07 — 0b (T0-2 program/slot) shipped (`/feature` full, same session as the ratification)

- **Ran `/feature` (full) on 0b** end-to-end in the same session the decision was ratified (owner: "начинай сразу, полный пайп в этой же сессии" — overrode the planned separate session; first/only `/feature` of the session, within budget). Branch `feat/compose-program-kind` off `main`; the D-ONTOLOGY ratification docs went in as commit 0 (`docs in the feature PR`, per [[closeout-before-pr]]).
- **Implemented D-ONTOLOGY exactly** (7 feature commits): `06bbb1d5` contract `composition += programKind` (one line — auto-propagates through every `compositionSchema.parse`, zero mapper/Prisma edits) · `aa07ea5d` delete both fat VOs + `STAGE_INDICATORS` (0 prod consumers, grep-clean) · `bd8b521b` platform `programKind` selector (`LabeledToggleGroup`, none/wave/cluster/drop_set) + card badge · `ed73a1f0` seed re-author (block-163 wave un-flattened to 3 stage-rows 70/80/90, block-164 cluster `rounds(5)`, block-008 drop_set; dup-ref fix week-1-friday → block-181; forced split of the pre-existing-304-LOC `phase-7-blocks.ts` → `phase-7-superset-blocks.ts`, the directory's house pattern) · `cbaeb302` the create/edit round-trip (**keystone**: `assembleEditedComposition` takes `programKind` from the EDITED draft `base`, never `original` — editable, unlike inert scoring) · `f535ae1f`/`258b45e2` tests.
- **Design forks resolved** (the two D-ONTOLOGY left open): `STAGE_INDICATORS` → DELETE (orphaned; `without_weight` already via the `withoutWeight()` load VO, `explode` via `media.appliesTo:"drop_stage"`); movement-homogeneity `superRefine` → NO (a hard refine would reject the real heterogeneous seeded drop-set block-008 split-squat→jump-squat); none/clear → an explicit 4th "none" toggle button.
- **Review A / QA B, 0 CRITICAL** (no Workflow fix-loop needed). Two QA WARNINGs found + **fixed in-pipeline**: **QA-001** (`9b3b7c98`) — `hasAnyAxis` create-guard missed `programKind` → classification on a block-root silently dropped (the exact failure the feature kills); **QA-002** (`1743bb10`) — a repetition-less wave leaked `programKind` into the card title via `formatSchemaHeader[0]`; split `formatStructuralSummary` (header) from the badge so the classifier never becomes the title (owner chose to fix it; the holistic axis-only-header fix stays as T3-RD-1). One Review INFO was a **false positive** (REVIEW-002: removing `?: T | undefined` would break `exactOptionalPropertyTypes`) — not applied.
- **Verification:** check-types ×3 packages + lint + `dep:check` (1661 modules, 0 violations) clean; contracts **1044** + platform **1054** tests green (~25 new, keystone + both QA fixes + nested round-trip + edit-mode editability pinned, the last two mutation-verified). Inert-scoring tripwire + archetype-excision untouched; Prisma untouched.
- **Disposed:** T0-2, T3-CT-1 (both CLOSED), T3-SEED-2 block-008. **Deepened:** T3-SEED-1 (coverage-matrix now references the deleted VOs — escalated, not code-consumed). **Deferred:** the holistic header fallback (T3-RD-1), property-test programKind arbitrary, the `mapToSchema` parse-500 class (T3-DB-1).
- **Gated pre-merge step left to the owner** (per project rules): `db:reset && db:seed` + the api-server suite (the live seed-coverage gate) before merge — not run in-pipeline.
- **Next.** Tier 1 correctness (T1-1 arrangement-ref create-skip, T1-2 create-path condition-strip, T1-3 depth-2 projector) — its own `/feature`/`/fix` session.

## 2026-06-07 — Tier 1 correctness (T1-1 + T1-3) shipped (`/feature small`)

- **Ran `/feature small` on T1-1 + T1-3** (the session's one feature run; budget ≤1/session). Branch `feat/compose-tier1-correctness` off `main`; board-sync (0b → merged #248) went in as commit 0, close-out docs in the same PR (per [[closeout-before-pr]]). api-server only, no schema/contract change, write-guards untouched.
- **verify-then-spec on live code reframed T1-3 minor → MAJOR.** The 2026-06-05 audit rated T1-3 "minor (latent), safe while nesting is shallow." Re-verifying found a **live seeded repro**: `block-010` (`BLOCK_NESTED_OVER_PARALLEL_LADDER_WK2_TUE`, `week-2-tuesday-compose.ts:125-202`) is depth-3 (rounds(2) → parallel → 2 ladder tracks), wired into the active seed (`week-2-tuesday.ts:29`), persisted recursively (`schema-emit.ts:61-88`). Every read path fetches only 2 levels (`buildSchemaWithBody` hardcoded `subSchemas:[]` at L2; week/day includes 2-deep) → both ladder tracks (the whole workout) silently vanish on the coach's week view, no error. Major current data-loss, not latent.
- **D: recurse, NOT cap-at-2.** The audit offered both; the owner picked "Bundle, /feature small" after the finding was surfaced. Cap-at-2 was **rejected** — it would contradict seeded block-010 + the uncapped "free nesting" authoring design (`addContainer` has no depth limit) + the recursive `SchemaWithBody` contract. The 2-level fetch was the odd-one-out; contract/cascade/draft/validator already recurse. Adversarial pass confirmed the **write-guards are sound per-level** (each write re-roots at the affected container → the local ladder-collision `superRefine` always sees its direct rows) → they stay 2-level, untouched.
- **Shipped (3 code commits + tests):** `d38208e0` T1-1 symmetric create guard — `assertCompositionUpdateValid(tx, created.id, data.composition)` after `tx.schema.create`, mirroring the update path + the `schema-row` create-then-validate-in-tx pattern. No-regression: the cascade defers arrangement to a phase-2 update (`wireArrangements`), `composeContainerToComposition` never emits arrangement on create, and a childless container is contract-valid — so the guard only rejects a **direct-API** non-ordered arrangement on a childless new schema (refs unresolvable → 400). · `4a67b6e2` T1-3 recursive read — new `buildSchemaForest` (flat-fetch all block schemas + in-memory rebuild by `parentSchemaId`, ordered, arbitrary depth); `block.mapper` consumes it; `week/admin` + `day/admin` includes flattened; `buildSchemaWithBody` + write-guards untouched. · `764c7441` tests.
- **T1-2 NOT taken** (ph.5-gated): edit-path already closed (T0-1 scoring-verbatim); create-path strip + full pass-through owed at conditional-authoring (ph.5). Tripwire test deliberately skipped — the draft `ScoringDirective` has no `condition` field, so the strip is unobservable without a cast pre-ph.5 (low value).
- **Review-light 0 CRITICAL / 0 WARNING** (all 4 findings INFO; reviewer hand-traced block-010 through the now-full-depth read validation → passes). Backwards-compat grep clean: `buildSchemaWithBody`'s only remaining callers are the 3 write-guards (unchanged 2-level shape); contract unchanged; no client break. **Bonus:** read-side `assertComposeTreeValid` now validates the full-depth tree (closes the validation-gap half of T1-3 too, not just the data-loss half).
- **Tests:** `buildSchemaForest` unit (DB-free, **6/6 green, ran**) is the primary depth-3 regression; T1-1 guard cases (leaf ok / parallel → 400 / ordered ok) + a **self-contained** depth-3 read round-trip written but **gated** (DB). Test agent caught a spec slip (`count:{value:3}` → correct is `count:3` per `exactOrRangeSchema`); impl unaffected (guard is composition-agnostic). Regression grep: no existing test asserted depth-3 truncation → none breaks; depth-2 + childless `subSchemas:[]` anchors still pass (output identical at depth ≤2).
- **Verification:** check-types + lint + `dep:check` (1662 modules, 0 violations) clean. **Gated pre-merge left to the owner:** `db:reset && db:seed` + the api-server suite (runs the gated T1-1 + round-trip integration tests + the live seed-coverage gate proving block-010 now round-trips its L3 tracks).
- **Disposed:** T1-1 + T1-3 CLOSED. **Next.** Tier 2 (read-honesty + owner snag-list, T2-1..7), then Tier 3 (hygiene).

## 2026-06-07 — Tier 2 read-honesty cut 1 (T2-1 + T2-2) shipped (`/feature small`)

- **Scope = Tier 2 read-honesty only** (T2-1 + T2-2), deliberately split from the owner-snag-list (T2-3..7 + QA-103 = a separate cut 2). Rationale: T2-1+T2-2 both live in one read-side formatter (`format-composition-summary.ts`) → a cohesive unit; the snag-list is authoring-side. Branch `feat/compose-read-card-honesty` off `main`; close-out docs in the same PR (per [[closeout-before-pr]]). **Read-side platform rendering only** — composition contract FROZEN-untouched, no api-server, no authoring (`axes-summary.ts` / drawer untouched).
- **verify-then-spec caught a stale audit citation.** The 2026-06-05 audit prose for T2-2 said the interleave orders are `round_by_round` vs **`athlete_by_athlete`**. Live contract (`composition.constants.ts:26`) is `PARALLEL_INTERLEAVE_ORDERS = ["round_by_round", "track_by_track"]` — specced against `track_by_track`. (Reinforces the state.md gotcha: re-verify any single `file:line`/string from the agent-reported audit before acting.)
- **D-SCORING-RENDER governs the EDITOR only** — re-read confirmed it ratified the editor's static-disabled treatment and explicitly left "the read-CARD honesty cue (T2-1) a separate Tier-2 item." So the read-card _direction_ (show-with-inert-cue, NOT hide; presentation, not execution) was settled; the _visual form_ was open. Surfaced the two genuinely-open coach-POV choices once via `AskUserQuestion` w/ ASCII previews (per the "surface design before lock" discipline). **Owner ratified:** (T2-1) **dashed muted chip** for inert scoring; (T2-2 superset) **per-row pair chips** (true row grouping). interleave-label + condition-suffix were decided mechanically (not asked).
- **Architecture.** `formatCompositionSummary` return `string[]` → `CompositionSummaryPart[] = {text, tone:"active"|"inert"}`. scoring → `inert`; everything else → `active`. `SchemaCardMeta` renders active parts as today (caption + `·` between active parts only) and the one inert part as `<InertScoringChip>` (a new dashed-outlined Chip + tooltip "Planned — not scored automatically yet"). interleave folds into the parallel label (`parallel (round by round)` / `(track by track)`, labels reused from authoring); `scoring.condition.appliesToRounds` folds into the inert part (`max-in-remaining · rounds 2, 3`) — **stored config rendered as text, zero score computation** (inert-guard intact). Superset = a per-row pair chip mirroring the `minuteLabel` leading-pill (`supersetLabelById` map in `SchemaRowList`, threaded `supersetLabel` → `SchemaRowCard` → `SchemaRowCardBody`).
- **Backwards-compat keystone:** `formatStructuralSummary` kept returning `string[]` (structural parts only) → its sole consumer `format-schema-header.ts:18` (header `[0]` = repetition label) is byte-identical. The return-shape change has exactly ONE consumer (`SchemaCardMeta`), updated in lockstep. One benign INFO: a scoring-only composition's header fallback now includes the condition suffix — a strict info-gain, no test asserted the bare string (accepted, not a regression).
- **`palette.tags` note.** Memory [[no-hex-outside-theme]] points domain-category colors at a `palette.tags.*` slot — that slot does **not** exist in the live `@repo/mui` theme (`packages/mui/src/theme/palette.ts` has `kind.*`, `text.{primary,secondary,muted,subtle,faint,disabled}`, `action.{hover,selected}`, `divider`, `dividerStrong`). Honored the rule's intent (no hex outside theme) using existing slots: inert chip = `text.disabled` + `divider` (dashed), superset chip = `divider` outline. The memory's `palette.tags` reference is aspirational/stale vs this theme — flagged here, not chased.
- **Don't-deepen-T3-RD-2:** extended the SHARED label home (`compose-axis-labels.ts` += `INTERLEAVE_ORDER_LABELS`, same strings as authoring `parallel-arrangement-fields.tsx`) rather than forking a third map. The read formatter's richer `{text,tone}[]` return now diverges in _shape_ from authoring `axes-summary.ts` (`string`) — intrinsic to read-honesty; recorded on the T3-RD-2 carry-forward so the future dedup reconciles a richer read core (it is NOT an accidental desync to "fix back").
- **Review-light 0 CRITICAL / 0 WARNING / 1 INFO** (the benign scoring-only-header info-gain). Machine-verified: frozen scope (exactly 8 read-side files), zero score-compute symbols, zero hex / no `palette.tags`, union-narrowing on `scoring.condition` sound without a cast.
- **Verification:** platform check-types + lint clean; full platform project suite **92 files / 1065 tests green** (+7: 3 superset-map + 2 superset chip-render + 2 inert-chip). No api-server / db:reset / dev-server run (read-side only; real seed already covers superset block-165/166, condition week-1-wednesday, parallel round_by_round — `track_by_track` is unseeded, covered by unit test).
- **Disposed:** T2-1 + T2-2 CLOSED. **Next.** Tier 2 cut 2 (owner-snag-list T2-3..7 + QA-103), then Tier 3 (hygiene). Budget ≤1 `/feature`/session → each its own session.

## 2026-06-07 — Tier 2 cut 2 (owner snag-list T2-3..7 + QA-103) shipped (`/feature` full)

- **Planner recon BEFORE the lock** (owner directive: "планёрская разведка, не прямой /feature"). Reason: 6 heterogeneous authoring-side UX edits with real coach-POV forks + 2026-06-05 audit citations needing re-verification on live code. Read every cited file verbatim; **three citations shifted in MEANING** (not just line numbers): **T2-4** — the flat `compositionSchema.safeParse` in `compose-to-create-requests.ts` does NOT catch the ladder collision (no children in scope); the rule is `composeContainerSchema.superRefine` firing ONLY server-side (`assertComposeTreeValidForWrite`) → today an opaque 400, no client check at all. **QA-103** — the edit `foldArrangement` resolves under an identity refMap (checks only ref-reachability), and a **self-paired track** is checked by NOBODY server-side (`parallelTrackSchema` has no sibling check) → edit could PERSIST a self-pairing create rejects = a real create/edit asymmetry, not just a worse error surface. **T2-7** — per-schema "Edit axes" already exists (T0-1), so "route the block-meta modal to the axes editor" is redundant + semantically wrong → re-label only.
- **One `AskUserQuestion` (ASCII previews), per the "surface forks before lock" discipline:** owner ratified **scope = Full** (all 6 in one `/feature` full, not split across 2 sessions) + **T2-6 = BUILD the demote action** (over softening the hint). Then ran `/feature` (full) end-to-end.
- **Shipped (7 code + 1 test commit):** `4f0bf3dd` extract shared `MinutePill` (read-side refactor) · `c2058580` T2-3 EMOM minute pill on the authoring canvas (derives over DIRECT rows via `composeContainerToComposition`+`deriveMinuteView`) · `2fa3476b` T2-5 contract-mirrored inline axis-field validation + T2-4 draft-`rowKind`-keyed ladder mutex (one commit — they share the inspector + the inverted `axis-fields-safety.test.tsx`) · `d15698b4` **T2-6 demote-to-row** (`demoteContainerInProgram` unwrap in `tree-ops` + `locateInProgram` relocated there cycle-free + `demoteNode`/`programRef` on the controller + inspector button, all create-mode-gated) · `9e870fb0` T2-7 re-label "Edit block" → "Block intensity & cap" · `aa727824` **QA-103** `arrangement-invalid` diff variant + recursive `collectArrangementIssues` + the drawer reuses the existing issues Alert · `f81c4a08` the QA-fix (below) · `5f69a046` the gap-fill tests.
- **2 design forks resolved as ratified D-entries** (`decisions.md`): **D-DEMOTE** (build the affordance; Option-2 wiring — controller→inspector bare callback, NOT `NodeHandlers`; create-mode-gated incl. the previously-leaking hint; re-select the surviving row) and **D-ARR-EDIT-VALID** (run the SAME `validateDeferredArrangement` on the edit path via the new diff variant; server self-pair gap NOT closed → **T3-CT-5**, Gate-A).
- **Review A / QA, 0 CRITICAL** (no Workflow fix-loop). **One QA WARNING fixed in-pipeline:** `MIN NaN` (`f81c4a08`) — clearing the Rounds field stores `rounds:0` (T2-5 surfaces the error but still stores the typed draft), which `deriveMinuteView`'s `index % 0` turned into `MIN NaN` pills on the canvas; guarded `deriveMinuteView` to return `{kind:"none"}` for non-positive/non-integer rounds (the read side is unaffected — server rounds is contract-valid). `tree-ops.ts` at 348 LOC (>300) adjudicated INFO/acceptable — the design deliberately consolidated the structural-ops home + relocated `locateInProgram` to break the `tree-ops↔sibling-ops` cycle (verified absent via `dep:check`); splitting for the line count alone would re-introduce the coupling.
- **Verification:** platform check-types + lint + `dep:check` (0 violations) clean; **full platform project 1084 tests green** (the 2 `row-editor-modal.test.tsx` flaky-under-load timeouts are pre-existing, not this branch — 25/25 in isolation). apps/platform-only: FROZEN composition contract, api-server, Prisma all untouched. Close-out docs landed IN the PR (per [[closeout-before-pr]]).
- **Disposed:** T2-3 / T2-4 / T2-5 / T2-6 / T2-7 / QA-103 CLOSED. **Spawned:** T3-CT-5 (server self-pair gap, Gate-A, OPEN). **Next.** Tier 3 (hygiene) — its own session.
