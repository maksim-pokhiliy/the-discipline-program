# compose-hardening — plan

Phased by tier (severity × dependency). Each code step ships via `/feature` (UI-first where it has UI). Budget ≤1 `/feature`/session → multiple sessions. **Tier 0 gates the rest** — UX-polish on a create-only surface is premature (audit bottom line).

| #   | Step                                                                                                                                                                                                                      | Gate                                                                      | Status                                                                                                           |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| 0a  | Edit-mode: inverse adapter `Composition+SchemaWithBody→ComposeProgram` (real `schemaId`) + edit drawer + save-via-`update` (T0-1)                                                                                         | coach edits axes of an existing block; round-trip verified                | ✅ done — merged #247                                                                                            |
| 0b  | program/slot authoring (T0-2): stages = multi-row + thin `programKind` on `composition` (badge) + delete fat VOs + seed re-author (D-ONTOLOGY)                                                                            | wave/cluster/drop-set author as rows, classified + round-trip; no flatten | ✅ done — `feat/compose-program-kind`                                                                            |
| 1   | Correctness: arrangement-ref scope on create (T1-1); condition-drop tripwire (T1-2); depth-2 guard (T1-3)                                                                                                                 | guards symmetric; no silent loss                                          | ✅ T1-1/T1-3 done (#249); T1-2 → ph.5                                                                            |
| 2   | Read honesty + UX: inert-scoring signal (T2-1); render interleave/superset/condition (T2-2); EMOM MIN in drawer (T2-3); late-reject→input (T2-4); field validation (T2-5); demote button (T2-6); BlockEditor label (T2-7) | coach reads structure truthfully; owner snag-list cleared                 | ✅ done — cut 1 (T2-1/T2-2) `feat/compose-read-card-honesty`; cut 2 (T2-3..7 + QA-103) `feat/compose-tier2-cut2` |
| 3   | Hygiene: zombies (T3-CT-1); seed fixes (T3-SEED-\*); formatter dedup (T3-RD-2); SCAN_ROOTS (T3-API-1); archive 06-formalization (T3-ARCH-1); coverage-matrix (T3-SEED-1); + the rest                                      | grep 'archetype' clean; no doc↔code drift                                | ✅ done — Cut A `fix/compose-tier3-read-boundary` (#252) + Cut B `fix/compose-tier3-cut-b` (initiative close)    |

**Open design details deferred to their step** (OPEN in `decisions.md` — do NOT execute the step past them): **D-MARKER** (marker deprecate-vs-seed). _RATIFIED 2026-06-06 (see `decisions.md`): **D-EDIT** (edit persistence granularity — per-node update, axis-only), **D-SCORING-RENDER** (inert presentation — static-disabled in editor), **D-ONTOLOGY** (stages = rows + thin `programKind` on `composition`; delete both fat VOs; Gate-A authorised)._

**Sequencing notes.** T3-RD-2 (formatter dedup) is blocked on T3-CT-4 (align `compose-tree.types` `range`). T2-7 compounds T0-1 (the "Edit block" label only stops misleading once axes-edit exists). ph.5 (scoring _execution_ + conditional-scoring _evaluation_) stays a separate future initiative — this plan only touches scoring _presentation/authoring-or-hiding_.

**CLOSED 2026-06-07.** All steps shipped (audit → 0a → 0b → Tier 1 → Tier 2 → Tier 3 Cut A + Cut B). Compose-authoring is complete: a coach can build AND iterate any real plan end-to-end. **Carried to ph.5** (the scoring-execution initiative): T1-2 (create-path condition pass-through), D-MARKER + T3-SEED-5 (marker deprecate-vs-seed). Final dispositions in `deferred.md`; the arc in `journal.md`.
