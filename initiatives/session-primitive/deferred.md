# session-primitive — deferred

Carry-forwards: findings/obligations not yet scheduled, with disposition + status. **Promote here at every gate.**

**Status:** `OPEN` (live) · `SCHEDULED` (assigned to a step) · `CLOSED` (done — kept for the trail) · `DROPPED` (decided not to).

Owner directive 2026-06-10: "всё что я попросил пометить для обсуждения пойдёт фоллоу-апами" — the F-rows below are those follow-ups; they gate spec freeze (plan step 1), owner-paced.

## Follow-ups from the founding design session (2026-06-10)

| ID                 | One-liner                                                                                                            | Disposition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Status |
| ------------------ | -------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| F-PLAQUE           | Owner-proposed "плашка-инструкция" primitive (thin centered strip: "OR" / "THEN" / "ANY" / "REST")                   | **CLOSED (2026-06-12, D-PLAQUE).** Plaque = render vocabulary, NOT a stored node (owner: "плашка это про… апиранс"). The ~93 strip lines resolved per case: rest 71 → the schema rest setting is the ONLY carrier (`RowKind.REST` + `raw` die; ONE rest per schema — multi-rest rejected for now, the dual-rest corpus schema carries rule 2 as a note); connectors 16 → schema order; EXAMPLE 4 → schema note; only-once 2 → its OWN schema via order (owner correction — a work item, not prose). Notes = MULTIPLE ordered short texts on ANY element (block/schema/row/both groups), rendered as a plaque stack. Row-group mechanics ratified: mirror of Group one floor down (contiguous, 2+ members, opaque label, membership-only); pixel UX → W4 design. Grid rows updated. | CLOSED |
| F-CHIPS            | Free-text-chips-as-mechanism NOT confirmed (owner: "костыль который появляется когда кто-то устал дизайнить модель") | **CLOSED (2026-06-12, D-CHIPS).** Dissolved — every channel-Ч item has a named per-case home (structure / typed settings / catalogs / the D-PLAQUE notes stack / box labels); no blanket "attach a textik" mechanism (owner: "механизм 'прицепи текстик' это уже notes… обсуждать 'чипсы' уже смысла нет"). "Chip" = MUI render word only                                                                                                                                                                                                                                                                                                                                                                                                                                          | CLOSED |
| F-POSITION-CARRIER | `Position` enum dies (ratified direction); the carrier is open                                                       | **CLOSED (2026-06-12, D-MODIFIER** — the same-day D-POSITION notes ruling SUPERSEDED by owner re-open: "я бы таки обсудил 'позицию' как часть модели, а не текстик").\*\* Carrier = the row MODIFIER library: coach-owned dictionary entity à la Label/Exercise, ordered multi-REF on rows (corpus carries 2–3 at once), searchable create-on-the-fly picker (component shared with LABEL-FLOW-UX), rename = dictionary wording (not prescription), delete-in-use refused. Enum + column + editor still die at W4; seed values → library entries + refs. 3-layer boundary ratified: machine-read → typed · dictionary how-to-execute → modifier · situational text → note                                                                                                          | CLOSED |
| F-WEIGHT-EXOTICS   | `split_tier`/`with_asymmetric_arm`/`with_depth_modifier` disposition NOT accepted                                    | **CLOSED (2026-06-12, D-LOAD-FINAL).** Consulted both load docs. Exotics die: `split_tier`→row-group (compound rows); `with_asymmetric_arm`→`single`+arm-role modifier; `with_depth_modifier`→`single`+depth modifier (`full_rom`/`partial` zero-corpus, die). `single`+`single_arm` merge (one-arm=SIDE). `compound_device` trims BOX/SOFA. absolute load=`{count:1\|2, kg}` (count explicit on row; implement type→equipment library). Original owner bar: "требует пересмотра… скверный запашок"                                                                                                                                                                                                                                                                                | CLOSED |
| F-TEMPO            | Tempo: typed structure vs text — and does the standard 4-digit `3-1-2-0` notation join the model?                    | **CLOSED (2026-06-12, D-TEMPO).** 4-digit `fullTempo` stays TYPED (each position `int \| "X"` — explosive phase; standard notation awaiting an engine, byProfile precedent); the verbal forms (pause-in-up / per-Nth-rep / slow-eccentric / hold-after-last) → MODIFIERS (D-MODIFIER). Original framing: structured-numbers vs free-text — resolved as BOTH (4-digit typed, verbal=modifier)                                                                                                                                                                                                                                                                                                                                                                                       | CLOSED |
| F-BLOCK-TIMECAP    | `Block.timeCap` ("PRACTICE [5-10 min]", 2 occ) — owner: "а это зачем? не понял"                                      | **CLOSED (2026-06-12, D-FLOORS).** `Block.timeCap` LEAVES the block — a block is a named part, not a load carrier. "PRACTICE [5-10 min]" = a schema with `repetition.timeCap`. The owner's "а это зачем?" answered: it was promoting a setting onto children; drop to the schema                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | CLOSED |
| F-SLOT             | Placeholder slot stays typed but smells (owner: "плохо пахнет, но я пока не понимаю почему")                         | **CLOSED (2026-06-12, D-ROW-GRAMMAR).** The placeholder slot is NOT a row kind — it is a placeholder-NATURE catalog record (`placeholderFlag` already exists; the picker filters on it). `RowKind.PLACEHOLDER` dies; the row is an exercise whose render-kind is inferred from catalog nature (`concrete\|placeholder\|rest`). The smell ("плохо пахнет") was the duplicate carrier — resolved by collapsing to catalog nature                                                                                                                                                                                                                                                                                                                                                     | CLOSED |
| F-HEADER           | `Schema.header` nearly empty as a concept post-grid                                                                  | **CLOSED (2026-06-12, D-HEADER-KEEP).** Owner: "всё нормально с хедером схемы, не трогаем, это уже косметика." Header STAYS a field as-is. NOT a model question — reduces to a UX-parity item: an in-group schema renders its header like a standalone one (today the in-group top zone is cut). The orchestrator's "header dies" rec was overridden (owner's UX domain)                                                                                                                                                                                                                                                                                                                                                                                                           | CLOSED |

## Inherited from compose-authoring-ux (CLOSED 2026-06-10)

| ID               | One-liner                                                   | Disposition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Status |
| ---------------- | ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| QA-004           | Kind-switch silently discards authored ladders (no confirm) | The editor is rebuilt in this initiative; confirm/undo rides the redesign. **CLOSED (W3 build 2026-06-12, D5 / DR-W3-8).** A `ConfirmationModal` gates a DIRTY repetition-kind switch (current value ≠ `REPETITION_DEFAULTS[kind]`, or any materialized parallel); clean switches stay silent. Shared `kind-switch-confirm.tsx` dialog + `is-repetition-dirty.ts` predicate at both discard sites (edit single-axis + create parallel→non-ladder). Owner edge ratified keep-confirm-always (QA-114 — a pristine parallel still confirms; it collapses the 2-track STRUCTURE). Pinned by tests.                            | CLOSED |
| MARKER-FATE      | Marker primitive has no authoring flow                      | **CLOSED (W2 build 2026-06-11, D-MARKER-DEATH).** The `INNER_LADDER_MARKER` row kind is removed (contract payload variant + Prisma enum value + the forbidden-fusion `superRefine` + test fixtures). The distinction survives as two STRUCTURES (N one-row ladder-schemas in a Group vs one ladder-schema with N rows). Seed was marker-FREE already (block-037 = two ladder sub-schemas); the stale `coverage-matrix.md` "marker ≥1" expectation dies unsatisfied (the doc's banner already declared that section dead; pruned in this close-out). Surviving rejection test pins the kill (`schema-row.schema.test.ts`). | CLOSED |
| BACKLOG-ROUNDS   | "N rounds OVER a parallel block"                            | Group label carries "N rounds:" (channel 3) until an engine reads rounds — then typed repetition-on-Group, designed fresh                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | OPEN   |
| BACKLOG-TAIL     | Closing/finisher tail after the parallel                    | Natively representable: a sibling schema after the Group — confirm in spec freeze                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | OPEN   |
| BACKLOG-PATTERNS | Parallelizing non-ladder patterns                           | Dissolves by design: a Group is kind-agnostic (members are any schemas) — confirm in spec freeze                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | OPEN   |

LABEL-COMBINE (predecessor) dissolved at design level: with explicit boxes there is no derivation predicate to collide — the Group's label is coach-owned text. Not carried as a row; noted here for the trail.

## From the W1 build (Group/box UX, 2026-06-10)

| ID                 | One-liner                                                                                                                                                                                                         | Disposition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Status     |
| ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| W1-DUP-RETRY       | Unchecked (independent-ladders) batch is non-atomic, no idempotency → retry after a partial failure duplicates the already-created cards (no `@@unique` on top-level schemas)                                     | **CLOSED (W2 build 2026-06-11).** Idempotency landed via the existing `@repo/api-routes` layer (server side was already complete — `wrapAuthHandler` baked into the auth factories, `prismaIdempotencyStore` bootstrapped; NO new server route work — DR-W2-4). `api.schemas.create` now forwards an `idempotencyKey`; `useCreateIndependentLadders` threads a stable `${draft.id}:${trackIndex}` per track. MT-19 pinned at the client-threading level. **Cross-remount boundary (DR-W2-4 / QA-004):** an in-modal retry dedups; a close+reopen mints a fresh `draft.id` → a fresh batch BY DESIGN (across-session re-submit = new intent). The §6.4 in-session-retry acceptance scenario is satisfied. The remount door → INFO carry-forward below. | CLOSED     |
| W1-RENDER-REPOINT  | The W1 box render keys box-ness off the live `isStructurallyParallel` (ADR-0040)                                                                                                                                  | **CLOSED (W2 build 2026-06-11).** Box-ness re-points to real Group membership via the ONE shared `buildBlockItems(schemas, groups)` helper in contracts (the `isStructurallyParallel` predicate is deleted); clustering moved UP to the block-list level (`block-card-body.tsx`, the single consumer). The `AccentGroupCard` visual is unchanged (owner-accepted). `schema-card.tsx` dropped its `isBox` compute + the `subSchemas` recursion. (DR-W1-1 consequence; DR-W2-FORK-6; prompt §D7.)                                                                                                                                                                                                                                                       | CLOSED     |
| W1-BOX-FRAME       | A boxed parent is a dashed `AccentGroupCard` nested inside the solid-border `SchemaCard` shell → double frame (triple at depth-3, block-010)                                                                      | By design (the box re-skins the sub-schema zone, which always lived inside the card shell). **Walkthrough verdict 2026-06-11: "всё работает, полировка будет потом" — accepted as-is.** The calmer solid-divider fallback stays documented as a one-file `schema-group-box.tsx` change if polish is ever scheduled; not scheduled now.                                                                                                                                                                                                                                                                                                                                                                                                                | CLOSED     |
| W1-INSESSION-CHECK | `linkIntoBox` resets to checked on modal reopen but NOT after an in-session toggle-off → remove-to-1-track → re-materialize-to-2                                                                                  | Minor; defensible (re-materializing after an explicit uncheck arguably preserves the coach's last choice). Revisit only if the owner wants "always default-checked on each fresh materialization" (reset inside `handleAppendTrack` on the flat→parallel transition).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | OPEN (low) |
| W1-SUBADD-BOX      | Unchecked «Group into one box» can't hold in the add-sub-schema context: N children land under ONE parent, and a repetition-less (incl. `once`) parent boxes by the live derivation law regardless of the uncheck | **CLOSED (W2 build 2026-06-11).** Dissolved: the add-sub-schema context dies with `parentSchemaId`; in-box add is now create-with-`groupId` (`POST /schemas` with `groupId`), and the «Group into one box» checkbox is HIDDEN in that modal context (`onLinkIntoBoxChange={isInGroupAdd ? undefined : …}`). Adding INTO a box never asks the grouping question. (DR-W2-FORK-6; prompt §D7.)                                                                                                                                                                                                                                                                                                                                                           | CLOSED     |

The new hook's sequential N-create loop is abort-less — pattern-consistent with the existing `useCreateParallelSchemas` (one request) and folds into the editor-rebuild disposition that already owns QA-004; not carried as a separate row.

## From W2 prompt-writing (2026-06-11)

| ID              | One-liner                                                             | Disposition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Status |
| --------------- | --------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| W2-ORDER-UNIQUE | No `@@unique([blockId, order])` on Schema after `parentSchemaId` dies | **CLOSED at orchestrator review (2026-06-11) — the deferral premise was false.** Enforcement already existed in the raw-SQL layer (`prisma/sql/lms-checks.sql` partial unique WHERE `parentSchemaId` IS NULL), which the W2 cut left referencing a dead column → owner's `db:reset` failed live. Review fix: full unique `schemas_block_order ON ("blockId","order")` + `resolveGroupedOrder` trailing shift made descending-sequential (the `Promise.all` +10 shift collides under a live unique) + two-trailing-schemas test pin. Reorder's two-phase dance survives unchanged. See DR-W2-8 (SUPERSEDED). | CLOSED |

## From the W2 build (model core, 2026-06-11) — owner-walkthrough validation + INFO carry-forwards

These are NOT blockers. The build is green on every runnable gate; the items below either await the owner's reseed/walkthrough (a 1-line seed fix if a content call reads wrong) or are conscious W3-deferred / low-priority cleanups.

### Owner-walkthrough seed-content validation (verify at the owner's reseed + §6 walkthrough; 1-line seed fix if wrong)

| ID                 | One-liner                                                                                                                               | Disposition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Status |
| ------------------ | --------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| W2-FOOTNOTE-LAST   | Footnote roles 2–4 re-expressed as plain EXERCISE rows — confirm they land LAST (D-6)                                                   | D-6 verbatim: "поставь его в конец". In the re-expressed block-183 (`week-2-tuesday-compounds.ts`) the footnote-derived rows currently sit inline-by-order (orders 2/3/4 interspersed with the base movement), with the `*`/`**` text in `notes` + timing in `sequence` (F-PLAQUE carrier, kept) — content preserved verbatim. **Owner confirms at §6.7 whether "LAST" was strict-positional (→ reorder the rows to the schema tail, 1-line seed fix) or shorthand for "de-specialize the kind" (→ current expression is correct).** (QA-010.) **2026-06-11: blanket stage-approve — no item-wise answer; re-confirm inside the W2-UX-POLISH pass.** **STAYS OPEN past W3 (2026-06-12):** the W3 runner-prompt scoped W2-UX-POLISH to EXACTLY 2 items (D1 idempotency + D2 proto card) — this §6.7 seed-content re-confirm was NOT in W3 scope, and W3 touched zero seed DATA (the only seed touch was a pure const rename, DR-W3-FORK-3). The earlier "CLOSED 2026-06-11" was premised on the polish pass covering it; W3 narrowed polish to D1+D2 → this re-confirm is still owed at the next seed walkthrough. **CLOSED (2026-06-12, owner verbatim): "это же просто строка с упражнением HSPU и пятью повторениями, это НЕ УНИКАЛЬНАЯ СТРОКА" — the de-specialize reading confirmed; block-183's current expression is correct as-is; no reorder, no seed fix. (The `sequence` timing text folds into notes when the VO dies at W4 — D-PLAQUE.)** | CLOSED |
| W2-URL-PLACEHOLDER | ex-`STANDALONE_URL` → `PLACEHOLDER(coach_choice_slot)` + media — confirm it reads as a demo                                             | `week-1-saturday-modality.ts` (`BLOCK_URL_ONLY_*`): a content-less "here is a demo video" row → `{ rowKind: "PLACEHOLDER", placeholder: { placeholderKind: "coach_choice_slot", text: "Demo (see video)" }, media: { url } }`. Defensible among the 4 surviving kinds (no EXERCISE identity), but it IS a semantic re-interpretation (a coach-fillable slot demonstrated by a video, vs the original standalone URL). **Owner confirms at §6.7 whether this reads correctly as a demo (vs media on an exercise row).** (INFO-5.) **2026-06-11: the walkthrough verdict was a blanket stage-approve — no item-wise answer; re-confirm inside the W2-UX-POLISH pass.** **STAYS OPEN past W3 (2026-06-12):** same as W2-FOOTNOTE-LAST — W3's W2-UX-POLISH was scoped to EXACTLY D1+D2, this seed-content re-confirm was not in scope, and W3 changed zero seed data. Still owed at the next seed walkthrough. **2026-06-12: owner asked for the fix ("правь, я гляну что вышло") — seed made coach-readable (header "URL only body" → "Yoga time"; slot text "Demo (see video)" → "Follow the yoga video" / "Warm up for feet — follow the video"; urls renamed to content slugs); api-server type-check green; UNCOMMITTED — rides the next PR. Owner looks at the reseed → close.** **CLOSED (2026-06-12): owner reseeded + accepted ("сид пересеял, принят"); the seed edit committed to main.**                                                      | CLOSED |
| W2-UX-POLISH       | Walkthrough verdict 2026-06-11: "есть косяки по UI/UX, но это уже формат полировки, на данном этапе аппрув" — the nits are UNENUMERATED | **Enumerated 2026-06-12 — exactly 2 items:** (1) the LIVE idempotency-key 400 on the unchecked independent-ladders path (colon in `${draft.id}:${trackIndex}` vs the `IDEMPOTENCY_KEY_REGEX` charset); (2) bring the group card to the hi-fi prototype (`plan-editor-hi-fi-v-2`: solid tinted frame, GROUP overline, accent rail, track-no badges, seg interleave, Add group/Add track/Ungroup gestures). Both rode the W3 prompt as D1/D2. **CLOSED (W3 build 2026-06-12).** D1 = DR-W3-1 (separator `:`→`-`, format-pin test against the real regex via the FORK-2 barrel export). D2 = DR-W3-2/3/4/5/6 (proto card built platform-local — solid tinted frame, GROUP overline, accent rail, 22px track badges, `.seg` interleave; the full gesture set wired onto the existing W2 API).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | CLOSED |

### INFO carry-forwards (non-blocking)

- **W2-DRAFT-RECURSION (CLOSED — W3 build 2026-06-12, D4 / DR-W3-7):** the recursive `ComposeContainer.children: ComposeNode[]` (the last recursion in the codebase) collapsed to flat NON-recursive types in `axis-draft.types.ts`: `SchemaDraft { …, rows: ComposeRow[] }` (single-schema; `children`→`rows`, never containers) + `GroupDraft { …, tracks: TrackDraft[] }` with `TrackDraft { id, header, steps }`; `ComposeNode`/`ComposeContainer` deleted from the platform layer; the modal seed became a `DraftSeed` union (`{mode:"schema"}|{mode:"group"}`) and the `nodeType` discriminator died entirely. DR-W1-2 / DR-W1-5 preserved byte-for-byte (checkbox fork, per-track validation parity, non-atomic unchecked path). `arrangement-tree.ts` was deleted (its split has no flat-shape meaning — see W2-STALE-NAMES) and a `repetition-defaults.ts` leaf was extracted to break a D5 import cycle. (INFO-1.)
- **W2-IDEM-REMOUNT (OPEN, low — DELIBERATELY KEPT through W3):** idempotency dedup is scoped to the live modal session — `draft.id` regenerates on close+reopen (the modal is conditionally mounted → unmount on close). A retry after reopening mints a new base key → not deduped. This is the correct UX semantics (reopening = a fresh authoring gesture, DR-W2-4) and is OUTSIDE the §6.4 acceptance scope; pinned by a regression-tripwire test (MT-19 sibling — second mount's `draft.id` differs) so a future "fix" is conscious. **W3 explicitly preserved this (runner-prompt red line #6 — "do not make keys survive remount"); the D4 draft collapse re-expressed the key base on `GroupDraft.id` but kept the remount semantics intact.** Only re-open as a fix if a real coach need surfaces. (QA-004 boundary / INFO-3.)
- **W2-STALE-FIXTURES (CLOSED — W3 build 2026-06-12, D6 / DR-W3-10):** the dead `compoundRep: null` line dropped from both `exercise-row-payload-form.test.tsx` + `rest-row-form-schema.test.ts`. (INFO-2.)
- **W2-VESTIGIAL-EXPORTS (CLOSED — W3 build 2026-06-12, D6 / DR-W3-10):** `composeNodeSchema` + `ComposeNode` deleted from contracts `composition` (a 1-line four-projection retype `ComposeNode`→`ComposeContainer`, which survives); the `POSITION_EQUIPMENT_MODIFIERS`/`positionEquipmentModifierSchema`/`PositionEquipmentModifier` trio + its test `describe` block deleted from `_shared/media.ts` (zero consumers verified). These were the two sanctioned contracts touches the W3 runner-prompt allowed. (INFO-6.)
- **W2-STALE-NAMES (CLOSED — W3 build 2026-06-12, D6 / DR-W3-10):** all three resolved. `arrangement-tree.ts` was DELETED (not renamed — the flat draft collapse killed its `collectTrackChildren`/`collectDirectRows` split; nothing left to rename, see DR-W3-7); the `DerivedLabelCard` caption fixed "computed (arrangement-first)" → "computed (repetition-derived)" (the one coach-visible item); the seed const renamed `BLOCK_FOOTNOTES_WK2_TUE` → `BLOCK_PER_ROUND_MARKERS_WK2_TUE` (owner-delegated — DR-W3-FORK-3; pure identifier, byte-identical data). (QA-014.)
- **W2-DELETE-PLAN-SCOPE (no action, noted):** the group PUT/DELETE routes ignore the URL `planId` (pass only `groupId` to the api) — a pre-existing house wart matching `schemas/[schemaId]/route.ts`; authorization is sound via `verifyGroupOwnership` (group→…→plan→creator walk), so it is a path-consistency wart, NOT an escalation. (QA-002.)

## From the leaf-design touches (2026-06-12)

| ID                | One-liner                                                                                                                                                                                                         | Disposition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Status |
| ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ |
| LABEL-FLOW-UX     | Owner-proposed: label dropdowns (day / session / block levels) get a search input + create-on-the-fly when the search misses — no admin-console detours                                                           | A standalone UX feature outside this initiative's gates; the searchable-create picker component is SHARED with the W4 row-modifier picker (D-MODIFIER) — build once, consume at both surfaces. Schedule owner-paced (own small, or alongside W4's editor work)                                                                                                                                                                                                                                                                                                                                                                                                           | OPEN   |
| EQUIPMENT-LIBRARY | Owner aside (2026-06-12, "запиши себе"): equipment → its own coach-managed library; Exercise carries MULTIPLE equipment refs (today: single `primaryEquipment` enum); `movementTypeTagPrimary/Secondary` — снести | Verified: movement types have NO machine reader (display label-maps + admin classification card only) — killable. Naming fork acknowledged by owner: equipment-fanout naming ("Squat (Barbell)") collides with distinct exercises (Front/Back/Goblet Squat) — the multi-equipment-refs path avoids it. Outside the session-primitive grid (Exercise catalog out of scope per charter); tie-in: implement TYPE on the row rides here (D-LOAD-FINAL: count is on the row, implement type from the exercise) — own touch (the CATALOG pass, after W4)                                                                                                                       | OPEN   |
| CATALOG-NATURE    | D-ROW-GRAMMAR collapses `RowKind` PLACEHOLDER/REST_SLOT/REST into a catalog-record NATURE (`concrete\|placeholder\|rest`); the row's render-kind is INFERRED, not picked                                          | The full nature enum is CATALOG-pass work (out of the W4 model boundary, charter). W4 interim: keep the existing `placeholderFlag` + add a seed Rest-natured exercise as the bridge; formalize `concrete\|placeholder\|rest` with the equipment-library catalog pass (same touch as EQUIPMENT-LIBRARY).                                                                                                                                                                                                                                                                                                                                                                  | OPEN   |
| W4-HEADER-PARITY  | D-HEADER-KEEP: a schema's header must render identically standalone vs in-group (today the in-group member's top zone is cut)                                                                                     | Pure W4 UX — no model change. The editor renders an in-group schema's header like a standalone schema's. Rides the W4 `/feature` page work. **CLOSED (W4-editor build 2026-06-13, D7 / D-HEADER-KEEP):** the `!isBoxed` gate on the title row (composition chip + `InlineEditText` title) in `schema-card-head.tsx` was REMOVED — an in-group schema renders its header identically to standalone; the drag handle stays `isDraggable`-gated (boxed members non-draggable, unchanged). Render-only; supersedes DR-W1-3 (the W1 in-box head-dedup). QA-105 verified clean. (Leftover: the now-dead `isBoxed` prop on `SchemaCardHead` — CODE-003 INFO, optional cleanup.) | CLOSED |
| W4-EXEC-DEFER     | D-EXEC-DEFER known-loss: execution/scoring semantics (straight-into / no-rest transition, for-time, score-only-on-rounds, not-for-score, effort-qualifier) are NOTES today                                        | NOT a hole — the execution layer is deferred whole (no timer/scoring engine; charter non-goal). Typed FRESH against the Phase-4 executor (re-introduce-fresh) — likely a sequential-continuous Group mode beside the parallel interleave. NO inert boolean now. Owner flagged "straight into" explicitly; recorded so it is not lost.                                                                                                                                                                                                                                                                                                                                    | OPEN   |

## From the W3 build (editor remap, 2026-06-12) — owner-paced follow-ups + carry-forwards

The W3 build is green on every runnable gate (`check-types` 16/16, `lint` 16/16, `dep:check` 0, platform vitest 844+, contracts 741). None of the rows below are blockers; they are owner-paced cleanups + two latent-bug notes surfaced during the build. The gated api-server suite is NOT required this wave (the single seed touch is a pure const rename, byte-identical data — DR-W3-FORK-3); the owner MAY run it at merge for belt-and-suspenders.

| ID              | One-liner                                                                          | Disposition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Status        |
| --------------- | ---------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| OQ1-DEMOTE-DEAD | `should-be-container` + the demote hint appear fully dead post-collapse            | Post-DR-W3-7 the "Demote to row" BUTTON never renders (`onDemoteNode={undefined}`) and the demote-hint ALERT is unreachable in create mode (`SchemaDraft.rows` is always `[]` there). Kept RETYPED to `SchemaDraft` in W3 (DR-W3-9 — deleting a coach-facing affordance is a product call, not a refactor call). **Owner chose CARRY at Gate A.** Clean ~3-file follow-up deletion (`should-be-container.ts` + its test + the `container-inspector` hint wiring + `DEMOTE_BUTTON_LABEL`) when the owner has the appetite.  | OPEN (low)    |
| QA-106          | `materializeParallel` 2nd-ladder default `[15,12,9]` ≠ proto/Add-group `[9,15,21]` | The single-ladder→2-track materialize seeds its second track `[15,12,9]`, but the proto + block-level Add-group seed `[9,15,21]` (DR-W3-6). Inconsistent default. Recommend aligning `materializeParallel`'s second-ladder default to the proto's `[9,15,21]`. Cosmetic; no correctness impact (the coach edits steps anyway).                                                                                                                                                                                             | OPEN (low)    |
| QA-108          | `materializeParallel` drops the schema header (latent)                             | When a single `SchemaDraft` materializes to a `GroupDraft`, the schema `header` is not carried onto the group. LATENT — unreachable until create-mode gets a header field (today create-mode never authors a header into the draft). Note for the future header work (F-HEADER).                                                                                                                                                                                                                                           | OPEN (latent) |
| W3-REV-002      | `TRACK_GAP` is two un-shared consts (8px coincidence)                              | The 8px track gap is defined twice (the `.group-card__tracks` Stack gap + the rail's `top:-8px` continuation offset) as separate literals that happen to match. The design (§5.4.1) called for ONE `TRACK_GAP_PX = 8` const reused by both so they can never drift. Consolidate.                                                                                                                                                                                                                                           | OPEN (low)    |
| W3-REV-003      | In-group-add materialize discards tracks → empty schema (W2-IDENTICAL)             | A specific in-group-add path can materialize to an empty schema by discarding tracks. **NOT a W3 regression — this shape is W2-IDENTICAL (pre-existing).** Low-probability. Carry; fix opportunistically when the create-flow is next touched.                                                                                                                                                                                                                                                                             | OPEN (low)    |
| W3-REV-004      | The `[21,15,9]` proto-ladder literal appears in 3 files                            | The default first-ladder `[21,15,9]` is duplicated across 3 files (the add-track default, the add-group seed, and a draft default). Consolidate into one shared const (`PROTO_FIRST_LADDER`) per the design's §8 named-const note. Cosmetic.                                                                                                                                                                                                                                                                               | OPEN (low)    |
| QA-101          | Remount idempotency-key tripwire pin                                               | **CLOSED (W3 Test stage 2026-06-12).** The remount pin landed in `use-create-independent-ladders.test.ts`: a fresh mount mints a fresh base id → the two mounts' key sets are disjoint. Locks the W2-IDEM-REMOUNT by-design semantics (DR-W2-4 / red line #6) so a future "fix" can't silently make keys survive remount. Complements the existing within-session stability pin.                                                                                                                                           | CLOSED        |
| QA-107          | Proto-fidelity coverage for the rebuilt card                                       | **CLOSED (W3 Test stage 2026-06-12).** `group-track-wrapper.test.tsx` added — renders the REAL `GroupTrackWrapper` (+ real `GroupTrackBadge` + `SchemaCard`): pins the numbered badge ("Track N" tooltip), the absent member drag handle (`isDraggable={false}`), and the rail continuation offset (`top` 0 at index 0 vs −8px for continuations). The add-track instant POST payload was already pinned in `add-track-button.test.tsx`.                                                                                   | CLOSED        |
| W3-TRACK-DRAG   | The refreshed prototype adds in-group member drag (`useTrackDrag`)                 | The owner's 2026-06-12 prototype refresh (drag handles top-left) ALSO ships member reorder INSIDE a group: a per-track handle next to the track badge, same-group-constrained, reordering the member run. NOT implemented with the walkthrough fixes (new behavior beyond the reported defect — needs its own contiguity/QA pass). **#263 merged without it.** **SCHEDULED (2026-06-12, owner: "согласен, фиксируй"):** ONE `/feature small` follow-up together with W3-DND-POLISH; timing owner-paced, independent of W4. | SCHEDULED     |
| W3-DND-POLISH   | Optional DnD feel polish: DragOverlay compact ghost + `restrictToVerticalAxis`     | Offered at the walkthrough fix round, unanswered: (a) a `DragOverlay` with a compact ghost (head-only) instead of dragging the full-height card in place — calmer visuals for big cards; (b) `restrictToVerticalAxis` so vertical lists can't be dragged sideways. Pure taste (owner's domain) — yes/no per item. **SCHEDULED (2026-06-12):** both items accepted (compact ghost + vertical-axis restraint); rides the same `/feature small` as W3-TRACK-DRAG.                                                             | SCHEDULED     |

## From the W4-model build (data-layer reshape, 2026-06-13) — the PENDING acceptance gate + carry-forwards

The W4-model build is green on every runnable gate (`check-types` 16/16, `lint` 16/16, `dep:check` 0, `@repo/contracts` 664, platform vitest 514), Review = A (zero CRITICAL), QA = B (zero CRITICAL; the one WARNING QA-001 FIXED in-PR). The single blocking obligation is the **api-server gated suite (PENDING — owner's manual ritual)**; the rest are owner-paced cleanups + two latent W4-editor-scoped edges. The branch is 3 commits: `07768bdb` (data layer — modifier + row-group entities, leaf reshape, seed), `9529998d` (platform render off reshaped contracts), `5b099c1f` (Review/QA fix — QA-001 rowIds-unique + W4R-008 dead-const drop; W4R-001/QA-002 deferred-to-W4-editor noted in the commit body).

### The acceptance gate — PENDING the owner's manual ritual (HARD prerequisite before the app runs)

| ID             | One-liner                                                                                                                          | Disposition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Status |
| -------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ |
| W4-RESEED-GATE | The api-server gated suite (seed round-trip + 100%-coverage assertion + mapper/endpoint integration) is UNVERIFIED by the pipeline | The pipeline ran only the no-DB gates; the no-DB `canonicalSeedSchema.safeParse` passed (152 exercises, 15 modifiers, 11 row-groups). The OWNER's manual ritual is the acceptance gate: **`pnpm db:reset && pnpm db:seed && pnpm --filter @repo/api-server test`** (~10 min, live Neon, serial). It runtime-confirms: the seed round-trip incl. the raw-SQL layer (`lms-checks.sql` swept clean of killed-column refs — ZERO matches confirmed by QA); `assertExerciseRefsResolve` catches a deliberately-broken top-level `row.exerciseId`; the coverage gate passes with the new modifier/row-group cells AND the dropped killed-branch cells; the evil-fixture A–E shapes are present. **Also the bridge-free 500-until-reseed is BY DESIGN (the W2 QA-001 precedent):** the mappers `.parse()` against the new contracts, so the week GET returns a clean handled `InternalServerError` (`kind:"DbCorruption"`, NOT a partial render) on any stale row until `db:reset && db:seed` — the reseed is a HARD prerequisite for the app to function, not just for the suite. **Highest residual (QA #7, api-server-GATED, NOT runtime-tested):** row-group create concurrency — two creates wrapping OVERLAPPING rows under Serializable → exactly one wins (P2034→retry or 409); double-membership is structurally impossible (single `rowGroupId` FK column), and each tx runs its own in-tx contiguity assert. **Coverage-cell tuning (A3) is validated only at reseed:** the achievable-counts trim (the new live cells vs the dropped killed-branch cells) is asserted by `seed-coverage.test.ts`, which runs only in the gated suite. | OPEN   |

### Carry-forwards to W4-editor (latent — NOT reachable in W4-model; the editor gesture is the next wave)

| ID                | One-liner                                                                                         | Disposition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Status |
| ----------------- | ------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| W4R-001 / QA-002  | Row-group create silently re-homes an already-grouped row + orphans the old empty group           | `createRowGroupWrapping` does `updateMany({where:{id:{in:rowIds}}, data:{rowGroupId}})` WITHOUT checking whether a named row already has a non-null `rowGroupId` — it silently re-points the row to the new group and the old group can empty (the empty `RowGroup` is dropped from render by `buildRowItems` but persists orphaned in the DB). Mirrors the schema-group create's analogous property; double-membership is structurally impossible (single FK column), so it is SAFE, just unguarded. **NOT reachable in W4-model** (no user gesture exists yet; the seed authors groups directly with no overlap). **CLOSED (W4-editor build 2026-06-13, F3 / DR-W4E-W4R-001-CLIENT):** the grouping gesture is client-guarded — already-grouped rows render inside `RowGroupBox` with NO checkbox → structurally unselectable → a grouped rowId cannot reach `api.rowGroups.create` from the UI (Review + QA both code-traced it; pinned by `schema-row-list.test.tsx:264`). The SERVER overlap reject (the `rowGroupId !== null` filter on `createRowGroupWrapping`) is STILL ABSENT → carried forward as **W4R-001-SERVER** (the server backstop, owner-gated). | CLOSED |
| W4R-005           | `notesListToText`/`textToNotesList` round-trip is asymmetric (collapses a multi-note list to one) | `notesListToText` joins a `string[]` with `\n`; `textToNotesList` returns `[text]` (a single-element list, no split on `\n`) — a 3-note list → text → list becomes one merged note (`apps/platform/.../lib/notes-list-text.ts`). NOT exercised in W4-model (the multi-note editor is W4-editor; this helper is only read-render-adjacent today). **CLOSED (W4-editor build 2026-06-13, F2 / DR-W4E-NOTES-LOCAL):** `NotesListEditor` edits `string[]` DIRECTLY (no string⇄list conversion); `notes-list-text.ts` DELETED (grep `notes-list-text`/`notesListToText`/`textToNotesList` = ZERO); the inline carriers commit via a `NotesListField` blur-commit wrapper; `compute-day-stats.ts` + the rest-day/session-head read-only displays keep an inline `notes.join("\n")` for the derive/display. Multi-note round-trip no longer collapses (QA-106 verified).                                                                                                                                                                                                                                                                                                   | CLOSED |
| W4-COACH-MODIFIER | No coach `createModifier` endpoint yet (the create-on-the-fly picker)                             | The platform modifier surface is search-only (`lmsModifierPlatformApi.list`); create is admin-only (`cmsModifierAdminApi`, `withAdminAuth`). The runner-prompt §1.1 mentions "search + create-on-the-fly"; design §5.3 explicitly defers coach-create to W4-editor. So a coach CANNOT yet mint a modifier on the fly — a W4-editor gap, BY DESIGN, NOT a W4-model bug (QA-005). **CLOSED (W4-editor build 2026-06-13, F1 / D3):** `lmsModifierPlatformApi.create` added (mirrors `cmsModifierAdminApi.createModifier` byte-for-byte + a `requireCoachLikeRole(userId)` prefix; route `POST /api/platform/modifiers` = `withCoachAuth(withAuthRateLimit(createAuthPostHandler(...)))`; client + `useCreateModifier`/`useModifierSearch` hooks; NO Prisma/seed/contract change). The endpoint test is written (coach-creates + nameLower, non-coach→Forbidden, dup→Conflict count-stays-1); **the gated api-server suite at merge is the owner's ritual.**                                                                                                                                                                                                            | CLOSED |

### INFO carry-forwards (non-blocking; recorded for the trail)

- **W4R-006 / QA-003 (misleading entity string, low priority):** a non-existent `modifierId` on a row write triggers P2003 on the `RowModifierAssignment.modifierId` FK; `handlePrismaError(error, {entity:"SchemaRow"})` maps it to `BadRequestError("Referenced SchemaRow does not exist")` — the message names SchemaRow but the failing FK is the modifier. SAFE (a clean 400, the tx rolls back); only the message misdirects debugging. Optional fix: catch P2003 in `replaceRowModifiers` → `BadRequestError("One or more modifierIds do not exist")`, or pass `{entity:"Modifier"}` for that path. Low priority (W4-editor validates the picker; create-on-the-fly mints real ids).
- **W4R-007 (row-group update/delete don't cross-check URL planId — matches the house template):** `rowGroupByIdParamsSchema` validates `planId` in the URL but neither the route nor `lmsRowGroupApi.update/delete` cross-checks it against the row-group's real plan (unlike `create`, which does `owner.planId !== planId → NotFound`). This is IDENTICAL to the `lmsSchemaGroupApi.update/delete` template (the pre-existing W2-DELETE-PLAN-SCOPE wart). Auth is NOT bypassed — `verifyRowGroupOwnership` enforces creator-or-admin via the full plan-ownership walk. No action; if the codebase ever tightens schema-group update/delete to cross-check planId, mirror it here.
- **W4R-002 (per-row intensity/pace variance flattened to notes — by-design, lossy):** the A3 call #2 (DR-W4-3 sub-note) — original per-row numericPace/hrZone/rpe → free-text schema/row notes, one representative promoted to `schema.intensity`. A structured→unstructured downgrade a future intensity-aware engine can't read; it IS the deliberate D-EXEC-DEFER / D-FLOORS consequence (intensity per-row is channel-3 until an engine reads it → re-introduce-fresh, ADR-0038). No information dropped (preserved as notes). Recorded so per-row intensity is known to be notes-carried.
- **W4R-003 (block-timeCap-on-a-rounds-schema → a schema note — by-design):** the A3 call #3 (DR-W4-3 sub-note) — the two-constraint (rounds + cap) case is genuinely unmodelable in the one-axis `repetition` without a separate cap field (out of scope). Faithful given the constraint.
- **W4R-004 (`single_arm` side inconsistency between two fixtures — cosmetic):** the A3 call #4 (DR-W4-3 sub-note) — `single_arm`→`count:1` is consistent, but `side: eachArm()` is applied in `week-2-monday-strength.ts` (BSS) and omitted in `week-2-monday-weights.ts` st-1 (a split-tier member where the per-row side wasn't authored). Minor authoring inconsistency, not a contract violation. If the gated coverage suite wants side-parity, add `side: eachArm()` to st-1.
- **W4R-008 / QA-001 (CLOSED in this PR):** the missing `.refine(unique)` on `createRowGroupRequestSchema.rowIds` (the lone QA WARNING — every sibling list-input has one; dup rowIds fell through to a misleading "non-existent rows" 400) and the dead `MODIFIER_CONSTANTS.MAX_NOTES_LENGTH` (label-template residue; modifier notes validate via `notesListSchema`) were BOTH fixed in commit `5b099c1f` (the rowIds refine + its test pin; the const dropped). Kept here for the trail.

### Orchestrator git-diff review (2026-06-13, D-7 — the live reseed surfaced what no static gate could)

- **W4R-SEED-EXREF / CRITICAL — FIXED in-branch (`82b0413b`).** The owner's `db:seed` crashed live: `RefResolver: unresolved exercise ref "c5f65…" — not registered in catalog`. Root cause: `emitSchemaRow` wrapped `row.exerciseId` in `resolver.getExercise(...)`, which is keyed by the kebab `entry.ref` — but exercise refs are `EX.* = exerciseCuid(name) = the real catalog DB id` (`catalog-emit:35`), used DIRECTLY on main and validated by `load-and-validate` against `exerciseCuid(canonicalName)`. (Modifiers/labels legitimately use kebab refs via `getModifier`/`getLabel`; exercises are identity-refs, NOT indirected — the runner applied the wrong pattern to exercises.) One-line revert to `exerciseId: row.exerciseId`; api-server `check-types` green. **Why every gate missed it:** the no-DB pipeline never runs the emit (`canonicalSeedSchema.safeParse` validates SHAPE, not resolution); `db:seed` is owner-gated; static Review/QA traced the contract, not the live resolver keying. This is EXACTLY the W4-RESEED-GATE blind spot — the reseed ritual IS the gate, working as designed.
- **W4R-GETEX-DEAD (cleanup, low):** after the fix, `RefResolver.getExercise` + the exercise branch of the resolver map (`setExercise` at `catalog-emit:50`) are DEAD (no caller). Harmless (write-only map) but vestigial — remove `getExercise` + the exercise map + the `setExercise(entry.ref)` call for exercises in a cleanup (touches `ref-resolver.ts` + `catalog-emit.ts` + their tests). Owner-paced.
- **W4R-EXID-FK (flag for the catalog pass):** `SchemaRow.exerciseId` is a LOOSE `String` with NO Prisma FK (DR-W4-3 — defensible: it was already loose inside the old `rowPayload` Json; the runtime degrades gracefully to an "exercise" fallback; the seed's `load-and-validate` guards seed-level dangs). BUT it is the one catalog ref WITHOUT a FK (modifier/label assignments + `OneRMRecord` all use `@relation … onDelete: Restrict`). The FK + the `concrete|placeholder|rest` nature enum should land in the CATALOG pass (CATALOG-NATURE / EQUIPMENT-LIBRARY) — confirm it there so a deleted exercise can't silently dangle a row.
- **Review verdict: PASS (one CRITICAL caught + fixed).** Verified against the diff, not the self-report: scope = data-layer + minimal platform + the modifier admin (3 admin route files = the modifier CRUD, in-scope); the two new entities mirror `Label`/`SchemaGroup` correctly (join-table à la `BlockLabelAssignment`; `rowGroupId` SetNull à la `groupId`); `buildRowItems` one-predicate holds (EXACTLY one consumer — `schema-row-list.tsx`); the raw-SQL layer is clean (`lms-checks.sql` references zero killed columns — the W2 trap is absent); the killed surfaces (`position`/weight-exotics/`sequence`/`rowKind`/`rowPayload`/`REST_SLOT`) have ZERO real live stragglers (the three grep signals were substring false positives — `com`positionSchema`, BLOCK_WEIGHT_VARIANTS const, rowKindCases fixture); the nine Gate-A DRs are well-reasoned. The ONLY defect was the runtime seed-resolver bug, now fixed.

## From the W4-editor build (proto-faithful authoring UX, 2026-06-13) — owner-paced fast-follows + carry-forwards

The W4-editor build is green on every runnable gate (`check-types` 16/16, `lint` clean, `dep:check` 0, **platform vitest 699/699**, +110 tests from the dedicated test stage). Review = **A−** (0 CRITICAL — every red line holds; the only material gap was a test hole, backfilled in the test stage). QA = **C** at the time of the hostile pass (no data-corruption CRITICAL survived client construction, but 3 coach-UX CRITICALs — QA-001/002/003 raw-zod-leak + no-submit-block + invalid-default — **ALL FIXED in the Stage-5-6 fix-loop**, commit `2bda02c2`, DR-W4E-COACH-ISSUE; plus QA-008 mint-tolerance, commit `7c146b96`, DR-W4E-PICKER-MINT-TOLERANT). The four owned `deferred` items above (W4R-001/QA-002, W4R-005, W4-COACH-MODIFIER, W4-HEADER-PARITY) are CLOSED. The rows below are NOT blockers — they are server-side fast-follows (client-guaranteed or unreachable today) + INFO. **The §6 owner browser walkthrough (jsdom-blind to DnD/pointer/modal) is the REAL acceptance gate, POST-merge.**

### Server-side fast-follows (client-guaranteed or latent today; re-arm the gated api-server suite)

| ID                              | One-liner                                                                                                    | Disposition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Status |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| W4R-001-SERVER                  | `createRowGroupWrapping` never rejects already-grouped rowIds (server backstop for the client guard)         | DR-W4E-W4R-001-CLIENT — the client guards by construction (already-grouped rows render inside `RowGroupBox`, structurally unselectable → a grouped rowId cannot reach `api.rowGroups.create` from the UI; Review + QA verified). The SERVER `updateMany({where:{id:{in:rowIds}}, data:{rowGroupId}})` still has NO `rowGroupId !== null` filter → a latent silent re-home/orphan if a grouped rowId ever reaches it (another path, a script, a future "add to group"). Fast-follow: add `namedRows.filter(r => r.rowGroupId !== null) → BadRequest("row already in a group")` + a test pin. Re-arms the gated api-server suite (~10 min owner ritual). Belt-and-suspenders.                                         | OPEN   |
| DR-W4E-ROWREORDER-CONTIG-SERVER | The row-floor server reorder (`schema-row/admin.ts`) has NO group-contiguity guard (unlike the schema floor) | Row reorder was WIRED this wave (Gate A — DR-W4E-ROW-REORDER). The CLIENT guarantees contiguity by construction (a row-group is ONE `RowItem`, `arrayMove` moves whole items, `flatMap(itemMemberIds)` emits members adjacently — Review + QA code-traced it UNREACHABLE from drag). But the server `reorder` validates existence/scope/completeness and NOT group contiguity (contrast: row-group CREATE asserts it via `assertRowGroupMembersContiguous`). Latent: if any future change makes a member individually draggable, the server would persist a non-contiguous group. Fast-follow: add `assertRowGroupMembersContiguous` to the row reorder (re-arms the gated suite). Not reachable from the UI today. | OPEN   |

### Owner-walkthrough / small-fix candidates

| ID     | One-liner                                                                                                                                                                                           | Disposition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Status |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| QA-007 | Create-on-the-fly: re-typing an existing modifier's exact name that isn't in the (stale/debounced) search results mints a duplicate-attempt → ConflictError toast instead of attaching the existing | `hasExactNameMatch(searchResults, query)` only consults the in-flight (debounced, possibly stale) result set, not the full library — so the synthetic "Create «query»" option can appear for a name that DOES exist. The coach clicks it → `useCreateModifier` POSTs → server P2002 → ConflictError toast, and the modifier is NOT attached. Flag for the §6.4 walkthrough; small-fix candidate: on ConflictError, fetch + attach the existing modifier instead of erroring (or re-resolve the exact `q` before minting). jsdom-assertable (mock search returning empty, type an existing name → assert the gap). | OPEN   |

### INFO carry-forwards (non-blocking; recorded for the trail)

- **QA-009 (chip falls back to raw cuid):** `ModifierPicker` fabricates `Modifier` objects for the value chips; until `resolvedRefs`/`searchResults` resolve the name, a chip whose id is neither in `resolvedRefs` (the Edit read-embed seed) nor the current search shows the raw cuid. The common case is handled (Edit passes `resolvedRefs={mode.row.modifiers}` → real names); the leak only bites a stale optimistic id. Optional: model the value as `string[]` + a resolved-name lookup in `renderTags`, or narrow the option type to `{id,name}`.
- **QA-010 (no `enabled` gate on `useModifierSearch`):** the hook has the short staleTime but no `enabled` gate → a `q=undefined` full-library fetch on every picker mount (every row-modal open). Fine for the small modifier table (research-confirmed; the no-list-caps house rule keeps it honest) and the picker debounces keystrokes; a deviation from the design's "`enabled` only when mounted-with-query" note. Add `enabled: q !== ""` or a server cap if the library grows.
- **QA-011 (`NotesListField` blur-commit lost-update window):** the buffered inline carrier commits on blur against its focus-time snapshot, not the freshly-arrived `value` prop — so a concurrent week-query invalidation during an edit can be overwritten last-writer-wins. This is the SAME window as the prior `useBlurCommit`/`InlineEditText` pattern (pre-existing, not a regression); low-probability for a single-author coach tool. The multi-note round-trip itself (the W4R-005 fix) is correct (QA-106). Optional: re-base the diff against the current `value` on blur.

- **`Performed*` / `OneRMRecord` are known-wrong** (per-row actuals vs per-workout scores; `@@unique([sessionId,userId])` latest-only; 1RM no-history contradicting roadmap A3). Redesign against the real logging UX in Phase 4. Do not build on them.
- **Phase-4 score-type model is stale** (was tracked as "`docs/roadmap.md` §4.2 per-archetype score-type" — post-ADR-0037/0038 wording). `docs/roadmap.md` was REMOVED in the W2 PR (commit `58de8394`, 2026-06-11, owner-requested chore). The debt itself survives the doc: the `Performed*` / `OneRMRecord` redesign (the bullet above) is the live carrier — reconcile the score-type model against the real logging UX when Phase 4 planning starts.
- **Reuse features** (clone week/day/block, saved compositions) — the persona's pain #1; app-level features over this model, after the primitive lands.
