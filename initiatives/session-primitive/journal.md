# session-primitive — journal

Append-only. One entry per session/step.

## 2026-06-10 — founded out of the domain-model review

- **Trigger.** Owner: "что не так с нашей тренировочной доменной моделью? я не могу понять почему, но мне она не нравится … нужно честно." Full review run (schema, live contracts, ADR-0037..0040, analysis genesis, persona, roadmap).
- **Diagnosis the owner accepted.** (1) Corpus-first genesis photographed ONE personal plan's Excel into types — parsing residue as types (Position enum both-orderings, `wrapped` bracket flag, 8-variant Weight with cardinality-1 members, 9 row kinds); owner: "фотография/музей похож на бредовую галлюцинацию программиста, я согласен." (2) Relations smeared across three mechanisms on three floors (derived parallel / stored superset pairs / fat in-row VOs). (3) The June drains (ADR-0037..0040) were the cure but stopped at the container level. (4) The model models the plan-document, not the coaching — though reuse/copy is feature-work, not model-work (owner correction), and cycles are future enrichment above the primitive (owner scoping).
- **The owner designed the cure's core himself across the session:** relations = boxes that hold contiguous members and carry an unread label ("связывает блок … я не вижу что на нём написано"); auto-linking = "медвежья услуга" (explicit gesture + batch checkbox); my typed relation core (parallel|choice|superset) rejected via the applicability-matrix argument ("запашок"); fixed floors session-блок-схема-строка; the instruction-plaque candidate primitive ("плашка … на ней может быть написано OR, THEN, ANY, и даже REST — одним махом закрывает несколько функционально однотипных пунктов").
- **Founding artifacts.** D-1..D-7 ratified + D-MARKER-DEATH OPEN (`decisions.md`); the notation grid over the whole corpus with verbatim owner verdicts folded in (`primitive-spec.md`); F-ledger of owner-marked follow-ups (`deferred.md`); rough 5-step plan (`plan.md`). Grid numbers verified against `schema-content-primitives.md`, `modifier-scope.md`, `compound-and-alternative.md`, `03-content/edge-cases.md` (re-read in full at founding); `load-representation.md` deferred to F-WEIGHT-EXOTICS by owner's own "не сейчас".
- **Predecessor closed.** `compose-authoring-ux` CLOSED the same session (both steps merged: PR #258; PRs #259+#260 + ADR-0040); its open carry-forwards transferred here; `initiatives/ACTIVE` → `session-primitive`.
- **Working model ratified (D-7).** Orchestrator (план → промпт → ревью) + owner as transport/ratifier; runner sessions via `/feature`; review via git diff.
- **Next.** Step 1 follow-ups, owner-paced, F-PLAQUE first. No runner sessions until spec freeze.

## 2026-06-10 — D-8 ratified (JIT freeze); W1 launched

- Owner: "а мы можем сейчас начать имплементацию?" → dependency-honest answer: the schema-level world (D-2/D-3) is fully ratified; OPEN items gate only the row-level grammar and part of the leaf. The founding "no runner sessions until spec freeze" gate was over-cautious — relaxed as **D-8 JIT-FREEZE** (owner: "давай промпт, я готов запускать"); `plan.md` restructured into waves W1–W4 with explicit needs-decided-first column.
- **W1 prompt issued** (Group/box UX on the existing model): box render gated by the live `isStructurallyParallel` predicate, label = parent `header` via the existing update path, explicit «связать в коробку» checkbox (default checked) in the ladder batch flow; DnD/ungroup persistence explicitly OUT (no re-parenting API → W2/W3); platform-only red line. Runner = `/feature` full in a fresh session, owner transports; orchestrator reviews the git diff on return.
- **Pending from owner before W2:** D-MARKER-DEATH (yes/no).

## 2026-06-10 — W1 built (Group/box UX, PR open on `feat/session-primitive-box-ux`)

- **Ran the W1 prompt via `/feature` full** (the runner + orchestrator-review roles collapsed into one session — the prompt was self-contained per D-7). 9 platform-only commits, 8 source files in `apps/platform/src/modules/plan-detail/` (3 new: `schema-group-box.tsx`, `group-into-box-checkbox.tsx`, `use-create-independent-ladders.ts`; 5 edits) + 7 test files. `git diff --stat` = `apps/platform` + these initiative docs only.
- **What shipped.** Box render = `AccentGroupCard` (`accent-dashed`), gated by the live one-predicate computed once in `schema-card.tsx` (byte-identical to the chip signal — ADR-0040 compliance verified independently by both the Review and QA agents, the highest-risk line). Box label = parent `header` via `InlineEditText` reusing `handleTitleCommit` (chip + head-title suppressed on boxed parents — header shown once; empty → "group…" placeholder, mechanism intact). «Group into one box» checkbox (default checked) = submit-branch flag in `AxisEditorModal`; unchecked → new `useCreateIndependentLadders` (N non-atomic flat creates). Depth-3 (block-010) boxes via recursion, outer rounds card stays plain; EMOM/cadence stay plain (predicate-gated).
- **Gates.** Gate A: owner chose `accent-dashed` over solid-divider + English copy "Group into one box" + build (the head-arrangement nuance — slim chrome head above the box — accepted as-is). Gate B: 0 source CRITICAL (Review SHIP; QA's "CRITICAL" QA-001 was the deferred test-migration, not a source defect); owner elected to fix QA-003 in-PR (independent-path client-side step validation + coach-message parity, `finally`-wrap). QA-002 (unchecked dup-on-retry) accepted per D-2 → W2 idempotency (W1-DUP-RETRY); QA-005 (double-frame) → owner eyeball at acceptance.
- **Verification (orchestrator-rerun, not agent self-report).** `pnpm --filter platform check-types` + `lint` exit 0; `SKIP_ENV_VALIDATION=1 pnpm exec vitest run --project platform` → **84 files / 1035 tests green** (incl. the migrated chip→box assertions + the MT-1 mock that un-RED'd the 42 modal-mounting tests + the new box/checkbox/hook coverage, MT-1..MT-20 less MT-14/MT-19 deferred-with-reason).
- **Red lines held:** platform-only (zero contracts/api-server/Prisma/seed); `isStructurallyParallel`/`INNER_LADDER_MARKER`/D-LADDER untouched; `@repo/ui`/`@repo/mui` imported not modified; no new field/enum/endpoint/relation-kind; no DnD/ungroup/member-removal persistence fakes.
- **Ratified calls promoted:** DR-W1-1..5 (`decisions.md`). **Carry-forwards:** W1-DUP-RETRY, W1-RENDER-REPOINT (both → W2), W1-BOX-FRAME (acceptance eyeball), W1-INSESSION-CHECK (`deferred.md`).
- **Next.** Owner acceptance walkthrough (the 4 seed parallels + block-010 — does the box feel right; eyeball the double-frame) + merge. Then D-MARKER-DEATH (yes/no) gates W2 (model core: Group entity, recursion/arrangement death — at which point W1-RENDER-REPOINT re-points box-ness from the predicate to Group membership).

## 2026-06-10 — W1 orchestrator review PASSED (second pair of eyes); session handed off

- **Independent review of PR #261 in the orchestrator session** — not redundant: the W1 build session collapsed runner+review roles, so per D-7 (self-reports untrusted) this was the first truly independent pass. Verified against the actual diff: scope = plan-detail + initiative docs only; one-predicate held (zero hand-rolled child-count checks; `INNER_LADDER_MARKER` 0 hits in the diff); draft transforms untouched; DR-W1-1..5 match what shipped. Gates re-run: root check-types + lint green; platform **1035/1035**.
- **One review finding promoted:** W1-SUBADD-BOX (`deferred.md`) — in the add-sub-schema context an UNCHECKED «Group into one box» can't hold: N children land under one parent, and a repetition-less parent boxes by the live derivation law regardless. By-design under ADR-0040; dissolves at W2; hide-checkbox-on-`parentSchemaId` is the cheap fix if it confuses at the walkthrough. Pushed to the PR (`12291982`).
- **Owner walkthrough was IN PROGRESS at handoff** — verdict NOT yet collected (browser pass: 4 seed boxes, block-010 triple-frame W1-BOX-FRAME, unchecked de-emphasis, label edit/clear, W1-SUBADD-BOX confusion check).
- **Handoff.** Next planner starts at `state.md` §Next action (rewritten as the handoff). A runner-prompt checklist was added to `plan.md` so the W2 brief carries the same discipline without this chat's context.
