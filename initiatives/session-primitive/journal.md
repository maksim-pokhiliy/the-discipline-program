# session-primitive — journal

Append-only. One entry per session/step.

## 2026-06-10 — founded out of the domain-model review

- **Trigger.** Owner: "что не так с нашей тренировочной доменной моделью? я не могу понять почему, но мне она не нравится … нужно честно." Full review run (schema, live contracts, ADR-0037..0040, analysis genesis, persona, roadmap).
- **Diagnosis the owner accepted.** (1) Corpus-first genesis photographed ONE personal plan's Excel into types — parsing residue as types (Position enum both-orderings, `wrapped` bracket flag, 8-variant Weight with cardinality-1 members, 9 row kinds); owner: "фотография/музей похож на бредовую галлюцинацию программиста, я согласен." (2) Relations smeared across three mechanisms on three floors (derived parallel / stored superset pairs / fat in-row VOs). (3) The June drains (ADR-0037..0040) were the cure but stopped at the container level. (4) The model models the plan-document, not the coaching — though reuse/copy is feature-work, not model-work (owner correction), and cycles are future enrichment above the primitive (owner scoping).
- **The owner designed the cure's core himself across the session:** relations = boxes that hold contiguous members and carry an unread label ("связывает блок … я не вижу что на нём написано"); auto-linking = "медвежья услуга" (explicit gesture + batch checkbox); my typed relation core (parallel|choice|superset) rejected via the applicability-matrix argument ("запашок"); fixed floors session-блок-схема-строка; the instruction-plaque candidate primitive ("плашка … на ней может быть написано OR, THEN, ANY, и даже REST — одним махом закрывает несколько функционально однотипных пунктов").
- **Founding artifacts.** D-1..D-7 ratified + D-MARKER-DEATH OPEN (`decisions.md`); the notation grid over the whole corpus with verbatim owner verdicts folded in (`primitive-spec.md`); F-ledger of owner-marked follow-ups (`deferred.md`); rough 5-step plan (`plan.md`). Grid numbers verified against `schema-content-primitives.md`, `modifier-scope.md`, `compound-and-alternative.md`, `03-content/edge-cases.md` (re-read in full at founding); `load-representation.md` deferred to F-WEIGHT-EXOTICS by owner's own "не сейчас".
- **Predecessor closed.** `compose-authoring-ux` CLOSED the same session (both steps merged: PR #258; PRs #259+#260 + ADR-0040); its open carry-forwards transferred here; `initiatives/ACTIVE` → `session-primitive`.
- **Working model ratified (D-7).** Orchestrator (план → промпт → ревью) + owner as transport/ratifier; runner sessions via `/feature`; review via git diff.
- **Next.** Step 1 follow-ups, owner-paced, F-PLAQUE first. No runner sessions until spec freeze.

## 2026-06-10 — D-8 ratified (JIT freeze); W1 launched

- Owner: "а мы можем сейчас начать имплементацию?" → dependency-honest answer: the schema-level world (D-2/D-3) is fully ratified; OPEN items gate only the row-level grammar and part of the leaf. The founding "no runner sessions until spec freeze" gate was over-cautious — relaxed as **D-8 JIT-FREEZE** (owner: "давай промпт, я готов запускать"); `plan.md` restructured into waves W1–W4 with explicit needs-decided-first column.
- **W1 prompt issued** (Group/box UX on the existing model): box render gated by the live `isStructurallyParallel` predicate, label = parent `header` via the existing update path, explicit «связать в коробку» checkbox (default checked) in the ladder batch flow; DnD/ungroup persistence explicitly OUT (no re-parenting API → W2/W3); platform-only red line. Runner = `/feature` full in a fresh session, owner transports; orchestrator reviews the git diff on return.
- **Pending from owner before W2:** D-MARKER-DEATH (yes/no).

## 2026-06-10 — W1 built (Group/box UX, PR open on `feat/session-primitive-box-ux`)

- **Ran the W1 prompt via `/feature` full** (the runner + orchestrator-review roles collapsed into one session — the prompt was self-contained per D-7). 9 platform-only commits, 8 source files in `apps/platform/src/modules/plan-detail/` (3 new: `schema-group-box.tsx`, `group-into-box-checkbox.tsx`, `use-create-independent-ladders.ts`; 5 edits) + 7 test files. `git diff --stat` = `apps/platform` + these initiative docs only.
- **What shipped.** Box render = `AccentGroupCard` (`accent-dashed`), gated by the live one-predicate computed once in `schema-card.tsx` (byte-identical to the chip signal — ADR-0040 compliance verified independently by both the Review and QA agents, the highest-risk line). Box label = parent `header` via `InlineEditText` reusing `handleTitleCommit` (chip + head-title suppressed on boxed parents — header shown once; empty → "group…" placeholder, mechanism intact). «Group into one box» checkbox (default checked) = submit-branch flag in `AxisEditorModal`; unchecked → new `useCreateIndependentLadders` (N non-atomic flat creates). Depth-3 (block-010) boxes via recursion, outer rounds card stays plain; EMOM/cadence stay plain (predicate-gated).
- **Gates.** Gate A: owner chose `accent-dashed` over solid-divider + English copy "Group into one box" + build (the head-arrangement nuance — slim chrome head above the box — accepted as-is). Gate B: 0 source CRITICAL (Review SHIP; QA's "CRITICAL" QA-001 was the deferred test-migration, not a source defect); owner elected to fix QA-003 in-PR (independent-path client-side step validation + coach-message parity, `finally`-wrap). QA-002 (unchecked dup-on-retry) accepted per D-2 → W2 idempotency (W1-DUP-RETRY); QA-005 (double-frame) → owner eyeball at acceptance.
- **Verification (orchestrator-rerun, not agent self-report).** `pnpm --filter platform check-types` + `lint` exit 0; `SKIP_ENV_VALIDATION=1 pnpm exec vitest run --project platform` → **84 files / 1035 tests green** (incl. the migrated chip→box assertions + the MT-1 mock that un-RED'd the 42 modal-mounting tests + the new box/checkbox/hook coverage, MT-1..MT-20 less MT-14/MT-19 deferred-with-reason).
- **Red lines held:** platform-only (zero contracts/api-server/Prisma/seed); `isStructurallyParallel`/`INNER_LADDER_MARKER`/D-LADDER untouched; `@repo/ui`/`@repo/mui` imported not modified; no new field/enum/endpoint/relation-kind; no DnD/ungroup/member-removal persistence fakes.
- **Ratified calls promoted:** DR-W1-1..5 (`decisions.md`). **Carry-forwards:** W1-DUP-RETRY, W1-RENDER-REPOINT (both → W2), W1-BOX-FRAME (acceptance eyeball), W1-INSESSION-CHECK (`deferred.md`).
- **Next.** Owner acceptance walkthrough (the 4 seed parallels + block-010 — does the box feel right; eyeball the double-frame) + merge. Then D-MARKER-DEATH (yes/no) gates W2 (model core: Group entity, recursion/arrangement death — at which point W1-RENDER-REPOINT re-points box-ness from the predicate to Group membership).

## 2026-06-10 — W1 orchestrator review PASSED (second pair of eyes); session handed off

- **Independent review of PR #261 in the orchestrator session** — not redundant: the W1 build session collapsed runner+review roles, so per D-7 (self-reports untrusted) this was the first truly independent pass. Verified against the actual diff: scope = plan-detail + initiative docs only; one-predicate held (zero hand-rolled child-count checks; `INNER_LADDER_MARKER` 0 hits in the diff); draft transforms untouched; DR-W1-1..5 match what shipped. Gates re-run: root check-types + lint green; platform **1035/1035**.
- **One review finding promoted:** W1-SUBADD-BOX (`deferred.md`) — in the add-sub-schema context an UNCHECKED «Group into one box» can't hold: N children land under one parent, and a repetition-less parent boxes by the live derivation law regardless. By-design under ADR-0040; dissolves at W2; hide-checkbox-on-`parentSchemaId` is the cheap fix if it confuses at the walkthrough. Pushed to the PR (`12291982`).
- **Owner walkthrough was IN PROGRESS at handoff** — verdict NOT yet collected (browser pass: 4 seed boxes, block-010 triple-frame W1-BOX-FRAME, unchecked de-emphasis, label edit/clear, W1-SUBADD-BOX confusion check).
- **Handoff.** Next planner starts at `state.md` §Next action (rewritten as the handoff). A runner-prompt checklist was added to `plan.md` so the W2 brief carries the same discipline without this chat's context.

## 2026-06-11 — W1 closed (merged + accepted); D-MARKER-DEATH ratified; W2 prompt issued

- **PR #261 merged** to main `6a45cc3d` by the owner (branch deleted) ahead of the recorded verdict; the walkthrough verdict came in this session: **"всё работает, полировка будет потом"** → W1-BOX-FRAME CLOSED (accepted as-is; solid-divider fallback stays documented), W1-SUBADD-BOX no standalone fix (dissolves in W2), de-emphasis + label behavior accepted. W1 fully closed.
- **D-MARKER-DEATH RATIFIED** (owner: "да, вырываем с корнем") — supersedes the predecessors' "marker is sacred" clause; mechanism dies, D-LADDER semantics survive as two structures. W2 unblocked.
- **Orchestrator verify-pass before locking the W2 prompt** (live code, not memory): contracts core read verbatim (composition/label/row/reps/load/weight/compounds/media, schema + api schemas); two Explore sweeps mapped every consumer (admin/marketing: ZERO touches — the whole surface is contracts + platform + api-server). Material corrections vs assumptions: (1) **seed is marker-FREE** — block-037 is already two ladder sub-schemas; the "38/~15" figures are corpus facts; `coverage-matrix.md`'s "marker ≥1" line is stale and dies unsatisfied; (2) the monorepo has a **complete dormant idempotency layer** (`@repo/api-routes` `wrapAuthHandler` + `Idempotency-Key` + replay store, bootstrapped in `bootstrap-backend-di.ts`, zero route consumers) — chosen as the W1-DUP-RETRY mechanism over a new DB column (house pattern); (3) `dual_value` (m/f) lives as a WEIGHT variant with an inert `resolver: "athlete_profile"` literal — moves to `load.byProfile {first, second}`, resolver dropped (ADR-0038 re-introduce-fresh); (4) seed superset arrangement exists only in `phase-7-superset-blocks.ts` (Phase-7 speculation, 0 corpus) — dies into plain ordered rows; (5) today's top-level schema order was never DB-enforced (unique keyed NULLable `parentSchemaId`) → `@@unique([blockId, order])` consciously deferred to W3's reorder rebuild (**W2-ORDER-UNIQUE**).
- **`w2-runner-prompt.md` issued** (self-contained per the plan.md checklist): target Prisma/zod shapes spelled out (SchemaGroup + SetNull dissolution; composition → `{repetition?, rest?}`; ROW_KINDS → 4; reps/load/media slims); 9 deliverables (model, group endpoints, guards incl. contiguity, idempotency, read surface, seed re-expression, platform parity adaptation, test migration, in-PR close-out); red lines fence every OPEN F-row surface + W3 scope; the new ONE-predicate = a single shared clustering helper in contracts; acceptance = a 9-step owner walkthrough + the owner's gated api-server ritual.
- **Next.** Owner transports the prompt body into a fresh `/feature` full session; orchestrator reviews the returned diff via git (D-7).

## 2026-06-11 — W2 built (model core; PR on `feat/session-primitive-w2-model-core`)

- **Ran the W2 prompt via `/feature` full** (self-contained per D-7). The box became REAL: a persisted **`SchemaGroup`** entity (block-owned `Cascade`; members link via `Schema.groupId` with `onDelete: SetNull` → deleting a group dissolves it, members survive; NO `order` column — position = `min(member.order)`; opaque `label`; `interleaveOrder` validated `String`). **Recursion died** (`parentSchemaId` + the self-relation + both indexes gone; `Schema` gains `groupId` + `@@index`; the three `z.lazy` recursive schemas became plain objects; `SchemaWithBody = { schema, rows }`). **The arrangement axis died whole** (`arrangement`/`supersetPair`/top-level `interleaveOrder`/`ARRANGEMENT_AXIS_KINDS`/`isStructurallyParallel`/`CompositionStructure`/the parallel+superset label kinds/the compose recursion + marker `superRefine` → `composition = { repetition?, rest? }`). **Ratified leaf kills landed** (reps `implicit`/`total_flag`/`compound_rep_unit` + `max→{tail?}`; load gained `byProfile`+`none`, dropped `without_weight`/`unspecified`; weight dropped only `dual_value`; media `{url, label?}`; `RowKind`→4; `compoundRep` column gone; `cyclical`/`sandwich` gone; `pairedConcreteRowId` killed — premise verified false). **Seed re-expressed** (4 parallel-parents → `composeGroup` + flat members; block-010 depth-3 → 1 group label-carrying the rounds; block-011 → 1-member group "3 rounds:"; block-015 → two sibling schemas "then"; EMOM/cadence → slots-as-rows; leaf re-expressions; the fragile `countStructurallyParallelParents` rewritten to count `schemaGroup` members `>= 2`). **Platform re-pointed** (clustering via the new `buildBlockItems` one-predicate at the block-list level; `schema-card` lost its `isBox`/recursion; modal fork preserved, checkbox hidden in-group; load editor gained `byProfile`/`none`; dying editors deleted). **Idempotency** threaded client-side (stable `${draft.id}:${trackIndex}` key) on the existing house layer.
- **The 9-commit sequence:** `bca55799` contracts (schema-group module + arrangement/leaf kills) → `58de8394` chore (remove stale `docs/roadmap.md`, owner-requested) → `ec567194` api-server (model + group endpoint + read-surface + guards) → `79109f10` (seed + coverage re-express) → `b79d6159` (bound `canonical-schema` inference for the tsc serialization budget — TS7056, behavior-neutral) → `6eb16dde` (api-server test migration) → `134064e6` platform (group routes + api + hooks + idempotency) → `507e1e97` (re-point render + slim editors) → `e3205bd0` (platform test migration).
- **State IN BOLD (QA-001): the migration is aggressive bridge-free — the read mappers `.parse()` against the new contracts, so the app 500s on a non-reseeded DB.** The owner's `pnpm --filter @repo/api-server db:reset` + seed is a **HARD PREREQUISITE for the app to function**, not merely for the gated test suite. Any pre-W2 row shape (marker / footnote / standalone / `compoundRep` / `arrangement` / `dual_value`) fails the read parse and 500s the whole week GET.
- **Verify (orchestrator-rerun, not agent self-report):** `pnpm check-types` 16/16, `pnpm lint` (`--max-warnings 0`) 16/16, `pnpm dep:check` 0 violations (1623 modules; the new schema-group module/endpoint/routes added no cycle, contracts stayed Prisma-free), contracts vitest **745/745**, platform vitest **801/801** (W1 baseline 1035 → 801: 5 dead-form test files + dead-kind fixtures removed + W1 box tests re-targeted to group fixtures). The Priority-1 backwards-compat grep is clean (zero live stragglers for every deleted identifier).
- **Owner-gated, NOT run by the pipeline:** the **api-server test SUITE** is rewritten + type-correct but owner-gated (live Neon, ~10 min serial). The owner runs `db:reset` + seed + the serial suite as the acceptance gate. The re-derived ≥2-member structural-parallel group count = **4** is verified STATICALLY (source inspection: block-037/009/087/010; block-011 is 1-member and correctly does NOT count) — confirmed LIVE only at reseed.
- **Process deviation (noted):** tests were folded INTO the Stage-4 implementation waves rather than run as a separate test stage — a package's `check-types` includes its tests, so source + tests migrated together per package (contracts, then api-server, then platform). The runnable gates (contracts + platform vitest) were re-run green; the api-server suite stays the owner's ritual.
- **Review + QA verdict:** Review = BLOCK solely on the (then-)missing D9 close-out (CR-1) + the out-of-scope roadmap deletion (WARN-1); engineering "excellent", every red line held, every judgment call sound. QA = B+ (zero CRITICAL; WARNINGs = QA-001 reseed-prerequisite + QA-004 close+reopen idempotency boundary). **This close-out commit resolves CR-1** (DR-W2-1..9 + DR-W2-FORK-1..6 promoted to `decisions.md`; the 4 W2-scheduled carry-forwards CLOSED; W2-ORDER-UNIQUE confirmed OPEN→W3; ADR-0041 written, 0040 superseded). The roadmap deletion (WARN-1) was an owner-requested chore — re-homed: both orphaned references (`deferred.md:55` standing-debt + `state.md` gotcha) updated, the Phase-4 score-type debt re-anchored to the `Performed*`/`OneRMRecord` carrier; `coverage-matrix.md` dead-kind rows pruned.
- **Next.** Owner ritual: `db:reset` + seed + the gated api-server suite + the §6 9-step acceptance walkthrough (the 4 ex-parallel boxes incl. block-037's multi-track; block-010/011 rounds-label boxes; EMOM slots; label round-trip; checked/unchecked batch; in-modal retry idempotency; in-box add; delete-to-dissolution; the 4 row kinds + footnote-LAST + URL-as-demo content calls; group-as-unit reorder). Then W3 (editor remap / reorder rebuild — W2-ORDER-UNIQUE + the recursive-draft collapse ride it).

## 2026-06-11 — W2 orchestrator review PASSED (two defects found via live ritual + review; fixed in-branch)

- **Independent review of PR #262** (10 commits, +4.5k/−13k) — gates re-run, never the self-report: check-types 16/16, lint 16/16, `dep:check` 0 violations (1623 modules), contracts **745/745**, platform **801/801** — all match the runner's claims. Scope bound verified: contracts + api-server + platform + docs only; **admin/marketing zero-touch**. Red lines held: every dead-identifier hit is a NEGATIVE pin ("rejects the dropped X") — correct regression tests, not stragglers; `weight.ts` diff = `dual_value` removal ONLY (exotics intact); `enums.ts` diff = footnote/standalone artifacts only (`or_alternative`/`placeholder` intact); tempo/sequence/side/position untouched. Prisma verbatim = the prompt's target shapes. One-predicate held: `buildBlockItems` has exactly ONE platform consumer (`block-card-body.tsx`); other `groupId` refs are mutations/props, not clustering. Seed spot-checks: block-037 `composeGroup` ✓, block-011 1-member "3 rounds:" ✓ (DR-W2-6), block-015 siblings ✓ (DR-W2-7), EMOM flattened (zero `composeGroup`/`parentSchemaId`) ✓, superset file drained (zero arrangement/pairs) ✓, 5 `composeGroup` sites = 4 multi-member + 1 single ✓. **Idempotency end-to-end verified** — the founding "zero route consumers" read was a grep artifact (the exclusion swallowed `auth-factories.ts`): every auth POST factory is `wrapAuthHandler`-wrapped, the client maps `idempotencyKey` → `Idempotency-Key`, the hook threads `${draft.id}:${trackIndex}` — DR-W2-4 confirmed.
- **Defect 1 (caught LIVE by the owner's `db:reset`, fixed `de0c5def`):** `prisma/sql/lms-checks.sql` still created the partial unique `schemas_block_top_order … WHERE "parentSchemaId" IS NULL` → 42703 on the dead column; `db:reset` exit 1, seed never ran. BOTH straggler sweeps (prompt verify-pass + runner Priority-1) missed `.sql` under `prisma/sql/` — tsc, vitest, and `.ts`-scoped greps are ALL blind to the raw-SQL layer. **This falsified DR-W2-8's premise** ("order was never DB-enforced" — it was, by this very check). Fix: FULL unique `schemas_block_order ON ("blockId","order")`.
- **Defect 2 (found by mutation-trace under the now-live unique, same commit):** `resolveGroupedOrder`'s trailing shift used `Promise.all` with `+ORDER_STEP` over a 10-spaced sequence — adjacent updates collide under the unique (30→40 while 40 exists). Rewritten descending-sequential; reorder's two-phase negative-order dance and the group tail-append survive verbatim; the into-group test now pins TWO trailing schemas (the collision shape).
- **Docs trail:** DR-W2-8 → SUPERSEDED, W2-ORDER-UNIQUE → CLOSED (`578365cb`), ADR-0041 consequences corrected — nothing order-related remains for W3.
- **Pending before merge:** owner re-runs `db:reset` + seed (now passes) → gated api-server suite (validates the unique + shift live) → §6 walkthrough → owner confirms the `roadmap.md` deletion was his request (Review WARN-1; runner recorded "owner-requested") → merge PR #262.

## 2026-06-11 — W2 MERGED + stage-accepted; polish ledger opened; ADR-0041 is the law of main

- **PR #262 merged — main `d76f6c8c`, branch deleted.** The owner's reseed went clean on the fixed `lms-checks.sql`; browser walkthrough verdict verbatim: **"есть косяки по UI/UX, но это уже формат полировки, на данном этапе аппрув."** The model core is live: `SchemaGroup` membership is the one relation mechanism; ADR-0040 superseded.
- **Dispositions.** The §6.7 content questions (W2-FOOTNOTE-LAST: strict-LAST vs de-specialize; W2-URL-PLACEHOLDER: demo-read) got NO item-wise answer inside the blanket approve — both re-confirm inside the polish pass. NEW carry-forward **W2-UX-POLISH** (owner enumerates the nits; W3's natural scoping input — collect before locking the W3 prompt). Roadmap WARN-1 closed: the owner merged with the flag explicitly raised. **Gated api-server suite run at merge unconfirmed — ask before W3; if unrun, one ritual covers it.**
- **Board:** W1 + W2 🟢 merged (plan.md + state.md). Next: collect W2-UX-POLISH → W3 planning (editor remap; W2-DRAFT-RECURSION rides it; orchestrator verify-pass must include the raw-SQL layer — the W2 lesson).

## 2026-06-12 — gated suite ran (post-merge): 2 stale DR-W2-8 pins flipped

- The owner ran the gated api-server suite: **2692/2694**. The 2 failures were the runner's `cross-cutting` tests pinning the SUPERSEDED DR-W2-8 behavior ("allows two block-level schemas to share an order") — written before the review fix added the full unique, and missed by the review-fix sweep (it strengthened the into-group test but never grepped the suite for no-unique pins). The gated run caught it — the gate doing exactly its job.
- Both tests flipped to pin the NEW invariant: duplicate `(blockId, order)` → P2002, including the member+ungrouped shape (the unique spans the whole block — the point of full-vs-partial). These are now the only LIVE guard on `lms-checks.sql` application: if the raw-SQL layer ever silently stops applying, they scream.
- api-server check-types + lint clean; expected **2694/2694** at the owner's next ritual run.

## 2026-06-12 — W2-UX-POLISH enumerated (2 items, one a LIVE bug); W3 prompt issued

- **The owner enumerated the polish list as exactly two items.** (1) A LIVE bug: the unchecked independent-ladders path 400s with "Idempotency-Key header malformed" — root-caused in code: the hook sends `${draft.id}:${trackIndex}` while `IDEMPOTENCY_KEY_REGEX = /^[A-Za-z0-9_-]{1,256}$/` (api-routes constants) has no COLON in the charset — length 38 was a red herring (max is 256); the client's default `crypto.randomUUID()` passes, which is why only this path breaks. W2's MT-19 pinned key STABILITY against a mocked client and never key FORMAT against the server contract — the W3 fix pins that seam (a test importing the real regex). (2) Bring the group card to the owner's hi-fi prototype.
- **The prototype was fetched and distilled** (the design share served gzip→tar → `plan-editor-hi-fi-v-2/`): GroupCard spec extracted VERBATIM into the prompt — solid tinted frame (`alpha(primary, .35)` border / `.03` bg — note: SOLID; W1's "calmer" Fork-A alternative won after all, by the owner's own design), GROUP overline + `view_column`, continuous accent rail down the tracks, 22px numbered track badges replacing member drag handles, segmented interleave in the head, Add group / Add track / Ungroup (dissolve) / Delete-with-tracks gestures, inline label.
- **Scoping discovery:** the prototype's ENTIRE gesture set runs on the existing W2 API (ungroup = `DELETE /groups`; delete-with-tracks = member deletes + the existing empty-group auto-cleanup; add-track = create-with-`groupId`; add-group = `POST /groups`) → **zero new endpoints; W3 is a platform wave; no owner gated-suite ritual expected** (sanctioned exceptions: a read-only regex import in a test + two dead-export deletions in contracts). Cross-boundary DnD-grouping stays OUT — the proto uses buttons, matching the standing cross-scope-drag deferral; D-2's gesture LIST evolves with the owner's own prototype, the principle (explicit gesture, no auto-link) is intact.
- **`w3-runner-prompt.md` issued:** D1 bug fix + seam pin · D2 proto fidelity (verbatim CSS→palette mapping; no hex) · D3 gesture persistence on existing API · D4 draft-layer collapse (W2-DRAFT-RECURSION) · D5 QA-004 kind-switch confirm · D6 W2-STALE-\* hygiene. The §6.7 re-confirms CLOSED (neither flagged at enumeration). Dispositions promoted to `deferred.md`; board updated.
- **Next:** owner transports the prompt body into a fresh `/feature` full session; orchestrator reviews the returned diff via git (D-7).

## 2026-06-12 — W3 built (editor remap; PR on `feat/session-primitive-w3-editor-remap`)

- **Ran the W3 prompt via `/feature` full** (self-contained per D-7). The editor caught up to the ADR-0041 model — a platform wave (NOT a new one-way door; no new ADR), 10 commits. The six deliverables: **D1** killed the LIVE idempotency 400 (separator `:`→`-`; a format-pin test imports the REAL `IDEMPOTENCY_KEY_REGEX`); **D2** rebuilt the group card to the hi-fi prototype (`plan-editor-hi-fi-v-2`) PLATFORM-LOCAL — solid tinted frame (`alpha(primary,.35)` border / `.03` bg — the dashed look died), GROUP overline + `view_column`, continuous accent rail, 22px numbered track badges replacing member drag handles, `.seg` interleave control, all colors via `palette` + `alpha` (zero hex); **D3** wired the full gesture set onto the EXISTING W2 API (Ungroup = `DELETE /groups`; Delete-with-tracks = client-orchestrated sequential `DELETE /schemas` + the empty-group auto-cleanup; Add-track = instant `POST /schemas` + `groupId`; Add-group = `POST /groups`; label/interleave = `PUT /groups`) — zero new endpoints; **D4** collapsed the last recursion in the codebase (`ComposeContainer.children: ComposeNode[]` → flat `SchemaDraft{rows}` + `GroupDraft{tracks: TrackDraft[]}`, `DraftSeed` union, `nodeType` discriminator gone); **D5** added the QA-004 kind-switch confirm (shared `kind-switch-confirm.tsx` + `is-repetition-dirty.ts`); **D6** landed the W2-STALE-\* hygiene.
- **The 10-commit sequence (built in waves):** `fe4e1760` (api-routes barrel export of `IDEMPOTENCY_KEY_REGEX` — FORK-2) → `76712304` (D1 dash + format-pin) → `8b34338f` (D4 draft collapse — `arrangement-tree.ts` deleted) → then a parallel wave: `adfc8aee` (card leaf primitives + delete-group-with-members hook), `4c64f69f` (D5 dirty-check + confirm dialog), `25637fda` (D6 hygiene — vestigial contracts exports + stale fixtures + caption), `827b7a2c` (D6 seed const rename — the single api-server touch) → `aa51452c` (the card rebuild + wired gestures + D5 gate) → `f56edb88` (D5 confirm-before-dirty-switch + `repetition-defaults.ts` cycle-break) → `40742f81` (the post-QA double-fire re-entry guard).
- **Gate A fork resolutions (DR-W3-FORK-\*, DR-W3-1/4/6):** FORK-1 → instant Add-track (`POST /schemas` + `groupId` + default ladder, proto-faithful; the modal stays for standalone). FORK-2 → an additive 1-line `@repo/api-routes` barrel export of `IDEMPOTENCY_KEY_REGEX` (NOT api-server; ungated; the format-pin test imports the real regex). FORK-3 → the seed const RENAME `BLOCK_FOOTNOTES_WK2_TUE`→`BLOCK_PER_ROUND_MARKERS_WK2_TUE`, **owner-delegated** ("реши сам" + "сид не охраняю, как раз думал его чистить"). FORK-4 → reuse `useCreateGroup` with a seeded 2-track draft for block-level Add-group.
- **The single api-server touch — flagged for audit.** The seed const rename is the wave's ONLY change outside platform/`@repo/api-routes`. It is a PURE identifier rename, byte-identical seed DATA (`blockInstanceRef: "block-183"`, order, labels, rows all unchanged — verified by `git show`; 3 sites: def + import + array ref), fully verified by `pnpm check-types` (which compiles api-server). The gated api-server suite is therefore NOT required (no behavior or data change); the owner may OPTIONALLY run it at merge for belt-and-suspenders. This is the one place the platform-only boundary is crossed, and only cosmetically, with explicit owner sign-off.
- **Implementation deviations from the prompt (recorded honestly):** (1) `DraftSeed` was hoisted INTO `axis-draft.types.ts` (not modal-local) to avoid a circular import. (2) `arrangement-tree.ts` was **DELETED, not renamed** as runner-prompt §7.3 said — the flat collapse made `collectTrackChildren`/`collectDirectRows` dead (tracks/rows are direct arrays now), so deletion > rename; this resolves the `arrangement-tree` half of W2-STALE-NAMES by deletion. (3) A cycle-break: `REPETITION_DEFAULTS`/`DEFAULT_TIME_CAP` extracted into a new leaf `axes/repetition-defaults.ts` — wiring D5 directly would have created a `repetition-axis-field ↔ is-repetition-dirty` import cycle (manifesto 2.1 — fixed at the source, not silenced). (4) The QA-114 owner UX call ratified as **keep-confirm-always** (a kind-switch on a pristine parallel group still confirms — it collapses the 2-track STRUCTURE, not just per-axis content).
- **A post-QA CRITICAL caught + fixed in-branch (`40742f81`):** QA-104 — a double-click on "Delete group" double-fired the sequential delete → a contradictory toast (the async `isPending` flips a render too late to block a synchronous second click). A synchronous re-entry guard (`isRunningRef`/`isFiredRef`, mirroring the existing `isSubmittingRef`) was added to ALL new gesture surfaces — delete-group, add-track, add-group, ungroup (QA-102/103/105 are the same class). Pinned by double-fire tests on each (DR-W3-REENTRY).
- **Verify (orchestrator must rerun on review, not the agent self-report):** the runner reported `check-types` 16/16, `lint` (`--max-warnings 0`) 16/16, `dep:check` 0 violations, platform vitest 844+, `@repo/contracts` 741. The branch is checked out with exactly the 10 commits above (confirmed via `git log main..`); the seed rename + `arrangement-tree.ts` deletion + `DraftSeed` hoist + the re-entry guards + the contracts dead-export deletions were all spot-verified against the actual diff during close-out.
- **Ratified calls promoted:** DR-W3-1..10 + DR-W3-FORK-2/3 + DR-W3-REENTRY (`decisions.md`). **Carry-forwards CLOSED:** W2-UX-POLISH (D1+D2), W2-DRAFT-RECURSION (D4), QA-004 (D5), W2-STALE-FIXTURES / W2-VESTIGIAL-EXPORTS / W2-STALE-NAMES (D6). **KEPT OPEN:** W2-FOOTNOTE-LAST + W2-URL-PLACEHOLDER (the runner-prompt scoped W2-UX-POLISH to EXACTLY D1+D2 — these two §6.7 seed-content re-confirms were NOT in W3 scope; W3 changed zero seed data → still owed at the next seed walkthrough), W2-IDEM-REMOUNT (deliberately kept — red line #6). **NEW carry-forwards (all owner-paced, none blocking):** OQ1-DEMOTE-DEAD (the dead demote feature — owner chose CARRY), QA-106 (materialize 2nd-ladder default ≠ proto), QA-108 (materialize drops the header — latent), W3-REV-002 (un-shared `TRACK_GAP`), W3-REV-003 (in-group-add empty-schema, W2-IDENTICAL), W3-REV-004 (`[21,15,9]` literal ×3), QA-101/QA-107 (test-stage-pending pins).
- **Next.** Owner: review the diff → `db:reset` is NOT required for this wave (no model change — the W2 reseed already covers it; the editor renders off the same `buildBlockItems`) → the §10 10-step walkthrough (the dead 400, proto side-by-side, the gestures, kind-switch confirm, the caption) → merge. Then W4 (row grammar + leaf residuals) still needs F-PLAQUE first.

## 2026-06-12 — W3 orchestrator review PASSED (zero defects); ledger reopen accepted

- **Independent review of PR #263** (12 commits incl. close-out) — diff, never the self-report. Scope bound EXACT: platform + the three sanctioned touches and nothing else (api-routes barrel = additive 2 lines; contracts dead-export deletions = 0 remaining consumers repo-wide; seed const rename = identifier-only at 3 sites, raw diff shows byte-identical data); admin/marketing/`@repo/ui` zero-touch (`AccentGroupCard` un-consumed, not modified — DR-W3-2 as stated). D1 verified at the call site (`` `${idempotencyBaseKey}-${trackIndex}` ``) + the format-pin test imports the REAL `IDEMPOTENCY_KEY_REGEX`. Zero hex in changed platform files. One-predicate held — `buildBlockItems` still has exactly one consumer. The QA-104 re-entry guard is correctly SYNCHRONOUS (`useRef` + `isPending`, settle-reset) with double-fire pins on every gesture surface. Clean one-component-per-file leaf split (badge / track wrapper / seg control / head / buttons each own file + tests).
- **Gates re-run independently — identical to the self-report:** check-types 16/16, lint 16/16, dep:check 0 (1636 modules), contracts **741/741** (745→741 = the deleted dead-export tests), platform **851/851** (801→851 = the new gesture/fidelity/draft coverage).
- **The runner's REOPEN of W2-FOOTNOTE-LAST / W2-URL-PLACEHOLDER reviewed and ACCEPTED** — a contentful correction of the orchestrator's own 2026-06-12 closure: that closure was premised on "re-confirm inside the polish pass", and the W3 prompt itself narrowed the polish pass to D1+D2 while touching zero seed data. The two §6.7 one-liners are still owed — collect them at the §10 walkthrough (the right kind of runner pushback; the ledger now carries the honest state).
- **Verdict: SHIP.** Pending: owner §10 walkthrough (+ the two §6.7 answers) → merge #263 → W4 gated on F-PLAQUE.

## 2026-06-12 — walkthrough caught a CRITICAL the suite could not; three in-branch fixes

- **Owner's §10 walkthrough verdict:** steps 1–4, 8–10 ok; 5–7 + 11 blocked by ONE defect — the group head was 100% dead (label/seg/ungroup/delete all unresponsive) AND standalone schemas did not drag at all. Under that one report sat THREE distinct roots, all invisible to jsdom (fireEvent.click bypasses dnd-kit's pointer capture — which is exactly why 851 tests were green over a dead UI):
  1. **Head interception:** `schema-group-box-head` spread `{...dragAttributes} {...dragListeners}` on the whole head Stack — dnd-kit's PointerSensor swallowed every pointerdown before the buttons saw it. Fix: the owner refreshed the prototype (drag handles added, top-left); the head now renders a dedicated grab handle (`DragIndicatorIcon`, first element, proto-faithful) carrying the listeners; the container is clean. Pinned: the handle has dnd attributes, the head container has none.
  2. **Sortable id mismatch:** `schema-card` registered `useSortable({ id: schema.schema.id })` (raw cuid) while the block's `SortableContext` + drag-end lookups speak composite `schema:${id}` — standalone schemas never participated. Fix: ONE shared id source (`lib/block-item-sortable-id.ts`) consumed by block-card-body + schema-card + schema-group-box; the format can no longer fork.
  3. **Row media never rendered:** `formatRow` derived `demoUrl` ONLY from the exercise catalog; every non-EXERCISE builder hardcoded `demoUrl: null`, so the W2 seed's row-level `media {url}` (the ex-STANDALONE_URL demo) was invisible — the §6.7 URL question was unanswerable. Fix: row `media.url` now overrides/fills `demoUrl` for ANY row kind (▶ demo link renders); pinned for placeholder + exercise + null-fallback.
- **Updated prototype ingested** (the owner added drag triggers "в левый верхний угол"): block-items (groups + standalone schemas) get head-left handles — implemented. The refresh ALSO adds `useTrackDrag` (member reorder INSIDE a group, same-group-constrained) — NOT implemented in this fix (new behavior beyond the defect, needs its own QA); logged as **W3-TRACK-DRAG**, owner call: ride this PR or follow-up.
- **Gates after fixes:** check-types 16/16, lint 16/16, platform **857/857** (+6 pins). Three commits on the PR branch.
- **Pending:** owner re-runs walkthrough steps 5–7 + 11 (+ the §6.7 one-liners — the block-183 footnote rows already sit AFTER the single base row, so "в конец" may already hold; the demo link is now visible for the URL check) → merge #263.

## 2026-06-12 — walkthrough round 2 (modal pattern + the mixed-height collision lesson); W3 MERGED

- **Round-2 owner findings:** (1+3) the ungroup/delete-group confirms closed BEFORE the callback while every other modal waits — "придуман мини-велосипед вместо существующего паттерна" → re-aligned to the house pending pattern (`32f5b5bc`, **DR-W3-12**): dialog stays open, Processing… disables, early-close + component re-entry refs removed (DR-W3-REENTRY narrowed to the modal-less buttons + the batch hook). (2) reorder involving the GROUP flapped/snapped while every UNIFORM list worked — the owner called the search direction: "оно стоит на столе". He was right twice over: the orchestrator's first fix (`5687c1df`, closestCorners) went to the WRONG list (block-list, while the repro lived in the inner block-items list), and the real difference was structural — **the block-items list is the app's only mixed-height list** (small schemas + tall group), and `closest*` rect metrics flap on heterogeneous heights. Fix: **pointer-first collision** (`pointerWithin` → corners fallback, `46d4c2f2`, **DR-W3-11**) — target = the element under the cursor, size-independent, deterministic.
- **Lesson (standing):** the DnD interaction layer (pointer capture, collision geometry) is INVISIBLE to jsdom — `fireEvent.click` bypasses dnd-kit sensors entirely, which is how 851 green tests coexisted with a 100%-dead group head and a non-functional reorder. The browser walkthrough IS the gate for this layer; suite-green claims about DnD mean nothing.
- **MERGED — PR #263 → main `9a9dfb8e`** (branch deleted; final platform 861/861, types/lint 16/16). Owner one-liners left UNANSWERED at merge → carried OPEN: W3-TRACK-DRAG, W2-FOOTNOTE-LAST, W2-URL-PLACEHOLDER, W3-DND-POLISH (new row), + the gated-suite 2694/2694 confirmation.
- **Next session:** collect the one-liners → **F-PLAQUE** (the W4 gate; orchestrator brings a corpus-grounded rec — one topic per touch) → leaf F-rows per plan order; the W4 prompt only after they close (D-8 JIT).

## 2026-06-12 — design session: one-liners collected; F-PLAQUE ratified (D-PLAQUE)

- **One-liner verdicts (owner):** W3-TRACK-DRAG + W3-DND-POLISH → ONE `/feature small` (both polish items accepted: compact DragOverlay ghost + `restrictToVerticalAxis`); W2-FOOTNOTE-LAST CLOSED ("это же просто строка с упражнением HSPU и пятью повторениями, это НЕ УНИКАЛЬНАЯ СТРОКА" — block-183 correct as-is, no reorder); the gated suite confirmed green by the owner; W2-URL-PLACEHOLDER — owner directed the fix ("правь, я гляну что вышло") → seed made coach-readable (header "URL only body" → "Yoga time", slot texts → "Follow the yoga video" / "Warm up for feet — follow the video", URL slugs renamed), api-server tsc green, UNCOMMITTED (rides the next PR), owner looks at the reseed.
- **F-PLAQUE — verify-then-spec against `analysis/`, not memory:** rest strips 71 occ / 17 distinct, ALL tail-of-schema (sheet grep: the next line is always a new header/marker; mid-body rest does not exist); a DUAL-REST corpus shape found that the grid missed (`schema-boundaries.md:2169` — ONE `3 sets:` schema carries between-sets-until-recovery + 5-min-after-3rd-set, ≥2 blocks); connectors 16; EXAMPLE 4; only-once 2. THREE live rest carriers in code (composition axis + `RowKind.REST {raw, parsed}` + `REST_SLOT`) with the seed mixing the first two interchangeably (`rest-coverage.ts` stacks 5 REST rows) — the smeared-carrier disease at the leaf.
- **The owner's contentful pushback reshaped the rec (three corrections, all accepted):** (1) the rest FLOW is fine — the existing modal (duration/scope/qualifier) stays THE edit surface; the disease was carrier duplication, not the flow; multi-rest (`RestSpec[]`) REJECTED — "второго отдыха в схеме сейчас не делаем" (the dual-rest rule 2 → a note at re-seed). (2) The notes model is owner-designed: MULTIPLE ordered notes on ANY element — block/schema/row/schema-group/row-group — rendered as a plaque stack under the owner ("не вижу причин ограничивать тренера количеством заметок"); the plaque IS the render of a note. (3) only-once → its OWN schema via order ("это рабочее задание атлету… должно быть добавлено просто как схема" — the orchestrator's notes answer contradicted its own connectors=order ruling; conceded). The orchestrator's fuzzy "boundary litmus" prose was called out ("мне кажется ты и сам не понимаешь") and RETRACTED — replaced by concrete per-case homes.
- **D-PLAQUE RATIFIED** (closes the F-PLAQUE gate; W4 now waits only on the leaf F-rows): plaque = render vocabulary (owner verbatim: "плашка это про визуальное отображение… апиранс элемента, а не его доменное значение"), NO stored node; rest single-carrier (`RowKind.REST` + `raw` die; ONE rest per schema); the notes stack; row-group mechanics = the mirror of Group one floor down (CONTIGUOUS members, **2+** — "OR может связывать 2+ строки", schema-owned, opaque label, membership-only, no typed kinds; pixel UX rides the W4 design stage); connectors re-confirmed = schema order. Grid rows updated (rest / OR / sequence / EXAMPLE / row-grouping / plaque + kill list + §8 cases + the stale D-MARKER-DEATH status fixed in passing); `deferred.md` dispositions promoted.
- **Next:** F-POSITION-CARRIER + F-CHIPS (next owner touch — rec in preparation) → F-WEIGHT-EXOTICS + F-TEMPO → F-HEADER + F-BLOCK-TIMECAP + F-SLOT → only then the W4 prompt (D-8 JIT). W4 carries the D-PLAQUE implementation set: REST-row kill + seed rest re-expression, notes → ordered lists (+ on both group entities), the row-group entity + editor, the sequence VO death.
