# session-primitive — primitive spec (the notation grid)

**Status: RE-FROZEN (2026-06-17 by primitive-v2).** Originally FROZEN 2026-06-12; re-frozen at the close of the `primitive-v2` leaf-reshape wave (5 shipped changes — see `initiatives/primitive-v2/reshape-design.md` §5 + `decisions.md` D-V2-\*). The re-freeze: (1) intensity scope trinity restored (block/schema/row + render overlay — D-V2-INTENSITY-TRINITY); (2) `rest` added as a per-row carrier alongside the schema rest axis (D-V2-ROW-REST); (3) `composition.cap` cross-cutting time-cap axis added, 6 repetition kinds untouched (D-V2-CAP-AXIS); (4) `interval` work/off carry a `{value, unit: sec|min}` (sub-minute — D-V2-INTERVAL-UNIT); (5) `load.byProfile` reshaped flat `entries` → `{axes, cells}` (D-V2-PROFILE-NESTING). Every grid row is RATIFIED/ACCEPTED — ZERO OPEN. The spec-freeze gate (plan step 1) is MET; the W4 `/feature` prompt reads the model from here. Founded 2026-06-10; the leaf F-ledger closed over the 2026-06-12 design session (D-PLAQUE/D-CHIPS/D-MODIFIER + D-FLOORS/D-ROW-GRAMMAR/D-LOAD-FINAL/D-TEMPO/D-EXEC-DEFER/D-HEADER-KEEP).

**Row statuses.** `RATIFIED` — explicit owner words this session (quoted in `decisions.md` D-6 or here) · `ACCEPTED` — uncontested in the owner's line-by-line review pass of 2026-06-10 ("я всё прочитал, сейчас больше сказать нечего") · `OPEN (F-…)` — owner-marked follow-up in `deferred.md`.

## 1. The skeleton (D-1..D-4)

```
Session
└─ Block                      секция тренировки (warm-up / strength / metcon); labels, intensity, notes
   └─ [Group?]                коробка схем: ordered contiguous members + opaque coach-owned label
      └─ Schema               примитив: repetition (once/count/ladder/timeCap/cadence/interval) + rest
         └─ [row-grouping?]   коробка на строках: contiguous члены 2+, opaque label (D-PLAQUE)
            └─ Row            payload + typed modifiers + human text
```

- No recursion: `Schema.parentSchemaId` dies (D-3).
- A Group never interprets its label; membership is the only relation; grouping is an explicit gesture (DnD / batch checkbox); nothing is derived from child count (D-2). No typed relation kinds (D-4).
- `composition` after this spec = `{ repetition?, rest? }` — the arrangement axis dies whole (ordered = natural order; parallel → Group; superset → row-grouping; `interleaveOrder` → a Group display setting).

## 2. The channels rule (D-5)

Every notation → exactly one channel: **С** structure · **Т** typed field (only what a machine actually reads) · **Ч** human text (carried, displayed, never interpreted; concrete carrier per case — library/modifier (D-MODIFIER) / notes (D-PLAQUE: the strip is render, not storage); no blanket chips (D-CHIPS)) · **✕** dropped syntax (brackets, case, word order). Expressibility is mandatory even at cardinality 1; a dedicated type is not. The corpus is the FLOOR, not the ceiling — it is ONE personal plan; group-programming notations (m/f loads, RX/SC) are systematically underrepresented in it.

## 3. Grid A — row payload (what a row IS)

| Notation                                                  | Corpus evidence                                                       | Channel | Verdict                                                                                                                                                                                                                                                                                                                                                                | Status                                  |
| --------------------------------------------------------- | --------------------------------------------------------------------- | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------- |
| Exercise + reps                                           | ~850 rows (684 int / 141 implicit / 25 range)                         | Т       | The core leaf. Stays.                                                                                                                                                                                                                                                                                                                                                  | RATIFIED                                |
| Compound `A + B (+ C…)`                                   | 97 (49 paired / 20 chained / 28 extended; repeated 14+9; sandwich ~5) | Т       | The `compound` row form DIES → a row-group (D-ROW-GRAMMAR; owner: "compound… работать будет по принципу параллельных схем в группах"): `A + B (+ C)` = ordered member rows in a box, each its own reps/load/side. `cyclical`/`sandwich`/`or_alternative` all dissolve into row-group members. Compact single-line render rides W4.                                     | ACCEPTED                                |
| `x OR y` (substitution choice)                            | 3 rows + 1 inside an annotation                                       | С       | Row-group (2+ members) carrying the opaque "OR" label; `or_alternative` VO (hard-coded 2 exercises) dies. Schema-level OR ("меткон OR бег") = Group("OR") — newly expressible; it is the war-era substitution shape.                                                                                                                                                   | RATIFIED (D-PLAQUE)                     |
| Rest line `- 5 min rest -`                                | 71 occ / 17 distinct strings                                          | Т       | All 17 reduce to `{fixed \| range \| until_recovery}` × scope `{between sets \| between rounds \| after Nth set}`. Carrier: the schema rest setting (existing modal flow) is the ONLY one — `RowKind.REST` + `raw` die; ONE rest per schema (multi-rest rejected for now; the dual-rest corpus schema carries rule 2 as a note). Renders as the strip the coach draws. | RATIFIED (D-PLAQUE)                     |
| Footnote `*`/`**` rows                                    | 7 (4 roles per `03-content/edge-cases.md`)                            | С/✕     | Roles 2–4 (extra work each round/set) = a plain exercise row placed **LAST** in the schema — ordering IS the semantics. No type, no flag, no chip. Markers = dropped syntax. Role 1 (placeholder pair) → slot row.                                                                                                                                                     | RATIFIED — owner: "поставь его в конец" |
| Placeholder slot (`ANY exercise for ABS`, `*DB exercise`) | 6 rows + 2 per-set pairs                                              | Т       | NOT a row kind — a placeholder-NATURE catalog record (D-ROW-GRAMMAR; `placeholderFlag` already exists). `RowKind.PLACEHOLDER` dies; the row is an exercise whose render-kind is INFERRED from the catalog nature (`concrete\|placeholder\|rest`). Closes F-SLOT.                                                                                                       | RATIFIED (D-ROW-GRAMMAR)                |
| Per-set substitution `[1 set: X \| 2 set: Y \| 3 set: Z]` | 2                                                                     | С       | A row-level grouping case; NO typed per-set mapping machinery.                                                                                                                                                                                                                                                                                                         | RATIFIED — owner: "уникальности ноль"   |
| `[ TOTAL ]`                                               | 4                                                                     | ✕       | **Dead entirely** — a plain exercise row with its reps; not even a flag.                                                                                                                                                                                                                                                                                               | RATIFIED — owner: "даже не думай"       |
| Inner ladder marker `36-28-20:`                           | 38 body lines / ~15 schemas                                           | С       | Dies → per-track rep-scheme = one-row ladder-schemas inside a Group (D-LADDER semantics survive as two STRUCTURES).                                                                                                                                                                                                                                                    | RATIFIED (D-MARKER-DEATH; landed W2)    |
| Rep-definition (`5 reps = 1 rep […]`, `{… = 1 rep}`)      | 3 occ / 2 patterns                                                    | Т/Ч     | Row kind + `compoundRep` column die; the content is a compound (elements with counts); the "= 1 rep" framing → notes until a counting engine exists.                                                                                                                                                                                                                   | ACCEPTED                                |
| Standalone `[ URL ]` row (+ bare, + `EXPLODE:` label)     | 50 + 2 + 9                                                            | Т       | Row kind dies → `media {url, label?}` on the row/schema it demonstrates. `wrapped` (square-bracket memory) dropped.                                                                                                                                                                                                                                                    | ACCEPTED                                |
| Standalone load row `[ 2x 15 kg ]`                        | 2                                                                     | ✕       | Row kind + its multi-row scope die → UI bulk-action ("проставить вес строкам выше") writes `load` onto each row.                                                                                                                                                                                                                                                       | ACCEPTED                                |
| Connectors `then:` / `…then…:` / `…then N rounds:`        | 16                                                                    | С/✕     | Die: schema order already says "then"; "then N rounds" = the NEXT schema's own repetition; render may draw "затем" glue for free. No authored strip — D-PLAQUE re-confirmed order as the carrier.                                                                                                                                                                      | RATIFIED (D-PLAQUE)                     |
| EMOM rest slot (`REST_SLOT`)                              | seed/UX (D-EMOM-SLOT)                                                 | Т       | `REST_SLOT` row kind DIES → a rest-NATURE catalog exercise (D-ROW-GRAMMAR; "отдых тоже задание"). An EMOM minute slot = a top-level list element (row\|box); a REST minute = a row carrying the Rest exercise. No schema-aware row kind.                                                                                                                               | ACCEPTED                                |
| Drop-set program (BSS `3 sets [ x5 … then … ]`)           | 9 (x5: 6 / x7: 3)                                                     | С       | Already resolved pre-initiative (compose-hardening D-CT-TIER3 / stages-as-rows): stages are ROWS of the schema; per-stage load on each row; "EXPLODE" = channel-Ч text on the last stage; `3 sets` = `repetition.count`.                                                                                                                                               | ACCEPTED (predecessor-ratified)         |

## 4. Grid B — row modifiers (today: 9 Json columns)

| Field         | Corpus evidence                                                        | Channel | Verdict                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Status                                                                   |
| ------------- | ---------------------------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------ |
| `reps`        | ~850                                                                   | Т       | Slim union: `count` / `range` / `unit(sec\|min\|km)` / `max` (ONE kind; optional free-text tail covers the 3 corpus sub-forms; the progressive seed "1-2-3-4-5 etc." is text). `implicit` is NOT a stored kind — reps = NULL, the render resolves from the ladder above (storing "absence" is the old disease). `total_flag` dies (TOTAL ruling). `compound_rep_unit` dies.                                                                                                                                                                                                                                                                                                                                              | RATIFIED (TOTAL part) · ACCEPTED (rest)                                  |
| `load`        | ~250 annotated; ~614 rows unannotated                                  | Т       | `absolute {count:1\|2, kg}` (D-LOAD-FINAL — count explicit; implement type → exercise/equipment lib) / `percentage {value, reference: self \| other-exercise}` (family ref dies with movement types; 1RM resolver → Phase 3) / `bodyweight` / `byProfile {axes:{name,values[]}[] (1–2), cells:{coords[],kg}[]}` (D-V2-PROFILE-NESTING — supersedes the flat `{label→weight}[]`; axes = coach dictionary m/f·rx/sc·age…; 1 axis = list, 2 = grid; `cells` cover the cartesian product; resolver with athlete context → Phase 3). `none` KILLED (= bodyweight). `without_weight`/`unspecified` die.                                                                                                                        | RATIFIED (`byProfile` reshaped — D-V2-PROFILE-NESTING) · ACCEPTED (rest) |
| `weight`      | dual 163 / single 37 / single_arm 14 / device-prefixed 28 / exotics 11 | Т       | Weight VO fused implement+count+kg → splits (D-LOAD-FINAL). Exotics die: `split_tier`→row-group (compound rows); `with_asymmetric_arm`→`single`+arm-role MODIFIER; `with_depth_modifier`→`single`+depth MODIFIER. `single`+`single_arm` MERGE (one-arm = SIDE). `compound_device` trims BOX/SOFA. Result: absolute load = `{count:1\|2, kg}` (count = explicit row choice; implement TYPE rides the exercise via equipment library).                                                                                                                                                                                                                                                                                     | RATIFIED (D-LOAD-FINAL)                                                  |
| `side`        | each leg 105 / each arm 45 / L-R pairs 18 / explicit per-limb 12       | Т       | Stays: `{each_limb \| left \| right}` + optional per-limb count. It MULTIPLIES volume (10 each leg = 20 total) — genuinely machine-read.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | ACCEPTED                                                                 |
| `tempo`       | ~67 (4 stable numeric forms)                                           | Т/Ч     | SMART UNION (D-TEMPO-SMART): `tempoModifierSchema = union(fullTempoSchema, free-string)`. 4-digit `fullTempo` STAYS typed (each position `int \| "X"`, `3-1-X-0`; awaiting an engine, byProfile precedent); any other non-empty text now ALSO lives as a FREE STRING in the tempo field — `parseTempo` is total (no "invalid tempo" error). So verbal forms have TWO homes: a free-string tempo OR a MODIFIER (D-TEMPO; D-MODIFIER boundary softened). The strict `{fullTempo}` wrapper + verbal-OBJECT shapes still reject.                                                                                                                                                                                             | RATIFIED (D-TEMPO-SMART; extends D-TEMPO)                                |
| `position`    | ~60 / 11 distinct strings                                              | Ч       | The enum + column + editor die — carrier = the **row MODIFIER library** (coach-owned dictionary à la Label/Exercise; ordered multi-REF on the row — corpus carries 2–3 at once; searchable create-on-the-fly picker shared with LABEL-FLOW-UX; rename edits wording not prescription; delete-in-use refused). 3-layer boundary: machine-read → typed · dictionary how-to-execute → modifier · situational text → note (`kind of wall balls`).                                                                                                                                                                                                                                                                            | RATIFIED (D-MODIFIER; D-POSITION superseded same day)                    |
| `sequence`    | 19 / 6 variants                                                        | Ч       | The typed VO dies. Carrier (D-PLAQUE): order/structure FIRST — "only once before METCON" = its own schema placed before the metcon (a work item, not prose; owner correction); genuine reading-aid prose ("before BAR DIPS complex") → a row note. Никаких графов.                                                                                                                                                                                                                                                                                                                                                                                                                                                       | RATIFIED (D-PLAQUE)                                                      |
| `intensity`   | 2 corpus + standard coaching axes                                      | Т       | Stays: `{effort% \| rpe \| pace \| hr_zone \| numeric_pace}` struct (≥1 dimension); scopes block/schema/row; partial-overlay inheritance is render-time, no storage cost. **TRUE in code as of primitive-v2 (D-V2-INTENSITY-TRINITY):** carried independently at all three levels (`Block.intensity` + `Schema.intensity` + `SchemaRow.intensity`), the render computes the effective intensity per node as a dimension-wise merge with precedence **row > schema > block** (each dimension resolves to the nearest level that set it). No storage promotion. This restores the line's original 2026-06-10 intent that `D-FLOORS` had over-corrected to schema-only.                                                     | ACCEPTED (intent) · RATIFIED (trinity — D-V2-INTENSITY-TRINITY)          |
| `media`       | ~374 refs / 37 distinct URLs                                           | Т       | Slims to `{url, label?}`; placement = which node carries it; `position`/`appliesTo` enums die.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | ACCEPTED                                                                 |
| `rest`        | per-exercise rest (group-strength evidence)                            | Т       | NEW carrier as of primitive-v2 (D-V2-ROW-REST): `SchemaRow.rest` reuses the full `restSpecSchema` (`{duration{value,unit,rangeMax?}, scope, qualifier?, setIndex?}`) — the per-exercise rest (between this movement's sets), the editor accents `between_sets` on a row. **Additive by scope**, NOT a second schema-rest: the schema rest axis (Grid C) stays as the round-rest of the whole schema; row rest is a different carrier/scope. Lets a strength block hold several movements each with its own rest (squat 3' / bench 90") without artificial single-row schemas. This re-opens `D-PLAQUE`'s ONE-rest-per-schema clause by SCOPE only (row vs schema) — not the multi-rest-on-one-schema the owner rejected. | RATIFIED (D-V2-ROW-REST)                                                 |
| `compoundRep` | 3                                                                      | —       | Column dies (see Grid A rep-definition).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | ACCEPTED                                                                 |

Result per row (D-ROW-GRAMMAR; + the primitive-v2 leaf reshape): **exercise (catalog ref; nature `concrete\|placeholder\|rest` inferred) + sets? + reps + load `{count,kg \| %self\|other \| bodyweight \| byProfile{axes,cells}}` + side + tempo? (4-digit, X allowed) + intensity? + rest? (D-V2-ROW-REST) + media + modifiers[] (D-MODIFIER) + notes[] (D-PLAQUE)** — ONE row kind, the first step is the exercise select. `compound`/`OR`/per-set → row-groups; `REST`/slot = catalog natures. Today's 9-column zoo + 4 row kinds gone.

## 5. Grid C — schema / block / groups

| Surface                       | Corpus evidence                                                                 | Channel | Verdict                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Status                                                                    |
| ----------------------------- | ------------------------------------------------------------------------------- | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| `repetition` (6 kinds)        | everywhere                                                                      | Т       | once / count / ladder / timeCap / cadence / interval — the sacred algebra core, untouched (still a SET of 6; `composition.cap` is orthogonal, NOT a 7th kind — D-V2-CAP-AXIS). **`interval` reshaped (D-V2-INTERVAL-UNIT):** `work`/`off` carry `{value, unit: sec\|min}` (`INTERVAL_DURATION_UNITS = ["sec","min"]`; `value` positive/nonnegative, NOT int — `:20 sec`; `off` may be 0; was int `workMin`/`offMin`). Renders `8×:20/:10` (sec) / `8×1’/1’` (min). The `timeCap` KIND now also renders sec as `Ns` (latent-format fix, this wave).                                                                                                   | RATIFIED (charter-sacred) · interval reshaped (D-V2-INTERVAL-UNIT)        |
| `composition.cap`             | "FOR TIME (12 min cap)"; AMRAP-with-cap                                         | Т       | NEW cross-cutting axis as of primitive-v2 (D-V2-CAP-AXIS): `compositionSchema` gains `cap: timeCapSchema.optional()` (`{min, max?, unit: min\|sec}`) — a ceiling over ANY repetition kind ("21-15-9 capped at 12 min"). Orthogonal to the 6 kinds: the `timeCap` KIND stays for AMRAP / pure time-bound ("the 15 minutes IS the scheme"); `composition.cap` is the ceiling. `cap` when `kind === "timeCap"` is redundant-but-valid — the UI hides the cap toggle there; NO reject `superRefine` (cost > value). Rides inside `Schema.composition` Json (no Prisma column). Renders unit-aware: `cap 12’` (min) / `cap 90s` (sec).                    | RATIFIED (D-V2-CAP-AXIS)                                                  |
| `rest` axis                   | block/schema rest specs                                                         | Т       | Stays as the schema's round-rest carrier (`RowKind.REST` + `raw` die, D-PLAQUE); the existing modal flow (duration/scope/qualifier) is the edit surface; ONE spec per schema; renders as the corpus strip. Per-exercise rest now ALSO lives on the row (`SchemaRow.rest` — D-V2-ROW-REST, Grid B); the schema axis is unchanged — row rest is additive by scope, not a second schema-rest.                                                                                                                                                                                                                                                           | RATIFIED (D-PLAQUE) · schema axis intact (row rest added — D-V2-ROW-REST) |
| `arrangement` axis            | superset: **0 corpus** (Phase-7 speculation); parallel: ~15 schemas             | С       | The axis dies whole: ordered = natural order; parallel → Group; superset → row-grouping (its stored rowId-pairs were the last sibling-ref surface); `interleaveOrder` → Group display setting.                                                                                                                                                                                                                                                                                                                                                                                                                                                       | RATIFIED-direction (D-2/D-4); lands step 3                                |
| `header`                      | rep-scheme headers = typed repetition; named programs                           | Ч       | STAYS as a field, untouched (D-HEADER-KEEP — owner: "всё нормально, не трогаем, косметика"). NOT a model question — F-HEADER reduces to a UX-parity item: an in-group schema renders its header like a standalone schema (today the in-group top zone is cut).                                                                                                                                                                                                                                                                                                                                                                                       | RATIFIED (D-HEADER-KEEP)                                                  |
| `[ EXAMPLE: … ]`              | 4                                                                               | Ч       | A schema note. Notes model (D-PLAQUE, owner-designed): MULTIPLE ordered notes on ANY element — block / schema / row / schema-group / row-group; render = a stack of plaques under the owner.                                                                                                                                                                                                                                                                                                                                                                                                                                                         | RATIFIED (D-PLAQUE)                                                       |
| Block labels (multi, ordered) | 17 canonical                                                                    | Т       | Stay — the section vocabulary (warm-up / strength / metcon …).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | ACCEPTED                                                                  |
| Block intensity               | 1 corpus + standard                                                             | Т       | RETURNS to the block as of primitive-v2 (`Block.intensity Json?` — D-V2-INTENSITY-TRINITY). **Reverses the D-FLOORS removal:** `D-FLOORS` cut intensity to schema-only fearing block→child PROMOTION (forbidding per-track variation); the timed-test surfaced the inverse need — a block carrying its OWN effort ("5 rounds @85%"). The block stores its own value; the render overlays it dimension-wise UNDER schema and row (precedence row > schema > block), so no level is forced. Block-055 `70% EFFORT` = `block.intensity.effortPercent=70`. (D-FLOORS RATIFIED 2026-06-12, SUPERSEDED 2026-06-17 by D-V2-INTENSITY-TRINITY — trail kept.) | RATIFIED (D-V2-INTENSITY-TRINITY; supersedes D-FLOORS for intensity)      |
| Block `timeCap`               | 2 ("PRACTICE [5-10 min]")                                                       | ?       | LEAVES the block (D-FLOORS) → expressible as the schema's `repetition.timeCap` ("PRACTICE [5-10 min]" = a timeCap schema). A block is a named part, not a load carrier.                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | RATIFIED (D-FLOORS)                                                       |
| **Group** (schema-level)      | parallel ladders ~15 schemas; alternating sets; OR-schemas                      | С       | NEW node: ordered contiguous member schemas + interleave display setting + Notes. Created by explicit gesture; dissolves in one click. The free-text `label` column DIES → notes (first note in the box header zone — D-FLOORS); no intensity (each track its own load).                                                                                                                                                                                                                                                                                                                                                                             | RATIFIED (D-2)                                                            |
| Row-level grouping            | per-set ruling, superset, OR                                                    | С       | The mirror of Group one floor down, inside the schema card: CONTIGUOUS member rows, **2+ members**, schema-owned, opaque coach-editable label, membership-only, no typed kinds (D-2/D-4 by symmetry); gestures mirror the schema-group set. Pixel-level UX rides the W4 design stage.                                                                                                                                                                                                                                                                                                                                                                | RATIFIED (D-PLAQUE) — UX detail → W4                                      |
| **Instruction plaque**        | ~93 strip-like corpus lines (rest 71 + connectors 16 + EXAMPLE 4 + only-once 2) | С/Ч     | DISSOLVED (D-PLAQUE): the plaque is APPEARANCE, not a domain entity (owner: "плашка это про визуальное отображение… апиранс"). The thin strip = render vocabulary for the rest setting / `REST_SLOT` / row-group label / notes stack. No stored primitive; a free-text instruction row is re-introduce-fresh on real need.                                                                                                                                                                                                                                                                                                                           | RATIFIED (D-PLAQUE)                                                       |

## 6. Kill list (where each dead thing's meaning goes)

**Dies → structure:** `compound` row form + `or_alternative` + `cyclical` + `sandwich` + `split_tier` weight (→ row-group — D-ROW-GRAMMAR / D-LOAD-FINAL) · `INNER_LADDER_MARKER` (→ one-row ladder-schemas in Group — D-MARKER-DEATH, landed W2) · per-set substitution mapping (→ row-grouping) · connectors (→ schema order) · footnote roles 2–4 (→ row placed last) · `sequence.only_once` (→ its own schema via order — D-PLAQUE) · arrangement axis incl. superset pairs (→ Group / row-grouping) · `Schema.parentSchemaId` (→ Group floor).

**Dies → catalog nature (D-ROW-GRAMMAR):** `RowKind.PLACEHOLDER` + `RowKind.REST_SLOT` + `RowKind.REST` row kinds (→ the catalog record's nature `concrete\|placeholder\|rest`; the row's render-kind is inferred, never picked — first step = the exercise select).

**Dies → settings re-homed (D-FLOORS):** ~~block `intensity` (→ schema — intensity is schema-only)~~ **SUPERSEDED 2026-06-17 by D-V2-INTENSITY-TRINITY** — `Block.intensity` RETURNS; intensity is now scoped block/schema/row with render-time overlay (see Grid B `intensity` + Grid C `Block intensity`) · block `timeCap` (→ schema — `repetition.timeCap`; NOT reversed — only the intensity clause of D-FLOORS was) · `SchemaGroup.label` + row-group label (→ notes stack) · ~~row-level intensity override (→ removed; intensity is schema-only)~~ **SUPERSEDED 2026-06-17 by D-V2-INTENSITY-TRINITY** — `SchemaRow.intensity` is back as an independent per-row carrier (render overlay precedence row > schema > block; not a promotion).

**Dies → typed-slim (D-LOAD-FINAL / D-TEMPO):** weight `single_arm` (→ SIDE) · weight `with_asymmetric_arm`/`with_depth_modifier` (→ `single` + a MODIFIER) · `compound_device` BOX/SOFA/BOX_OR_SOFA equipment · `load.none` (→ bodyweight) · `load.percentage` family ref (→ self\|other only) · tempo verbal forms `pauseInUp`/`perNthRepPause`/`slowEccentric`/`holdAfterLast` (→ MODIFIERS).

**Dies → typed-slim:** `reps.implicit` (→ NULL + render inheritance) · `reps.total_flag` (→ nothing — TOTAL ruling) · `load.without_weight`/`load.unspecified` (→ none/NULL) · `media.position`/`appliesTo` (→ node placement) · `compoundRep` + `REP_DEFINITION` (→ compound + notes) · `STANDALONE_URL` (→ `media`) · `STANDALONE_LOAD` (→ per-row load via UI bulk action) · `RowKind.REST` + its `raw` (→ the schema rest setting — D-PLAQUE). **Scope note (D-V2-ROW-REST, 2026-06-17):** D-PLAQUE's ONE-rest-per-schema clause is re-opened by SCOPE only — per-exercise rest now also lives on `SchemaRow.rest` (Grid B); the schema rest axis stays the round-rest, additive, not a second schema-rest.

**Dies → human text (the D-PLAQUE notes stack + the D-MODIFIER library):** `position` enum + column (→ row MODIFIER library refs — D-MODIFIER, superseding the D-POSITION notes ruling) · `sequence` VO prose remainder (→ row notes; only-once → structure) · MAX sub-form tails + progressive seeds · "EXPLODE" stage label · `[ EXAMPLE: … ]` (→ schema note). No blanket chips mechanism (D-CHIPS) — "chip" is an MUI render word only.

**Dies → dropped syntax:** `wrapped` · `*`/`**` markers · `[ TOTAL ]` · bracket/case/word-order variants (`2x15` vs `2x 15`, `FROM_SOFA_BOX` vs `FROM_BOX_OR_SOFA`).

**Stays typed:** the exercise payload (ONE row kind — D-ROW-GRAMMAR; compound/OR → row-groups, rest/slot → catalog natures) · reps/load(`{count,kg}`+`byProfile{axes,cells}` — D-V2-PROFILE-NESTING)/side/media + per-row `intensity` + per-row `rest` (D-V2-ROW-REST) · tempo 4-digit (D-TEMPO) · repetition 6 kinds (+ orthogonal `composition.cap` — D-V2-CAP-AXIS; `interval` work/off `{value,unit}` — D-V2-INTERVAL-UNIT) + rest axis (schema) · Block labels/notes + **`Block.intensity`** (intensity scoped block/schema/row — D-V2-INTENSITY-TRINITY, superseding the D-FLOORS schema-only move).

## 7. What we consciously lose (eyes open)

- **Depth beyond the fixed floors.** Group-in-group is inexpressible. Corpus has zero occurrences; athlete readability is the feature, not the bug. Re-opened only by a real coach need.
- **Machine-readable relation semantics** — until an executor/scoring engine exists. Then a HARDENING pass types the relation kinds that engine reads, designed against it (ADR-0038 principle). BACKLOG-ROUNDS ("5 rounds over the group") lives as label text until that day.
- **Positional auto-application scopes** (standalone load "applies to all preceding") — replaced by explicit per-row data + UI conveniences. The model stops encoding reading-order tricks.
- **Execution & scoring semantics** (D-EXEC-DEFER) — "straight into / no-rest transition", "for time", "score only on rounds 2 & 3", "not for score", "AMRAP at 90%+ effort" are NOTES today. Nothing reads them (no timer/scoring engine — charter non-goal); schema order says "then" but not "without pause". Typed FRESH against the Phase-4 executor (re-introduce-fresh) — likely a sequential-continuous Group mode beside the parallel interleave. NO inert boolean now. The owner flagged the "straight into" gap explicitly; it is the execution layer, deferred whole, not a hole in the primitive.

## 8. Re-expression of the hardest corpus cases

1. **Parallel ladders** (block-005 `12-9-6:` ‖ `6-9-12:`): Group of 2 ladder-schemas (each `repetition.ladder` + movement rows). Label optional — the box is the message.
2. **Rounds over parallel** (block-010, depth-3 today): Group labeled "5 rounds:" of N ladder-schemas. Typed rounds-on-Group deferred (BACKLOG-ROUNDS).
3. **BSS drop-set** (block-008): one schema, `repetition.count = 3 sets`, stages as rows: `5 reps @ DB {count:2, 15kg}` / `5 reps @ DB {count:1, 15kg}` / `5 reps, load bodyweight, text "EXPLODE"` + `media{EXPLODE-url}`. Header stays a field (D-HEADER-KEEP).
4. **`5 strict bar dips OR 10 push ups`** (block-105): two exercise rows in a row-group labeled "OR" (D-PLAQUE).
5. **Asymmetric L/R pairs** (block-073): two plain rows with `side: left` / `side: right` — no special case (confirmed non-edge by the analysis itself).
6. **`12 strict pull-ups [ before BAR DIPS complex ]`** (block-051): exercise row + a row note (D-PLAQUE).
7. **EMOM with MAX slot** (block-080): cadence schema; slots as rows; `MAX DB front squats` = `reps.max` + load.
8. **Split-tier** `[5 KB 24 + 10 DB 15]` (block-119): a row-group of 2 rows with own reps+loads (D-LOAD-FINAL — `split_tier` weight variant dies into structure).
9. **Per-set substitution** (block-020): slot row + per-set rows under a row-grouping (no typed mapping) — RATIFIED.
10. **Wall-clock window** (ex-`repetition.window`, deleted by ADR-0039): a 20-min cap stays the faithful expression (`timeCap`); a literal clock-span, if ever needed, is a note until an engine reads clocks.

**Re-expressed through the primitive-v2 leaf reshape (2026-06-17 — the timed-test corpus, `primitive-v2/reshape-design.md` §3). Cases 11–16 now BUILD through the reshaped primitive; 17–18 stay DEFERRED (no inert field):**

11. **Fran capped** (was ❌ GAP-1): `repetition.ladder [21,15,9]` + `composition.cap {min:12, unit:"min"}` (D-V2-CAP-AXIS). The cap is now a TYPED field, not a note; "FOR TIME" stays a note (D-EXEC-DEFER).
12. **Tabata** (was ❌ GAP-2): `repetition.interval { work:{value:20,unit:"sec"}, off:{value:10,unit:"sec"}, count:8 }` (D-V2-INTERVAL-UNIT). Sub-minute now builds (`value` not int).
13. **Back Squat 5×5 @75% RPE 8, rest 2–3'** (S1-B): the ROW carries it — `SchemaRow.intensity.rpe {value:8}` + `SchemaRow.rest {duration:{value:2, unit:"range_min", rangeMax:3}, scope:"between_sets"}` (D-V2-INTENSITY-TRINITY + D-V2-ROW-REST). Was forced onto the schema; now clean when a strength block holds several movements.
14. **Metcon @85% effort** (S3-B, was schema-only): `block.intensity.effortPercent {value:85}` (D-V2-INTENSITY-TRINITY); member schemas inherit via render overlay (precedence row > schema > block); a harder schema overrides its own dimension.
15. **Wall Ball RX♂9 / RX♀6 / SC♂6 / SC♀4** (S3): `load.byProfile { axes:[{name:"level",values:["RX","SC"]},{name:"sex",values:["♂","♀"]}], cells:[…4…] }` (D-V2-PROFILE-NESTING) → renders as a 2×2 grid, not the garbled flat line.
16. **Row 2:00 @ Zone 2** (S2): `reps.unit_bound {unit:"min", value:2}` (already expressible) + `SchemaRow.intensity.hrZone {zone:"Z2"}` ON THE ROW (D-V2-INTENSITY-TRINITY).
17. **"score = rounds+reps, last 3 min @90%"** (S2-C, #11): still schema notes — DEFERRED (D-V2-EXEC-DEFER-HOLD; no engine reads score/time-window — NOT built).
18. **":20 between the two ladders"** (S3, #20): "2 min between rounds" already = `schema.rest {between_rounds}`; the ":20 between ladders" remnant stays a note — DEFERRED (D-V2-EXEC-DEFER-HOLD; group-track transition, brushes D-2 — NOT built).

## 9. Verification appendix

Grid numbers cross-checked at founding against: `analysis/artifacts/03-content/schema-content-primitives.md` (full read) · `modifier-scope.md` (full) · `compound-and-alternative.md` (full; repeated-pattern recount 14+9=23 noted there) · `03-content/edge-cases.md` (full; the "предлагаемые для standalone first-class" section is the genesis disease's smoking gun — drop-set/slot/split-tier/dual-value promoted to first-class from ≤9 occurrences) · `05-synthesis/domain-model.md` §1–2 (founding session) · live contracts (`composition.schema.ts`, `schema-row.schema.ts`, `load.ts`, `weight.ts`, `compounds.ts`) · `schema.prisma`. NOT re-read at founding (deliberately, owner's "не сейчас"): `load-representation.md`, `load-edge-cases.md`, `load-stability-per-exercise.md` — consult at F-WEIGHT-EXOTICS. Exercise-catalog files (`exercise-*.md`) untouched — the Exercise library is out of this grid's scope.

**primitive-v2 re-freeze cross-check (2026-06-17 — the 5 reshaped shapes verified verbatim against the shipped Zod + Prisma; documented shapes MATCH the code):**

1. **intensity trinity** — `intensitySchema` (`_shared/intensity.ts`) unchanged: `{ effortPercent?, rpe?, pace?, hrZone?, numericPace? }` with a `.refine` requiring ≥1 dimension. Carried at all three levels: `SchemaRow.intensity: intensitySchema.nullable()` (`schema-row.schema.ts` — `create` optional, `update` inherits via `.partial()`); `Block.intensity: intensitySchema.nullable()` (`block.schema.ts` — `create` optional, `update = create`); `Schema.intensity` pre-existing. Prisma: `Block.intensity Json?` + `Schema.intensity Json?` + `SchemaRow.intensity Json?`. The dimension-wise render overlay (precedence row > schema > block) is render-time — no storage promotion (no contract surface). ✅ matches Grid B `intensity` / Grid C `Block intensity`.
2. **row rest** — `SchemaRow.rest: restSpecSchema.nullable()` (`schema-row.schema.ts`; `create` optional). `restSpecSchema` (`_shared/cap-spec.ts`) reused unchanged: `{ duration:{value, unit∈[sec,min,range_sec,range_min], rangeMax?}, scope∈[between_sets,between_rounds,between_intervals,after_specific_set], qualifier?∈[until_recovery,fixed,range], setIndex? }`. Prisma: `SchemaRow.rest Json?`. Scope is NOT over-validated (any scope accepted on a row). ✅ matches Grid B `rest`.
3. **composition.cap** — `compositionSchema` (`composition.schema.ts`) = `{ repetition?, rest?, cap? }.strict()` with `cap: timeCapSchema.optional()`. `timeCapSchema` (`_shared/time-cap.ts`) = `{ min:positive, max?:positive, unit∈[min,sec] }` with `max > min` refine. The 6 `repetitionAxisSchema` kinds are untouched — `cap` is a sibling key, NOT a 7th kind. No reject `superRefine` for `cap` + `kind:"timeCap"`. Renders unit-aware via `capLabel` (`format-composition-summary.ts`): `cap 12’` (min) / `cap 90s` (sec); the `timeCap` KIND routes through the same `capLabel`, so its sec form renders `Ns` (the latent-format fix). ✅ matches Grid C `composition.cap`.
4. **interval unit** — the `interval` variant (`composition.schema.ts`): `{ kind:"interval", work:{value:positive, unit∈INTERVAL_DURATION_UNITS}, off:{value:nonnegative, unit∈INTERVAL_DURATION_UNITS}, count:int positive }.strict()`, `INTERVAL_DURATION_UNITS = ["sec","min"]`. `value` is positive/nonnegative, NOT int (sub-minute). Renders `8×:20/:10` (sec) / `8×1’/1’` (min) via `intervalDurationLabel`. ✅ matches Grid C `repetition` interval note.
5. **byProfile axes/cells** — the `byProfile` variant (`load.ts`): `{ kind:"byProfile", axes:{name:trimmed≥1, values:string[]≥1}[] (min 1 max 2), cells:{coords:string[] (min 1 max 2), kg:positive}[] (min 1) }` + a `superRefine` that enforces (a) unique values per axis, (b) `cells.length === Π(axis.values.length)` cartesian cover, (c) `coords.length === axes.length`, (d) each coord ∈ its axis values, (e) unique coord-tuples. Renders 1-axis as a list, 2-axis as a grid via `formatByProfile` (`format-load.ts`). ✅ matches Grid B `load` byProfile.

**One deliberate non-change flagged (not a discrepancy):** `composeRowSchema` (`composition.schema.ts`) does NOT gain `intensity`/`rest` — the compose/draft row payload stays `{reps, load, side, tempo, media, notes}`. `intensity` and `rest` are sibling columns on `SchemaRow`, applied through the row create/update path, not inside the compose-container draft (the design's §2.1/§2.2 "check `composeRowSchema`" resolved to: it carries neither). No drift — the spec and the code agree.
