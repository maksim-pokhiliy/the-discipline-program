# session-primitive — state (the board)

**Updated:** 2026-06-14 (**W4E live-test follow-ups BUILT** on `feat/session-primitive-w4e-abde`, 5 commits — A tempo smart-union (`ce096ba4`) · E session-creates-with-one-block (`8677b158`) · B schema-group create-as-rows / W3 add-group deleted whole (`b81745e0`) · D in-group member drag both floors (`334aac02`) · fix Review/QA hardening (`f1d507e1`); Review = A / QA = A, 0 CRITICAL, 3 WARNING fixed in-PR; **W4R-001-SERVER + DR-W4E-ROWREORDER-CONTIG-SERVER CLOSED**; + a follow-on row-summary per-category-chips refinement (`ceb356ab`, DR-W4E-ROW-CHIPS); the gated api-server suite + the §6 browser walkthrough are the owner's acceptance gates — PENDING; then merge → catalog pass per `plan.md` roadmap)

_(Prior: 2026-06-13 (late) — W4-editor MERGED PR #265 → main; D-7 review PASS; full suite 2549/2549 incl. gated api-server; review-fix `c8579775` QA-009 CLOSED. The W4-editor history follows below.)_

Resume here (SessionStart hook force-loads this). Narrative → `journal.md`; why → `decisions.md`; carry-forwards → `deferred.md`; the design itself → `primitive-spec.md`.

## Board

| #         | Wave                                                                                                           | Status                                                                                                                                                   | Pointer                                                        |
| --------- | -------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------- |
| 0         | Founding: review → skeleton + grid + spec                                                                      | 🟢 done                                                                                                                                                  | `primitive-spec.md` · D-1..D-8                                 |
| W1        | Group/box UX on the existing model (platform-only)                                                             | 🟢 merged (PR #261)                                                                                                                                      | plan §W1 · DR-W1-1..5                                          |
| W2        | Model core (Group entity, recursion/arrangement death)                                                         | 🟢 merged (PR #262)                                                                                                                                      | `0041` · DR-W2-1..9 · FORK-1..6                                |
| W3        | Editor remap (proto fidelity + gestures + draft flat)                                                          | 🟢 merged (PR #263)                                                                                                                                      | DR-W3-1..12 · FORK-2/3                                         |
| W4-model  | Data-layer reshape (modifier + row-group, row collapse, leaf VOs, D-FLOORS, notes multi-list, seed)            | 🟢 merged (PR #264) + hotfixes pushed                                                                                                                    | plan §W4 · DR-W4-1..TEMPO-SHAPE                                |
| W4-editor | Proto-faithful authoring UX (modifier picker, row-group box, sets/count, row form)                             | 🟢 merged (PR #265) — D-7 PASS, full suite 2549/2549 (incl. gated api-server) + review-fix `c8579775`                                                    | plan §W4 · DR-W4E-\*                                           |
| W4E-f/u   | Live-test follow-ups (A tempo smart-union · B schema-group create-as-rows · D in-group drag · E session+block) | 🟡 BUILT on `feat/session-primitive-w4e-abde` (5 commits) — Review A / QA A, 0 CRITICAL, 3 WARN fixed; gated suite + browser walkthrough PENDING → merge | D-TEMPO-SMART · DR-W4E-SG-WRAP/-INGROUP-REORDER/-SESSION-BLOCK |

## Next action

**▶ SESSION HANDOFF 2026-06-14 — START HERE:**
**The W4E live-test follow-up batch (A/B/D/E) is BUILT** on `feat/session-primitive-w4e-abde` (5 commits: A=`ce096ba4` tempo · E=`8677b158` session-block · B=`b81745e0` schema-group · D=`334aac02` in-group drag · fix=`f1d507e1` Review/QA hardening). The four sub-features surfaced in the 2026-06-14 live browser test: **A** tempo = smart union (4-digit typed OR free string, `parseTempo` total, no "invalid tempo" error — D-TEMPO-SMART); **B** schema groups created by select-mode-wrapping 2+ existing contiguous schemas (the row-group mirror), the W3 instant-ladder "Add group" path DELETED WHOLE (net −LOC), Gate-A route reuse = the existing `groups` POST repurposed to the WRAP shape (NOT a new `from-schemas` route — DR-W4E-SG-WRAP); **D** in-group member drag for both group families (inner `DndContext`, reuse the reorder endpoints, contiguity guarded on both floors — DR-W4E-INGROUP-REORDER); **E** session-create seeds one block atomically (DR-W4E-SESSION-BLOCK). Review = A / QA = A, 0 CRITICAL, **3 WARNING fixed in-PR** (`f1d507e1`: QA-A-01 coach tempo message · QA-B-06 select-mode drag disabled both floors · REV-B-1 dead `groupId?` dropped). **Two `deferred` items CLOSED:** W4R-001-SERVER (DR-W4E-SG-WRAP — wrap-create rejects already-grouped; the row-group sibling got the same backstop) + DR-W4E-ROWREORDER-CONTIG-SERVER (DR-W4E-INGROUP-REORDER — row reorder now asserts contiguity).

**NEXT (the acceptance gate):** the owner runs **the gated api-server suite** (`pnpm db:reset && pnpm db:seed && pnpm --filter @repo/api-server test`, ~10 min live-Neon serial — B/D/E touch api-server) **+ the §6 browser walkthrough** (DnD/UX, incl. **QA-D-03** — the nested member-drag-vs-box-drag double-activation check, jsdom-blind per DR-W3-11) as the acceptance gate → then **merge** (PR for `feat/session-primitive-w4e-abde`, this docs commit lands IN it per closeout-before-pr) → then the **catalog-pass PLANNER session** (the roadmap below, unchanged). The W4E server fast-follows are now CLOSED, so nothing extra rides the catalog gated-suite re-arm from this batch.

---

**▶ PRIOR HANDOFF 2026-06-13 (late) — the W4-editor merge trail:**
**W4-editor is MERGED** (PR #265 → main, merge-commit; branch `feat/session-primitive-w4-editor` deleted local+remote). **D-7 orchestrator git-diff review = PASS** (3 parallel reviewers: row-form/picker/coach-fixes · row-group/reorder/one-predicate · D3/header/hygiene): 0 CRITICAL, every red line holds, the reorder Gate-A scope-change verified legit (reorder WAS dead on main — orphan `useSortable`, unconsumed hook, no `SortableContext`) + correctly wired, D3 auth sound (`withCoachAuth` + `requireCoachLikeRole`, no admin path), zero Prisma/seed/contract change. **One review-fix `c8579775`** — the create-on-the-fly minted modifier rendered its raw cuid on the chip (QA-009; the runner under-rated it as a stale-optimistic INFO, it bit the F1 happy path) → a local minted id→name map + a pinned test (platform 700). **Full suite ran green: 2549/2549 across 243 files**, incl. the gated api-server live-Neon run (~11 min) — D3 + the whole leaf round-trip confirmed on the real DB; seed-coverage clean (no reseed run — it would have wiped the owner's live browser session; D3 changed no data).

**NEXT = the catalog-pass PLANNER session.** The verified recon is already captured → **`catalog-pass-recon.md`** (two parallel agents over main, 2026-06-13 evening — so don't re-run it): the Exercise model + the **4× enum duplication**, the `exerciseId` **FK table** (only `OneRMRecord` is a real FK), the **nature smear** (`canonicalCompoundType` + redundant `placeholderFlag`) + the 3 load-bearing `placeholderFlag` readers, the **equipment-library + naming-fork**, the **CONFIRMED-killable** movement-type tags (`movementFamily` kept separate), the dead `getExercise`/orphan `defaultLoad`, the seed touch surface, and **§8 the design forks** to resolve WITH the owner (F-INIT initiative-vs-tail · F-NATURE · F-EQUIP · F-FK · F-KILL-SCOPE · F-GATE). **Flow:** `/initiative-resume` → read `catalog-pass-recon.md` → resolve §8 forks with the owner → write the catalog-pass `/feature` prompt. **It touches Prisma + seed → reseed + gated suite (the W4-model ritual), NOT platform-only.** Then e2e (owner builds evil CrossFit workouts by hand). The W4-editor server fast-follows (W4R-001-SERVER, ROWREORDER-CONTIG-SERVER) can ride this wave's gated-suite re-arm. The `W3-TRACK-DRAG + W3-DND-POLISH` DnD polish is DONE (PR #268, `a2cc5de4` — compact DragOverlay ghost + vertical-axis lock; the in-group member drag rode sub-feature D). The §6 W4-editor browser walkthrough remains the owner's live-UI acceptance (post-merge); it did not gate the merge.

**Scope shipped (platform-only EXCEPT one api-server endpoint, D3):** D1 the row form (exercise-select-first) · D2 the modifier picker (searchable + create-on-the-fly) · D3 the coach modifier-create endpoint · D4 the row-group box + select-mode grouping gesture · D5 the notes multi-list editor · D6 `sets`/`count` authoring · D7 in-group schema header parity (supersedes DR-W1-3) · **+ row reorder WIRED** (Gate-A change to the plan). The W3 schema-group box + gestures + `AxisEditorModal` were NOT rebuilt; the row create/update plumbing + `modifierIds[]` + `api.modifiers.search`/`api.rowGroups.*` clients pre-existed (the wave added the missing hooks + the modifier `create` endpoint).

**Gate-A change to the plan, recorded:** the RFC's red line #7 ("don't regress row reorder") was FACTUALLY WRONG — design-time recon found row reorder was NEVER wired (orphan `useSortable`, `useReorderSchemaRows` unconsumed, no `SortableContext`). The owner approved WIRING it at Gate A (DR-W4E-ROW-REORDER) — a row-floor `DndContext`/`SortableContext` mirroring `block-card-body` one floor down, `pointerFirstCollision` for the mixed-height row↔row-group list, contiguity preserved by construction. So the wave shipped D1–D7 PLUS row reorder.

**Three design forks were owner-RATIFIED + built (F1/F2/F3 → `decisions.md`):** F1 the coach create-on-the-fly modifier endpoint (the one platform-only-boundary crossing) · F2 `NotesListEditor` editing `string[]` directly (retired `notes-list-text.ts` — W4R-005 dead) · F3 the select-mode row-grouping gesture (wrap contiguous existing rows; already-grouped structurally unselectable; client contiguity check). The UX-language law was the `plan-editor-hi-fi-v-2` bundle (the SAME family as W3) — the build took the interface LANGUAGE, mapping the domain onto the W4 contracts (the prototype's `row-editor.jsx` lagged on the OLD leaf).

**Review = A−, QA = C (the 4 CRITICALs ALL fixed in the Stage-5-6 fix-loop), platform 699/699.** Review (the load-bearing static check): 0 CRITICAL, every red line holds, both ratified residuals (W4R-001 client guard + reorder contiguity construction) correctly client-guaranteed and code-traced; the one WARNING was a test hole (CODE-001), backfilled in the test stage (+110 tests). QA's CRITICALs were a coach-UX cluster — QA-001/002/003 (raw-zod-leak / no-submit-block / invalid-`0`-default) FIXED via DR-W4E-COACH-ISSUE (`2bda02c2`); QA-008 (mint failure dropped selection) FIXED via DR-W4E-PICKER-MINT-TOLERANT (`7c146b96`). 0 data-corruption CRITICAL survived client construction.

**New deferred fast-follows (`deferred.md` → "From the W4-editor build"; none blocking, all client-guaranteed or latent):** **W4R-001-SERVER** (the server overlap reject `createRowGroupWrapping` still lacks — client-guarded by construction) · **DR-W4E-ROWREORDER-CONTIG-SERVER** (no server contiguity guard on the row reorder — client guarantees it by construction) · **QA-007** (re-typing an existing name not in the stale search results → ConflictError instead of attach — §6.4 walkthrough flag) · INFO QA-009/010/011. Both server guards re-arm the gated api-server suite when done.

_(The W4-model build + its merge/review history follows below as the trail; the catalog-pass flags from PR #264 — loose `exerciseId` FK + the nature enum + the dead `getExercise` cleanup — are still OPEN in `deferred.md`, untouched by W4-editor.)_

---

W4-model history (the merged build): the data-layer reshape shipped on `feat/session-primitive-w4-model` (PR #264). W4-model lands the ratified row grammar as the DATA layer; the **authoring-UX rebuild is the separate W4-editor wave next** (the proven W2→W3 model-then-editor rhythm — W4 ≈ 2× W2, too big for one `/feature`). NOT a new one-way door — rides **ADR-0041** (reshapes the leaf ONTO the model-core; no new ADR). The four batches landed: **foundation** (the `modifier/` + `row-group/` contract modules à la `label`/`schema-group`; `_shared/notes.ts`; the load/tempo/side VO reshapes; `weight.ts` + `sequence.ts` + `compounds.ts` deleted); **server** (the `Modifier` + `RowModifierAssignment` + `RowGroup` Prisma entities; `RowKind`+`Position` enums dropped; `exerciseId String` (loose, no FK) + `sets` + `rowGroupId` on the row; all 7 `notes` carriers `String?`→`Json?`; the CRUD/search/row-group endpoints; `verifyRowGroupOwnership`; `assertRowGroupMembersContiguous`; the reshaped mappers + the shared `SCHEMA_BODY_INCLUDE`); **seed** (every evil-fixture shape re-expressed — compound/OR/split-tier → row-groups, position/verbal-tempo → modifier refs, REST → `composition.rest`, EMOM REST minute → a seed Rest exercise, block intensity/timeCap → schema); **platform** (typechecks + renders off the reshaped contracts via the new `buildRowItems` one-predicate + `RowGroupBox`; ~84 dead editor files deleted; Edit/Add stubbed `disabled` with "coming in W4-editor"). **All runnable gates green** (check-types 16/16, lint 16/16, dep:check 0, `@repo/contracts` 664, platform vitest 514). DR-W4-1..5 + DR-W4-PAIRED + DR-W4-RG-CREATE + DR-W4-MAPPER-INPUT + DR-W4-SWB + DR-W4-TEMPO-SHAPE ratified; net −11656 LOC drained (303 files; a bridge-free reshape lands atomically — DR-W4-5).

**Review = A, QA = B, 0 CRITICAL.** Review (static deep-trace, the load-bearing check since the api-server suite is owner-gated): every one of the 9 ratified DR-W4-\* implemented exactly; the seed re-expresses every evil-fixture shape; the one-predicate rule holds (`buildRowItems` — exactly ONE consumer, `schema-row-list.tsx`); module boundaries clean; type-safety spotless (zero `any`/`as`/`!`); the read render complete; the editor stubs honest; the BC-surface grep ZERO live stragglers for every killed identifier. QA (hostile static trace) = B, one WARNING (QA-001, the missing `.refine(unique)` on `rowIds`) **FIXED in-PR** + a cluster of INFO. **The one fix landed (`5b099c1f`): QA-001 rowIds-unique refine + its test pin, AND W4R-008 (dead `MODIFIER_CONSTANTS.MAX_NOTES_LENGTH` dropped).** **One deferral to W4-editor: W4R-001/QA-002** — row-group create silently re-homes an already-grouped row + orphans the old empty group (NOT reachable in W4-model — no gesture; the seed authors directly; the editor designs the grouping gesture + the guard).

**PENDING — the owner's manual acceptance ritual (a HARD prerequisite before the app runs):** `pnpm db:reset && pnpm db:seed && pnpm --filter @repo/api-server test` (~10 min, live Neon, serial). The pipeline ran only the no-DB gates; the no-DB `canonicalSeedSchema.safeParse` passed (152 exercises, 15 modifiers, 11 row-groups), but the seed round-trip + the 100%-coverage assertion + the mapper/endpoint integration are UNVERIFIED by the pipeline. **The bridge-free 500-until-reseed is BY DESIGN (the W2 QA-001 precedent):** the mappers `.parse()` against the new contracts → the week GET returns a clean handled `InternalServerError` (`kind:"DbCorruption"`, NOT a partial render) on any stale row until the reseed. Highest residual = QA #7 (row-group create concurrency — api-server-GATED, not runtime-tested; double-membership structurally impossible via the single `rowGroupId` FK). Coverage-cell tuning (A3 achievable-counts trim) validated only at reseed. **See `deferred.md` → W4-RESEED-GATE.**

**Orchestrator review of the diff is PENDING (D-7 — via git diff, not the runner self-report).** Next planner / the owner starts here:

**DONE this handoff:** W4-model reseed + merge + push, **and the W4-editor `/feature` prompt is written** (`w4-editor-runner-prompt.md`). Roadmap sequence (`plan.md`): **W4-model ✅ → W4-editor (prompt ready, owner runs `/feature`) → catalog pass (equipment library, `concrete\|placeholder\|rest` nature, movement types) → e2e (owner builds evil CrossFit workouts by hand) → "ОК" → next roadmap phase.**

W4-editor carries (the UX over the now-shipped data layer):

- The **modifier picker** — searchable create-on-the-fly (the coach `createModifier` path — W4-COACH-MODIFIER; component shared with LABEL-FLOW-UX); W4-model shipped admin-create + coach-search only.
- The **row-group box** — the grouping gesture (mirror `schema-group-box`) + the W4R-001/QA-002 guard decision (reject overlap or clean the empty source group) + the W4R-005 notes-list round-trip fix.
- The **row form rebuild** — the exercise select (first step), `sets`/`count` fields, the load/tempo typed forms, the modifier multi-ref, the notes multi-list editor.
- **W4-HEADER-PARITY** (D-HEADER-KEEP) — an in-group schema's header renders like a standalone one.
- Still deferred to the catalog pass (NOT W4-editor): the full `concrete\|placeholder\|rest` nature enum + equipment library (CATALOG-NATURE / EQUIPMENT-LIBRARY) — W4-model bridges via the existing `placeholderFlag` + a seed Rest-natured exercise.

## Open decisions awaiting ratification

- **NONE — the F-ledger is fully closed; `primitive-spec.md` is FROZEN (zero OPEN grid rows).** Closed 2026-06-12: F-PLAQUE→D-PLAQUE · F-CHIPS→D-CHIPS · F-POSITION-CARRIER→D-MODIFIER (D-POSITION superseded same day) · **F-WEIGHT-EXOTICS→D-LOAD-FINAL · F-TEMPO→D-TEMPO · F-BLOCK-TIMECAP+block-intensity→D-FLOORS · F-SLOT→D-ROW-GRAMMAR · F-HEADER→D-HEADER-KEEP** + D-EXEC-DEFER. D-MARKER-DEATH landed W2. The spec-freeze gate (plan step 1) is MET; **W4-model + W4-editor BUILT+MERGED, the W4E live-test follow-ups (A/B/D/E) BUILT (2026-06-14) → the NEXT action is the owner's gated suite + §6 browser walkthrough + merge** (no open decision gates either). The 4 W4E-followup decisions are RATIFIED (owner, live browser test): **D-TEMPO-SMART · DR-W4E-SG-WRAP · DR-W4E-INGROUP-REORDER · DR-W4E-SESSION-BLOCK** — nothing awaiting ratification. W4-editor closed W4-HEADER-PARITY (→ D7) + W4-COACH-MODIFIER (→ F1/D3). Remaining ledger items (owner-paced): LABEL-FLOW-UX (shares the picker built this wave) · EQUIPMENT-LIBRARY · CATALOG-NATURE · W4-EXEC-DEFER.

## Live carry-forwards

**W4E live-test batch (branch `feat/session-primitive-w4e-abde`, 2026-06-14):** **CLOSED this batch** — **W4R-001-SERVER** (DR-W4E-SG-WRAP — `createSchemaGroupWrapping` rejects already-grouped; the row-group sibling `createRowGroupWrapping` got the same `rowGroupId !== null` reject) · **DR-W4E-ROWREORDER-CONTIG-SERVER** (DR-W4E-INGROUP-REORDER — the row-floor reorder now asserts `assertRowGroupMembersContiguous`). **FIXED in-PR (`f1d507e1`):** QA-A-01 (coach tempo over-length message) · QA-B-06 (select-mode drag disabled both floors) · REV-B-1 (dead `groupId?` dropped). **NEW OPEN (owner-browser-walkthrough acceptance item):** **QA-D-03** — the nested member-drag-vs-box-drag double-activation is jsdom-blind (D's inner `DndContext` + the box head handle; DR-W3-11 lesson) → §6 walkthrough confirms no cross-capture on either floor. See `deferred.md` → "From the W4E live-test batch (2026-06-14)".
**W4-editor (branch `feat/session-primitive-w4-editor`, 2026-06-13):** the 4 owned items CLOSED — **W4R-001/QA-002** (client-guarded by construction — already-grouped rows structurally unselectable; DR-W4E-W4R-001-CLIENT) · **W4R-005** (`notes-list-text.ts` deleted, grep zero; `NotesListEditor` edits `string[]` directly; F2) · **W4-COACH-MODIFIER** (D3 endpoint shipped; F1) · **W4-HEADER-PARITY** (D7, supersedes DR-W1-3). **The two server fast-follows now CLOSED in the W4E batch (above):** **W4R-001-SERVER** + **DR-W4E-ROWREORDER-CONTIG-SERVER**. Still OPEN: **QA-007** (re-typing a stale-search existing name → ConflictError instead of attach; §6.4 flag + small-fix) · INFO QA-009 (CLOSED) / QA-010 / QA-011 (no `enabled` gate · `NotesListField` blur lost-update — pre-existing pattern). See `deferred.md` → "From the W4-editor build".
**W4-model (PR #264, 2026-06-13):** **W4-RESEED-GATE — CLOSED** (owner ran `db:reset`+`db:seed`+gated suite; 2 live-DB defects caught + fixed + pushed). **CLOSED in W4-editor (above):** W4R-001/QA-002 · W4R-005 · W4-COACH-MODIFIER · W4-HEADER-PARITY. **FIXED in-PR:** QA-001 (rowIds `.refine(unique)` + pin) + W4R-008 (dead `MAX_NOTES_LENGTH` dropped) — commit `5b099c1f`. **INFO (no action):** W4R-006/QA-003 (misleading P2003 "SchemaRow" message on a bad modifierId) · W4R-007 (row-group update/delete don't cross-check URL planId — matches the W2-DELETE-PLAN-SCOPE template; auth sound via the ownership walk) · W4R-002/003/004 (the by-design structured→notes A3 relocations). See `deferred.md` → "From the W4-model build".
**CLOSED in W3 (PR #263):** **W2-UX-POLISH** (D1 idempotency-key bug + D2 proto card — DR-W3-1..6) · **W2-DRAFT-RECURSION** (D4 flat draft collapse — DR-W3-7; `arrangement-tree.ts` deleted) · **QA-004** (D5 kind-switch confirm — DR-W3-8; keep-confirm-always per QA-114) · **W2-STALE-FIXTURES / W2-VESTIGIAL-EXPORTS / W2-STALE-NAMES** (D6 hygiene — DR-W3-10; caption fixed, seed const renamed, contracts dead-exports deleted).
**CLOSED earlier (W2 PR):** W1-DUP-RETRY · W1-RENDER-REPOINT · W1-SUBADD-BOX · MARKER-FATE / D-MARKER-DEATH · W2-ORDER-UNIQUE (DR-W2-8 SUPERSEDED).
**CLOSED 2026-06-12 (design session):** **F-PLAQUE → D-PLAQUE** · **W2-FOOTNOTE-LAST** (owner: plain row, expression correct) · the gated-suite confirmation (owner ran it green). **W2-URL-PLACEHOLDER** — seed fixed to coach-readable content; owner reseeded + accepted; committed to main. ALL §6.7 re-confirms now CLOSED.
DONE in PR #268: **W3-TRACK-DRAG + W3-DND-POLISH** (in-group member drag rode sub-feature D; compact DragOverlay ghost + vertical-axis lock = `a2cc5de4`). NEW deferred (owner-requested 2026-06-14, NOT quick): **DND-GROUP-CREATE** — create groups by dragging schema-onto-schema / row-onto-row + drag-in/drag-out; scheduled WITH training-plan entity duplication in Phase 2 (see `deferred.md` → "Deferred from this batch"). OPEN (W3-new, none blocking): OQ1-DEMOTE-DEAD (the dead demote feature — owner chose CARRY) · QA-106 (materialize 2nd-ladder default `[15,12,9]` ≠ proto `[9,15,21]`) · QA-108 (materialize drops the header — latent) · W3-REV-002 (un-shared `TRACK_GAP`) · W3-REV-003 (in-group-add empty-schema — W2-IDENTICAL, not a W3 regression) · W3-REV-004 (`[21,15,9]` literal ×3) · QA-101 / QA-107 (test-stage CLOSED pins — kept for the trail).
OPEN (prior): W2-IDEM-REMOUNT (low — by-design; W3 deliberately kept it, red line #6) · W1-INSESSION-CHECK (low) · BACKLOG-ROUNDS (Group label until an engine) · BACKLOG-TAIL / BACKLOG-PATTERNS (dissolve by design — confirm at freeze). W1-BOX-FRAME CLOSED (and the W3 proto restyle replaced the dashed look with the solid-tint frame anyway). Standing debts out-of-scope: `Performed*`/`OneRMRecord` known-wrong (Phase-4 redesign, now the carrier for the ex-roadmap score-type debt); reuse features post-primitive.

## Gotchas a resuming session must know

- **ADR-0041 is the law** — the box is a real `SchemaGroup` entity (membership-based, opaque label, no recursion, no typed relation kinds, no child-count semantics); parallelism is explicit Group membership, not derived. **ADR-0040 (derive-parallelism) is SUPERSEDED by 0041** — `isStructurallyParallel` / the arrangement axis / `parentSchemaId` / `POST …/schemas/parallel` are deleted. W3 is an editor remap ONTO 0041 (no new ADR); **W4-model reshapes the LEAF onto 0041 (no new ADR either — DR-W4-\* in `decisions.md`, not a new `docs/adr/`).**
- **W4-model is the new LEAF law (BUILT 2026-06-13, PR pending)** — the row is ONE authored kind: `exerciseId` (loose String, NO FK) + `sets?` + `reps` + `load` + `side` + `tempo?` + `intensity?` (schema-only override removed) + `media` + `modifiers: ModifierRef[]` + `notes: string[]`. `RowKind`+`Position` enums + `rowPayload` are GONE; render-kind is INFERRED from `placeholderFlag` (never authored). TWO new entities: `Modifier` (+ `RowModifierAssignment`, à la `Label`/`BlockLabelAssignment`) and `RowGroup` (à la `SchemaGroup` one floor down — schema-owned, SetNull dissolve, NO order/label column, notes-first-as-label). `notes` is a `Json?` `string[]` on EVERY carrier (Week/Day/Session/Block/Schema/Row/SchemaGroup/RowGroup). The `weight`/`sequence`/`compounds` VOs are DELETED; load = `absolute{count,kg}|percentage{self|other}|bodyweight|byProfile{entries}`; tempo = flat 4-digit (`int|"X"`). **`buildRowItems` is the row-floor one-predicate** (mirror `buildBlockItems`; SOLE clustering source — never hand-roll). The catalog `concrete|placeholder|rest` nature enum + equipment library are NOT W4 — the separate catalog pass (W4 bridges via `placeholderFlag` + a seed Rest exercise).
- **Recursion is GONE EVERYWHERE post-W3** — the authoring DRAFT layer was the last holdout (`ComposeContainer.children: ComposeNode[]`); it collapsed to flat `SchemaDraft{rows}` + `GroupDraft{tracks: TrackDraft[]}` (DR-W3-7), the `nodeType` discriminator died, `arrangement-tree.ts` was deleted. The modal seed is a `DraftSeed` union (`{mode:"schema"}|{mode:"group"}`).
- **W3 was model-neutral (NO reseed) — but W4-model is NOT:** W4-model reshapes Prisma + contracts + seed, so a fresh reseed is MANDATORY again (the W2/W3 reseed no longer covers the app — a pre-W4 row shape now 500s the week GET). W3's single api-server touch was a cosmetic seed const rename (`BLOCK_FOOTNOTES_WK2_TUE`→`BLOCK_PER_ROUND_MARKERS_WK2_TUE`, owner-delegated — DR-W3-FORK-3, byte-identical data). The **W2 raw-SQL lesson held in W4-model**: `prisma/sql/lms-checks.sql` was swept clean of killed-column refs (`position`, dropped block columns, `SchemaGroup.label`) — QA confirmed ZERO killed-token matches; tsc/vitest/ts-greps stay blind to `.sql`, so the owner's `db:reset` is the only executor (W4-RESEED-GATE).
- **The reseed is mandatory before the app runs against the new code** (aggressive bridge-free — the W2 QA-001 precedent, now the W4-model law): the read mappers `.parse()` against the new contracts and reject any pre-W4 row shape → a clean handled `InternalServerError` (`kind:"DbCorruption"`, NOT a partial render) on the week GET. `pnpm db:reset && pnpm db:seed` is the stale-data remedy (ADR-0019, non-prod Neon); the gated api-server suite is the owner's manual acceptance ritual (W4-RESEED-GATE, PENDING).
- **One-predicate rule (now at TWO floors)** — schema-floor clustering ONLY via `schema.groupId` + the shared `buildBlockItems(schemas, groups)`; row-floor clustering ONLY via `row.rowGroupId` + the shared `buildRowItems(rows, rowGroups)` (W4-model, mirror of `buildBlockItems`). A hand-rolled child-count/cluster check is how the last CRITICAL shipped. The Group / RowGroup label (the first note) is opaque text the system NEVER reads.
- **The corpus is the floor, not the ceiling** — one PERSONAL plan; m/f (`byProfile`) and RX/SC are real despite corpus cardinality 1/0 (D-5/D-6).
- **No typed relation kinds; no chips-as-blanket-mechanism** (D-4; F-CHIPS rejected as "костыль") — channel-3 carriers decided per case in follow-ups; a relation gains typed semantics ONLY against a real engine (ADR-0038 re-introduce-fresh).
- **Owner verbatim bars** in `decisions.md` D-6 (TOTAL dead, footnote=ordering, per-set=row-group) — quote them in runner prompts, don't re-litigate.
- **`docs/roadmap.md` was removed** (W2 commit `58de8394`, owner-requested) — don't expect it; the Phase-4 score-type debt it carried is re-anchored to the `Performed*`/`OneRMRecord` standing debt in `deferred.md`.
- Predecessors (`plan-editor-compose`, `compose-authoring-ux`) are CLOSED; their decisions describe main's current code, not this redesign's constraints — except the charter's Sacred list.
