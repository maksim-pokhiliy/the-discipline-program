# storefront-billing — journal

Append-only. One entry per session/step.

## 2026-09-22 — Trigger + the Apple-vs-Monobank debate

- **Trigger.** Denys in the project chat: «По оплате через сайт [монобанк] ещё нет инфы?!» · «заебался уже напоминать об оплате и гоняться за определённой категорией людей». Owner's reply: work starts tomorrow, first an estimate; the target is "subscribe once, charged monthly until the athlete unsubscribes in the platform cabinet"; Denys asked for 4-week periods.
- **Vladyslav's challenge (two rounds).** (1) "Through Apple this would have taken 2 hours a year ago; did you count the manual management time and the scaling limit?" — his "scaling limit" was Denys's manual payment chasing, not the platform (Denys confirmed the reading). (2) "Better give Apple 15% and sell more; Mono limits you to the poor Ukrainian market (100–200 subs at best); Apple = the whole world." Planner analysis, verified against sources: Mono accepts foreign cards (2%); Apple has no 4-week period; there is no App Store listing to sell IAP through (AS-1); IAP serves iOS only; Apple's 3.1.3(b) allows web + IAP to coexist so IAP is a later adapter, not a fork. The genuinely strong part of his case — Apple as merchant of record for a global English push — is kept as a seam (ADR-0044 `provider`).
- **Verified facts banked** (see `monobank-notes.md`): mono native subscriptions (`interval` `{n}{d|w|m|y}`, `edit` = cancel only, `remove` = pre-payment only), tokenized `wallet/payment` with `initiationKind: merchant`, ECDSA `x-sign` webhooks with 3 retries, test mode on any Luhn card, 1.3% / 2% fees, next-day settlement, FOP-only connection; App Store: 15% Small Business, periods 1w/1m/2m/3m/6m/1y, guideline 3.1.1 / 3.1.3(b) text.

## 2026-09-23 — Scope dialogue, model ratified, founding

- **Scope dialogue with the owner** (co-owner mode, four corrections accepted): no GROUP/PERSONAL plan kind → delivery on the binding (D-4); one subscription per user → per product with 1..N plans (D-2, supersedes ADR-0008); "cash clients extended by hand" → one-off periods, no cash flow, MANUAL only for comps (D-5, «не делать второй Excel»); the storefront is fully admin-managed; pause and the iOS text moved into our decision box (D-6, D-8). Model ratified: «модель заходит».
- **Repo recon that shaped it.** Billing skeleton exists in the schema only (`Product/Price/Subscription/Transaction/RequestIdempotency`; `endpoints/billing` + `contracts/billing` are READMEs; `PaymentPort` is Stripe-checkout-shaped); BOUNDED-CONTEXTS §8 already plans "Access = Subscription State"; dep-cruiser denies Billing to LMS/Coaching/mobile-compat, exempts `authz/`; web athlete access = ACTIVE enrollment (`plan-timetable.ts`, `session-access.ts`); the iOS shim resolves days from `MobilePublishedDay` by legacy identity and never consults enrollment (`get-program.ts`); the app shows 404 → "no program", 403 → sign-out, and renders raw text cells; onboarding = `createPendingUser` + invite token (no self-registration); storefront = card → modal → lead form; admin product form = one price, USD default, no plan link; cron pattern = admin `/api/cron/retention` with `CRON_SECRET`; `Week.startDate` is absolute and week clone exists (COPY is buildable).
- **Denys, coach-side pack** sent 15:43 (12 questions with defaults = D-11). **FOP-side answers:** «у меня только ФОП» — no mono business account; his accountant «такие нюансы не знает»; «все сейчас из Украины… для иностранцев я NoName». Owner sent him the connection steps (mono FOP account → business cabinet → "Додати інструмент" → internet acquiring; Ukrainian site + offer page needed; tokens after approval) and declined to advise on РРО (get a tax consultation).
- **Dates given to Denys** (appetite): P0 → 02.10 · P1 → 09.10 · P2 → 16.10 · P3 → 23.10 · P4 → ~28.10; first live subscription by end of October; full cohort cutover at X.
- **Founded** `initiatives/storefront-billing/` (charter, plan P0–P4, D-1..D-12 with D-3 OPEN, SB-1..SB-13, `domain-model.md`, `monobank-notes.md`), ADR-0044 drafted (supersedes ADR-0008 + ADR-0014), roadmap / BOUNDED-CONTEXTS / post-uat W7 pointers updated, slug registered in `ACTIVE`. Code work starts 2026-09-24 with the 0.2 spike + the 0.3 migration corpus.

## 2026-09-24 — Denys's answers folded; the app sign-in scare

- **Coach-side pack answered** (verbatim, in order): 1 «Можем в гривне» · 2 «4 недели, как показывает практика Фронинга и Фрейзера» · 3 🤝 · 4 «3 дня может и много… 1-2 достаточно» · 5 «Не видел, чтобы кто-то возвращал деньги…» · 6 «А тут все зависит от запроса… ПРО соревновательный или ПРО для души))) или Func BB style» · 7 🤝 · 8 🤝 · 9 🤝 · 10 «Можно будет создать пробный / ознакомительный шаблон… на три дня… скачал… посмотрел, пощупал» · 11 🤝 · 12 «Закрытый – тот который перестал оплачивать?! Есть смысл оставлять ему видимость старых программ?! Или это гемор?!». Folded as D-11 amendments (grace 2 days; personal products per style, each COPY from its own template; closed = no program visibility, profile + records stay) and **D-13** (trial = a zero-price 3-day `FREE` product bound COPY to a trial template). **SB-10 CLOSED** (English). Model unchanged otherwise; `BillingProvider` gains `FREE`.
- **The sign-in scare.** After the owner's AS-22 notice (five attach athletes whose app password becomes the website password), Denys wrote «не могу зайти в приложение и сделать синхрон с платформы… получается, мой тоже поменялся?!». Verified against apex-sunset: Denys (legacy id 2) was a **CREATE** row — «his platform coach account lives on a different address, so his legacy login + old password carried over intact»; the P2 DoD sample (#388, 2026-09-17) was exactly him signing into the stand with his old app password. So: NOT in the AS-22 set; the app takes his old app login + old app password; his coach website password belongs to a different account and never worked in the app; no "sync" exists — the app reads the platform directly. Most likely he typed the coach credentials after reading the notice. Reset path if the old app password is lost: the website's Forgot password on the app login address. Vercel runtime logs (Hobby retention ≈ 1 h) showed only web athlete traffic in the window, no `/api/v1/*` requests — inconclusive. Answered in chat; nothing to change in code. Carry-forward into 4.1: Denys's own app identity needs a permanent comp (SB-11 detail).
- **Next unchanged:** 0.2 Mono spike + 0.3 migration corpus.

## 2026-09-25 — P0.0: the local stack

- **Built** `docker-compose.yml` (project `tdp-platform`, `postgres:17-alpine`, `127.0.0.1:5432`, 512 MB) with `tdp` / `tdp_test` / `tdp_shadow`; `task stack:*` (up, down, reset, migrate, seed, demo, psql, logs, env, tunnel) + `task test:api`; `docs/runbooks/local-stack.md`; pointers in `.env.example`, README, DEPLOY, ADR-0026 (the compose item is resolved), ADR-0042 (the shadow database now lives in the stack). The owner's env files carry the local URLs with the Neon dev lines commented above them.
- **Gate passed.** `task stack:reset` took 10 s (11 migrations into both databases, then the seed); `task stack:demo` added the demo coach, the demo athlete and 64 published days. Platform (3001) and admin (3012 — 3002 is another project's Django server on this box) answered `/api/ready` on the first poll. Sign-ins: demo athlete → `/athlete` 200, dev-coach → `/coach` 200, dev-admin → admin `/`, `/exercises`, `/labels` 200, wrong password → `CredentialsSignin` and a 307 to login, anonymous admin → 307 to login; iOS shim `POST /api/v1/auth/signin` → 200 with `accessToken`. `task test:api` against `tdp_test`: 168 files / 1966 tests green in 2 min 32 s (about ten minutes on Neon before).
- **Postgres 17, not the plan's 16.** Production Neon is 17.5 and migration rehearsals restore production dumps into the stack, which PG16 `pg_restore` cannot read (apex-sunset 0.2). CI keeps 16 as the floor (SB-15).
- **Tunnel finding.** `cloudflared` 2026.9.3 installed as `~/.local/bin/cloudflared`; quick tunnels fail from this network — `api.trycloudflare.com` times out on TCP (v4 and v6) while `api.cloudflare.com` and the edge port 7844 are reachable. The `thedisciplineprogram.com` zone lives in Vladyslav's Cloudflare account (Free plan); the named-tunnel recipe is in the runbook and the one-time login is the owner's (SB-14).
- **Format call.** P0.0 ran as a planner-hands-on manual session (no `/step` executor): infra scaffolding wanted tight run-verify loops on the WSL box; the artefacts still land through a PR for the owner's check. D-14 records the rule.
- **Next:** 0.2 spike against the stack once the test token and the named tunnel exist; the 0.3 corpus can be written in parallel.

## 2026-09-25 (later) — 0.2 spike against the test merchant

- **Token.** The owner's personal `api.monobank.ua` token («Токен фізичної особи та ФОП», placed in `apps/platform/.env.local`) is the test-mode acquiring token: `merchant/details` → `test_7qr44168CQ` / «Test Caption». The auto-mode classifier refused to let the planner read the token file directly (credential materialization) and refused an external webhook catcher (exfil scouting); both stayed refused — the token is read in-process by `spike/mono-spike.mjs` only, and webhooks wait for the SB-14 named tunnel.
- **Run.** invoice 1 UAH with `saveCardData` → owner paid with a Luhn card → `walletData.cardToken` → `wallet/payment` `initiationKind: merchant` ×3 (synchronous `success`, ordinary invoices, one refunded via `invoice/cancel` → `reversed`) · `initiationKind: client` → `processing` + fake 3DS `tdsUrl` · native `subscription/create` `4w` → owner paid → `active`, `nextChargeDate` = +28 d, same `cardToken` → `edit cancel` → `cancelled` · `subscription/list` + `/payments` need RFC 3339 dates · second-round pair removed (`invoice/remove` → `expired`, `subscription/remove` → `removed`). Every payload is in `monobank-notes.md` §0.2 spike; raw captures in the gitignored `spike/captures/`.
- **D-3 proposed = (b)** (own scheduler + merchant-initiated charges): the (a) lean condition fails on retry semantics and the fiscal basket; (b) is proven end to end and keeps one charge path for auto-renew and one-off. Awaiting the owner's word.
- **Next:** owner ratifies D-3 → 0.3 corpus; webhook capture when the tunnel exists.

## 2026-09-25 (evening) — the hosted page, read from its bundle and by the owner's eyes

- The payment page is client-rendered; its bundle shows the save-card toggle is gated by `saveCardData.allowTokenizationChoice`, the card form sends `saveCard`, the Apple Pay chunk sends no save flag. Three look-only invoices confirmed the toggle rule with the owner's eyes (toggle only with `allowTokenizationChoice: true`). Consequences folded into D-3 and `monobank-notes.md` (open items 7, 8). Owner's read on the renewal step: «если на этом этапе моно не влупит 3DS, то вся схема выглядит рабочей» — the spike says it does not (`merchant` kind, no `tdsUrl`). D-3 still awaits the owner's explicit word.
