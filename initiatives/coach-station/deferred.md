# coach-station — deferred

Carry-forwards: findings/obligations not yet scheduled, with disposition + status. **Promote here at every gate.**

**Status:** `OPEN` (live) · `SCHEDULED` (assigned to a step) · `CLOSED` (done — kept for the trail) · `DROPPED` (decided not to).

| ID                     | One-liner                                                                                                  | Disposition                                                                                                                                                                                                                                                                 | Status    |
| ---------------------- | ---------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- |
| TEMPLATES              | Saved compositions / archetypes (R2) — the persona's "EMOM ×200" reuse                                     | PARKED on the radar (D-2); a new entity + its own mini design-cycle; reuses R1's deep-clone engine; slot TBD at R1 close                                                                                                                                                    | OPEN      |
| DND-GROUP-CREATE       | Create groups by dragging schema-on-schema / row-on-row + drag-in / drag-out, both floors                  | Scheduled as wave **G** (own `/feature`). Detail inherited from `session-primitive/deferred.md` → DND-GROUP-CREATE                                                                                                                                                          | SCHEDULED |
| LABEL-FLOW-UX          | Label dropdowns (day/session/block) get search + create-on-the-fly                                         | **CLOSED by D-10** (`feat/coach-inline-create`) — day/session/block host the lifted `CreatablePicker` (search + create-on-the-fly) via the dual-mode `LabelPickerChip`; mints at the surface level + idempotent-attach. Browser walkthrough recommended                     | CLOSED    |
| QA-007                 | Create-on-the-fly: re-typing an existing name missed by the stale search → ConflictError instead of attach | **CLOSED by D-10.** Server-side fix: `create` returns the existing row on P2002 (idempotent-attach on `nameLower` / `canonicalNameLower`) — re-typing an existing name attaches, never 409s. Both label + exercise inline create                                            | CLOSED    |
| A-E2E-POLISH           | Authoring papercuts surfaced by the in-flight session-primitive e2e                                        | **HELD OPEN — list still filling.** Fed by the e2e (P-6 reps-unit, QA-D-03, + new). Do NOT build assuming a shape                                                                                                                                                           | OPEN      |
| P6-REPS-UNIT           | Distance/calorie reps unit (800m run, 500m row, 10 cal) may be a real papercut                             | **NOT this initiative's call** — P-6's outcome is a session-primitive FREEZE decision (parallel session). If it re-opens the leaf, clone/archetypes inherit the new shape for free (they copy live shapes); coach-station polish must not assume a unit form until it lands | OPEN      |
| CLONE-WEEK-DESTRUCTIVE | Week/day clone is destructive (replace) — deletes the current subtree                                      | Tracked as a design/QA risk for the R1 wave: explicit warning + empty-source guard are RATIFIED (D-4); adversarial pass owes concurrent-clone / partial-failure / undo questions                                                                                            | OPEN      |
| ACTIVE-FLIP            | `initiatives/ACTIVE` flip to `coach-station` + merge coordination                                          | **CLOSED** — `ACTIVE=coach-station` landed in `main` via the PR #270 merge (session-primitive e2e done; coach-station is the active initiative now).                                                                                                                        | CLOSED    |

## Detail on the live ones

### TEMPLATES (R2 — parked, D-2)

A saved composition / archetype is a **detached, re-instantiable subtree** (same structure, catalog-referencing) the coach saves once and drops into a plan. Open design questions for its own cycle: storage model (mirror Template\* models? a serialized JSON body? same models with a null-plan/`isTemplate` flag?); the save-from-existing flow; the instantiate-into-plan flow (which **reuses R1's deep-clone engine**); a template-management surface. Persona motive is verbatim ("писать EMOM по 200 раз в неделю — отсюда архетипы"). Do not instinct-spec the storage — it's a mini-charter at slotting time.

### DND-GROUP-CREATE (wave G)

From `session-primitive/deferred.md` (assessed 2026-06-14, NOT quick): drag-to-GROUP needs drop-ONTO semantics that conflict with the stabilized reorder collision (`pointerFirstCollision`, DR-W3-11) and a custom combine/droppable layer; drag-IN needs an **add-to-existing path that is currently OUT** (DR-W4E-SG-WRAP / DR-W4-RG-CREATE — `createSchemaRow` strips `rowGroupId`), i.e. a contract+decision change; drag-OUT = null the membership + orphan-cleanup + a contiguity-split re-order; 2 floors × 3 gestures = 6 interaction paths; it partly reverses the just-shipped select-mode-create (B). **Coordinates with QA-D-03** (the nested member-drag-vs-box-drag double-activation, owner-walkthrough item) — G should absorb it, not depend on it. Owner originally framed it "together with duplication" (both manipulate subtrees); orchestrator recommends a **separate adjacent wave** (different risk/verification profile: clone is backend + a trigger, jsdom-testable; G is interaction-heavy + jsdom-blind + browser-walkthrough-gated) — confirm at wave G's start.

## R1a build — Review/QA dispositions (2026-06-15)

| ID                   | One-liner                                                                                              | Disposition                                                                                                                                                                                                                                                                                                                                                                                         | Status |
| -------------------- | ------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| R1A-GATED-ACCEPTANCE | The gated api-server suite (live Neon, ~10 min) — the DoD "verified for real" gate                     | **R1a MERGED to main (PR #270) on green no-DB gates + Review/QA, before the gated run.** The 6 `admin.clone.test.ts` suites are now in main's api-server suite (self-fixture). Owner-owed verify: `pnpm db:reset && pnpm --filter @repo/api-server test`. Until run, the live round-trip/cascade/contiguity assertions are unconfirmed (a known, accepted post-merge gap for a solo non-prod tool). | OPEN   |
| QA-005               | Concurrent grouped-duplicate order-collision can surface as a non-retried P2002 → 409                  | DEFERRED — no corruption (the dup insert is rejected + rolled back); rare in a solo-tool; extending `retryOnP2034` to P2002 globally would touch every call site (risky). Revisit if concurrent editing surfaces it.                                                                                                                                                                                | OPEN   |
| CLONE-002            | `row.duplicate`'s `revalidateSchemaTree` witness is asymmetric with `schema.duplicate` (no revalidate) | DOCUMENTED (intentional, do NOT "tidy"): `row.duplicate` revalidates the parent schema's compose-tree as a defensive witness (one read); `schema.duplicate` does not — the engine builds the new schema valid-by-construction from an already-valid source, and there is no parent tree to revalidate.                                                                                              | CLOSED |

INFO accepted (no action): QA-006 (faithful reproduction of a source-side empty group — benign), QA-007 (strict-body ZodError → 400 at the factory boundary; MT-11 asserts the schema shape), QA-008 (`resolveWeekStartDate` throws outside the try — clean AppError 400 anyway), QA-009 (subtree clone gives a less-specific FK message on a near-unreachable Restrict path), CLONE-003 (self-clone is a safe in-memory full-rebuild — churns ids, no loss), CLONE-004 (barrel-vs-leaf import asymmetry after the cycle-fix — cosmetic).

## R1b build — Review/QA dispositions (2026-06-15)

R1b Review **B+** / QA **B−**, **0 CRITICAL** (destructive core proven safe — atomic tx, source-read-before-delete, empty-source guard before delete, IDOR closed). The R1b finding IDs below are **R1b-local** — do NOT confuse with the R1a `QA-00x` above.

| ID (R1b)         | One-liner                                                                                         | Disposition                                                                                                                                                                | Status          |
| ---------------- | ------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- |
| QA-003           | highlight clock started at POST-resolve → raced the refetch; feedback no-op on big weeks          | FIXED (`91031f00`) — clock re-anchored to node-mount in `use-clone-highlight`, cleanup on unmount                                                                          | CLOSED          |
| QA-008           | day-flow Back left `pickedWeek` stale + no loading guard → wrong/empty days (destructive footgun) | FIXED (`91031f00`) — Back clears `pickedWeek`/`pickedDay`; `DaySourceList` skeleton + day-pick disabled while the source week fetches                                      | CLOSED          |
| QA-005 / REL-001 | highlight provider `setTimeout` never cleared on unmount                                          | FIXED (`91031f00`) — provider timer removed (clock moved to the hook with cleanup)                                                                                         | CLOSED          |
| QA-001-day       | day success toast counted the picked source, not the server result                                | FIXED (`91031f00`) — day toast reads `result.day.sessions.length`                                                                                                          | CLOSED          |
| QA-007           | the target week was offered in its OWN week-clone picker (pointless self-rebuild)                 | FIXED (`91031f00`) — target excluded from the WEEK picker only (the day flow keeps it — a valid source)                                                                    | CLOSED          |
| QA-006           | reduced-motion gated only the transition, not the flash opacity                                   | FIXED (`91031f00`) — reduced-motion now suppresses the flash (scroll only)                                                                                                 | CLOSED          |
| CODE-001         | two back-to-back `@app/lib/hooks` imports in `block-card.tsx`                                     | FIXED (`91031f00`)                                                                                                                                                         | CLOSED          |
| TEST-001         | 123 card jsdom tests red ("No QueryClient set") after Phase 2/T3.7                                | FIXED (`b1116a29`) — `render.tsx` harness wraps `QueryClientProvider retry:false` + `CloneHighlightProvider`                                                               | CLOSED          |
| QA-001-week      | week success toast count from the picker, not the server                                          | ACCEPTED — the frozen `cloneWeekResponseSchema` `cloned:true` arm carries no session count; the picker count = what the coach saw. No server count without scope-out work. | OPEN (accepted) |
| QA-002           | toast lives in the modal, not the hook (split vs `useWeekMutation`)                               | ACCEPTED (intentional) — hook owns cache invalidation, modal owns user-facing feedback (it holds the source count). Flagged so a future 2nd caller toasts itself.          | OPEN (accepted) |
| QA-009           | all-7-days-empty in a picked week (race-only) → all disabled                                      | ACCEPTED — unreachable on the happy path (populated filter); race-only; Back+Close escape exists                                                                           | OPEN (accepted) |
| QA-010           | empty-source info-notice is a dead-end (close+reopen to retry)                                    | ACCEPTED — race-only path; close+reopen is acceptable                                                                                                                      | OPEN (accepted) |

## Wave P (coach-profile) carry-forwards (2026-06-15)

| ID                 | One-liner                                                                              | Disposition                                                                                                                                                                                                                                                | Status |
| ------------------ | -------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| P-GATED-ACCEPTANCE | Wave P gated api-server suite + owner browser walkthrough — the real acceptance        | Owner ritual: `pnpm db:reset && pnpm --filter @repo/api-server test` (incl. new coach-profile + coach-credential suites, written-not-run) + walkthrough `/coach/profile`. **Set `BLOB_READ_WRITE_TOKEN` in `apps/platform/.env.local`** for avatar upload. | OPEN   |
| QA-006-CRED-ROW    | Credentials section shares one `isMutating` → all rows disable during any one mutation | DEFERRED (cosmetic; per-row mutation state is the fix). Polish pass / wave A-known.                                                                                                                                                                        | OPEN   |
| QA-007-TZ-NUDGE    | Workspace tz device-nudge re-fires on mount for a coach who deliberately chose UTC     | DEFERRED — first-visit auto-detect is valuable; narrow edge. Fix = drop the silent mount-write / explicit "use device tz" affordance.                                                                                                                      | OPEN   |

## Wave A-known (inline-create) carry-forwards (2026-06-16)

D-10 shipped inline label + exercise create. Review **A** / QA **B**, **0 CRITICAL**; 6 correctness/hygiene findings fixed in-PR (block-label cap, modal-close-mid-submit, modal form-reset-on-open, `usePromiseModal` reopen-settle, dead `level` prop, block-card multi test). The below are deferred — INFO, a ratified R-3, or owner-owed gates.

| ID                       | One-liner                                                                                                                                                                                                     | Disposition                                                                                                                                                                                                                                                                                                                                              | Status |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| A-KNOWN-GATED-ACCEPTANCE | The gated api-server suite + `db:reset` + owner browser walkthrough — the real acceptance                                                                                                                     | Owner ritual: `pnpm db:reset` (drops the `training_exercises.movementFamily` column — ADR-0019, no migration files) + `pnpm --filter @repo/api-server test` (coach exercise/label create happy-path + idempotent-attach + role-gate, written-not-run). Walkthrough the inline-create flows (esp. labels — D-10d).                                        | OPEN   |
| QA-002-CROSS-LEVEL       | Inline label create at one level when the name exists at a DIFFERENT level → idempotent-attach returns the wrong-level row → the attach validation rejects (toast); coach sees "created" but nothing attaches | DEFERRED — ratified **R-3** (do NOT silently widen another label's `applicableLevels`). A smoother UX (e.g. "add this level to the existing label?") is a product follow-up, not built. Reachable only on a cross-level name collision.                                                                                                                  | OPEN   |
| LABEL-MULTI-UX-RATIFY    | Block-label multi-picker now LISTS already-applied labels, and re-clicking one DESELECTS it (standard MUI multi toggle); the old Menu excluded applied labels                                                 | Awaiting owner ratification in the walkthrough. Deliberate consequence of `CreatablePicker` multi mode; the behavior is pinned by a block-card test. Flag if the owner expected re-click to be a no-op rather than a deselect.                                                                                                                           | OPEN   |
| INLINE-CREATE-INFO       | INFO findings accepted (no action)                                                                                                                                                                            | QA-001 (duplicate aliases/URLs accepted in the create payload), QA-006 (notes `""` vs `null` normalization), QA-007/DEC-7 (server label-search `q` not wired — full-load + client-filter equivalent at current scale; additive later), QA-008 (`placeholderOnly` + create mints CONCRETE — no live consumer). Revisit if scale/a consumer surfaces them. | OPEN   |

## Closed history

_(none yet)_
