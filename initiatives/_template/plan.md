# <initiative-slug> — plan

Phased. Each code step ships via `/feature` (UI-first where it has UI). Budget ≤1 `/feature` per session → expect multiple sessions.

| #   | Step   | Gate                | Status  |
| --- | ------ | ------------------- | ------- |
| 1   | <step> | <how it's accepted> | pending |
| 2   | <step> | <gate>              | pending |

Open design details deferred to their step: <list, so they aren't silently decided early>.
