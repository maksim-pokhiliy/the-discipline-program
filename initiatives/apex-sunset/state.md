# apex-sunset — state (the board)

**Updated:** 2026-08-26 (night) — **P3.2a MERGED AND PROD-VERIFIED — `6a30c52db` (#383, squash, owner-merged).** Independent DEEP review (44 pooled → 34 deduped → 16 ranked, cap not hit) returned REQUEST CHANGES; 15 fix-now + RF-5 accepted; the same reviewer's re-verdict on the fix round was MERGE-READY (15 mutations: 14 killed, 1 equivalent; RF-1 recompiled with the real `@vercel/routing-utils`, the anchored value correct under both edge semantics; the vertical on a throwaway PG17 byte-identical to round one); the final prose round fixed the PR body and close-out numbers and CORRECTED THE PLANNER: a duplicate program schema WAS committed (`7c5278da9`) and deleted (`8c01c0854`) — the reviewer's whole-file `--diff-filter=D` could not see it, `git log -S` can; the wording now holds for history and for the net diff. Planner verification on the frozen code: scripts **430/430**, platform config **23/23**, golden **43/43** off a freshly seeded harness, `dep:check` 2 351/4 928 clean, `check-types` api-server + platform clean, CI 9/9, no comments / stray console / `.only` / real hosts in the diff. **Prod-verified after the deploy went READY:** the RF-1 post-deploy check — `platform.…/` answers its OWN auth redirect (307 → `/login`), not a 308 to www; `/login` 200; `/api/v1/trainingLevel/all` 200; `www` 200; `admin` its own login; the apex still the VPS (rule not attached) — and the shim smokes on the new deployment (demo signin 200, program 200, garbage token 403, user 200, 5/5 security headers). New carry-forward **AS-24** (CLI hygiene the re-verdict named: attest the target before parsing the source, the probe re-deriving guard helpers, `withHostWithheld` scrubbing the operator's own flag value) rides P4.1 with AS-21. Board: 3.2a ✅ — next **3.2b, the cutover** (`docs/runbooks/apex-cutover.md`).

Prior: **P3.2a SHIPPED (PR open, `feat/apex-cutover-prep`): everything the flip needs is in code, dormant.** Three deliverables, one PR. **(1) Apex redirect** — one rule in `apps/platform/vercel.json` sends every non-`/api/v1` path on `thedisciplineprogram.com` to `www` with a 308. Both open routing questions were settled by EXECUTING against the installed `next@16.2.6` rather than reading docs: the param form **does** match the root (so the extra bare-`/` rule is unnecessary), and the host condition is written **escaped and anchored** (`^thedisciplineprogram\.com$`). The first version trusted Next's `matchHas`, which anchors — but the review established that **the Vercel edge, not Next, executes this file**, its docs call `has.value` a regex without promising anchoring, and its CLI anchors equality conditions itself. Unanchored, the rule would have caught `platform.thedisciplineprogram.com` on merge and sent the whole product to marketing while `/api/v1` kept working and hid it. The committed form is correct under both readings (asserted for six hosts against an anchored and an unanchored compile); since `vercel dev` does not evaluate `has` at all, a **post-deploy check** (`platform.…/` and `platform.…/api/v1/trainingLevel/all` must both answer 200) rides in the PR Rollout section and `apex-cutover.md` §1. `/.well-known` is reserved by Vercel, so cert issuance at the flip is unaffected. **A planner fact was wrong and is fixed:** the three `vercel.json` files have NOT been identical since admin gained its retention `crons` — the documented `diff` check was already failing on `main`; `vercel-json.md` now states the true invariant with a `jq` recipe (and a `jq`-free fallback) that is empty when in sync. **(2) AS-22** — `ACTION REQUIRED — app password changes` + `app-password changes N`, computed from the plan: export credential present, platform credential present, not the export's hash, and **no marker**. Marker-absence is proof because `importedPasswordHash` has exactly ONE writer in the repo (the import's own apply, verified by grep). Marker-set-but-differing rows stay out (AS-7 re-hash vs a later change is unknowable, and the script must not claim); so do rows with no platform password, whose own warning already says the true thing — the runbook checkbox names BOTH lists. It rides `ImportPlan.appPasswordChanges`, a derived field outside the digest (the `reconciliation` precedent), and the **digest is proven unmoved empirically**: the same fixture plans hash to `7239ddd29d56` / `8818b3a1dd7f` on both trees, pinned in a test. **(3) AS-13 backfill** — `legacy-days-backfill*.ts`, guard reused verbatim, "fills nulls only" enforced TWICE (the read filters `isRestDay: null`; the write is an `updateMany` carrying the same predicate with a zero-count refusal that rolls the whole transaction back, so a day published mid-run cannot be overwritten), writer type with no create/upsert/delete, `contentHash` from the shared publish helper. Planner's condition discharged before the lossy-body conflict class was allowed: **665 content-carrying bodies of the 1 038 legacy rows, 0 schema rejections, 0 round-trip mismatches.** **The local vertical earned its keep:** the CLI would not boot — it dragged `@repo/env` through two barrels (`src/utils` → token-cipher; `infrastructure/legacy-mobile` → the REST adapter) and died before reading anything, invisible to CI because vitest injects those vars. Root cause: **`import { type X }` keeps a module alive at run time where `import type { X }` erases it.** Fixed by leaf imports and guarded by a module-graph test that follows **re-exports as well as imports** AND resolves `@repo/*` workspace packages rather than stopping at their names — verified failing on BOTH reintroductions (the `utils` barrel, and the `wire-schemas.ts` inline type import). The third env path was one inline type import in `mobile-compat/wire-schemas.ts`; the planner granted a one-line fence extension (`import { type X }` → `import type { X }`), which removes the runtime edge that importing the shim's schema dragged in. **No duplicate schema ships**: the backfill validates with the shim's own, as it did when it first landed; the local copy the branch carried for two commits in between — added when the boot fix could not reach `wire-schemas.ts` through the fence, removed when the fence opened — is gone. Proven safe: the shim reaches its env through its own explicit `mobile-compat/shim-token.ts` import, and `program-dto.ts` (on the `/program` serve path) still carries the identical barrel edge, so the shim's load behaviour is unchanged — golden 43/43 re-run off a fresh harness on the final tree confirms it. The erasure holds because the package sets `verbatimModuleSyntax: true`, under which a statement-level `import type` is dropped and an inline one is not. A **third** copy of the daily-program shape lives in `packages/contracts/src/entities/coaching/legacy-mobile/legacy-mobile.schema.ts:15` and has already drifted (`trainingNumber` int vs number) — connector-era code, recorded under AS-5, out of fence here. **Review round (44 pooled → 34 deduped → 16 ranked, REQUEST CHANGES) is fixed in full** — the blocking one was the host anchoring above; the sharpest of the rest was **RF-3**, a conflict-arm test that never entered its arm (deleting the arm left the suite green), and **RF-6**, which showed the guard attested a _server_ while two databases on one host — the rehearsal and the real one — differ only by the name at the end of the DSN: both scripts now require `--expect-database`, and `--expect-host` must name the port when the DSN does. Verified: **430/430 script tests · golden 43/43 (re-run on the final tree) · mobile-publish + mobile-compat 245 passed / 14 skipped · platform config test 23/23 (all three `vercel.json` files, every header value, the anchoring matrix) · dep:check clean · 49 mutations applied, 48 killed + 1 equivalent** · local vertical green end to end (fill / fill-from-newer-row / missing-in-legacy untouched / content-bearing row byte-identical incl. `publishedAt` / stale pin and tampered export both refused with zero writes / hashes equal the publish helper's / XOR satisfied / idempotent re-run). Runbooks: `legacy-days-backfill.md` + `apex-cutover.md` (new), `legacy-users-import.md` + `vercel-json.md` (corrected).

Prior: **P3.2 opened: D-9 ratified (DNS flip, apex = platform custom domain, no Worker), 3.2a executor round teed up, and a cutover-blocking finding banked as AS-22.** The owner reproduced the P2 DoD on his own legacy row (id 3, an ATTACH at P2.1): legacy app password → 403 from the shim, because attach rows keep their PLATFORM credential by ratified design. Prod audit (ids only): all 7 attach athletes (1/3/9/18/22/23/24) set their own platform password through the invite flow and five train on the web — the platform credential stays; the consequence (app password = website password from the flip) is managed by a new `ACTION REQUIRED — app password changes` report section + a mandatory pre-cutover checkbox (3.2a), and the DoD's real-account sample still needs a CREATE row (Denys). AS-13 sized: **134 of 254** ledger rows content-less (6 links, GENERAL 77 / INDIVIDUAL 57, 2026-06-22 … 08-16, all with `legacyRowId`) → a guarded backfill script is a 3.2a deliverable. Owner calls: AS-9 → product polish (post-cutover), AS-1 → owner informs Denys. New AS-23 (the zone lives in Vladyslav's Cloudflare account; owner has full rights, not ownership → move at P4). 3.2a = `/feature` full round off `initiatives/apex-sunset/step-3.2a-prompt.md`: apex → www redirect in `apps/platform/vercel.json` (dormant until the flip), the AS-22 section, the AS-13 backfill (`legacy-days-backfill*.ts`, import-grade guard + digest, fills nulls only, matches by link target + date with `legacyRowId` cross-check), `apex-cutover.md` skeleton.

Prior: **P3.1 DONE — the REAL App-Store binary passed against the prod shim.** Owner ops + session (no executor): on a real iPhone the v1.0.9 App-Store build signed in as the demo athlete, browsed six published days (08-25 … 08-30), opened the profile and signed out — every request `200` through the LAN stand, planner-verified in the caddy access log under the app's own User-Agent (`TheDisciplineProgram/9 CFNetwork/3860.700.1 Darwin`), with the phone's apex lookups answered by the DNS override. That is D-5's third and last e2e layer: the exact production binary, unchanged, works with every byte from Neon. The stand that worked (runbook `docs/runbooks/prod-build-rehearsal.md` + scripts folder): a Windows-side PowerShell DNS responder (apex → LAN IP, NODATA for every non-A type) + caddy in Docker Desktop terminating TLS with a genuine Let's Encrypt DNS-01 cert and forwarding to `platform.…` with the Host header rewritten. Facts banked on the way: **the Vercel edge refuses Host≠SNI with 403** (so P3.2 must add the apex as a custom domain on the Vercel project — a bare DNS flip cannot work); **no certificate pinning** in the app; **the App Store listing is DELISTED** (AS-1 materialized — installed copies work, no new installs); **WSL mirrored mode + Docker Desktop: UDP published ports are unreachable from the LAN and TCP ones need Hyper-V-firewall AND port-based Windows-Firewall rules** — the cause of three rounds of "endless spinner" before the DNS moved to the Windows side; an IPv4-only dnsmasq `host-record` forwards AAAA upstream (an iPhone would have bypassed the stand over IPv6 — caught before the gate). Ride-alongs not consumed by this step: AS-9 (rate limiting — owner decision before cutover), the Denys old-password sample, the $99 renewal now that delisting is real.

Prior: **P2.2 PROD-VERIFIED — P2 (Accounts) IS COMPLETE.** Merged as `a124edcba` (#379, squash; independent review REQUEST CHANGES → fix round → re-verdict MERGE-READY; planner re-ran golden 43/43 + probe 13/13 personally — the two on-faith numbers). Migration `20260821093030` applied by `db-migrate` BEFORE the deploy went READY (the RF-9 ordering watched live). Prod re-run off a fresh same-day SSH dump: dry-run and pinned apply both `create 0 · attach 0 · refresh 19 · mirror diffs 0 · credentials replaced 0 · markers backfilled 11 · conflicts 0 · warnings 9`, `RECONCILIATION individual links 5 · matched 5 · violations 0`, first live `--expect-plan` pin held (digest recomputed in-transaction, matched); the executor's falsifiable prediction matched VERBATIM including the `warnings = 9 + (11 − backfilled)` formula and `backfilled = 11` (no imported athlete has signed in yet, so AS-7 had upgraded nobody). Planner-verified in prod: 20 identities, 11 markers, all 11 byte-equal to `User.password`, 9 honest nulls (7 attach + id 17 + demo). Idempotent re-run: `markers backfilled 0`, digest changed (backfill actions gone — correct). Prod smokes green (demo signin, program 200, garbage 403, catalogs 200). One operational note: the first pinned apply attempt failed CLOSED on a cold prod Neon connect — a retry loop woke it; same AS-8 class as dev. Step context: Three carry-forwards closed on the shipped import surface, and the reconciliation obligation became a gate asserted on EVERY run rather than an audit done once. **AS-12:** each INDIVIDUAL `MobilePublishLink` carrying a legacy id is cross-checked against the identity holding it; a disagreement is the new conflict `link-and-identity-disagree` and aborts the whole run. Every report head now carries `RECONCILIATION individual links N · matched to a stored identity M · violations V` (the middle number on purpose — `violations 0` proves nothing if nothing was checkable) and `mirror diffs`, which is the plan/level fidelity `/program` routes on. Links are read UNFILTERED, because a link whose legacy id fell out of the export is exactly the row the gate exists to catch; the user query stays export-scoped, so matching is provably unchanged, and `identity-target-drift` narrowed to its address half. **AS-19:** `MobileLegacyIdentity.importedPasswordHash` (migration `20260821093030`, one additive nullable column — the phase's only migration) records the hash the import itself last wrote; restore needs the marker set AND `User.password` still equal to it, then writes the new hash to both; a stored credential already equal to the export gets the marker recorded (`markers backfilled`); everything else reports `not-import-written`. The bcrypt-cost heuristic is GONE from the import (`readBcryptCost` stays in `iam/` for AS-7 and gained its own test file — planner-adjudicated fence extension). **AS-20:** every report prints `plan digest <12 hex>` over a canonicalized plan (sorted, so row order cannot move it; credentials enter only as fingerprints), and `--write` now REQUIRES `--expect-plan` — the apply re-decides in its transaction as always and refuses under its own heading when the recomputed digest differs. **Runbook law:** post-cutover `--write` re-runs are forbidden by default (`PUT /user` writes profile fields into the identity, `changePassword` writes the credential — a re-run would overwrite live athlete edits); dry runs stay safe. Verified: **273/273** script tests (was 219) · **296/296** iam + mobile-compat against dev Neon carrying the column · **golden 43/43** off a fresh harness (the proof the additive column is transparent to the shim) · probe **12/12** now driving the real dry→apply flow · `dep:check` clean · **7/7 hand-applied mutations killed** · a full local vertical proving unpinned-write refusal, marker writes, tamper→decline, restore→accept with the marker moving, the prod-shaped backfill, a stale pin refusing with row counts unchanged, and a hand-seeded contradicting link aborting the run with the pending backfill unwritten. **Review round complete (2026-08-21, later):** the independent review returned REQUEST CHANGES with six blocking findings, all fixed on the branch — the sharpest being **RF-2**, which proved the digest WAS row-order-sensitive through the claimed-twice conflict's id list (the "row order cannot move it" claim was false in four documents when written, and is true now), and **RF-1**, an impossible remedy printed for a conflict the export cannot clear. **RF-4** found 8 of 13 mutations surviving, all on branches this PR added; the table now stands at **21 applied, 21 killed**. Harness lesson banked: the mutation runner reverts with `git checkout --`, so it wipes uncommitted work in the file it mutates — mutations run only against a committed baseline, and the first (invalid) table was discarded rather than reported. New carry-forward **AS-21** (select-less RETURNING on the two non-import identity writes — hygiene, rides P4.1); planner dropped RF-9. **Falsifiable prediction for the prod re-run:** `create 0 · attach 0 · refresh 19 · mirror diffs 0 · conflicts 0 · credentials replaced 0`, `violations 0`, `markers backfilled` = the imported athletes who have not signed in since 2026-08-20 (**≤ 11**, since AS-7 moves a hash off the legacy value on first login), and the 7 attach rows plus every signed-in create row reporting `not-import-written`.

Prior: **P2.1 COMPLETE: the users import is APPLIED IN PROD.** Off a fresh same-day SSH dump (which caught real 13-day drift: id 24 changed their app password, id 18 was moved Individual→General — the freshness rule paid for itself): prod dry-run then apply, both `create 12 · attach 7 (link 4 / address 3) · login-address changes 0 · credentials replaced 0 · conflicts 0` — counts identical, ATTACH set = ids 1/3/22/23 by address + 3/9/18/24 by link (owner id 3 via his own mobile-publish-era INDIVIDUAL link), warnings exactly the expected pair (id 17 synthetic/force-disabled/no-credential + 990001 demo outside the export, nothing deleted). Planner-verified in prod: **20 identities (19 + demo), all 11 credential-carrying creates byte-exact to the legacy hashes, 0 mismatches, id 17 password null, every identity-holder ATHLETE, enabled 11** (10 imported + demo). Idempotent re-run: `create 0 · refresh 19 · conflicts 0`, the 7 attach rows honestly reporting "platform credential kept". Prod smokes green: demo signin → `userId 990001`, program 200, garbage token 403, catalogs 200; deploy `5fc623ee` (with AS-7 upgrade-on-verify) READY. The gate's live old-password sample rides as an owner action (Denys in the Appetize stand — or naturally at cutover, when every phone becomes the test). Earlier same day, the step itself: `packages/api-server/scripts/legacy-users-import.ts` carries every legacy `users` row into a platform `User` + `MobileLegacyIdentity` with the bcrypt hash verbatim (D-3): dry run by default, `--write` behind `--expect-host`, one transaction, whole-run abort on any conflict, and **no delete path in the writer's type at all**. Riders **AS-7** (upgrade-on-verify in `validateUser`, `tokenVersion` untouched), **AS-10** (catalog validation against the `legacy-catalogs.ts` SSOT, checked before any match level) and **AS-15** (every layer exported + unit-tested behind `isDirectRun`; **13 mutations applied to the final tree, 13 killed**) are **CLOSED**. Verified: **507 targeted api-server tests across 39 files** (216 of them the import's own), the 7-case opt-in probe, **golden 43/43** with AS-7 in the signin path, a full dev vertical on a local throwaway PG17 container (per R1 — dev Neon untouched), and a dry run of the REAL 19-row `prod_snap` export showing **zero conflicts** with every class count cross-checked against the dump. Ratified this step: **R1** local throwaway target · **R2** id 17 imports with `password: null` (the one deliberate D-3 deviation — its synthetic address is public in a committed file) · **R3** link-match address changes get their own `ACTION REQUIRED` report section · **R4** planner-granted fence extension to extract `mobile-compat/legacy-date.ts` rather than duplicate wire date semantics · **R5** the demo trio keeps its own guard. New carry-forwards: **AS-16** (a link-matched athlete's app login address changes — owner must warn them before cutover), **AS-17** (imported accounts, including the 8 legacy-disabled ones, gain a web login they never had), **AS-18** (the demo trio still prints its host). An adversarial review (32 candidates pooled → 9 reported) returned FIX-FIRST and **all nine are fixed**, each re-proven by executing the reviewer's own repro: the blocking pair was **LI-01** (a `host=` query parameter in the DSN overrides the authority host at connect time, so `--expect-host` could attest to a database the run never touched — now refused outright) and **LI-02** (the credential-restore cost heuristic was a claim about the database's history, not an enforced property — restore is now opt-in behind `--restore-credentials`, so the default can never overwrite a platform password; durable marker-column fix carried forward as **AS-19**). Nothing real — no address, no hash — entered a committed file. Prior context: **P1 (the shim) is COMPLETE.** The Appetize stand is live: the REAL iOS app (fork `maksim-pokhiliy/the-discipline-program-ios`, ad-hoc-signed simulator build, base URL patched at build time) streams at `https://appetize.io/app/pbyf27bzwidoccs2nooylpshti` against the PROD shim, and the owner drove login → training day → rest day → date navigation → profile clean (2026-08-14). The prod demo universe is seeded (demo athlete `legacyUserId 990001`, INDIVIDUAL channel — invisible to every real athlete; 64 days, window 2026-08-11..2026-10-13; PR #373 `a2db94d1`). D-8 ratified (stand targets prod). P1.3 facts stand (snapshot serve, fatalError unrepresentable at 3 layers, DbNull rest days, token-resolved reads). `GET /api/v1/program?userId=&scheduledDate=` now serves the athlete's day from a publish SNAPSHOT in our DB (D-4), and the publish write path persists the rendered day's content. Golden 43/43 vs the live harness (planner-re-run, cold, twice); review MERGE-READY (empty blocking set — the one substantive candidate refuted by scanning the real prod dump: prod already enforces a stricter XOR). Key facts banked: (1) **`/program` binding failures → 403-empty, not 400** — a malformed/missing `userId`/`scheduledDate` hits the same Spring `/error` swallow as P1.1's auth failures, and 403 signs the app out; the param guard emits 403 and uses `z.string().regex/refine`, NOT `z.coerce` (so a missing param can't collapse to a silent 404). (2) **the fatalError state (`isRestDay:false`+`dailyProgram:null`) is unrepresentable at THREE layers** — projection type, DB XOR CHECK, serializer throw. (3) **rest-day content = `Prisma.DbNull`, not `Prisma.JsonNull`** (else the CHECK rejects every rest-day write). (4) read + scope resolved from the TOKEN (channel by `legacyPlanId`; `userId` mismatch → 404). P1.2 still current: 3 athlete endpoints served; the outcome unions (`LegacyUserOutcome`/`LegacyProgramOutcome`) have no 403 variant, so a business-403 is a compile error; scope-to-self → 404. P1.1: legacy auth-failure surface is 403-empty for EVERY failure. P0: legacy DB holds ZERO athlete-generated data (19 users, 640 general + 321 individual days); compat surface = 9 endpoints, contract in `mobile-publish/legacy-contract.md`.

## Board

| #   | Phase                        | Status     | Pointer                                                                                                                                                      |
| --- | ---------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| P0  | Facts & safety net           | ✅ done    | `plan.md` 0.1–0.5 all ✅; journal 2026-08-07                                                                                                                 |
| P1  | The shim (`/api/v1/*`)       | ✅ done    | 1.1 ✅ (#364) · 1.2 ✅ (#367) · 1.3 ✅ (#370) · 1.4 ✅ (#373 + stand); `legacy-contract.md` = the spec                                                       |
| P2  | Accounts (full users import) | ✅ done    | 2.1 ✅ (#376 + prod applied 2026-08-20) · 2.2 ✅ (#379 `a124edcba` + prod re-run verified 2026-08-21: refresh 19 / violations 0 / markers 11)                |
| P3  | Rehearsal & cutover          | 🔄 active  | 3.1 ✅ (2026-08-25) · 3.2a ✅ (#383 `6a30c52db`, prod-verified 2026-08-26) · **3.2b cutover next** (D-9, `docs/runbooks/apex-cutover.md`) · 3.3 decommission |
| P4  | Cleanup & close-outs         | ⬜ pending | AS-4/AS-5; `mobile-publish` → superseded                                                                                                                     |

## Next action

**▶ P3.2a COMPLETE — next: P3.2b, THE CUTOVER (owner ops + session, D-9).** Run `docs/runbooks/apex-cutover.md` top to bottom in one session with the owner: §1 the post-deploy check is already green (2026-08-26) · §2 add `thedisciplineprogram.com` to the platform Vercel project (owner, dashboard; TXT `_vercel` + the A record Vercel names) → pre-flip live routing check with `curl -k --resolve` against the Vercel IP (308 on `/`, 200 on `/api/v1/trainingLevel/all`, 403 on a garbage-token `/api/v1/program`) · TTL of the apex record → 60 s · FINAL users sync: fresh SSH dump → export → dry run (`--expect-host` + `--expect-database`) → owner reviews the report incl. `app-password changes` (expect the seven: 1/3/9/18/22/23/24) and `RECONCILIATION … violations 0` → pinned apply WITH `--restore-credentials` (RF-10 ruling: marker-matched rows are import-owned; the legacy hash at that moment is the athlete's current app password) · AS-13 backfill: programs export from the same dump → dry run (expect 134 targets: GENERAL 77 / INDIVIDUAL 57, 6 links, 06-22 … 08-16; `legacy-row-older-than-ledger` or `duplicate-legacy-row` firing = stop and understand, not bypass) → pinned apply · the flip: apex A → Vercel, proxy off · wait for the Vercel cert · phone smoke: demo athlete + the owner's own account with the PLATFORM password in the real app · Sentry quiet · soak window agreed with the owner → 3.3. Owner actions BEFORE the flip: tell the seven attach athletes (app password = website password; reset on the website) · Denys as the CREATE-row old-password sample (Appetize stand — his row keeps the legacy hash) · pick the cutover date/time with Denys (a low-traffic hour; the rollback is the A record back to the VPS with proxying on, minutes at TTL 60) · optional: a scoped Cloudflare API token (this zone, DNS:Edit) so the flip and its rollback run from the session. Keep `LEGACY_MOBILE_API_BASE_URL` / `MOBILE_PUBLISH_ENCRYPTION_KEY` in Vercel until P4.1 (RF-11: `/api/v1/program` still module-inits the connector env). Local assets: rehearsal cert + configs in `~/projects/contrib/tdp/rehearsal/` (delete after 3.2b), dumps under `~/projects/contrib/tdp/dumps/`, `tdp-dump-verify` (`fresh_snap` 2026-08-21 — the cutover uses a NEW dump), `.legacy-import/` (operator machine only).

## Open decisions awaiting ratification

- **(none open)** — D-1..D-8 ratified. **D-8 (new, 2026-08-14): the Appetize stand targets PROD with a synthetic INDIVIDUAL demo universe seeded in prod Neon** — Vercel SSO (`all_except_custom_domains`) closes every preview URL to the app, so the prod custom domain is the only reachable shim; the demo account doubles as the P3.1 rehearsal + post-cutover smoke login. P1.3 resolved D-4's storage shape (journal 2026-08-12). Remaining step-level design: apex routing at 3.2.

## Live carry-forwards

AS-1 (App Store listing DELISTED by 2026-08-21 — installed copies work, no new installs; $99 renewal = owner+Denys product call; App Store Connect access rides for the redesign; NOT a cutover blocker) · AS-2 (`/dev-api` dies with the VPS, accepted) · AS-3 (XCUITest smoke in CI — optional) · AS-4 (surviving `mobile-publish` UI carry-forwards re-home at P4) · AS-5 (`MobileConnection`/cipher/env-vars fate — P4 design) · **AS-8** (cold-Neon-wake flake on pooled dev Neon — golden warmup mitigates; ordering-dependent) · **AS-9** (prod rate limiting no-op — owner 2026-08-26: product polish, post-cutover; the `x-ratelimit-*` headers are the noop adapter's statics) · **AS-11** (the golden IDOR case is proven by the unit test, not the golden — optional test-strengthening) · **AS-13** (134 of 254 `MobilePublishedDay` rows content-less → backfill script at 3.2a, prod apply at 3.2b before the flip) · **AS-14** (post-cutover the wire `id`/`legacyRowId` must be minted locally when the legacy push is removed — P4.1/P3.2) · **AS-17** (imported accounts, incl. 8 legacy-disabled, hold a live web login — owner-acknowledged) · **AS-18** (demo trio still prints its host — rewire when reopened) · **AS-21** (the two non-import `MobileLegacyIdentity` writes use no `select`, so every `PUT /user` RETURNINGs the credential marker into memory for a value it discards — hygiene, rides P4.1) · **AS-22** (the 7 ATTACH athletes' app password becomes their website password at the flip — report section at 3.2a, owner tells each of them before 3.2b) · **AS-23** (the zone lives in Vladyslav's Cloudflare account — move at P4). CLOSED at P2.2: **AS-12** (the reconciliation + mirror-fidelity gate, asserted on every run; prod artifact rides the owner's re-run) · **AS-19** (`importedPasswordHash` marker; the bcrypt-cost heuristic is out of the import) · **AS-20** (`--expect-plan` digest pins the reviewed plan to the applied one). CLOSED at P2.1: AS-7 (upgrade-on-verify live in prod, measured residual accepted) · AS-10 (catalog validation in the import) · AS-15 (isDirectRun + 13/13 mutations killed) · AS-16 (prod run: login-address changes = 0, nobody to warn).

## Gotchas a resuming session must know

- **The iOS app's wire traps are sacred** (charter): raw `Authorization` (no `Bearer `), HTTP 200-only success, `yyyy-MM-dd` dates everywhere, `PROGRAM_ID` header on 409, never `isRestDay:false` + `dailyProgram:null` (a `fatalError` on the app's main screen), 403 — not 401 — drives sign-out.
- **The app sends `userId: Int`** — the users import MUST preserve legacy integer ids in a map (D-3); a cuid in `JwtDTO.userId` kills the decode.
- **Do NOT patch the legacy backend** (IDOR etc.) — it dies; and legacy prod stays inviolable until the final snapshot (charter Sacred).
- `mobile-publish` is still ACTIVE in parallel until P4 closes it as superseded — don't double-book its MP- numbers; this initiative uses AS-.
- The local legacy harness (`tdp/local/docker-compose.yml`, backend `localhost:8080/api/v1`, DB host 5433) is the golden-test target — recipe in `mobile-publish/journal.md`.
- **Prod Postgres is 17.5; the harness compose pins `postgres:16-alpine`** — PG16 `pg_restore` cannot read PG17 custom-format dumps. Bump the harness image to `17-alpine` the first time a step touches it (P1.1). The restored prod snapshot lives in local container `tdp-dump-verify` (`prod_snap`/`dev_snap`).
- **Rehearsal-stand host quirks** (runbook `prod-build-rehearsal.md`): WSL mirrored mode + Docker Desktop — UDP published ports are unreachable from the LAN (the DNS override must be a Windows-side process), TCP ones need BOTH a Hyper-V-firewall rule and a port-based Windows-Firewall rule, the network profile must be Private, Windows-side reachability tests are not diagnostic, bind-mounts go stale after a reboot. And the Vercel edge refuses Host≠SNI (403) — the apex must become a Vercel custom domain at 3.2.
- Real prod `users` columns (drifted from `schema.sql`): `user_role_id`, `training_level_id`, `user_plan_id`, `is_enabled` — 8 of 19 users are disabled; the shim's auth + the P2.1 import must honor disabled semantics per the legacy contract.
