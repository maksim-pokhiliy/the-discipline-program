# apex-sunset — state (the board)

**Updated:** 2026-08-12 — **P1.3 DONE + prod-verified** (publish snapshot + program serve, PR #370 squash `4d3e7283`, owner-merged; `db-migrate (production)` success — additive columns + `rest_xor_program` CHECK + 2 indexes applied cleanly on the populated prod table; platform deploy Ready; prod smoke green — `GET /program` no-token/garbage/malformed → 403-empty, catalogs byte-correct). `GET /api/v1/program?userId=&scheduledDate=` now serves the athlete's day from a publish SNAPSHOT in our DB (D-4), and the publish write path persists the rendered day's content. Golden 43/43 vs the live harness (planner-re-run, cold, twice); review MERGE-READY (empty blocking set — the one substantive candidate refuted by scanning the real prod dump: prod already enforces a stricter XOR). Key facts banked: (1) **`/program` binding failures → 403-empty, not 400** — a malformed/missing `userId`/`scheduledDate` hits the same Spring `/error` swallow as P1.1's auth failures, and 403 signs the app out; the param guard emits 403 and uses `z.string().regex/refine`, NOT `z.coerce` (so a missing param can't collapse to a silent 404). (2) **the fatalError state (`isRestDay:false`+`dailyProgram:null`) is unrepresentable at THREE layers** — projection type, DB XOR CHECK, serializer throw. (3) **rest-day content = `Prisma.DbNull`, not `Prisma.JsonNull`** (else the CHECK rejects every rest-day write). (4) read + scope resolved from the TOKEN (channel by `legacyPlanId`; `userId` mismatch → 404). P1.2 still current: 3 athlete endpoints served; the outcome unions (`LegacyUserOutcome`/`LegacyProgramOutcome`) have no 403 variant, so a business-403 is a compile error; scope-to-self → 404. P1.1: legacy auth-failure surface is 403-empty for EVERY failure. P0: legacy DB holds ZERO athlete-generated data (19 users, 640 general + 321 individual days); compat surface = 9 endpoints, contract in `mobile-publish/legacy-contract.md`.

## Board

| #   | Phase                        | Status          | Pointer                                                                                     |
| --- | ---------------------------- | --------------- | ------------------------------------------------------------------------------------------- |
| P0  | Facts & safety net           | ✅ done         | `plan.md` 0.1–0.5 all ✅; journal 2026-08-07                                                |
| P1  | The shim (`/api/v1/*`)       | 🔶 1.1–1.3 done | 1.1 ✅ (#364) · 1.2 ✅ (#367) · 1.3 ✅ (#370); 1.4 pending; `legacy-contract.md` = the spec |
| P2  | Accounts (full users import) | ⬜ pending      | D-3; dry-run on the P0 dump first                                                           |
| P3  | Rehearsal & cutover          | ⬜ pending      | D-5 rehearsal; apex DNS → Vercel                                                            |
| P4  | Cleanup & close-outs         | ⬜ pending      | AS-4/AS-5; `mobile-publish` → superseded                                                    |

## Next action

**▶ P1.3 DONE + prod-verified (2026-08-12).** Program read + publish snapshot shipped (PR #370 squash `4d3e7283`, owner-merged). `db-migrate (production)` completed/success — the additive `MobilePublishedDay.isRestDay`/`dailyProgram` columns + the `rest_xor_program` CHECK + 2 link indexes applied cleanly on the POPULATED prod table (pre-P1.3 rows carry null content, so the XOR holds). Platform deploy Ready; prod smoke green (`GET /program` no-token / garbage-token / malformed-date → 403-empty; `trainingLevel/all` + `userPlans` byte-correct). One post-merge lesson banked (`step-golden-not-full-ci-surface`): CI's Tests job caught the new `./test-helpers/published-snapshot` export tripping `endpoints-di-bootstrap.test.ts` — a guard the platform-scoped golden never runs; one-line fix landed before merge (`627eabec`). Golden recipe (43 cases): `scripts/legacy-harness-seed.sh` (idempotent) → `RUN_LEGACY_INTEGRATION=1 SKIP_ENV_VALIDATION=1 pnpm exec vitest run --project platform src/app/api/v1/`. The restored prod snapshot `tdp-dump-verify` (PG 17.5, `prod_snap`/`dev_snap`) remains the P2.1 dry-run playground; dumps at `~/projects/contrib/tdp/dumps/2026-08-07/`. **Next: P1.4 (Appetize stand) via `/step`** — a signing-free simulator build on a macOS runner from our fork (CI patches the base URL to a preview shim), streamed in-browser via Appetize; the FIRST owner browser-drive (login → day → profile) against the live shim. P2.1 (users import) unblocked (verified dump + live identity table), carries **AS-10** (catalog-id validation) + **AS-12** (identity-mirror fidelity now gates correct TRAINING CONTENT, not just profile fields). Owner-side ride-alongs unchanged: **AS-1** ($99 membership) · the 5432-exposure ask to Vladyslav · Trello board setup · **AS-9** (prod rate limiting no-op — Upstash unset in Vercel — owner decides).

## Open decisions awaiting ratification

- **(none open)** — D-1..D-7 ratified. P1.2's and P1.3's design calls were planner-ratified at their plan gates and recorded in the journal; none rose to a new D-number. **P1.3 resolved D-4's deferred storage shape** → content columns on `MobilePublishedDay` + a `rest_xor_program` XOR CHECK + token-resolved read (journal 2026-08-12). Remaining step-level design: apex routing at 3.2.

## Live carry-forwards

AS-1 (App Store Connect access — future redesign + bus factor, NOT a cutover blocker) · AS-2 (`/dev-api` dies with the VPS, accepted) · AS-3 (XCUITest smoke in CI — optional) · AS-4 (surviving `mobile-publish` UI carry-forwards re-home at P4) · AS-5 (`MobileConnection`/cipher/env-vars fate — P4 design) · **AS-7** (bcrypt timing oracle cost 10-vs-12 — fix rides the P2.1 import) · **AS-8** (cold-Neon-wake flake on pooled dev Neon — the P1.2 golden warmup mitigates it, but `get-program.test` (P1.3, gated) shows the same isolated-cold-first-touch flake, masked in the warmed serial suite; ordering-dependent) · **AS-9** (prod rate limiting no-op — Upstash absent in Vercel; owner decision) · **AS-10** (the shim 500s on an identity whose legacy catalog id is out-of-range — the P2.1 import MUST validate role/plan/level ids) · **AS-11** (the golden IDOR case is proven by the unit test, not the golden — optional test-strengthening) · **AS-12** (`/program` routes on the MIRRORED legacy plan/level — a mirror drift after P2.1 serves WRONG TRAINING CONTENT; the P2.2 reconciliation gate must guarantee fidelity) · **AS-13** (pre-P1.3 `MobilePublishedDay` rows carry null content → need a cutover backfill from the final dump before they're servable) · **AS-14** (post-cutover the wire `id`/`legacyRowId` must be minted locally when the legacy push is removed — P4.1/P3.2).

## Gotchas a resuming session must know

- **The iOS app's wire traps are sacred** (charter): raw `Authorization` (no `Bearer `), HTTP 200-only success, `yyyy-MM-dd` dates everywhere, `PROGRAM_ID` header on 409, never `isRestDay:false` + `dailyProgram:null` (a `fatalError` on the app's main screen), 403 — not 401 — drives sign-out.
- **The app sends `userId: Int`** — the users import MUST preserve legacy integer ids in a map (D-3); a cuid in `JwtDTO.userId` kills the decode.
- **Do NOT patch the legacy backend** (IDOR etc.) — it dies; and legacy prod stays inviolable until the final snapshot (charter Sacred).
- `mobile-publish` is still ACTIVE in parallel until P4 closes it as superseded — don't double-book its MP- numbers; this initiative uses AS-.
- The local legacy harness (`tdp/local/docker-compose.yml`, backend `localhost:8080/api/v1`, DB host 5433) is the golden-test target — recipe in `mobile-publish/journal.md`.
- **Prod Postgres is 17.5; the harness compose pins `postgres:16-alpine`** — PG16 `pg_restore` cannot read PG17 custom-format dumps. Bump the harness image to `17-alpine` the first time a step touches it (P1.1). The restored prod snapshot lives in local container `tdp-dump-verify` (`prod_snap`/`dev_snap`).
- Real prod `users` columns (drifted from `schema.sql`): `user_role_id`, `training_level_id`, `user_plan_id`, `is_enabled` — 8 of 19 users are disabled; the shim's auth + the P2.1 import must honor disabled semantics per the legacy contract.
